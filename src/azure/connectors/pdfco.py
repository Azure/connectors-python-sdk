# Copyright (c) Microsoft Corporation. All rights reserved.

# PdfcoClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class HtmlToPdfInput:
    """
    HTML to PDF
    """

    html: Optional[str] = None
    """Input HTML code to be converted."""
    name: Optional[str] = None
    """Base file name for new PDF files."""
    margins: Optional[str] = None
    """
    Override default margins with custom values (the order is `top right bottom
    left`). You can als set all margins: `10px`, `5mm`, `5in` for all sides.
    """
    paper_size: Optional[str] = field(
        default=None,
        metadata={"wire_name": "paperSize"},
    )
    """
    Select PDF paper size. Can be Letter, Legal, Tabloid, Ledger, A0, A1, A2,
    A3, A4, A5, A6 or a custom size. Custom size can be set in px (pixels), mm
    or in (inches) with width and height separated by space like this: 200 300,
    200px 300px, 200mm 300mm, 20cm 30cm or 6in 8in.
    """
    orientation: Optional[str] = None
    """PDF page orientation."""
    print_background: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "printBackground"},
    )
    """Enable or disable printing of document background"""
    header: Optional[str] = None
    """Set to HTML for header to be applied on every page at the header."""
    footer: Optional[str] = None
    """Set to HTML for footer to be applied on every page at the bottom."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """


@dataclass
class HtmlToPdfResponse:
    """
    Response for HTML to PDF
    """

    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class UrlToPdfInput:
    """
    URL to PDF
    """

    url: Optional[str] = None
    """
    Link to input HTML file or website page to be converted. You can pass link
    to file from Google Drive, Dropbox or another online file service that can
    generate shareable links.
    """
    margins: Optional[str] = None
    """
    Margins to be used in output document. Set to css style margins like 10px,
    5mm, 5in for all sides or 5px 5px 5px 5px (the order of margins is top,
    right, bottom, left).
    """
    paper_size: Optional[str] = field(
        default=None,
        metadata={"wire_name": "paperSize"},
    )
    """
    Letter is set by default. Can be Letter, Legal, Tabloid, Ledger, A0, A1,
    A2, A3, A4, A5, A6 or a custom size. Custom size can be set in px (pixels),
    mm or in (inches) with width and height separated by space like this: 200
    300, 200px 300px, 200mm 300mm, 20cm 30cm or 6in 8in.
    """
    orientation: Optional[str] = None
    """Output page orientation"""
    print_background: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "printBackground"},
    )
    """Enable or disable printing of document background"""
    header: Optional[str] = None
    """Set to HTML for header to be applied on every page at the header."""
    footer: Optional[str] = None
    """Set to HTML for footer to be applied on every page at the bottom."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """


@dataclass
class UrlToPdfResponse:
    """
    Response for URL to PDF
    """

    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class PdfFillerInput:
    """
    Add Text, Images, Form Fields to PDF
    """

    url: Optional[str] = None
    """Url to the input PDF document"""
    annotations_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationsString"},
    )
    """
    Annotation strings to be added to the PDF document. Format should be like
    \"x;y;page;text\". Refer to PDF.co documentations for more advanced formats
    and examples.
    """
    images_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "imagesString"},
    )
    """
    Images to be added to the PDF document. Format should be like
    \"x;y;page;urltoimage;link;width;height\". Refer to PDF.co documentations
    for more advanced formats and examples.
    """
    fields_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "fieldsString"},
    )
    """
    Fill PDF form fields. Format should be like \"page;fieldName;value\". Refer
    to PDF.co documentations for more advanced formats and examples.
    """
    name: Optional[str] = None
    """Output PDF Name"""
    inline: Optional[bool] = None
    """
    Determines whether to return output in response or link to output file.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """


@dataclass
class PdfFillerResponse:
    """
    Response for Add Text, Images, Form Fields to PDF
    """

    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class MergePdfSimplifiedInput:
    """
    Merge PDF
    """

    url: Optional[str] = None
    """
    The comma-separated list of links to PDFs files. Supports links to external
    sites, `filetoken://..` from PDF.co Files storage and links to G Drive,
    Dropbox and other services. Links should be accessible without password.
    For password protected PDF files, use PDF Security module first to unlock
    input pdf.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class MergePdfSimplifiedResponse:
    """
    Response for Merge PDF
    """

    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class MergePdfInput:
    """
    Merge PDF (Advanced)
    """

    url: Optional[str] = None
    """
    The comma-separated list of links to PDFs, documents, spreadsheets, images.
    Supports links to external sites, `filetoken://..` from PDF.co Files
    storage and links to G Drive, Dropbox and other services. Links should be
    accessible without password. For password protected PDF files, use PDF
    Security module first to unlock input pdf.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class MergePdfResponse:
    """
    Response for Merge PDF (Advanced)
    """

    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class SplitPdfInput:
    """
    Split PDF
    """

    url: Optional[str] = None
    """Url to the source PDF document"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to split into. First page
    is 1 (one). Example: \"1,2-5,7-\". Special cases: to extract each page into
    separate PDF use to `*`.
    """
    inline: Optional[bool] = None
    """
    Determines whether to return output in response or link to output file.
    """
    name: Optional[str] = None
    """Base file name for new PDF files"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """Creates a background processing job."""


@dataclass
class SplitPdfResponse:
    """
    Response for Split PDF
    """

    urls: Optional[List[str]] = None
    """urls"""
    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class SplitPdf2Input:
    """
    Split PDF (Advanced)
    """

    url: Optional[str] = None
    """Url to the source PDF document"""
    search_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "searchString"},
    )
    """
    Text to search for on pages. Can also search by barcode, for more info
    visit
    https://developer.pdf.co/api/pdf-split/index.html?utm_source=api-docs#post-tag-pdf-split2
    """
    exclude_key_pages: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "excludeKeyPages"},
    )
    """Set to true if you want to exclude pages where text was found"""
    regex_search: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "regexSearch"},
    )
    """Set to true to enable regular expressions for the search string"""
    case_sensitive: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "caseSensitive"},
    )
    """Set to true to enable case-sensitive search."""
    lang: Optional[str] = None
    """
    the language for OCR (text from image) to use for scanned PDF, PNG, and JPG
    documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is at
    https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    inline: Optional[bool] = None
    """
    Determines whether to return output in response or link to output file.
    """
    name: Optional[str] = None
    """Base file name for new PDF files"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """Creates a background processing job."""


@dataclass
class SplitPdf2Response:
    """
    Response for Split PDF (Advanced)
    """

    urls: Optional[List[str]] = None
    """urls"""
    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


@dataclass
class PDFSearchTextInput:
    """
    PDF Search Text
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    search_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "searchString"},
    )
    """
    Text to search can support regular expressions if you set the \"Regex
    Search\" parameter to true.
    """
    regex_search: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "regexSearch"},
    )
    name: Optional[str] = None
    """name"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. IMPORTANT: the
    very first page starts at 0 (zero). To set a range use the dash -, for
    example: 0,2-5,7-. To set a range from index to the last page use range
    like this: 2- (from page #3 as the index starts at zero and till the of the
    document) for ALL pages just leave this param empty. Example: 0,2-5,7-
    means the first page, then the 3rd page to the 6th page, and then the range
    from the 8th (index = 7) page till the end of the document, The input must
    be in string format.
    """
    inline: Optional[bool] = None
    word_matching_mode: Optional[str] = field(
        default=None,
        metadata={"wire_name": "wordMatchingMode"},
    )
    """Values can be either `SmartMatch', `ExactMatch', or `None'."""
    password: Optional[str] = None
    """Password of the PDF file"""
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFSearchTextResponse:
    """
    Response for PDF Search Text
    """

    body: Optional[List[Dict[str, Any]]] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class DocumentParserInput:
    """
    Document Parser
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage. To upload files via API, Check out the Files
    Upload section at https://developer.pdf.co/api/file-upload/index.html
    """
    template_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "templateId"},
    )
    """
    Set ID of document parser template to be used. View and manage your
    templates at https://app.pdf.co/document-parser
    """
    output_format: Optional[str] = field(
        default=None,
        metadata={"wire_name": "outputFormat"},
    )
    """
    Default is JSON. You can override the default output format to CSV or XML
    to generate CSV or XML output accordingly.
    """
    inline: Optional[bool] = None
    """
    Set to true to return results inside the response. Otherwise, the endpoint
    will return a link to the output file generated
    """
    password: Optional[str] = None
    """Password of PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    https://developer.pdf.co/api/profiles/index.html for profile examples,
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with \"Job Check\" to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class DocumentParserResponse:
    """
    Response for Document Parser
    """

    url: Optional[str] = None
    """Output URL"""
    body: Optional[Dict[str, Any]] = None
    """body"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    error: Optional[bool] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    status: Optional[int] = None
    name: Optional[str] = None
    credits: Optional[int] = None
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    duration: Optional[int] = None
    message: Optional[str] = None


@dataclass
class JobCheckInput:
    """
    Background Jobs Check
    """

    jobid: Optional[str] = None
    """PDF.co background Job Id"""


@dataclass
class JobCheckResponse:
    """
    Response for Background Jobs Check
    """

    status: Optional[str] = None
    """status"""
    message: Optional[str] = None
    """message"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    job_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "jobDuration"},
    )
    """jobDuration"""
    duration: Optional[int] = None
    """duration"""
    error: Optional[bool] = None
    """error"""
    error_code: Optional[int] = field(
        default=None,
        metadata={"wire_name": "errorCode"},
    )
    """errorCode"""


@dataclass
class BarcodeGeneratorInput:
    """
    Barcode Generator
    """

    name: Optional[str] = None
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with /job/check endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """
    value: Optional[str] = None
    """Set the string value to encode inside the barcode."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Set the barcode type, QRCode By default."""
    decoration_image: Optional[str] = field(
        default=None,
        metadata={"wire_name": "decorationImage"},
    )
    """
    Set this to the image that you want to be inserted the logo inside the
    QR-Code barcode.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck Module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co Knowledgebase
    https://developer.pdf.co/api/profiles/index.html for profile examples
    """


@dataclass
class BarcodeGeneratorResponse:
    """
    Response for Barcode Generator
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class BarcodeReaderInput:
    """
    Barcode Reader
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    types: Optional[str] = None
    """
    Set the barcode type, QRCode is default. You can also combine types by
    comma, example \"QRCode,Code128,Code39,Interleaved2of5,EAN13\". All
    supported barcode types are AustralianPostCode, Aztec, BorderedTable,
    Checkbox, Circle, CircularI2of5, Codabar, CodablockF, Code128, Code16K,
    Code39, Code39Extended, Code39Mod43, Code39Mod43Extended, Code93,
    DataMatrix, DPMDataMatrix, EAN13, EAN2, EAN5, EAN8, GS1,
    GS1DataBarExpanded, GS1DataBarExpandedStacked, GS1DataBarLimited,
    GS1DataBarOmnidirectional, GS1DataBarStacked, GTIN12, GTIN13, GTIN14,
    GTIN8, HorizontalLine, IntelligentMail, Interleaved2of5, ITF14, MaxiCode,
    MICR, MicroPDF, MSI, Oval, PatchCode, PDF417, Pharmacode, PostNet, PZN,
    QRCode, Rectangle, RoyalMail, RoyalMailKIX, Segment, Trioptic,
    UnderlinedField, UPCA, UPCE, UPU, VerticalLine.
    """
    pages: Optional[str] = None
    """
    Comma-Separated list of page indices (or ranges) to process. IMPORTANT: the
    very first page starts at 0 (zero). To set a range use the dash -, for
    example: 0,2-5,7-. To set a range from the index to the last page use range
    like this: 2- (from page #3 as the index starts at zero and till the end of
    the document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means the first page, then the 3rd page to the 6th page, and then the range
    from the 8th (index = 7) page till the end of the document.
    """
    encrypt: Optional[bool] = None
    """encrypt"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class BarcodeReaderResponse:
    """
    Response for Barcode Reader
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    barcodes: Optional[List[Dict[str, Any]]] = None
    """barcodes"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFInfoReaderInput:
    """
    PDF Info Reader
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck module to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configuration for fine-tuning and
    extra options, Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFInfoReaderResponse:
    """
    Response for PDF Info Reader
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    info: Optional[Dict[str, Any]] = None
    """info"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFormsInfoReaderInput:
    """
    PDF Forms Info Reader
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck module to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configuration for fine-tuning and
    extra options, Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFormsInfoReaderResponse:
    """
    Response for PDF Forms Info Reader
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    info: Optional[Dict[str, Any]] = None
    """info"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFindTableInput:
    """
    PDF Find Table
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. IMPORTANT: the
    very first page starts at 0 (zero). To set a range use the dash -, for
    example: 0,2-5,7-. To set a range from the index to the last page use range
    like this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    inline: Optional[bool] = None
    password: Optional[str] = None
    """Password of PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with /job/check endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFFindTableResponse:
    """
    Response for PDF Find Table
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    body: Optional[Dict[str, Any]] = None
    """body"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """Credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class SearchAndReplaceInput:
    """
    PDF Search and Replace Text
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    search_strings: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "searchStrings"},
    )
    """Search Strings"""
    replace_strings: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "replaceStrings"},
    )
    """Replacement Strings"""
    case_sensitive: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "caseSensitive"},
    )
    replacement_limit: Optional[int] = field(
        default=None,
        metadata={"wire_name": "replacementLimit"},
    )
    """
    Limit the number of searches & replacements for every item. The default
    value is 0 which means unlimited searches and replacements so every found
    occurrence will be replaced.
    """
    regex: Optional[bool] = None
    """Set to true to use regular expression for a search string(s)."""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    password: Optional[str] = None
    """The password of the PDF file"""
    name: Optional[str] = None
    """File name for the generated output"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class SearchAndReplaceResponse:
    """
    Response for PDF Search and Replace Text
    """

    url: Optional[str] = None
    """url"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None


@dataclass
class SearchAndReplaceWithImageInput:
    """
    PDF Search and Replace Text with Image
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    case_sensitive: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "caseSensitive"},
    )
    search_string: Optional[str] = field(
        default=None,
        metadata={"wire_name": "searchString"},
    )
    """Single text replacement. Word or phrase to be replaced"""
    replace_image: Optional[str] = field(
        default=None,
        metadata={"wire_name": "replaceImage"},
    )
    """Image URL to be inserted in the doc."""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with Job Check module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish..
    """
    regex: Optional[bool] = None
    replacement_limit: Optional[int] = field(
        default=None,
        metadata={"wire_name": "replacementLimit"},
    )
    """
    Limit the number of searches & replacements for every item. The default
    value is 0 which means unlimited searches and replacements so every found
    occurrence will be replaced.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    password: Optional[str] = None
    """Password of the PDF file"""


@dataclass
class SearchAndReplaceWithImageResponse:
    """
    Response for PDF Search and Replace Text with Image
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    url: Optional[str] = None
    """url"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class SearchAndDeleteTextInput:
    """
    PDF Search and Delete Text from PDF
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    search_strings: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "searchStrings"},
    )
    """searchStrings"""
    case_sensitive: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "caseSensitive"},
    )
    regex: Optional[bool] = None
    """Set to true to use regular expression for a search string(s)."""
    replacement_limit: Optional[int] = field(
        default=None,
        metadata={"wire_name": "replacementLimit"},
    )
    """
    Limit the number of searches & replacements for every item. The default
    value is 0 which means unlimited searches and replacements so every found
    occurrence will be replaced.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """Password of the PDF file"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class SearchAndDeleteTextResponse:
    """
    Response for PDF Search and Delete Text from PDF
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFSearchableInput:
    """
    Make Scanned PDF to Text Searchable
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    the language for OCR (text from image) to use for scanned PDF, PNG, and JPG
    documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is at
    https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    password: Optional[str] = None
    """Password of PDF file"""
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set Async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFSearchableResponse:
    """
    Response for Make Scanned PDF to Text Searchable
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFUnSearchableInput:
    """
    PDF to Scanned PDF
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    password: Optional[str] = None
    """Password of PDF file"""
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set Async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck module to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFUnSearchableResponse:
    """
    Response for PDF to Scanned PDF
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToCSVInput:
    """
    PDF To CSV
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    inline: Optional[bool] = None
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToCSVResponse:
    """
    Response for PDF To CSV
    """

    body: Optional[str] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToJSONInput:
    """
    PDF To JSON
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    inline: Optional[bool] = None
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToJSONResponse:
    """
    Response for PDF To JSON
    """

    body: Optional[Dict[str, Any]] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToJSONMetaInput:
    """
    PDF To JSON (AI Powered)
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    inline: Optional[bool] = None
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToJSONMetaResponse:
    """
    Response for PDF To JSON (AI Powered)
    """

    body: Optional[Dict[str, Any]] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToTextInput:
    """
    PDF To Text (with layout and ocr)
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    inline: Optional[bool] = None
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToTextResponse:
    """
    Response for PDF To Text (with layout and ocr)
    """

    body: Optional[str] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToTextSimpleInput:
    """
    PDF To Text (without layout and ocr)
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """


@dataclass
class PDFToTextSimpleResponse:
    """
    Response for PDF To Text (without layout and ocr)
    """

    body: Optional[str] = None
    """body"""
    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToXLSInput:
    """
    PDF To XLS
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToXLSResponse:
    """
    Response for PDF To XLS
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToXLSXInput:
    """
    PDF To XLSX
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToXLSXResponse:
    """
    Response for PDF To XLSX
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToXMLInput:
    """
    PDF To XML
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    lang: Optional[str] = None
    """
    Set the language for OCR (text from image) to use for scanned PDF, PNG, and
    JPG documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is here
    (https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    unwrap: Optional[str] = None
    """
    Unwrap lines into a single line within table cells when lineGrouping is
    enabled.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    password: Optional[str] = None
    """password"""
    line_grouping: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lineGrouping"},
    )
    """Line grouping within table cells. Set to 1 to enable the grouping."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    This parameter can be used to set additional configurations for fine-tuning
    and to enable more options. Visit PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples and
    more.
    """


@dataclass
class PDFToXMLResponse:
    """
    Response for PDF To XML
    """

    url: Optional[str] = None
    """url"""
    body: Optional[str] = None
    """body"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToJPGInput:
    """
    PDF to JPG
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    """inline"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFToJPGResponse:
    """
    Response for PDF to JPG
    """

    urls: Optional[List[str]] = None
    """urls"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToPNGInput:
    """
    PDF to PNG
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    """inline"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFToPNGResponse:
    """
    Response for PDF to PNG
    """

    urls: Optional[List[str]] = None
    """urls"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToWEBPInput:
    """
    PDF to WEBP
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    """inline"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFToWEBPResponse:
    """
    Response for PDF to WEBP
    """

    urls: Optional[List[str]] = None
    """urls"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFToTIFFInput:
    """
    PDF to TIFF
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    inline: Optional[bool] = None
    """inline"""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    rect: Optional[str] = None
    """
    Defines coordinates for extraction, e.g. 51.8, 114.8, 235.5, 204.0. Use
    PDF.co PDF Edit Add Helper (https://app.pdf.co/pdf-edit-add-helper) to get
    or measure pdf coordinates.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFToTIFFResponse:
    """
    Response for PDF to TIFF
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFromCSVInput:
    """
    PDF from CSV
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFromCSVResponse:
    """
    Response for PDF from CSV
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFromDocInput:
    """
    PDF from Document (Doc, DocX, RTF, TXT, XPS)
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFromDocResponse:
    """
    Response for PDF from Document (Doc, DocX, RTF, TXT, XPS)
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFromImagesInput:
    """
    PDF from Image
    """

    url: Optional[str] = None
    """
    URL to the source file. Provide multiple URLS in comma separated format.
    Supports links from Google Drive, Dropbox, and PDF.co built-in files
    storage.
    """
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFromImagesResponse:
    """
    Response for PDF from Image
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFromEmailInput:
    """
    PDF from Email
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    embed_attachments: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "embedAttachments"},
    )
    """
    Set to true to automatically embeds all attachments from original input
    email MSG or EML files into the final output PDF. Set it to false if you
    don't want to embed attachments so it will convert only the body of the
    input email.
    """
    convert_attachments: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "convertAttachments"},
    )
    """
    Set to false if you don't want to convert attachments from the original
    email and want to embed them as original files (as embedded pdf
    attachments). Converts attachments that are supported by API (DOC, DOCx,
    HTML, PNG, JPG .etc) into PDF and merges into output final PDF.
    Non-supported file types are added as PDF attachments (Adobe Reader or
    another viewer may be required to view PDF attachments).
    """
    margins: Optional[str] = None
    """
    set to CSS style margins like 10px, 5mm, 5in for all sides or 5px 5px 5px
    5px (the order of margins is top, right, bottom, left).
    """
    paper_size: Optional[str] = field(
        default=None,
        metadata={"wire_name": "paperSize"},
    )
    """
    Letter is set by default. Can be Letter, Legal, Tabloid, Ledger, A0, A1,
    A2, A3, A4, A5, A6 or a custom size. Custom size can be set in px (pixels),
    mm or in (inches) with width and height separated by space like this: 200
    300, 200px 300px, 200mm 300mm, 20cm 30cm or 6in 8in.
    """
    orientation: Optional[str] = None
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFromEmailResponse:
    """
    Response for PDF from Email
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFAddSecurityInput:
    """
    PDF Add Security
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    owner_password: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ownerPassword"},
    )
    """
    The main owner password that is used for document encryption and for
    setting/removing restrictions.
    """
    user_password: Optional[str] = field(
        default=None,
        metadata={"wire_name": "userPassword"},
    )
    """
    The optional user password will be asked for viewing and printing document.
    """
    encryption_algorithm: Optional[str] = field(
        default=None,
        metadata={"wire_name": "encryptionAlgorithm"},
    )
    """
    Encryption algorithm. Valid values: RC4_40bit, RC4_128bit, AES_128bit,
    AES_256bit. AES_128bit or higher is recommended
    """
    allow_accessibility_support: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowAccessibilitySupport"},
    )
    """
    Allow or prohibit content extraction for accessibility needs. Note: this
    restriction applies when userPassword (if any) is entered. This restriction
    does not apply if the user enters Owner Password.
    """
    allow_assembly_document: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowAssemblyDocument"},
    )
    """
    Allow or prohibit assembling the document. Note: this restriction applies
    when userPassword (if any) is entered. This restriction does not apply if
    the user enters Owner Password.
    """
    allow_print_document: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowPrintDocument"},
    )
    """
    Allow or prohibit printing PDF document. Note: this restriction applies
    when userPassword (if any) is entered. This restriction does not apply if
    the user enters Owner Password.
    """
    allow_fill_forms: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowFillForms"},
    )
    """
    Allow or prohibit the filling of interactive form fields (including
    signature fields) in the PDF documents. Note: this restriction applies when
    userPassword (if any) is entered. This restriction does not apply if the
    user enters Owner Password.
    """
    allow_modify_document: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowModifyDocument"},
    )
    """
    Allow or prohibit modification of PDF document. Note: this restriction
    applies when userPassword (if any) is entered. This restriction does not
    apply if the user enters Owner Password.
    """
    allow_content_extraction: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowContentExtraction"},
    )
    """
    Allow or prohibit copying content from PDF document. Note: this restriction
    applies when userPassword (if any) is entered. This restriction does not
    apply if the user enters Owner Password.
    """
    allow_modify_annotations: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowModifyAnnotations"},
    )
    """
    Allow or prohibit interacting with text annotations and forms in PDF
    document. Note: this restriction applies when userPassword (if any) is
    entered. This restriction does not apply if the user enters Owner Password.
    """
    print_quality: Optional[str] = field(
        default=None,
        metadata={"wire_name": "printQuality"},
    )
    """
    Allowed printing quality. Valid values: HighResolution, LowResolution.
    Note: this restriction applies when userPassword (if any) is entered. This
    restriction does not apply if the user enters Owner Password.
    """
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFAddSecurityResponse:
    """
    Response for PDF Add Security
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFSecurityRemoveInput:
    """
    PDF Remove Security
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    password: Optional[str] = None
    """The owner/user password to open files and remove security features."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFSecurityRemoveResponse:
    """
    Response for PDF Remove Security
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFFromXLSXLSXInput:
    """
    PDF From Spreadsheet
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class PDFFromXLSXLSXResponse:
    """
    Response for PDF From Spreadsheet
    """

    url: Optional[str] = None
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class XLStoCSVInput:
    """
    Spreadsheet to CSV
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    quotation_symbol: Optional[str] = field(
        default=None,
        metadata={"wire_name": "quotationSymbol"},
    )
    """Set the CSV Quotation Symbol"""
    separator_symbol: Optional[str] = field(
        default=None,
        metadata={"wire_name": "separatorSymbol"},
    )
    """Set the CSV Separator Symbol"""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class XLStoCSVResponse:
    """
    Response for Spreadsheet to CSV
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class XLStoJSONInput:
    """
    Spreadsheet to JSON
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class XLStoJSONResponse:
    """
    Response for Spreadsheet to JSON
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class XLStoHTMLInput:
    """
    Spreadsheet to HTML
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class XLStoHTMLResponse:
    """
    Response for Spreadsheet to HTML
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class XLStoTXTInput:
    """
    Spreadsheet to TXT
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class XLStoTXTResponse:
    """
    Response for Spreadsheet to TXT
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class XLStoXMLInput:
    """
    Spreadsheet to XML
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    worksheet_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "worksheetIndex"},
    )
    """Set the WorkSheet index (Default is 0)."""
    name: Optional[str] = None
    """File name for the generated output."""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set to true for long processes to run in the background, API will then
    return a jobId which you can use with JobCheck endpoint to check the status
    of the process and retrieve the output while you can proceed with other
    tasks without waiting for this process to finish.
    """
    profiles: Optional[str] = None
    """
    You can set additional and extra options using this parameter that allows
    you to set custom configuration. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """


@dataclass
class XLStoXMLResponse:
    """
    Response for Spreadsheet to XML
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFRotatePagesInput:
    """
    Rotate PDF Pages
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    angle: Optional[int] = None
    """Angle in degrees. Supported values 90, 180, and 270."""
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFRotatePagesResponse:
    """
    Response for Rotate PDF Pages
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFAutoRotatePagesInput:
    """
    Rotate PDF Pages (Auto)
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    lang: Optional[str] = None
    """
    the language for OCR (text from image) to use for scanned PDF, PNG, and JPG
    documents input when extracting text. The default is \"eng\". Other
    languages are also supported: deu, spa, chi_sim, jpn, and many others (full
    list of supported OCR languages is at
    https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#language-support).
    You can also use 2 languages simultaneously like this: eng+deu or jpn+kor
    (any combination).
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFAutoRotatePagesResponse:
    """
    Response for Rotate PDF Pages (Auto)
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFDeletePagesInput:
    """
    PDF Delete Pages
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    pages: Optional[str] = None
    """
    Comma-separated list of page indices (or ranges) to process. The very first
    page starts at 0 (zero). To set a range use the dash -, for example:
    0,2-5,7-. To set a range from the index to the last page use range like
    this: 2- (from page #3 as the index starts at zero and till the of the
    document). For ALL pages just leave this param empty. Example: 0,2-5,7-
    means first page, then 3rd page to 6th page, and then the range from 8th
    (index = 7) page till the end of the document.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFDeletePagesResponse:
    """
    Response for PDF Delete Pages
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFCompressInput:
    """
    PDF Compress and Optimize
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    name: Optional[str] = None
    """File name for the generated output"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFCompressResponse:
    """
    Response for PDF Compress and Optimize
    """

    url: Optional[str] = None
    """url"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFClassifierInput:
    """
    Document Classifier
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    rulescsv: Optional[str] = None
    """
    Define custom classification rules in CSV format. Rules are in CSV format
    where each row contains: \"class name, logic (AND or OR (default)), and
    keywords\" separated by a comma. Each row is separated by the \n symbol.
    """
    rulescsvurl: Optional[str] = None
    """
    Instead of inline CSV you can use this parameter and set the URL to a CSV
    file with classification rules. This is useful if you have a separate
    developer working on CSV rules.
    """
    case_sensitive: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "caseSensitive"},
    )
    """Defines if keywords in rules are case-sensitive or not."""
    inline: Optional[bool] = None
    """
    Set to true to return results inside the response. Otherwise, the endpoint
    will return a link to the output file generated.
    """
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    password: Optional[str] = None
    """Password of the PDF file"""
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class PDFClassifierResponse:
    """
    Response for Document Classifier
    """

    url: Optional[str] = None
    """url"""
    body: Optional[Dict[str, Any]] = None
    """body"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """pageCount"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class EmailSendInput:
    """
    Send Email with Attachments
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    from_: Optional[str] = field(default=None, metadata={"wire_name": "from"})
    """
    The \"From\" field with sender name and email. Example: John Doe
    <john@example.com>
    """
    to: Optional[str] = None
    """
    The \"To\" field with receiver name and email. Example: Partner
    <partner@example.com>
    """
    subject: Optional[str] = None
    """The subject for the outgoing email"""
    bodytext: Optional[str] = None
    """Plain text version of the outgoing email message."""
    bodyhtml: Optional[str] = None
    """HTML version of the outgoing email message."""
    smtpserver: Optional[str] = None
    """SMTP server address"""
    smtpport: Optional[str] = None
    """SMTP port"""
    smtpusername: Optional[str] = None
    """SMTP server username"""
    smtppassword: Optional[str] = None
    """
    SMTP server password, If you use Gmail then you need to generate an
    app-specific password as instructed at
    https://support.google.com/accounts/answer/185833.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class EmailSendResponse:
    """
    Response for Send Email with Attachments
    """

    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class EmailDecodeInput:
    """
    Email Information
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class EmailDecodeResponse:
    """
    Response for Email Information
    """

    url: Optional[str] = None
    """url"""
    body: Optional[Dict[str, Any]] = None
    """body"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class EmailAttachmentExtractionInput:
    """
    Email Attachment Extraction
    """

    url: Optional[str] = None
    """
    URL to the source file. Supports links from Google Drive, Dropbox, and
    PDF.co built-in files storage.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """
    Set async to true for long processes to run in the background, API will
    then return a jobId which you can use with JobCheck endpoint to check the
    status of the process and retrieve the output while you can proceed with
    other tasks without waiting for this process to finish.
    """


@dataclass
class EmailAttachmentExtractionResponse:
    """
    Response for Email Attachment Extraction
    """

    url: Optional[str] = None
    """url"""
    body: Optional[Dict[str, Any]] = None
    """body"""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    error: Optional[bool] = None
    """error"""
    status: Optional[int] = None
    """status"""
    name: Optional[str] = None
    """name"""
    credits: Optional[int] = None
    """credits"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """remainingCredits"""
    duration: Optional[int] = None
    """duration"""


@dataclass
class PDFAttachmentExtractionInput:
    """
    PDF Attachment Extraction
    """

    url: Optional[str] = None
    """Url to the source PDF document"""
    inline: Optional[bool] = None
    """
    Determines whether to return output in response or link to output file.
    """
    name: Optional[str] = None
    """Base file name for new PDF files"""
    expiration: Optional[int] = None
    """
    Set the expiration time for the output link in minutes (default is 60 i.e
    60 minutes or 1 hour), After this specified duration, any generated output
    file(s) will be automatically deleted from PDF.co temporary files storage.
    The maximum duration for link expiration varies based on your current
    subscription plan.
    """
    profiles: Optional[str] = None
    """
    Use this parameter to set additional configurations for fine-tuning and
    extra options. Explore PDF.co knowledgebase
    (https://developer.pdf.co/api/profiles/index.html) for profile examples.
    """
    async_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "async"},
    )
    """Creates a background processing job."""


@dataclass
class PDFAttachmentExtractionResponse:
    """
    Response for PDF Attachment Extraction
    """

    urls: Optional[List[str]] = None
    """urls"""
    url: Optional[str] = None
    """Output document URL."""
    job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "jobId"},
    )
    """jobId"""
    page_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageCount"},
    )
    """Number of pages in the output document."""
    error: Optional[bool] = None
    """Indicates whether the operation is successful or not."""
    status: Optional[int] = None
    """Status code for HTTP response"""
    name: Optional[str] = None
    """Output document name"""
    credits: Optional[int] = None
    """PDF.co credits used for this request"""
    duration: Optional[int] = None
    """Request execution duration"""
    remaining_credits: Optional[int] = field(
        default=None,
        metadata={"wire_name": "remainingCredits"},
    )
    """PDF.co account remaining credits"""


# Client Class

class PdfcoClient(ConnectorClientBase):
    """Typed client for pdfco connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a PdfcoClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "pdfco"

    async def html_to_pdf_async(
        self,
        input: HtmlToPdfInput,
    ) -> dict[str, Any] | None:
        """
        HTML to PDF

        Convert HTML code snippet into full featured PDF. Please see api
        documentation at https://developer.pdf.co/api/pdf-to-html/index.html
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/convert/from/html"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def url_to_pdf_async(
        self,
        input: UrlToPdfInput,
    ) -> dict[str, Any] | None:
        """
        URL to PDF

        Create PDF from URL. Please see api documentation at
        https://developer.pdf.co/api/pdf-from-url/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/from/url"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def pdf_filler_async(
        self,
        input: PdfFillerInput,
    ) -> dict[str, Any] | None:
        """
        Add Text, Images, Form Fields to PDF

        Add text, images, signatures to pdf. Fill PDF form fields and create
        new pdf from templates. Please see api documentation at
        https://developer.pdf.co/api/pdf-add/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/edit/add"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def merge_pdf_simplified_async(
        self,
        input: MergePdfSimplifiedInput,
    ) -> dict[str, Any] | None:
        """
        Merge PDF

        Merge PDF from two or more PDF files into a new one. Please see api
        documentation at https://developer.pdf.co/api/pdf-merge/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/merge"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def merge_pdf_async(
        self,
        input: MergePdfInput,
    ) -> dict[str, Any] | None:
        """
        Merge PDF (Advanced)

        Merge PDF from two or more pdf, doc, xls, images, even zip with
        documents and images into a new PDF. Supports zip, doc, docx, xls,
        xlsx, rtf, txt, png, jpg. Please see api documentation at
        https://developer.pdf.co/api/pdf-merge/index.html#post-tag-pdf-merge2
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/merge2"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def split_pdf_async(
        self,
        input: SplitPdfInput,
    ) -> dict[str, Any] | None:
        """
        Split PDF

        Splits pages from PDF document into multiple new PDF files using page
        numbers and page ranges. Please see api documentation at
        https://developer.pdf.co/api/pdf-split/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/split"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def split_pdf2_async(
        self,
        input: SplitPdf2Input,
    ) -> dict[str, Any] | None:
        """
        Split PDF (Advanced)

        Split PDF into multiple PDF files by text search (support regular
        expressions) or barcode. Please see api documentation at
        https://developer.pdf.co/api/pdf-split/index.html#post-tag-pdf-split2
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/split2"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_search_text_async(
        self,
        input: PDFSearchTextInput,
    ) -> dict[str, Any] | None:
        """
        PDF Search Text

        Search text in PDF and get coordinates. Supports regular expressions.
        Please see api documentation at
        https://developer.pdf.co/api/pdf-find/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/find"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def document_parser_async(
        self,
        input: DocumentParserInput,
    ) -> dict[str, Any] | None:
        """
        Document Parser

        Document Parser can automatically parse PDF, JPG, and PNG documents to
        extract fields, tables, values, and barcodes from invoices, statements,
        orders, and other PDF and scanned documents. Please see api
        documentation at
        https://developer.pdf.co/api/document-parser/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/documentparser"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def job_check_async(
        self,
        input: JobCheckInput,
    ) -> dict[str, Any] | None:
        """
        Background Jobs Check

        Checks status of background job that was previously created with PDF.co
        API. Please see api documentation at
        https://developer.pdf.co/api/background-job-check/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/job/check"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def barcode_generator_async(
        self,
        input: BarcodeGeneratorInput,
    ) -> dict[str, Any] | None:
        """
        Barcode Generator

        Generate high quality barcode images. Supports QR Code, Datamatrix,
        Code 39, Code 128, PDF417 and many other barcode types. Please see api
        documentation at
        https://developer.pdf.co/api/barcode-generator/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/barcode/generate"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def barcode_reader_async(
        self,
        input: BarcodeReaderInput,
    ) -> dict[str, Any] | None:
        """
        Barcode Reader

        Read barcodes from images and PDF. Can read all popular barcode types
        from QR Code and Code 128, EAN to Datamatrix, PDF417, GS1 and many
        other barcodes. Please see api documentation at
        https://developer.pdf.co/api/barcode-reader/index.html for
        comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/barcode/read/from/url"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_info_reader_async(
        self,
        input: PDFInfoReaderInput,
    ) -> dict[str, Any] | None:
        """
        PDF Info Reader

        Get detailed information about the PDF document, it's properties and
        security permissions. Please see api documentation at
        https://developer.pdf.co/api/pdf-info-reader/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/info"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_forms_info_reader_async(
        self,
        input: PDFFormsInfoReaderInput,
    ) -> dict[str, Any] | None:
        """
        PDF Forms Info Reader

        Get information about fillable fields inside PDF form file. Please see
        api documentation at
        https://developer.pdf.co/api/pdf-forms-info-reader/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/info/fields"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_find_table_async(
        self,
        input: PDFFindTableInput,
    ) -> dict[str, Any] | None:
        """
        PDF Find Table

        AI powered document analysis can scan your document for tables and
        return the array of tables on pages with coordinates and information
        about columns detected in these tables. Please see api documentation at
        https://developer.pdf.co/api/pdf-find/index.html#post-tag-pdf-find-table
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/find/table"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def search_and_replace_async(
        self,
        input: SearchAndReplaceInput,
    ) -> dict[str, Any] | None:
        """
        PDF Search and Replace Text

        Modify a PDF file by searching for specific text and replacing it with
        new text. Please see api documentation at
        https://developer.pdf.co/api/pdf-search-and-replace-text/index.html#post-tag-pdf-edit-replace-text
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/edit/replace-text"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def search_and_replace_with_image_async(
        self,
        input: SearchAndReplaceWithImageInput,
    ) -> dict[str, Any] | None:
        """
        PDF Search and Replace Text with Image

        Replace specific text in a PDF document with an image. Please see api
        documentation at
        https://developer.pdf.co/api/pdf-search-and-replace-text/index.html#post-tag-pdf-edit-replace-text-with-image
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v1/pdf/edit/replace-text-with-image"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def search_and_delete_text_async(
        self,
        input: SearchAndDeleteTextInput,
    ) -> dict[str, Any] | None:
        """
        PDF Search and Delete Text from PDF

        Search text in PDF and remove found text from PDF. Please see api
        documentation at
        https://developer.pdf.co/api/pdf-search-and-delete-text/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/edit/delete-text"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_searchable_async(
        self,
        input: PDFSearchableInput,
    ) -> dict[str, Any] | None:
        """
        Make Scanned PDF to Text Searchable

        Turn PDF and scanned JPG, PNG images into text-searchable PDF. Please
        see api documentation at
        https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#post-tag-pdf-makesearchable
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/makesearchable"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_un_searchable_async(
        self,
        input: PDFUnSearchableInput,
    ) -> dict[str, Any] | None:
        """
        PDF to Scanned PDF

        Convert PDF to Scanned PDF. Result PDF is same as if you printed and
        then scanned your PDF. Please see api documentation at
        https://developer.pdf.co/api/pdf-make-text-searchable-or-unsearchable/index.html#post-tag-pdf-makeunsearchable
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/makeunsearchable"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_c_s_v_async(
        self,
        input: PDFToCSVInput,
    ) -> dict[str, Any] | None:
        """
        PDF To CSV

        Convert PDF and scanned images into CSV representation with layout,
        columns, rows, and tables. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-csv/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/csv"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_j_s_o_n_async(
        self,
        input: PDFToJSONInput,
    ) -> dict[str, Any] | None:
        """
        PDF To JSON

        Convert PDF and scanned images into JSON representation with text,
        fonts, images, vectors, and formatting preserved. Please see api
        documentation at https://developer.pdf.co/api/pdf-to-json/index.html
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/json2"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_j_s_o_n_meta_async(
        self,
        input: PDFToJSONMetaInput,
    ) -> dict[str, Any] | None:
        """
        PDF To JSON (AI Powered)

        Convert PDF and scanned images into JSON representation with text,
        fonts, images, vectors, and formatting preserved. Please see api
        documentation at
        https://developer.pdf.co/api/pdf-to-json/index.html#post-tag-pdf-convert-to-json-meta
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/convert/to/json-meta"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_text_async(
        self,
        input: PDFToTextInput,
    ) -> dict[str, Any] | None:
        """
        PDF To Text (with layout and ocr)

        Convert PDF and scanned images to Text with layout preserved. Please
        see api documentation at
        https://developer.pdf.co/api/pdf-to-text/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/text"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_text_simple_async(
        self,
        input: PDFToTextSimpleInput,
    ) -> dict[str, Any] | None:
        """
        PDF To Text (without layout and ocr)

        Convert PDF to plain text without layout preserved and OCR support.
        Please see api documentation at
        https://developer.pdf.co/api/pdf-to-text/index.html#post-tag-pdf-convert-to-text-simple
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/convert/to/text-simple"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_x_l_s_async(
        self,
        input: PDFToXLSInput,
    ) -> dict[str, Any] | None:
        """
        PDF To XLS

        Convert PDF and scanned images to spreadsheet with layout and fonts
        preserved. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-excel/index.html#post-tag-pdf-convert-to-xls
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/xls"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_x_l_s_x_async(
        self,
        input: PDFToXLSXInput,
    ) -> dict[str, Any] | None:
        """
        PDF To XLSX

        Convert PDF and scanned images to spreadsheet with layout and fonts
        preserved. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-excel/index.html#post-tag-pdf-convert-to-xlsx
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/xlsx"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_x_m_l_async(
        self,
        input: PDFToXMLInput,
    ) -> dict[str, Any] | None:
        """
        PDF To XML

        Convert PDF to XML with information about text value, tables, fonts,
        images, objects positions. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-xml/index.html for comprehensive
        guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/xml"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_j_p_g_async(
        self,
        input: PDFToJPGInput,
    ) -> dict[str, Any] | None:
        """
        PDF to JPG

        PDF to JPG conversion. High-quality rendering. Also works great for
        thumbnail generation and previews. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-image/index.html#post-tag-pdf-convert-to-jpg
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/jpg"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_p_n_g_async(
        self,
        input: PDFToPNGInput,
    ) -> dict[str, Any] | None:
        """
        PDF to PNG

        PDF to PNG conversion. High-quality rendering. Also works great for
        thumbnail generation and previews. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-image/index.html#post-tag-pdf-convert-to-png
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/png"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_w_e_b_p_async(
        self,
        input: PDFToWEBPInput,
    ) -> dict[str, Any] | None:
        """
        PDF to WEBP

        PDF to WEBP conversion. High-quality rendering. Also works great for
        thumbnail generation and previews. Please see api documentation at
        https://developer.pdf.co/api/pdf-to-image/index.html#post-tag-pdf-convert-to-webp
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/webp"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_to_t_i_f_f_async(
        self,
        input: PDFToTIFFInput,
    ) -> dict[str, Any] | None:
        """
        PDF to TIFF

        PDF to high quality TIFF images conversion. High quality rendering.
        Also works great for thumbnail generation and previews. Please see api
        documentation at
        https://developer.pdf.co/api/pdf-to-image/index.html#post-tag-pdf-convert-to-tiff
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/to/tiff"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_from_c_s_v_async(
        self,
        input: PDFFromCSVInput,
    ) -> dict[str, Any] | None:
        """
        PDF from CSV

        Convert CSV document to PDF. Works also with XLS or XLSX input. Please
        see api documentation at
        https://developer.pdf.co/api/pdf-from-document/index.html#post-tag-pdf-convert-from-csv
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/from/csv"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_from_doc_async(
        self,
        input: PDFFromDocInput,
    ) -> dict[str, Any] | None:
        """
        PDF from Document (Doc, DocX, RTF, TXT, XPS)

        Convert DOC, DOCX, RTF, TXT, XPS files into PDF. Please see api
        documentation at
        https://developer.pdf.co/api/pdf-from-document/index.html#post-tag-pdf-convert-from-doc
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/convert/from/doc"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_from_images_async(
        self,
        input: PDFFromImagesInput,
    ) -> dict[str, Any] | None:
        """
        PDF from Image

        Create a PDF file from one or more JPG, PNG, TIFF images. Please see
        api documentation at
        https://developer.pdf.co/api/pdf-from-image/index.html for
        comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/convert/from/image"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_from_email_async(
        self,
        input: PDFFromEmailInput,
    ) -> dict[str, Any] | None:
        """
        PDF from Email

        Convert email files (MSG or EML) code into PDF. Extracts attachments
        (if any) from input email and embeds into PDF as pdf attachments.
        Please see api documentation at
        https://developer.pdf.co/api/pdf-from-email/index.html for
        comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/convert/from/email"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_add_security_async(
        self,
        input: PDFAddSecurityInput,
    ) -> dict[str, Any] | None:
        """
        PDF Add Security

        Add password and security limitations to existing PDF. Please see api
        documentation at
        https://developer.pdf.co/api/password-and-security/index.html#post-tag-pdf-security-add
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/security/add"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_security_remove_async(
        self,
        input: PDFSecurityRemoveInput,
    ) -> dict[str, Any] | None:
        """
        PDF Remove Security

        Remove password and security from existing PDF files. Please see api
        documentation at
        https://developer.pdf.co/api/password-and-security/index.html#post-tag-pdf-security-remove
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/security/remove"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_from_x_l_s_x_l_s_x_async(
        self,
        input: PDFFromXLSXLSXInput,
    ) -> dict[str, Any] | None:
        """
        PDF From Spreadsheet

        Create PDF from XLS, XLSX spreadsheet. Please see api documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-pdf
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/pdf"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def x_l_sto_c_s_v_async(
        self,
        input: XLStoCSVInput,
    ) -> dict[str, Any] | None:
        """
        Spreadsheet to CSV

        Convert XLS/XLSX into CSV comma separated values file. Please see api
        documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-csv
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/csv"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def x_l_sto_j_s_o_n_async(
        self,
        input: XLStoJSONInput,
    ) -> dict[str, Any] | None:
        """
        Spreadsheet to JSON

        Convert XLS/XLSX into JSON file. Please see api documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-json
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/json"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def x_l_sto_h_t_m_l_async(
        self,
        input: XLStoHTMLInput,
    ) -> dict[str, Any] | None:
        """
        Spreadsheet to HTML

        Convert XLS/XLSX into HTML file. Please see api documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-html
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/html"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def x_l_sto_t_x_t_async(
        self,
        input: XLStoTXTInput,
    ) -> dict[str, Any] | None:
        """
        Spreadsheet to TXT

        Convert XLS/XLSX or CSV into Text file. Please see api documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-txt
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/txt"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def x_l_sto_x_m_l_async(
        self,
        input: XLStoXMLInput,
    ) -> dict[str, Any] | None:
        """
        Spreadsheet to XML

        Convert XLS/XLSX or CSV into XML file. Please see api documentation at
        https://developer.pdf.co/api/convert-excel/index.html#post-tag-xls-convert-to-xml
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/xls/convert/to/xml"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_rotate_pages_async(
        self,
        input: PDFRotatePagesInput,
    ) -> dict[str, Any] | None:
        """
        Rotate PDF Pages

        Rotate PDF Pages. Please see api documentation at
        https://developer.pdf.co/api/pdf-rotate-pages/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/edit/rotate"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_auto_rotate_pages_async(
        self,
        input: PDFAutoRotatePagesInput,
    ) -> dict[str, Any] | None:
        """
        Rotate PDF Pages (Auto)

        Uses AI to automatically fix the rotation of pages inside scanned PDF
        based on text analysis. Please see api documentation at
        https://developer.pdf.co/api/pdf-rotate-pages/index.html#post-tag-pdf-edit-rotate-auto
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/edit/rotate/auto"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_delete_pages_async(
        self,
        input: PDFDeletePagesInput,
    ) -> dict[str, Any] | None:
        """
        PDF Delete Pages

        Delete pages in PDF. Please see api documentation at
        https://developer.pdf.co/api/pdf-delete-pages/index.html for
        comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/edit/delete-pages"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_compress_async(
        self,
        input: PDFCompressInput,
    ) -> dict[str, Any] | None:
        """
        PDF Compress and Optimize

        Optimize PDF document to reduce its size. Please see api documentation
        at https://developer.pdf.co/api/pdf-optimize/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/optimize"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_classifier_async(
        self,
        input: PDFClassifierInput,
    ) -> dict[str, Any] | None:
        """
        Document Classifier

        Auto classification Of Incoming Documents based on keywords-based
        rules. For example, you can define rules to find which vendor provided
        the document to find which template to apply accordingly. Please see
        api documentation at
        https://developer.pdf.co/api/document-classifier/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/pdf/classifier"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def email_send_async(
        self,
        input: EmailSendInput,
    ) -> dict[str, Any] | None:
        """
        Send Email with Attachments

        Send Email with Attachments. Please see api documentation at
        https://developer.pdf.co/api/email-send-and-decode/index.html for
        comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/email/send"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def email_decode_async(
        self,
        input: EmailDecodeInput,
    ) -> dict[str, Any] | None:
        """
        Email Information

        Get Email Information such as sender/recipient, subject, body, etc.
        Please see api documentation at
        https://developer.pdf.co/api/email-send-and-decode/index.html#post-tag-email-decode
        for comprehensive guidelines.
        """
        request_url = f"{self._connection_runtime_url}/v1/email/decode"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def email_attachment_extraction_async(
        self,
        input: EmailAttachmentExtractionInput,
    ) -> dict[str, Any] | None:
        """
        Email Attachment Extraction

        Extract Email attachments with basic email information. Please see api
        documentation at
        https://developer.pdf.co/api/email-send-and-decode/index.html#post-tag-email-extract-attachments
        for comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/email/extract-attachments"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def p_d_f_attachment_extraction_async(
        self,
        input: PDFAttachmentExtractionInput,
    ) -> dict[str, Any] | None:
        """
        PDF Attachment Extraction

        Extract attachments from input PDF document. Please see api
        documentation at
        https://developer.pdf.co/api/extract-attachments/index.html for
        comprehensive guidelines.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/pdf/attachments/extract"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
