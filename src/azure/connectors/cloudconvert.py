# Copyright (c) Microsoft Corporation. All rights reserved.

# CloudconvertClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass
from typing import Optional, Any, Dict
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class ConvertFileInput:
    """
    Convert File
    """

    tasks: Optional[Dict[str, Any]] = None
    """tasks"""


@dataclass
class OptimizeFileInput:
    """
    Compress File
    """

    tasks: Optional[Dict[str, Any]] = None
    """tasks"""


@dataclass
class CaptureWebsiteInput:
    """
    Create Website Screenshot
    """

    tasks: Optional[Dict[str, Any]] = None
    """tasks"""


@dataclass
class MergeFilesInput:
    """
    Merge Files
    """

    number_of_inputs: Optional[int] = None
    """Number of input files to merge."""
    additional_inputs: Optional[Dict[str, Any]] = None
    tasks: Optional[Dict[str, Any]] = None
    """tasks"""


@dataclass
class GetConvertOptionsResponse:
    """
    Response for Get Convert Options
    """

    data: Optional[Dict[str, Any]] = None


@dataclass
class GetOptimizeOptionsResponse:
    """
    Response for Get Optimize Options
    """

    data: Optional[Dict[str, Any]] = None


@dataclass
class GetCaptureWebsiteOptionsResponse:
    """
    Response for Get Capture Website Options
    """

    data: Optional[Dict[str, Any]] = None


@dataclass
class GetMergeInputsResponse:
    """
    Response for Get Merge Inputs
    """

    data: Optional[Dict[str, Any]] = None


@dataclass
class GetMergeOptionsResponse:
    """
    Response for Get Merge Options
    """

    data: Optional[Dict[str, Any]] = None


# Client Class

class CloudconvertClient(ConnectorClientBase):
    """Typed client for cloudconvert connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a CloudconvertClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "cloudconvert"

    async def convert_file_async(
        self,
        input: ConvertFileInput,
    ) -> bytes:
        """
        Convert File

        Convert a file to another format
        """
        request_url = f"{self._connection_runtime_url}/flow/jobs/convertfile"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def optimize_file_async(
        self,
        input: OptimizeFileInput,
    ) -> bytes:
        """
        Compress File

        Compress a PDF, PNG or JPG file
        """
        request_url = f"{self._connection_runtime_url}/flow/jobs/optimize"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def capture_website_async(
        self,
        input: CaptureWebsiteInput,
    ) -> bytes:
        """
        Create Website Screenshot

        Create a website screenshot as PDF, PNG or JPG
        """
        request_url = (
            f"{self._connection_runtime_url}/flow/jobs/capture-website"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def merge_files_async(
        self,
        input: MergeFilesInput,
    ) -> bytes:
        """
        Merge Files

        Merge multiple files in one PDF
        """
        request_url = f"{self._connection_runtime_url}/flow/jobs/merge"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def get_convert_options_async(
        self,
        input_format: Optional[str] = None,
        output_format: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Convert Options
        """
        request_url = f"{self._connection_runtime_url}/flow/options/convert"
        query_params = []
        if input_format is not None:
            value = str(input_format)
            if isinstance(input_format, bool):
                value = value.lower()
            query_params.append(f"input_format={quote(value)}")
        if output_format is not None:
            value = str(output_format)
            if isinstance(output_format, bool):
                value = value.lower()
            query_params.append(f"output_format={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_optimize_options_async(
        self,
        input_format: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Optimize Options
        """
        request_url = f"{self._connection_runtime_url}/flow/options/optimize"
        query_params = []
        if input_format is not None:
            value = str(input_format)
            if isinstance(input_format, bool):
                value = value.lower()
            query_params.append(f"input_format={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_capture_website_options_async(
        self,
        output_format: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Capture Website Options
        """
        request_url = (
            f"{self._connection_runtime_url}/flow/options/capture-website"
        )
        query_params = []
        if output_format is not None:
            value = str(output_format)
            if isinstance(output_format, bool):
                value = value.lower()
            query_params.append(f"output_format={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_merge_inputs_async(
        self,
        number_of_inputs: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get Merge Inputs
        """
        request_url = f"{self._connection_runtime_url}/flow/inputs/merge"
        query_params = []
        if number_of_inputs is not None:
            value = str(number_of_inputs)
            if isinstance(number_of_inputs, bool):
                value = value.lower()
            query_params.append(f"number_of_inputs={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_merge_options_async(
        self,
        output_format: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Merge Options
        """
        request_url = f"{self._connection_runtime_url}/flow/options/merge"
        query_params = []
        if output_format is not None:
            value = str(output_format)
            if isinstance(output_format, bool):
                value = value.lower()
            query_params.append(f"output_format={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
