# Copyright (c) Microsoft Corporation. All rights reserved.

# ElfsquaddataClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CreateTriggerInput:
    """
    Custom trigger
    """

    callback_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callbackUrl"},
    )


@dataclass
class GetEntitiesResponse:
    """
    Response for Get entities
    """

    value: Optional[List[Any]] = None
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "@odata.nextLink"},
    )
    """Link to get next page of results"""


@dataclass
class PostEntityByIdInput:
    """
    Create entity
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class PostEntityByIdResponse:
    """
    Response for Create entity
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetEntityByIdResponse:
    """
    Response for Get entity by id
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class PutEntityByIdInput:
    """
    Update entity
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class InvokeFunctionInput:
    """
    Invoke function
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class InvokeFunctionResponse:
    """
    Response for Invoke function
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


# Client Class

class ElfsquaddataClient(ConnectorClientBase):
    """Typed client for elfsquaddata connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a ElfsquaddataClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "elfsquaddata"

    async def get_entities_async(
        self,
        entity_name: str,
        top: Optional[int] = None,
        skip: Optional[int] = None,
        orderby: Optional[str] = None,
        filter: Optional[str] = None,
        select: Optional[str] = None,
        expand: Optional[str] = None,
        count: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get entities

        Get entities
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data/1/{quote(str(entity_name), safe='')}"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if orderby is not None:
            value = str(orderby)
            if isinstance(orderby, bool):
                value = value.lower()
            query_params.append(f"$orderby={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if select is not None:
            value = str(select)
            if isinstance(select, bool):
                value = value.lower()
            query_params.append(f"$select={quote(value)}")
        if expand is not None:
            value = str(expand)
            if isinstance(expand, bool):
                value = value.lower()
            query_params.append(f"$expand={quote(value)}")
        if count is not None:
            value = str(count)
            if isinstance(count, bool):
                value = value.lower()
            query_params.append(f"$count={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def post_entity_by_id_async(
        self,
        input: PostEntityByIdInput,
        entity_name: str,
    ) -> dict[str, Any] | None:
        """
        Create entity

        Create entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data/1/{quote(str(entity_name), safe='')}"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_entity_by_id_async(
        self,
        entity_name: str,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get entity by id

        Get entity by id
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data"
            f"/1"
            f"/{quote(str(entity_name), safe='')}({quote(str(id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_entity_by_id_async(
        self,
        entity_name: str,
        id: str,
    ) -> None:
        """
        Delete entity

        Delete entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data"
            f"/1"
            f"/{quote(str(entity_name), safe='')}({quote(str(id), safe='')})"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def put_entity_by_id_async(
        self,
        input: PutEntityByIdInput,
        entity_name: str,
        id: str,
    ) -> None:
        """
        Update entity

        Update entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data"
            f"/1"
            f"/{quote(str(entity_name), safe='')}({quote(str(id), safe='')})"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def invoke_function_async(
        self,
        input: InvokeFunctionInput,
        function_path: str,
    ) -> dict[str, Any] | None:
        """
        Invoke function

        Invoke function
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/{quote(str(function_path), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_triggers_async(
        self,
    ) -> None:
        """
        Get triggers
        """
        request_url = f"{self._connection_runtime_url}/data/1/$triggers"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_schemas_async(
        self,
    ) -> None:
        """
        Get schemas
        """
        request_url = f"{self._connection_runtime_url}/data/1"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_schema_async(
        self,
        entity_name: str,
    ) -> None:
        """
        Get schema
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data/1/{quote(str(entity_name), safe='')}/$schema"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_functions_async(
        self,
    ) -> None:
        """
        Get function
        """
        request_url = f"{self._connection_runtime_url}/data/1/$functions"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_function_definition_async(
        self,
        function_path: str,
    ) -> None:
        """
        Get function definition
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data/1/$functions/$schema/{quote(str(function_path), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_trigger_schema_async(
        self,
        trigger_name: str,
    ) -> None:
        """
        Get trigger schema
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/data/1/$triggers/{quote(str(trigger_name), safe='')}/$schema"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "create_trigger": {
        "operation_id": "create_trigger",
        "path": "/{connectionId}/api/2/webhooks/{trigger_name}",
        "method": "post",
        "required_parameters": ["trigger_name"],
        "callback_payload_type": None,
    },
}
