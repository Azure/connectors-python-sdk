# Copyright (c) Microsoft Corporation. All rights reserved.

# SlackClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class SetDNDResponse:
    """Response for Set do not disturb"""

    snooze_enabled: Optional[bool] = None
    """Whether snooze is enabled."""


@dataclass
class CreateGroupResponse:
    """Response for Create a group"""

    group: Optional[Dict[str, Any]] = None


@dataclass
class CreateChannelResponse:
    """Response for Create a channel"""

    channel: Optional[Channel] = None


@dataclass
class JoinChannelResponse:
    """Response for Join a public channel"""

    channel: Optional[Channel] = None
    warning: Optional[str] = None
    """Whether the user is already in the channel or not."""


@dataclass
class ListChannelsResponse:
    """Response for List public channels (Pagination support)"""

    value: Optional[List[Channel]] = None
    next_link: Optional[str] = None
    """Link to get next page of results"""


@dataclass
class PostMessageResponse:
    """Response for Post message (V2)"""

    ok: Optional[bool] = None
    """Indicates if the operation was successful."""
    channel: Optional[str] = None
    """The channel which the message was posted to."""
    ts: Optional[str] = None
    """Unix timestamp for when the message was created."""
    message: Optional[Dict[str, Any]] = None
    """A channel message."""
    error: Optional[str] = None
    """Details of the error messages, if any."""


@dataclass
class Channel:
    """Definition: Channel"""

    id: Optional[str] = None
    """The id of the channel."""
    name: Optional[str] = None
    """The name of channel."""


@dataclass
class ListChannelsResponseV3:
    """Definition: ListChannels_ResponseV3"""

    value: Optional[List[Channel]] = None
    next_link: Optional[str] = None
    """Link to get next page of results"""


@dataclass
class JoinChannelResponseV2:
    """Definition: JoinChannel_ResponseV2"""

    channel: Optional[Channel] = None
    warning: Optional[str] = None
    """Whether the user is already in the channel or not."""


@dataclass
class PostMessageRequest:
    """Definition: PostMessageRequest"""

    channel: Optional[str] = None
    """
    Channel, private group, or IM channel to send message to. Can be a name(ex:
    #general) or an encoded ID.
    """
    text: Optional[str] = None
    """
    Body of the message to send. Slack API will truncate messages containing
    more than 40,000 characters. For formatting options, see
    https://api.slack.com/docs/formatting
    """
    username: Optional[str] = None
    """Name of the bot."""
    as_user: Optional[bool] = None
    """
    Pass true to post the message as the authenticated user, instead of as a
    bot.
    """
    parse: Optional[str] = None
    """
    Change how messages are treated. For details, see
    https://api.slack.com/docs/formatting
    """
    mrkdwn: Optional[bool] = None
    """Slack markup parsing setting. Enabled by default."""
    link_names: Optional[int] = None
    """Find and link channel names and usernames."""
    unfurl_links: Optional[bool] = None
    """Pass true to enable unfurling of primarily text-based content."""
    unfurl_media: Optional[bool] = None
    """Pass false to disable unfurling of media content."""
    icon_url: Optional[str] = None
    """URL to an image to use as an icon for this message."""
    icon_emoji: Optional[str] = None
    """Emoji to use as an icon for this message."""


# Client Class

class SlackClient(ConnectorClientBase):
    """Typed client for slack connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a SlackClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "slack"

    async def set_d_n_d_async(
        self,
        num_minutes: Optional[str] = None,
    ):
        """
        Set do not disturb

        Set the do not disturb status for the user.
        """
        path = f"{self._connection_runtime_url}/dnd.setSnooze"
        query_params = []
        if num_minutes is not None:
            value = str(num_minutes)
            if isinstance(num_minutes, bool):
                value = value.lower()
            query_params.append(f"num_minutes={quote(value)}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_group_async(
        self,
        name: Optional[str] = None,
    ):
        """
        Create a group

        Creates a group in slack.
        """
        path = f"{self._connection_runtime_url}/groups.create"
        query_params = []
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"name={quote(value)}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def on_new_file_async(
        self,
        channel: Optional[str],
    ):
        """
        When a file is created

        When a file is created
        """
        path = f"{self._connection_runtime_url}/trigger/files.list"
        query_params = []
        if channel is not None:
            value = str(channel)
            if isinstance(channel, bool):
                value = value.lower()
            query_params.append(f"channel={quote(value)}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_channel_async(
        self,
        name: Optional[str] = None,
        is_private: Optional[str] = None,
    ):
        """
        Create a channel

        Create a channel in slack.
        """
        path = f"{self._connection_runtime_url}/conversations.create"
        query_params = []
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"name={quote(value)}")
        if is_private is not None:
            value = str(is_private)
            if isinstance(is_private, bool):
                value = value.lower()
            query_params.append(f"is_private={quote(value)}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def join_channel_async(
        self,
        channel: Optional[str] = None,
    ):
        """
        Join a public channel

        Join a public channel in slack.
        """
        path = f"{self._connection_runtime_url}/conversations.join"
        query_params = []
        if channel is not None:
            value = str(channel)
            if isinstance(channel, bool):
                value = value.lower()
            query_params.append(f"channel={quote(value)}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_channels_async(
        self,
    ):
        """
        List public channels (Pagination support)

        List the public channels in slack.
        """
        path = f"{self._connection_runtime_url}/v3/conversations.list"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def post_message_async(
        self,
        input: PostMessageRequest,
    ):
        """
        Post message (V2)

        This operation is used to post a message to the specified channel.
        """
        path = f"{self._connection_runtime_url}/v2/chat.postMessage"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                path,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
