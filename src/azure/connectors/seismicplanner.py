# Copyright (c) Microsoft Corporation. All rights reserved.

# SeismicplannerClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CommentQueryResponse:
    """
    Response for Get comments
    """

    count: Optional[int] = None
    """Number of records returned"""
    cursor: Optional[str] = None
    """Cursor used for pagination"""
    items: Optional[List[Comment]] = None
    """List of items in the response"""
    limit: Optional[int] = None
    """Number to limit records"""
    meta: Optional[Meta] = None


@dataclass
class Comment:
    """
    Response for Create comment
    """

    annotation_payload: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationPayload"},
    )
    annotation_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationType"},
    )
    comment_content: Optional[str] = field(
        default=None,
        metadata={"wire_name": "commentContent"},
    )
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date string of date created"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of the creator"""
    id: Optional[str] = None
    """Comment ID"""
    is_resolved: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isResolved"},
    )
    """True/false on whether the comment is resolved"""
    node_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeId"},
    )
    """Node ID"""
    node_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeType"},
    )
    """Node type of the comment"""
    page_index: Optional[str] = field(
        default=None,
        metadata={"wire_name": "pageIndex"},
    )
    """Number page index"""
    parent_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentId"},
    )
    """Parent comment ID"""
    reply_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "replyCount"},
    )
    """Number of replies"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    """Space ID"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date string of updated date"""


@dataclass
class PlannerProjectQueryResponse:
    """
    Response for Get projects
    """

    count: Optional[int] = None
    """Number of records returned"""
    cursor: Optional[str] = None
    """Cursor used for pagination"""
    items: Optional[List[PlannerProject]] = None
    """List of project items in the response"""
    limit: Optional[int] = None
    """Number to limit records"""
    meta: Optional[Meta] = None


@dataclass
class AsyncOperationResponse:
    """
    Response for Delete projects
    """

    async_operation_ids: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "asyncOperationIds"},
    )
    """List of async action IDs"""


@dataclass
class PlannerProject:
    """
    Response for Create project
    """

    actual_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "actualDuration"},
    )
    """Calculated actual duration in days"""
    actual_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualEndDate"},
    )
    """Calculated end date from when the project actually ended"""
    actual_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualStartDate"},
    )
    """Calculated start date from when the project actually started"""
    associated_nodes: Optional[List[ProjectAssociatedNodes]] = field(
        default=None,
        metadata={"wire_name": "associatedNodes"},
    )
    """List of association data"""
    associations: Optional[List[Association]] = None
    """List of associations (Library content)"""
    copy_from: Optional[str] = field(
        default=None,
        metadata={"wire_name": "copyFrom"},
    )
    """Project ID the project is copied from"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date string of created date"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of creator"""
    custom_properties: Optional[List[CustomPropertyValues]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    description: Optional[str] = None
    """Description of project"""
    followers: Optional[List[Follower]] = None
    """List of followers"""
    id: Optional[str] = None
    is_active: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isActive"},
    )
    """Indicator on if a project is active or not"""
    is_template: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isTemplate"},
    )
    """Indicator on if the project is a template or not"""
    manager_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "managerId"},
    )
    """User ID of project manager"""
    max_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "maxRank"},
    )
    """Maximum rank number"""
    min_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "minRank"},
    )
    """Minimum rank number"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    template_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "templateId"},
    )
    """Template ID the the project was created from"""
    title: Optional[str] = None
    """Title of project"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date project was last updated"""
    works: Optional[List[PlannerTask]] = None
    """List of tasks and requests"""


@dataclass
class PlannerRequestQueryResponse:
    """
    Response for Get requests
    """

    count: Optional[int] = None
    """Number of records returned"""
    cursor: Optional[str] = None
    """Cursor used for pagination"""
    items: Optional[List[PlannerRequest]] = None
    """List of request objects in the response"""
    limit: Optional[int] = None
    """Number to limit records"""
    meta: Optional[Meta] = None


@dataclass
class PlannerRequest:
    """
    Response for Create request
    """

    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of the assignee"""
    assigner_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assignerId"},
    )
    """User ID of the assigner"""
    associations: Optional[List[Association]] = None
    """List of associations (Library content)"""
    content_refs: Optional[List[ContentRef]] = field(
        default=None,
        metadata={"wire_name": "contentRefs"},
    )
    """List of content references (WorkSpace and DocCenter content)"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date string for created date"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of the creator"""
    custom_properties: Optional[List[CustomPropertyValues]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    followers: Optional[List[Follower]] = None
    """List of followers"""
    form_ref: Optional[Any] = field(
        default=None,
        metadata={"wire_name": "formRef"},
    )
    """Form reference object"""
    id: Optional[str] = None
    note: Optional[str] = None
    """Note for notifications on status changes"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """Project ID of the project the request is associated with"""
    rank: Optional[str] = None
    """Rank of request in request list"""
    request_form_custom_property_option: Optional[RequestFormCustomProperties] = field(
        default=None,
        metadata={"wire_name": "requestFormCustomPropertyOption"},
    )
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    status_schema_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "statusSchemaId"},
    )
    step_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "stepId"},
    )
    """Step ID indicating the status value"""
    title: Optional[str] = None
    """Title of the request"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date the request was last updated"""


@dataclass
class StatusSchemaQueryResponse:
    """
    Response for Get status schemas
    """

    count: Optional[int] = None
    """Number of records returned"""
    cursor: Optional[str] = None
    """Cursor used for pagination"""
    items: Optional[List[StatusSchema]] = None
    """List of status schema objects in the response"""
    limit: Optional[int] = None
    """Number to limit records"""
    meta: Optional[Meta] = None


@dataclass
class StatusSchema:
    """
    Response for Get status schema
    """

    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date string of created date"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of creator"""
    id: Optional[str] = None
    is_default: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isDefault"},
    )
    """Indicator on if the status schema is the default schema"""
    name: Optional[str] = None
    """Name of status schema"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    steps: Optional[List[str]] = None
    """List of steps and their values"""
    transitions: Optional[List[List[int]]] = None
    """List of transition rules"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Node type the status schema can be used for"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date the status schema was last updated"""


@dataclass
class PlannerTaskQueryResponse:
    """
    Response for Get tasks
    """

    count: Optional[int] = None
    """Number of records returned"""
    cursor: Optional[str] = None
    """Cursor used for pagination"""
    items: Optional[List[PlannerTask]] = None
    """List of task objects in the response"""
    limit: Optional[int] = None
    """Number to limit records"""
    meta: Optional[Meta] = None


@dataclass
class PlannerTask:
    """
    Response for Create task
    """

    actual_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "actualDuration"},
    )
    """Calculated actual duration in days"""
    actual_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualEndDate"},
    )
    """Date when work on the task was completed"""
    actual_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualStartDate"},
    )
    """Date when work on the task started"""
    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of assignee"""
    assigner_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assignerId"},
    )
    """User ID of the last user to add an assignee"""
    associations: Optional[List[Association]] = None
    """List of associations on a task"""
    copy_from: Optional[str] = field(
        default=None,
        metadata={"wire_name": "copyFrom"},
    )
    """Task ID that task was copied from"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date the task was created"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of creator"""
    custom_properties: Optional[List[CustomPropertyValues]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties on a task"""
    description: Optional[str] = None
    """Description of task"""
    followers: Optional[List[Follower]] = None
    """List of followers on a task"""
    id: Optional[str] = None
    is_template: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isTemplate"},
    )
    """Indicates if a task is part of a template"""
    max_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "maxRank"},
    )
    """Maximum rank"""
    min_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "minRank"},
    )
    """Minimum rank"""
    parent_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentId"},
    )
    """Task ID of parent if task is a subtask"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date (due date)"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """Project ID the task belongs to"""
    rank: Optional[str] = None
    """Rank of task in a task list"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    status_schema_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "statusSchemaId"},
    )
    step_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "stepId"},
    )
    """Id of step in status schema"""
    sub_task_ids: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "subTaskIds"},
    )
    """List of subtask IDs"""
    sub_tasks: Optional[List[PlannerSubTask]] = field(
        default=None,
        metadata={"wire_name": "subTasks"},
    )
    """List of subtasks on the task"""
    template_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "templateId"},
    )
    """Template ID of the template the task belongs to"""
    title: Optional[str] = None
    """Title of task"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of task"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date the task was last updated"""


@dataclass
class Association:
    """
    Definition: Association
    """

    description: Optional[str] = None
    """Description of association"""
    name: Optional[str] = None
    """Name of association"""
    node_guid: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeGuid"},
    )
    """Longer GUID format"""
    node_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeId"},
    )
    """Node ID"""
    node_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeType"},
    )
    """Node type of the association"""
    space_guid: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceGuid"},
    )
    """Longer GUID format"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    """Space ID"""


@dataclass
class AssociationReq:
    """
    Definition: AssociationReq
    """

    node_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeId"},
    )
    """Node ID"""
    node_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeType"},
    )
    """Node type of the association"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    """Space ID"""


@dataclass
class ContentRef:
    """
    Definition: ContentRef
    """

    content_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "contentId"},
    )
    """Content ID"""
    doc_center_full_path: Optional[str] = field(
        default=None,
        metadata={"wire_name": "docCenterFullPath"},
    )
    """Full location path of where content exists in DocCenter"""
    major_version: Optional[int] = field(
        default=None,
        metadata={"wire_name": "majorVersion"},
    )
    """Number of major version"""
    minor_version: Optional[int] = field(
        default=None,
        metadata={"wire_name": "minorVersion"},
    )
    """Number of minor version"""
    modified_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "modifiedDate"},
    )
    """Date the content reference was last updated"""
    order_no: Optional[int] = field(
        default=None,
        metadata={"wire_name": "orderNo"},
    )
    """Number to indicate order"""
    profile_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "profileId"},
    )
    """Profile ID"""
    profile_version_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "profileVersionId"},
    )
    """Version ID of profile"""
    source: Optional[str] = None
    """Source of content"""
    team_site_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "teamSiteId"},
    )
    """TeamSite ID"""
    thumbnail_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "thumbnailId"},
    )
    """ID for image thumbnail"""
    version_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "versionId"},
    )
    """Version ID of content"""


@dataclass
class CreateCommentModel:
    """
    Definition: CreateCommentModel
    """

    annotation_payload: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationPayload"},
    )
    annotation_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationType"},
    )
    """Annotation type"""
    comment_content: Optional[str] = field(
        default=None,
        metadata={"wire_name": "commentContent"},
    )
    """Content of the comment"""
    is_resolved: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isResolved"},
    )
    """True/false on whether the comment is resolved"""


@dataclass
class CreateProject:
    """
    Definition: CreateProject
    """

    associations: Optional[List[AssociationReq]] = None
    """List of association objects"""
    custom_properties: Optional[List[CustomPropertyValuesInput]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    description: Optional[str] = None
    """Title of description"""
    manager_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "managerId"},
    )
    """User ID of manager"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of start date"""
    title: Optional[str] = None
    """Title of project"""


@dataclass
class CreateRequest:
    """
    Definition: CreateRequest
    """

    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of assignee"""
    associations: Optional[List[AssociationReq]] = None
    """List of associations (Library content)"""
    content_refs: Optional[List[ContentRef]] = field(
        default=None,
        metadata={"wire_name": "contentRefs"},
    )
    """List of content references (DocCenter and Workspace associations)"""
    custom_properties: Optional[List[CustomPropertyValuesInput]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    form_ref: Optional[Any] = field(
        default=None,
        metadata={"wire_name": "formRef"},
    )
    """Form reference object"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """ID of project the request belongs to"""
    status_schema_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "statusSchemaId"},
    )
    title: Optional[str] = None
    """Title of request"""


@dataclass
class CreateTask:
    """
    Definition: CreateTask
    """

    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of assignee"""
    associations: Optional[List[AssociationReq]] = None
    """List of associations (Library content)"""
    custom_properties: Optional[List[CustomPropertyValuesInput]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    description: Optional[str] = None
    """Description of task"""
    parent_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentId"},
    )
    """Task ID of parent"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of start date"""
    priority: Optional[str] = None
    """Priority values"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """ID of project the task is associated with"""
    title: Optional[str] = None
    """Title of task"""


@dataclass
class CustomProperty:
    """
    Definition: CustomProperty
    """

    allow_multiple_values: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowMultipleValues"},
    )
    """Indicates if custom property allows for multiple values"""
    created_by_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdById"},
    )
    """User ID of the creator"""
    has_domain_of_values: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "hasDomainOfValues"},
    )
    """Indicates if custom property has possible values"""
    hint: Optional[str] = None
    """
    Value that describes the hint displayed under the custom property name
    """
    id: Optional[str] = None
    is_required: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isRequired"},
    )
    """Indicates whether the custom property is required or not"""
    last_modified_by_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastModifiedById"},
    )
    """User ID of the user that last updated the custom property"""
    name: Optional[str] = None
    """Name of custom property"""
    operation_setting: Optional[str] = field(
        default=None,
        metadata={"wire_name": "operationSetting"},
    )
    """
    Object that describes settings for different operations on a custom
    property
    """
    order: Optional[int] = None
    """Number indicating order within custom property list"""
    possible_values: Optional[List[PossibleValuesItem]] = field(
        default=None,
        metadata={"wire_name": "possibleValues"},
    )
    """List of possible values for the custom property (if applicable)"""
    scope_settings: Optional[List[ScopeSettingsItemDefinition]] = field(
        default=None,
        metadata={"wire_name": "scopeSettings"},
    )
    """
    Object that describes settings for different scopes on a custom property
    """
    scopes: Optional[List[str]] = None
    """List of scopes"""
    value_schema_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "valueSchemaName"},
    )
    """Name of custom property value schema"""
    value_schema_property: Optional[ValueSchemaPropertyDefinition] = field(
        default=None,
        metadata={"wire_name": "valueSchemaProperty"},
    )
    value_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "valueType"},
    )
    """Type of custom property value"""


@dataclass
class CustomPropertyDataDisplay:
    """
    Definition: CustomPropertyDataDisplay
    """

    name: Optional[str] = None
    """Name of custom property"""


@dataclass
class CustomPropertyValues:
    """
    Definition: CustomPropertyValues
    """

    id: Optional[str] = None
    localizations: Optional[Dict[str, Any]] = None
    """Localization object"""
    multiple_value: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "multipleValue"},
    )
    """Indicate if the custom property allows multiple values"""
    name: Optional[str] = None
    """Name of custom property"""
    values: Optional[List[CustomPropertyValuesValues]] = None
    """List of custom property values"""


@dataclass
class CustomPropertyValuesInput:
    """
    Definition: CustomPropertyValuesInput
    """

    id: Optional[str] = None
    values: Optional[List[CustomPropertyValuesValues]] = None
    """List of custom property values"""


@dataclass
class CustomPropertyValuesValues:
    """
    Definition: CustomPropertyValuesValues
    """

    id: Optional[str] = None
    value: Optional[Any] = None
    """Value data for custom property value"""


@dataclass
class DeleteRequest:
    """
    Definition: DeleteRequest
    """

    ids: Optional[List[str]] = None
    """List of Request IDs to delete"""


@dataclass
class Follower:
    """
    Definition: Follower
    """

    id: Optional[str] = None
    """User/User group ID of follower"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Either user or user group follower type"""


@dataclass
class Meta:
    """
    Definition: Meta
    """

    count: Optional[int] = None
    """Number of records returned"""
    query: Optional[List[MetaQueryInfo]] = None
    """Query object"""


@dataclass
class MetaQueryInfo:
    """
    Definition: MetaQueryInfo
    """

    sort: Optional[List[str]] = None
    """Sort query"""


@dataclass
class PlannerSubTask:
    """
    Definition: PlannerSubTask
    """

    actual_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "actualDuration"},
    )
    """Calculated actual duration in days"""
    actual_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualEndDate"},
    )
    """Date when work on the task was completed"""
    actual_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "actualStartDate"},
    )
    """Date when work on the task started"""
    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of assignee"""
    assigner_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assignerId"},
    )
    """User ID of the last user to add an assignee"""
    associations: Optional[List[Association]] = None
    """List of associations on a task"""
    copy_from: Optional[str] = field(
        default=None,
        metadata={"wire_name": "copyFrom"},
    )
    """Task ID that task was copied from"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date the task was created"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """User ID of creator"""
    custom_properties: Optional[List[CustomPropertyValues]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties on a task"""
    description: Optional[str] = None
    """Description of task"""
    followers: Optional[List[Follower]] = None
    """List of followers on a task"""
    id: Optional[str] = None
    is_template: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isTemplate"},
    )
    """Indicates if a task is part of a template"""
    max_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "maxRank"},
    )
    """Maximum rank"""
    min_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "minRank"},
    )
    """Minimum rank"""
    parent_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentId"},
    )
    """Task ID of parent if task is a subtask"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of end date (due date)"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """Project ID the task belongs to"""
    rank: Optional[str] = None
    """Rank of task in a task list"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )
    status_schema_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "statusSchemaId"},
    )
    step_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "stepId"},
    )
    """Id of step in status schema"""
    template_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "templateId"},
    )
    """Template ID of the template the task belongs to"""
    title: Optional[str] = None
    """Title of task"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of task"""
    updated_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedAt"},
    )
    """Date the task was last updated"""


@dataclass
class PossibleValuesItem:
    """
    Definition: PossibleValuesItem
    """

    id: Optional[str] = None
    order: Optional[int] = None
    """Number to indicate order"""
    value: Optional[Any] = None
    """Actual value data"""


@dataclass
class ProblemDetails:
    """
    Definition: ProblemDetails
    """

    detail: Optional[str] = None
    instance: Optional[str] = None
    status: Optional[int] = None
    title: Optional[str] = None
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})


@dataclass
class ProjectAssociatedNodes:
    """
    Definition: ProjectAssociatedNodes
    """

    node_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeId"},
    )
    node_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nodeType"},
    )
    """Node type of the association"""
    space_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spaceId"},
    )


@dataclass
class RequestFormCustomProperties:
    """
    Definition: RequestFormCustomProperties
    """

    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdAt"},
    )
    """Date string of created date"""
    custom_properties: Optional[List[CustomProperty]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""


@dataclass
class ScopeSettingsItemDefinition:
    """
    Definition: ScopeSettingsItemDefinition
    """

    is_required: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isRequired"},
    )
    """Indicator on whether a field is required or not"""
    scope: Optional[str] = None
    """
    Type list of node types that the custom property definition belongs to
    """


@dataclass
class UpdateCommentModel:
    """
    Definition: UpdateCommentModel
    """

    annotation_payload: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationPayload"},
    )
    annotation_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "annotationType"},
    )
    comment_content: Optional[str] = field(
        default=None,
        metadata={"wire_name": "commentContent"},
    )
    """Content of the comment"""
    is_resolved: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isResolved"},
    )
    """True/false on whether the comment is resolved"""


@dataclass
class UpdateProject:
    """
    Definition: UpdateProject
    """

    custom_properties: Optional[List[CustomPropertyValues]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    description: Optional[str] = None
    """Description of project"""
    is_active: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isActive"},
    )
    """Indicator on whether a project is active?"""
    manager_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "managerId"},
    )
    """User ID of project manager"""
    max_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "maxRank"},
    )
    """Maximum rank number"""
    min_rank: Optional[str] = field(
        default=None,
        metadata={"wire_name": "minRank"},
    )
    """Minimum rank number"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    title: Optional[str] = None
    """Title of project"""


@dataclass
class UpdateRequest:
    """
    Definition: UpdateRequest
    """

    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of the assignee"""
    content_refs: Optional[List[ContentRef]] = field(
        default=None,
        metadata={"wire_name": "contentRefs"},
    )
    """List of content references (WorkSpace and DocCenter content)"""
    custom_properties: Optional[List[CustomPropertyValuesInput]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    form_ref: Optional[Any] = field(
        default=None,
        metadata={"wire_name": "formRef"},
    )
    """Form reference object"""
    note: Optional[str] = None
    """Note used for notifications on status changes"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date (due date)"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """Project ID of the project the request belongs to"""
    step_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "stepId"},
    )
    """Step ID indicating status"""
    title: Optional[str] = None
    """Title of the request"""


@dataclass
class UpdateTask:
    """
    Definition: UpdateTask
    """

    assignee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assigneeId"},
    )
    """User ID of the assignee"""
    custom_properties: Optional[List[CustomPropertyValuesInput]] = field(
        default=None,
        metadata={"wire_name": "customProperties"},
    )
    """List of custom properties"""
    description: Optional[str] = None
    """Description of task"""
    parent_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentId"},
    )
    """Task ID of parent task"""
    planned_duration: Optional[int] = field(
        default=None,
        metadata={"wire_name": "plannedDuration"},
    )
    """Planned duration in days"""
    planned_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedEndDate"},
    )
    """Date string of planned end date"""
    planned_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "plannedStartDate"},
    )
    """Date string of planned start date"""
    priority: Optional[str] = None
    """Priority value"""
    project_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectId"},
    )
    """Project ID that the task belongs to"""
    step_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "stepId"},
    )
    """Step ID indicating the task status"""
    title: Optional[str] = None
    """Title of task"""


@dataclass
class ValueSchemaPropertyDefinition:
    """
    Definition: ValueSchemaPropertyDefinition
    """

    max: Optional[str] = None
    min: Optional[str] = None


# Client Class

class SeismicplannerClient(ConnectorClientBase):
    """Typed client for seismicplanner connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a SeismicplannerClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "seismicplanner"

    async def get_comments_async(
        self,
        space_id: str,
        node_id: str,
        creator_ids: Optional[List[str]] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
        sort: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get comments

        Get all comments for a given project, task, or request. Requires node
        (project, task, or request) ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/nodes"
            f"/{quote(str(node_id), safe='')}"
            f"/comments"
        )
        query_params = []
        item: object
        if creator_ids is not None:
            if isinstance(creator_ids, list):
                joined_parts = []
                for item in creator_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"creatorIds={quote(joined)}")
            else:
                value = str(creator_ids)
                if isinstance(creator_ids, bool):
                    value = value.lower()
                query_params.append(f"creatorIds={quote(value)}")
        if cursor is not None:
            value = str(cursor)
            if isinstance(cursor, bool):
                value = value.lower()
            query_params.append(f"cursor={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if sort is not None:
            if isinstance(sort, list):
                joined_parts = []
                for item in sort:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"sort={quote(joined)}")
            else:
                value = str(sort)
                if isinstance(sort, bool):
                    value = value.lower()
                query_params.append(f"sort={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_comment_async(
        self,
        input: CreateCommentModel,
        space_id: str,
        node_id: str,
    ) -> dict[str, Any] | None:
        """
        Create comment

        Create a comment on a given project, task, or request. Requires node
        (project, task, or request) ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/nodes"
            f"/{quote(str(node_id), safe='')}"
            f"/comments"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_comment_async(
        self,
        space_id: str,
        node_id: str,
        comment_id: str,
    ) -> dict[str, Any] | None:
        """
        Get comment

        Get a single comment. Requires node (project, task, or request) and
        comment ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/nodes"
            f"/{quote(str(node_id), safe='')}"
            f"/comments"
            f"/{quote(str(comment_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_comment_async(
        self,
        space_id: str,
        node_id: str,
        comment_id: str,
    ) -> None:
        """
        Delete comment

        Delete a comment. Requires node (project, task, or request) and comment
        ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/nodes"
            f"/{quote(str(node_id), safe='')}"
            f"/comments"
            f"/{quote(str(comment_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def update_comment_async(
        self,
        input: UpdateCommentModel,
        space_id: str,
        node_id: str,
        comment_id: str,
    ) -> dict[str, Any] | None:
        """
        Update comment

        Update an existing comment. Requires node (project, task, or request)
        and comment ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/nodes"
            f"/{quote(str(node_id), safe='')}"
            f"/comments"
            f"/{quote(str(comment_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_projects_async(
        self,
        space_id: str,
        planned_end_date_from: Optional[str] = None,
        planned_end_date_to: Optional[str] = None,
        planned_start_date_from: Optional[str] = None,
        planned_start_date_to: Optional[str] = None,
        ids: Optional[List[str]] = None,
        title: Optional[str] = None,
        manager_ids: Optional[List[str]] = None,
        creator_ids: Optional[List[str]] = None,
        associated_node_ids: Optional[List[str]] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
        sort: Optional[List[str]] = None,
        custom_properties: Optional[str] = None,
        follower_ids: Optional[List[str]] = None,
        include_associations: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get projects

        Get a list of projects with their attributes and associations.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/projects"
        )
        query_params = []
        item: object
        if planned_end_date_from is not None:
            value = str(planned_end_date_from)
            if isinstance(planned_end_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateFrom={quote(value)}")
        if planned_end_date_to is not None:
            value = str(planned_end_date_to)
            if isinstance(planned_end_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateTo={quote(value)}")
        if planned_start_date_from is not None:
            value = str(planned_start_date_from)
            if isinstance(planned_start_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateFrom={quote(value)}")
        if planned_start_date_to is not None:
            value = str(planned_start_date_to)
            if isinstance(planned_start_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateTo={quote(value)}")
        if ids is not None:
            if isinstance(ids, list):
                joined_parts = []
                for item in ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"ids={quote(joined)}")
            else:
                value = str(ids)
                if isinstance(ids, bool):
                    value = value.lower()
                query_params.append(f"ids={quote(value)}")
        if title is not None:
            value = str(title)
            if isinstance(title, bool):
                value = value.lower()
            query_params.append(f"title={quote(value)}")
        if manager_ids is not None:
            if isinstance(manager_ids, list):
                joined_parts = []
                for item in manager_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"managerIds={quote(joined)}")
            else:
                value = str(manager_ids)
                if isinstance(manager_ids, bool):
                    value = value.lower()
                query_params.append(f"managerIds={quote(value)}")
        if creator_ids is not None:
            if isinstance(creator_ids, list):
                joined_parts = []
                for item in creator_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"creatorIds={quote(joined)}")
            else:
                value = str(creator_ids)
                if isinstance(creator_ids, bool):
                    value = value.lower()
                query_params.append(f"creatorIds={quote(value)}")
        if associated_node_ids is not None:
            if isinstance(associated_node_ids, list):
                joined_parts = []
                for item in associated_node_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"associatedNodeIds={quote(joined)}")
            else:
                value = str(associated_node_ids)
                if isinstance(associated_node_ids, bool):
                    value = value.lower()
                query_params.append(f"associatedNodeIds={quote(value)}")
        if cursor is not None:
            value = str(cursor)
            if isinstance(cursor, bool):
                value = value.lower()
            query_params.append(f"cursor={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if sort is not None:
            if isinstance(sort, list):
                joined_parts = []
                for item in sort:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"sort={quote(joined)}")
            else:
                value = str(sort)
                if isinstance(sort, bool):
                    value = value.lower()
                query_params.append(f"sort={quote(value)}")
        if custom_properties is not None:
            value = str(custom_properties)
            if isinstance(custom_properties, bool):
                value = value.lower()
            query_params.append(f"customProperties={quote(value)}")
        if follower_ids is not None:
            if isinstance(follower_ids, list):
                joined_parts = []
                for item in follower_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"followerIds={quote(joined)}")
            else:
                value = str(follower_ids)
                if isinstance(follower_ids, bool):
                    value = value.lower()
                query_params.append(f"followerIds={quote(value)}")
        if include_associations is not None:
            value = str(include_associations)
            if isinstance(include_associations, bool):
                value = value.lower()
            query_params.append(f"includeAssociations={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_projects_async(
        self,
        space_id: str,
        ids: List[str],
        delete_tasks: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Delete projects

        Delete one or more projects.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/projects"
        )
        query_params = []
        item: object
        if isinstance(ids, list):
            joined_parts = []
            for item in ids:
                value = str(item)
                if isinstance(item, bool):
                    value = value.lower()
                joined_parts.append(value)
            joined = ','.join(joined_parts)
            query_params.append(f"ids={quote(joined)}")
        else:
            value = str(ids)
            if isinstance(ids, bool):
                value = value.lower()
            query_params.append(f"ids={quote(value)}")
        if delete_tasks is not None:
            value = str(delete_tasks)
            if isinstance(delete_tasks, bool):
                value = value.lower()
            query_params.append(f"deleteTasks={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_project_async(
        self,
        input: CreateProject,
        space_id: str,
    ) -> dict[str, Any] | None:
        """
        Create project

        Create a project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/projects"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_project_async(
        self,
        space_id: str,
        project_id: str,
        associated_nodes_depth: Optional[int] = None,
        include_works: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get project

        Get a single project by ID. Return tasks if desired.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/projects"
            f"/{quote(str(project_id), safe='')}"
        )
        query_params = []
        if associated_nodes_depth is not None:
            value = str(associated_nodes_depth)
            if isinstance(associated_nodes_depth, bool):
                value = value.lower()
            query_params.append(f"associatedNodesDepth={quote(value)}")
        if include_works is not None:
            value = str(include_works)
            if isinstance(include_works, bool):
                value = value.lower()
            query_params.append(f"includeWorks={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_project_async(
        self,
        space_id: str,
        project_id: str,
        delete_tasks: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Delete project

        Delete a project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/projects"
            f"/{quote(str(project_id), safe='')}"
        )
        query_params = []
        if delete_tasks is not None:
            value = str(delete_tasks)
            if isinstance(delete_tasks, bool):
                value = value.lower()
            query_params.append(f"deleteTasks={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_project_async(
        self,
        input: UpdateProject,
        space_id: str,
        project_id: str,
    ) -> dict[str, Any] | None:
        """
        Update a project

        Update and return a project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/projects"
            f"/{quote(str(project_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_requests_async(
        self,
        space_id: str,
        planned_end_date_from: Optional[str] = None,
        planned_end_date_to: Optional[str] = None,
        planned_start_date_from: Optional[str] = None,
        planned_start_date_to: Optional[str] = None,
        created_at_from: Optional[str] = None,
        created_at_to: Optional[str] = None,
        updated_at_from: Optional[str] = None,
        updated_at_to: Optional[str] = None,
        ids: Optional[List[str]] = None,
        title: Optional[str] = None,
        assignee_ids: Optional[List[str]] = None,
        priorities: Optional[List[str]] = None,
        keywords: Optional[str] = None,
        assigner_ids: Optional[List[str]] = None,
        creator_ids: Optional[List[str]] = None,
        step_ids: Optional[List[int]] = None,
        status_schema_id: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
        sort: Optional[List[str]] = None,
        project_id: Optional[str] = None,
        has_project: Optional[bool] = None,
        custom_properties: Optional[str] = None,
        follower_ids: Optional[List[str]] = None,
        associated_node_ids: Optional[List[str]] = None,
        content_refs: Optional[List[str]] = None,
        include_request_form_custom_properties: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get requests

        Get a list of requests.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/requests"
        )
        query_params = []
        item: object
        if planned_end_date_from is not None:
            value = str(planned_end_date_from)
            if isinstance(planned_end_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateFrom={quote(value)}")
        if planned_end_date_to is not None:
            value = str(planned_end_date_to)
            if isinstance(planned_end_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateTo={quote(value)}")
        if planned_start_date_from is not None:
            value = str(planned_start_date_from)
            if isinstance(planned_start_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateFrom={quote(value)}")
        if planned_start_date_to is not None:
            value = str(planned_start_date_to)
            if isinstance(planned_start_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateTo={quote(value)}")
        if created_at_from is not None:
            value = str(created_at_from)
            if isinstance(created_at_from, bool):
                value = value.lower()
            query_params.append(f"createdAtFrom={quote(value)}")
        if created_at_to is not None:
            value = str(created_at_to)
            if isinstance(created_at_to, bool):
                value = value.lower()
            query_params.append(f"createdAtTo={quote(value)}")
        if updated_at_from is not None:
            value = str(updated_at_from)
            if isinstance(updated_at_from, bool):
                value = value.lower()
            query_params.append(f"updatedAtFrom={quote(value)}")
        if updated_at_to is not None:
            value = str(updated_at_to)
            if isinstance(updated_at_to, bool):
                value = value.lower()
            query_params.append(f"updatedAtTo={quote(value)}")
        if ids is not None:
            if isinstance(ids, list):
                joined_parts = []
                for item in ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"ids={quote(joined)}")
            else:
                value = str(ids)
                if isinstance(ids, bool):
                    value = value.lower()
                query_params.append(f"ids={quote(value)}")
        if title is not None:
            value = str(title)
            if isinstance(title, bool):
                value = value.lower()
            query_params.append(f"title={quote(value)}")
        if assignee_ids is not None:
            if isinstance(assignee_ids, list):
                joined_parts = []
                for item in assignee_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"assigneeIds={quote(joined)}")
            else:
                value = str(assignee_ids)
                if isinstance(assignee_ids, bool):
                    value = value.lower()
                query_params.append(f"assigneeIds={quote(value)}")
        if priorities is not None:
            if isinstance(priorities, list):
                joined_parts = []
                for item in priorities:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"priorities={quote(joined)}")
            else:
                value = str(priorities)
                if isinstance(priorities, bool):
                    value = value.lower()
                query_params.append(f"priorities={quote(value)}")
        if keywords is not None:
            value = str(keywords)
            if isinstance(keywords, bool):
                value = value.lower()
            query_params.append(f"keywords={quote(value)}")
        if assigner_ids is not None:
            if isinstance(assigner_ids, list):
                joined_parts = []
                for item in assigner_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"assignerIds={quote(joined)}")
            else:
                value = str(assigner_ids)
                if isinstance(assigner_ids, bool):
                    value = value.lower()
                query_params.append(f"assignerIds={quote(value)}")
        if creator_ids is not None:
            if isinstance(creator_ids, list):
                joined_parts = []
                for item in creator_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"creatorIds={quote(joined)}")
            else:
                value = str(creator_ids)
                if isinstance(creator_ids, bool):
                    value = value.lower()
                query_params.append(f"creatorIds={quote(value)}")
        if step_ids is not None:
            if isinstance(step_ids, list):
                joined_parts = []
                for item in step_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"stepIds={quote(joined)}")
            else:
                value = str(step_ids)
                if isinstance(step_ids, bool):
                    value = value.lower()
                query_params.append(f"stepIds={quote(value)}")
        if status_schema_id is not None:
            value = str(status_schema_id)
            if isinstance(status_schema_id, bool):
                value = value.lower()
            query_params.append(f"statusSchemaId={quote(value)}")
        if cursor is not None:
            value = str(cursor)
            if isinstance(cursor, bool):
                value = value.lower()
            query_params.append(f"cursor={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if sort is not None:
            if isinstance(sort, list):
                joined_parts = []
                for item in sort:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"sort={quote(joined)}")
            else:
                value = str(sort)
                if isinstance(sort, bool):
                    value = value.lower()
                query_params.append(f"sort={quote(value)}")
        if project_id is not None:
            value = str(project_id)
            if isinstance(project_id, bool):
                value = value.lower()
            query_params.append(f"projectId={quote(value)}")
        if has_project is not None:
            value = str(has_project)
            if isinstance(has_project, bool):
                value = value.lower()
            query_params.append(f"hasProject={quote(value)}")
        if custom_properties is not None:
            value = str(custom_properties)
            if isinstance(custom_properties, bool):
                value = value.lower()
            query_params.append(f"customProperties={quote(value)}")
        if follower_ids is not None:
            if isinstance(follower_ids, list):
                joined_parts = []
                for item in follower_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"followerIds={quote(joined)}")
            else:
                value = str(follower_ids)
                if isinstance(follower_ids, bool):
                    value = value.lower()
                query_params.append(f"followerIds={quote(value)}")
        if associated_node_ids is not None:
            if isinstance(associated_node_ids, list):
                joined_parts = []
                for item in associated_node_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"associatedNodeIds={quote(joined)}")
            else:
                value = str(associated_node_ids)
                if isinstance(associated_node_ids, bool):
                    value = value.lower()
                query_params.append(f"associatedNodeIds={quote(value)}")
        if content_refs is not None:
            if isinstance(content_refs, list):
                joined_parts = []
                for item in content_refs:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"contentRefs={quote(joined)}")
            else:
                value = str(content_refs)
                if isinstance(content_refs, bool):
                    value = value.lower()
                query_params.append(f"contentRefs={quote(value)}")
        if include_request_form_custom_properties is not None:
            value = str(include_request_form_custom_properties)
            if isinstance(include_request_form_custom_properties, bool):
                value = value.lower()
            query_params.append(f"includeRequestFormCustomProperties={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_requests_async(
        self,
        input: DeleteRequest,
        space_id: str,
    ) -> dict[str, Any] | None:
        """
        Delete requests

        Bulk delete requests.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/requests"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_request_async(
        self,
        input: CreateRequest,
        space_id: str,
    ) -> dict[str, Any] | None:
        """
        Create request

        Create a request.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/requests"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_request_async(
        self,
        space_id: str,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        Get request

        Get a single request.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/requests"
            f"/{quote(str(request_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_request_async(
        self,
        space_id: str,
        request_id: str,
    ) -> None:
        """
        Delete request

        Delete a request.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/requests"
            f"/{quote(str(request_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def update_request_async(
        self,
        input: UpdateRequest,
        space_id: str,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        Update request

        Update a request.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/requests"
            f"/{quote(str(request_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_status_schemas_async(
        self,
        space_id: str,
        is_default: Optional[bool] = None,
        type_: Optional[str] = None,
        ids: Optional[List[str]] = None,
        creator_ids: Optional[List[str]] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get status schemas

        Get a list of task/request status schemas. These schemas hold the data
        for set statuses for specific work. Example: `(Not started, In
        progress, Complete)`
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/statusschema"
        )
        query_params = []
        item: object
        if is_default is not None:
            value = str(is_default)
            if isinstance(is_default, bool):
                value = value.lower()
            query_params.append(f"isDefault={quote(value)}")
        if type_ is not None:
            value = str(type_)
            if isinstance(type_, bool):
                value = value.lower()
            query_params.append(f"type={quote(value)}")
        if ids is not None:
            if isinstance(ids, list):
                joined_parts = []
                for item in ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"ids={quote(joined)}")
            else:
                value = str(ids)
                if isinstance(ids, bool):
                    value = value.lower()
                query_params.append(f"ids={quote(value)}")
        if creator_ids is not None:
            if isinstance(creator_ids, list):
                joined_parts = []
                for item in creator_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"creatorIds={quote(joined)}")
            else:
                value = str(creator_ids)
                if isinstance(creator_ids, bool):
                    value = value.lower()
                query_params.append(f"creatorIds={quote(value)}")
        if cursor is not None:
            value = str(cursor)
            if isinstance(cursor, bool):
                value = value.lower()
            query_params.append(f"cursor={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_status_schema_async(
        self,
        space_id: str,
        status_schema_id: str,
    ) -> dict[str, Any] | None:
        """
        Get status schema

        Get a single status schema by ID. These schemas hold the data for set
        statuses for specific work. Example: `(Not started, In progress,
        Complete)`
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/statusschema"
            f"/{quote(str(status_schema_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_tasks_async(
        self,
        space_id: str,
        planned_end_date_from: Optional[str] = None,
        planned_end_date_to: Optional[str] = None,
        planned_start_date_from: Optional[str] = None,
        planned_start_date_to: Optional[str] = None,
        created_at_from: Optional[str] = None,
        created_at_to: Optional[str] = None,
        updated_at_from: Optional[str] = None,
        updated_at_to: Optional[str] = None,
        ids: Optional[List[str]] = None,
        title: Optional[str] = None,
        description: Optional[str] = None,
        assignee_ids: Optional[List[str]] = None,
        priorities: Optional[List[str]] = None,
        keywords: Optional[str] = None,
        assigner_ids: Optional[List[str]] = None,
        step_ids: Optional[List[int]] = None,
        status_schema_id: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
        sort: Optional[List[str]] = None,
        recursive: Optional[bool] = None,
        project_id: Optional[str] = None,
        has_project: Optional[bool] = None,
        custom_properties: Optional[str] = None,
        follower_ids: Optional[List[str]] = None,
        associated_node_ids: Optional[List[str]] = None,
        creator_ids: Optional[List[str]] = None,
        include_associations: Optional[bool] = None,
        parent_id: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get tasks

        Get a list of tasks.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/tasks"
        )
        query_params = []
        item: object
        if planned_end_date_from is not None:
            value = str(planned_end_date_from)
            if isinstance(planned_end_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateFrom={quote(value)}")
        if planned_end_date_to is not None:
            value = str(planned_end_date_to)
            if isinstance(planned_end_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedEndDateTo={quote(value)}")
        if planned_start_date_from is not None:
            value = str(planned_start_date_from)
            if isinstance(planned_start_date_from, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateFrom={quote(value)}")
        if planned_start_date_to is not None:
            value = str(planned_start_date_to)
            if isinstance(planned_start_date_to, bool):
                value = value.lower()
            query_params.append(f"plannedStartDateTo={quote(value)}")
        if created_at_from is not None:
            value = str(created_at_from)
            if isinstance(created_at_from, bool):
                value = value.lower()
            query_params.append(f"createdAtFrom={quote(value)}")
        if created_at_to is not None:
            value = str(created_at_to)
            if isinstance(created_at_to, bool):
                value = value.lower()
            query_params.append(f"createdAtTo={quote(value)}")
        if updated_at_from is not None:
            value = str(updated_at_from)
            if isinstance(updated_at_from, bool):
                value = value.lower()
            query_params.append(f"updatedAtFrom={quote(value)}")
        if updated_at_to is not None:
            value = str(updated_at_to)
            if isinstance(updated_at_to, bool):
                value = value.lower()
            query_params.append(f"updatedAtTo={quote(value)}")
        if ids is not None:
            if isinstance(ids, list):
                joined_parts = []
                for item in ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"ids={quote(joined)}")
            else:
                value = str(ids)
                if isinstance(ids, bool):
                    value = value.lower()
                query_params.append(f"ids={quote(value)}")
        if title is not None:
            value = str(title)
            if isinstance(title, bool):
                value = value.lower()
            query_params.append(f"title={quote(value)}")
        if description is not None:
            value = str(description)
            if isinstance(description, bool):
                value = value.lower()
            query_params.append(f"description={quote(value)}")
        if assignee_ids is not None:
            if isinstance(assignee_ids, list):
                joined_parts = []
                for item in assignee_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"assigneeIds={quote(joined)}")
            else:
                value = str(assignee_ids)
                if isinstance(assignee_ids, bool):
                    value = value.lower()
                query_params.append(f"assigneeIds={quote(value)}")
        if priorities is not None:
            if isinstance(priorities, list):
                joined_parts = []
                for item in priorities:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"priorities={quote(joined)}")
            else:
                value = str(priorities)
                if isinstance(priorities, bool):
                    value = value.lower()
                query_params.append(f"priorities={quote(value)}")
        if keywords is not None:
            value = str(keywords)
            if isinstance(keywords, bool):
                value = value.lower()
            query_params.append(f"keywords={quote(value)}")
        if assigner_ids is not None:
            if isinstance(assigner_ids, list):
                joined_parts = []
                for item in assigner_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"assignerIds={quote(joined)}")
            else:
                value = str(assigner_ids)
                if isinstance(assigner_ids, bool):
                    value = value.lower()
                query_params.append(f"assignerIds={quote(value)}")
        if step_ids is not None:
            if isinstance(step_ids, list):
                joined_parts = []
                for item in step_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"stepIds={quote(joined)}")
            else:
                value = str(step_ids)
                if isinstance(step_ids, bool):
                    value = value.lower()
                query_params.append(f"stepIds={quote(value)}")
        if status_schema_id is not None:
            value = str(status_schema_id)
            if isinstance(status_schema_id, bool):
                value = value.lower()
            query_params.append(f"statusSchemaId={quote(value)}")
        if cursor is not None:
            value = str(cursor)
            if isinstance(cursor, bool):
                value = value.lower()
            query_params.append(f"cursor={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if sort is not None:
            if isinstance(sort, list):
                joined_parts = []
                for item in sort:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"sort={quote(joined)}")
            else:
                value = str(sort)
                if isinstance(sort, bool):
                    value = value.lower()
                query_params.append(f"sort={quote(value)}")
        if recursive is not None:
            value = str(recursive)
            if isinstance(recursive, bool):
                value = value.lower()
            query_params.append(f"recursive={quote(value)}")
        if project_id is not None:
            value = str(project_id)
            if isinstance(project_id, bool):
                value = value.lower()
            query_params.append(f"projectId={quote(value)}")
        if has_project is not None:
            value = str(has_project)
            if isinstance(has_project, bool):
                value = value.lower()
            query_params.append(f"hasProject={quote(value)}")
        if custom_properties is not None:
            value = str(custom_properties)
            if isinstance(custom_properties, bool):
                value = value.lower()
            query_params.append(f"customProperties={quote(value)}")
        if follower_ids is not None:
            if isinstance(follower_ids, list):
                joined_parts = []
                for item in follower_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"followerIds={quote(joined)}")
            else:
                value = str(follower_ids)
                if isinstance(follower_ids, bool):
                    value = value.lower()
                query_params.append(f"followerIds={quote(value)}")
        if associated_node_ids is not None:
            if isinstance(associated_node_ids, list):
                joined_parts = []
                for item in associated_node_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"associatedNodeIds={quote(joined)}")
            else:
                value = str(associated_node_ids)
                if isinstance(associated_node_ids, bool):
                    value = value.lower()
                query_params.append(f"associatedNodeIds={quote(value)}")
        if creator_ids is not None:
            if isinstance(creator_ids, list):
                joined_parts = []
                for item in creator_ids:
                    value = str(item)
                    if isinstance(item, bool):
                        value = value.lower()
                    joined_parts.append(value)
                joined = ','.join(joined_parts)
                query_params.append(f"creatorIds={quote(joined)}")
            else:
                value = str(creator_ids)
                if isinstance(creator_ids, bool):
                    value = value.lower()
                query_params.append(f"creatorIds={quote(value)}")
        if include_associations is not None:
            value = str(include_associations)
            if isinstance(include_associations, bool):
                value = value.lower()
            query_params.append(f"includeAssociations={quote(value)}")
        if parent_id is not None:
            value = str(parent_id)
            if isinstance(parent_id, bool):
                value = value.lower()
            query_params.append(f"parentId={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_task_async(
        self,
        input: CreateTask,
        space_id: str,
    ) -> dict[str, Any] | None:
        """
        Create task

        Create a task.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner/v2/spaces/{quote(str(space_id), safe='')}/tasks"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_task_async(
        self,
        space_id: str,
        task_id: str,
    ) -> dict[str, Any] | None:
        """
        Get task

        Get a single task by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/tasks"
            f"/{quote(str(task_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_task_async(
        self,
        space_id: str,
        task_id: str,
    ) -> dict[str, Any] | None:
        """
        Delete task

        Delete a task.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/tasks"
            f"/{quote(str(task_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_task_async(
        self,
        input: UpdateTask,
        space_id: str,
        task_id: str,
    ) -> dict[str, Any] | None:
        """
        Update task

        Update an existing task.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/planner"
            f"/v2"
            f"/spaces"
            f"/{quote(str(space_id), safe='')}"
            f"/tasks"
            f"/{quote(str(task_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
