# Copyright (c) Microsoft Corporation. All rights reserved.

# DocuwareClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class SearchForDocumentsInFileCabinetInput:
    """
    Search in file cabinet
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SearchForDocumentsInFileCabinetResponse:
    """
    Response for Search in file cabinet
    """

    count: Optional[int] = field(default=None, metadata={"wire_name": "Count"})
    """Count"""
    documents: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Documents"},
    )
    """Documents"""


@dataclass
class GetOrganizationResponse:
    """
    Response for Get organization
    """

    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """Name"""


@dataclass
class GetFileCabinetsResponse:
    """
    Response for Get file cabinets and document trays
    """

    file_cabinets: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "FileCabinets"},
    )
    """FileCabinets"""


@dataclass
class GetDocumentInformationResponse:
    """
    Response for Get document information
    """

    sections: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Sections"},
    )
    """Sections"""
    document_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "DocumentId"},
    )
    """DocumentId"""
    index_fields: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "IndexFields"},
    )
    """IndexFields"""
    document_title: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DocumentTitle"},
    )
    """DocumentTitle"""
    file_cabinet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FileCabinetId"},
    )
    """FileCabinetId"""
    total_pages: Optional[int] = field(
        default=None,
        metadata={"wire_name": "TotalPages"},
    )
    """TotalPages"""
    file_size: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FileSize"},
    )
    """FileSize"""
    content_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ContentType"},
    )
    """ContentType"""
    version_status: Optional[str] = field(
        default=None,
        metadata={"wire_name": "VersionStatus"},
    )
    """VersionStatus"""
    document_flags: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "DocumentFlags"},
    )
    """DocumentFlags"""


@dataclass
class UpdateIndexFieldsInput:
    """
    Update index fields
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class UpdateIndexFieldsResponse:
    """
    Response for Update index fields
    """

    sections: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Sections"},
    )
    """Sections"""
    document_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "DocumentId"},
    )
    """DocumentId"""
    index_fields: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "IndexFields"},
    )
    """IndexFields"""
    document_title: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DocumentTitle"},
    )
    """DocumentTitle"""
    file_cabinet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FileCabinetId"},
    )
    """FileCabinetId"""
    total_pages: Optional[int] = field(
        default=None,
        metadata={"wire_name": "TotalPages"},
    )
    """TotalPages"""
    file_size: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FileSize"},
    )
    """FileSize"""
    content_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ContentType"},
    )
    """ContentType"""
    version_status: Optional[str] = field(
        default=None,
        metadata={"wire_name": "VersionStatus"},
    )
    """VersionStatus"""
    document_flags: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "DocumentFlags"},
    )
    """DocumentFlags"""


@dataclass
class TransferDocumentInput:
    """
    Transfer documents
    """

    source_file_cabinet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "SourceFileCabinetId"},
    )
    """Select the source File Cabinet or Document Tray."""
    documents: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Documents"},
    )
    keep_source: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "KeepSource"},
    )
    """Choose whether the source documents should be deleted or not."""
    fill_intellix: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "FillIntellix"},
    )
    """Choose whether intelligent indexing should be used when storing."""


@dataclass
class TransferDocumentResponse:
    """
    Response for Transfer documents
    """

    count: Optional[int] = field(default=None, metadata={"wire_name": "Count"})
    """Count"""
    documents: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Documents"},
    )
    """Documents"""


@dataclass
class PlaceAStampInput:
    """
    Place a stamp
    """

    file_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FileNumber"},
    )
    """Specify the file/section containing the page to be stamped."""
    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "PageNumber"},
    )
    """Specify the page number to place the stamp on."""
    layer: Optional[int] = field(default=None, metadata={"wire_name": "Layer"})
    """Specify the document layer to place the stamp on [1-5]"""
    position_x: Optional[float] = field(
        default=None,
        metadata={"wire_name": "PositionX"},
    )
    """[Optional] Position from the left edge of the page in mm."""
    position_y: Optional[float] = field(
        default=None,
        metadata={"wire_name": "PositionY"},
    )
    """[Optional] Position from the upper edge of the page in mm."""
    stamp_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "StampId"},
    )
    """Select or specify the stamp to be used."""
    password: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Password"},
    )
    """Specify the password for the stamp (if required)."""
    fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Fields"},
    )


@dataclass
class PlaceAStampResponse:
    """
    Response for Place a stamp
    """

    created: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Created"},
    )
    """Created"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})
    """Type"""
    color: Optional[str] = field(default=None, metadata={"wire_name": "Color"})
    """Color"""
    rotation: Optional[int] = field(
        default=None,
        metadata={"wire_name": "Rotation"},
    )
    """Rotation"""
    transparent: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Transparent"},
    )
    """Transparent"""
    stroke_width: Optional[int] = field(
        default=None,
        metadata={"wire_name": "StrokeWidth"},
    )
    """StrokeWidth"""
    guid: Optional[str] = field(default=None, metadata={"wire_name": "Guid"})
    """Guid"""


@dataclass
class GetDialogsResponse:
    """
    Response for Get dialogs
    """

    dialogs: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Dialogs"},
    )
    """Dialogs"""


@dataclass
class GetStampsResponse:
    """
    Response for Get stamps
    """

    stamps: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Stamps"},
    )
    """Stamps"""


@dataclass
class GetStampFieldsResponse:
    """
    Response for Get stamp fields
    """

    fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Fields"},
    )
    """Fields"""


@dataclass
class GetFileCabinetFieldsResponse:
    """
    Response for Get file cabinet fields
    """

    fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Fields"},
    )
    """Fields"""


@dataclass
class GetDialogFieldsResponse:
    """
    Response for Get dialog fields
    """

    fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Fields"},
    )
    """Fields"""


@dataclass
class ListDocumentsInDocumentTrayResponse:
    """
    Response for List documents in document tray
    """

    count: Optional[int] = field(default=None, metadata={"wire_name": "Count"})
    """Count"""
    documents: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Documents"},
    )


# Client Class

class DocuwareClient(ConnectorClientBase):
    """Typed client for docuware connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a DocuwareClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "docuware"

    async def search_for_documents_in_file_cabinet_async(
        self,
        input: SearchForDocumentsInFileCabinetInput,
        file_cabinet: str,
        search_dialog_id: str,
    ) -> dict[str, Any] | None:
        """
        Search in file cabinet

        Search a file cabinet for documents matching the specified criteria.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets/{quote(str(file_cabinet), safe='')}/Search"
        )
        query_params = []
        value = str(search_dialog_id)
        if isinstance(search_dialog_id, bool):
            value = value.lower()
        query_params.append(f"SearchDialogId={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organization_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get organization

        Get the organization name.
        """
        request_url = f"{self._connection_runtime_url}/Organization"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_file_cabinets_async(
        self,
        file_cabinet_type: str,
    ) -> dict[str, Any] | None:
        """
        Get file cabinets and document trays

        Gets a list of file cabinets and/or document trays.
        """
        request_url = f"{self._connection_runtime_url}/FileCabinets"
        query_params = []
        value = str(file_cabinet_type)
        if isinstance(file_cabinet_type, bool):
            value = value.lower()
        query_params.append(f"FileCabinetType={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_document_information_async(
        self,
        file_cabinet_id: str,
        document_id: str,
    ) -> dict[str, Any] | None:
        """
        Get document information

        Gets information about a document.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_document_async(
        self,
        file_cabinet_id: str,
        document_id: str,
    ) -> None:
        """
        Delete a document

        Deletes a document from a file cabinet or document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def download_file_async(
        self,
        file_cabinet_id: str,
        document_id: str,
        file_number: str,
        document_format: str,
    ) -> bytes:
        """
        Download a file

        Downloads a file/section of a document stored in a file cabinet or
        document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
            f"/Sections"
            f"/{quote(str(file_number), safe='')}"
            f"/Download"
        )
        query_params = []
        value = str(document_format)
        if isinstance(document_format, bool):
            value = value.lower()
        query_params.append(f"DocumentFormat={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def download_document_async(
        self,
        file_cabinet_id: str,
        document_id: str,
        document_format: str,
    ) -> bytes:
        """
        Download a document

        Downloads a document from a file cabinet or document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
            f"/Download"
        )
        query_params = []
        value = str(document_format)
        if isinstance(document_format, bool):
            value = value.lower()
        query_params.append(f"DocumentFormat={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def update_index_fields_async(
        self,
        input: UpdateIndexFieldsInput,
        file_cabinet_id: str,
        document_id: str,
    ) -> dict[str, Any] | None:
        """
        Update index fields

        Updates the index fields of a document.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
            f"/Fields"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def transfer_document_async(
        self,
        input: TransferDocumentInput,
        destination_file_cabinet_id: str,
        store_dialog_id: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Transfer documents

        Moves one or more documents from one file cabinet/document tray to
        another.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(destination_file_cabinet_id), safe='')}"
            f"/Task"
            f"/Transfer"
        )
        query_params = []
        if store_dialog_id is not None:
            value = str(store_dialog_id)
            if isinstance(store_dialog_id, bool):
                value = value.lower()
            query_params.append(f"StoreDialogID={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def place_a_stamp_async(
        self,
        input: PlaceAStampInput,
        file_cabinet_id: str,
        document_id: str,
    ) -> dict[str, Any] | None:
        """
        Place a stamp

        Places a stamp on a document.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet_id), safe='')}"
            f"/Documents"
            f"/{quote(str(document_id), safe='')}"
            f"/Annotation"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_dialogs_async(
        self,
        file_cabinet: str,
        dialog_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get dialogs

        Gets a list of dialogs for a file cabinet or document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets/{quote(str(file_cabinet), safe='')}/Dialogs"
        )
        query_params = []
        if dialog_type is not None:
            value = str(dialog_type)
            if isinstance(dialog_type, bool):
                value = value.lower()
            query_params.append(f"DialogType={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_stamps_async(
        self,
        file_cabinet: str,
    ) -> dict[str, Any] | None:
        """
        Get stamps

        Gets a list of stamps for a file cabinet or document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets/{quote(str(file_cabinet), safe='')}/Stamps"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_stamp_fields_async(
        self,
        file_cabinet: str,
        stamp: str,
    ) -> dict[str, Any] | None:
        """
        Get stamp fields

        Gets a list of fields for a stamp.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet), safe='')}"
            f"/Stamps"
            f"/{quote(str(stamp), safe='')}"
            f"/Fields"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_file_cabinet_fields_async(
        self,
        file_cabinet: str,
        field_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get file cabinet fields

        Gets a list of fields for a file cabinet.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets/{quote(str(file_cabinet), safe='')}/Fields"
        )
        query_params = []
        if field_type is not None:
            value = str(field_type)
            if isinstance(field_type, bool):
                value = value.lower()
            query_params.append(f"FieldType={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_dialog_fields_async(
        self,
        file_cabinet: str,
        dialog_id: str,
    ) -> dict[str, Any] | None:
        """
        Get dialog fields

        Gets the fields for a dialog
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/FileCabinets"
            f"/{quote(str(file_cabinet), safe='')}"
            f"/Dialogs"
            f"/{quote(str(dialog_id), safe='')}"
            f"/Fields"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_documents_in_document_tray_async(
        self,
        document_tray: str,
    ) -> dict[str, Any] | None:
        """
        List documents in document tray

        List documents from the specified document tray.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/DocumentTrays/{quote(str(document_tray), safe='')}/Search"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
