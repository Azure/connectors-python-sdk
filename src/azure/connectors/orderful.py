# Copyright (c) Microsoft Corporation. All rights reserved.

# OrderfulClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict
from urllib.parse import quote

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CreateTransactionInput:
    """
    Create Transaction
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class CommunicationChannelInput:
    """
    Listen to Communication Channel
    """

    is_active: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isActive"},
    )
    name: Optional[str] = None
    """Communication Channel Name."""
    config: Optional[Dict[str, Any]] = None
    """Configuration of the Trigger"""
    owner_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "ownerId"},
    )
    """Owner of the Orderful account."""


@dataclass
class CommunicationChannelCreationResponse:
    """
    Response for Listen to Communication Channel
    """

    id: Optional[int] = None


@dataclass
class CommunicationChannelEventResponse:
    """
    Definition: CommunicationChannelEventResponse
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class CommunicationChannelRequestBody:
    """
    Definition: CommunicationChannelRequestBody
    """

    is_active: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isActive"},
    )
    """If the State of the Trigger is active."""
    name: Optional[str] = None
    """Communication Channel Name."""
    config: Optional[Dict[str, Any]] = None
    """Configuration of the Trigger."""
    owner_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "ownerId"},
    )
    """Owner of the Orderful account."""


# Client Class

class OrderfulClient(ConnectorClientBase):
    """Typed client for orderful connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a OrderfulClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "orderful"

    async def list_transactions_async(
        self,
    ) -> None:
        """
        List Transactions

        Fetches a list of transactions as specified by the filter/sort
        parameters supplied.
        """
        request_url = f"{self._connection_runtime_url}/v2/transactions"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def create_transaction_async(
        self,
        input: CreateTransactionInput,
    ) -> None:
        """
        Create Transaction

        Create Transaction.
        """
        request_url = f"{self._connection_runtime_url}/v2/transactions"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_transaction_by_id_async(
        self,
        transaction_id: int,
    ) -> None:
        """
        Get Transaction by ID

        Get transaction by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v2/transactions/{quote(str(transaction_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "Communication-Channel": {
        "operation_id": "Communication-Channel",
        "path": "/{connectionId}/v2/destinations",
        "method": "post",
        "required_parameters": [],
        "callback_payload_type": "CommunicationChannelCreationResponse",
    },
}
