# Copyright (c) Microsoft Corporation. All rights reserved.

# MeetingroommapClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class GetCustomLocationsByImageNameResponse:
    """
    Response for Get locations by image
    """

    custom_location_collection: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "customLocationCollection"},
    )
    """customLocationCollection"""
    map_image: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "mapImage"},
    )
    """mapImage"""


@dataclass
class GetOfficeLocationsByImageResponse:
    """
    Response for Get office locations on image
    """

    image: Optional[Dict[str, Any]] = None
    """image"""
    attached_office_locations: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "attachedOfficeLocations"},
    )
    """attachedOfficeLocations"""


@dataclass
class GetRoomWithPersonsDetailsResponse:
    """
    Response for Get office location details
    """

    office_location_map: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "officeLocationMap"},
    )
    """officeLocationMap"""
    map_image: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "mapImage"},
    )
    """mapImage"""


@dataclass
class AADMeetingRoomCollection:
    """
    Response for Get all rooms
    """

    rooms: Optional[List[AADMeetingRoom]] = None


@dataclass
class GetRoomsByImageNameResponse:
    """
    Response for Get all meeting rooms by image
    """

    meeting_room_collection: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "meetingRoomCollection"},
    )
    """meetingRoomCollection"""
    map_image: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "mapImage"},
    )
    """mapImage"""


@dataclass
class GetCustomLocationsByCategoriesQuery:
    """
    Definition: GetCustomLocationsByCategoriesQuery
    """

    categories: Optional[List[str]] = None


@dataclass
class GetCustomLocationListByImageNameQuery:
    """
    Definition: GetCustomLocationListByImageNameQuery
    """

    image_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "imageName"},
    )
    """The unique image name"""
    categories: Optional[List[str]] = None


@dataclass
class AADMeetingRoom:
    """
    Definition: AADMeetingRoom
    """

    name: Optional[str] = None
    address: Optional[str] = None


@dataclass
class UserDetailsDTO:
    """
    Definition: UserDetailsDTO
    """

    tenant_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tenantId"},
    )
    email: Optional[str] = None


# Client Class

class MeetingroommapClient(ConnectorClientBase):
    """Typed client for meetingroommap connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a MeetingroommapClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "meetingroommap"

    async def get_custom_locations_async(
        self,
        input: GetCustomLocationsByCategoriesQuery,
    ) -> None:
        """
        Get all custom locations

        Get all custom locations
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/CustomLocations/GetCustomLocations"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_custom_locations_by_image_name_async(
        self,
        input: GetCustomLocationListByImageNameQuery,
    ) -> dict[str, Any] | None:
        """
        Get locations by image

        Get all custom locations for a given image
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/CustomLocations/GetCustomLocationsByImageName"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_categories_async(
        self,
    ) -> dict[str, Any] | None:
        """
        GetCategories

        Get custom categories
        """
        request_url = (
            f"{self._connection_runtime_url}/api/CustomLocations/categories"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def location_details_async(
        self,
        location_id: str,
    ) -> None:
        """
        Get location by id

        Get location details including image url by the location id
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/CustomLocations/{quote(str(location_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def search_locations_async(
        self,
        location_name: str,
        category: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Search location by name

        Search custom locations by name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/CustomLocations"
            f"/findbyname"
            f"/{quote(str(location_name), safe='')}"
        )
        query_params = []
        if category is not None:
            value = str(category)
            if isinstance(category, bool):
                value = value.lower()
            query_params.append(f"Category={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_custom_location_image_async(
        self,
        location_id: str,
        large: Optional[str] = None,
    ) -> bytes:
        """
        Get image for location

        Get image for specific custom location
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/CustomLocations"
            f"/createimage"
            f"/{quote(str(location_id), safe='')}"
        )
        query_params = []
        if large is not None:
            value = str(large)
            if isinstance(large, bool):
                value = value.lower()
            query_params.append(f"Large={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def images_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get all images

        Get list of all floorplans/images
        """
        request_url = f"{self._connection_runtime_url}/api/MapImage/thumbnails"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_meeting_room_image_async(
        self,
        room_name: str,
        large: Optional[str] = None,
    ) -> bytes:
        """
        Get image for meeting room

        Get image for a specific meeting room
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/MapImage/create/{quote(str(room_name), safe='')}"
        )
        query_params = []
        if large is not None:
            value = str(large)
            if isinstance(large, bool):
                value = value.lower()
            query_params.append(f"Large={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def next_meetings_async(
        self,
        meeting_count: str,
    ) -> None:
        """
        Get next meetings

        Get the next meetings for the current user
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/MapImage"
            f"/meetings"
            f"/{quote(str(meeting_count), safe='')}"
            f"/roomdetails"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_meeting_room_details_async(
        self,
        room_name: str,
    ) -> None:
        """
        Get meetingroom details

        Get meetingroom details
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/MapImage/roomdetails_v2/{quote(str(room_name), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_office_locations_async(
        self,
    ) -> None:
        """
        Get all office locations

        Get all office locations
        """
        request_url = f"{self._connection_runtime_url}/api/officelocations"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def search_coworkers_async(
        self,
        person_search: str,
    ) -> dict[str, Any] | None:
        """
        Search coworker

        Search coworker by name/email
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/officelocations"
            f"/searchCoworkers"
            f"/{quote(str(person_search), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_office_locations_by_image_async(
        self,
        image_name: str,
    ) -> dict[str, Any] | None:
        """
        Get office locations on image

        Get all office locations mapped to a specific image
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/officelocations"
            f"/bymapimage"
            f"/{quote(str(image_name), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_room_with_persons_details_async(
        self,
        office_location_name: str,
        inlude_user_info: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get office location details

        Get office location details including list of people for that office
        location
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/officelocations"
            f"/mapimagewithpersoninfo"
            f"/{quote(str(office_location_name), safe='')}"
        )
        query_params = []
        if inlude_user_info is not None:
            value = str(inlude_user_info)
            if isinstance(inlude_user_info, bool):
                value = value.lower()
            query_params.append(f"InludeUserInfo={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_office_location_image_async(
        self,
        office_location_name: str,
        large: Optional[str] = None,
    ) -> bytes:
        """
        Get image of office location

        Get image of specific office location
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/officelocationimage"
            f"/create"
            f"/{quote(str(office_location_name), safe='')}"
        )
        query_params = []
        if large is not None:
            value = str(large)
            if isinstance(large, bool):
                value = value.lower()
            query_params.append(f"Large={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def get_rooms_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get all rooms

        Get all rooms (defined in Azure AD as meeting rooms)
        """
        request_url = f"{self._connection_runtime_url}/api/rooms"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def search_meeting_rooms_async(
        self,
        name: str,
    ) -> dict[str, Any] | None:
        """
        Search for meeting room

        Get rooms by name search (from Azure AD meeting rooms)
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/rooms/findbyname/{quote(str(name), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def room_lists_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get meeting rooms lists

        Get meeting rooms lists as defined in Azure AD
        """
        request_url = f"{self._connection_runtime_url}/api/rooms/lists"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def rooms_by_list_address_async(
        self,
        meeting_room_list_address: str,
    ) -> dict[str, Any] | None:
        """
        Get meeting rooms by list

        Get all meeting rooms from specific list name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/rooms/{quote(str(meeting_room_list_address), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_rooms_by_image_name_async(
        self,
        image_name: str,
    ) -> dict[str, Any] | None:
        """
        Get all meeting rooms by image

        Get all meeting rooms mapped to a specific image
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/rooms/GetRoomsByImageName/{quote(str(image_name), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
