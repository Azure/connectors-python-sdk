# Copyright (c) Microsoft Corporation. All rights reserved.

# TwitterClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class TweetModel:
    """
    Response for Get user timeline
    """

    tweet_text: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetText"},
    )
    """Text content of the tweet"""
    tweet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetId"},
    )
    """Id of the tweet"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CreatedAt"},
    )
    """Time at which the tweet was posted"""
    created_at_iso: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CreatedAtIso"},
    )
    """Time at which the tweet was posted"""
    retweet_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "RetweetCount"},
    )
    """Total number of re-tweets for the tweet"""
    tweeted_by: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetedBy"},
    )
    """Name of the user who has posted the tweet"""
    media_urls: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "MediaUrls"},
    )
    """Url of the media posted along with the tweet"""
    tweet_language_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetLanguageCode"},
    )
    """Language code of the tweet"""
    tweet_in_reply_to_user_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetInReplyToUserId"},
    )
    """
    User Id of the author of the tweet that the current tweet is a reply to
    """
    favorited: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Favorited"},
    )
    """Indicates whether the tweet is marked as favorited or not"""
    user_mentions: Optional[List[UserMentionsModel]] = field(
        default=None,
        metadata={"wire_name": "UserMentions"},
    )
    """List of users mentioned in the tweet"""
    original_tweet: Optional[OriginalTweetModel] = field(
        default=None,
        metadata={"wire_name": "OriginalTweet"},
    )
    user_details: Optional[UserDetailsModel] = field(
        default=None,
        metadata={"wire_name": "UserDetails"},
    )


@dataclass
class UserDetailsModel:
    """
    Response for Get followers
    """

    full_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FullName"},
    )
    """Name of the user"""
    location: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Location"},
    )
    """Location of the user"""
    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    """X id (handle) of the user"""
    user_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "UserName"},
    )
    """Screen name of the user"""
    followers_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FollowersCount"},
    )
    """Number of followers"""
    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    """User description"""
    statuses_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "StatusesCount"},
    )
    """User status count"""
    friends_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FriendsCount"},
    )
    """Number of friends"""
    favourites_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FavouritesCount"},
    )
    """Number of tweets that the user has favorited"""
    profile_image_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ProfileImageUrl"},
    )
    """Url of the profile image"""


@dataclass
class TweetResponseModel:
    """
    Response for Post a tweet
    """

    tweet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetId"},
    )
    """ID of the tweet posted"""


@dataclass
class TriggerBatchResponseTweetModel:
    """
    Response for When a new tweet is posted
    """

    value: Optional[List[TweetModel]] = None
    """A list of the response objects"""


@dataclass
class UserMentionsModel:
    """
    Definition: UserMentionsModel
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    """X id (handle) of the user"""
    full_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FullName"},
    )
    """Full name of the user"""
    user_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "UserName"},
    )
    """Screen name of the user"""


@dataclass
class OriginalTweetModel:
    """
    Definition: OriginalTweetModel
    """

    tweet_text: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetText"},
    )
    """Text content of the original retrieved tweet"""
    tweet_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetId"},
    )
    """Id of the original tweet"""
    created_at: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CreatedAt"},
    )
    """Time at which original tweet was posted"""
    created_at_iso: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CreatedAtIso"},
    )
    """Time at which original tweet was posted"""
    retweet_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "RetweetCount"},
    )
    """Total number of re-tweets for this original tweet"""
    tweeted_by: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetedBy"},
    )
    """User name of the person who posted the original tweet"""
    media_urls: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "MediaUrls"},
    )
    """Url of the media posted along with the original tweet"""
    tweet_language_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetLanguageCode"},
    )
    """Language code for the original tweet"""
    tweet_in_reply_to_user_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "TweetInReplyToUserId"},
    )
    """
    User Id of the author of the tweet that the current original tweet is a
    reply to
    """
    favorited: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Favorited"},
    )
    """Indicates whether the original tweet is marked as favorited"""
    user_mentions: Optional[List[OriginalTweetUserMentionsModel]] = field(
        default=None,
        metadata={"wire_name": "UserMentions"},
    )
    """The list of user mentioned in the tweet"""
    user_details: Optional[OriginalTweetUserDetailsModel] = field(
        default=None,
        metadata={"wire_name": "UserDetails"},
    )


@dataclass
class OriginalTweetUserMentionsModel:
    """
    Definition: OriginalTweetUserMentionsModel
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    """X id (handle) of the user"""
    full_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FullName"},
    )
    """Full name of the user"""
    user_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "UserName"},
    )
    """Screen name of the user"""


@dataclass
class OriginalTweetUserDetailsModel:
    """
    Definition: OriginalTweetUserDetailsModel
    """

    full_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FullName"},
    )
    """Name of the user"""
    location: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Location"},
    )
    """Location of the user"""
    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    """X id (handle) of the user"""
    user_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "UserName"},
    )
    """Screen name of the user"""
    followers_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FollowersCount"},
    )
    """Number of followers"""
    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    """User description"""
    statuses_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "StatusesCount"},
    )
    """User status count"""
    friends_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FriendsCount"},
    )
    """Number of friends"""
    favourites_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FavouritesCount"},
    )
    """Number of tweets that the user has favorited"""
    profile_image_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ProfileImageUrl"},
    )
    """Url of the profile image"""


# Client Class

class TwitterClient(ConnectorClientBase):
    """Typed client for twitter connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a TwitterClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "twitter"

    async def user_timeline_async(
        self,
        user_name: str,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get user timeline

        This operation gets a list of the most recent tweets posted by a given
        user.
        """
        request_url = f"{self._connection_runtime_url}/usertimeline"
        query_params = []
        value = str(user_name)
        if isinstance(user_name, bool):
            value = value.lower()
        query_params.append(f"userName={quote(value)}")
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def home_timeline_async(
        self,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get home timeline

        This operation gets the most recent tweets and re-tweets posted by me
        and my followers.
        """
        request_url = f"{self._connection_runtime_url}/hometimeline"
        query_params = []
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def search_tweet_async(
        self,
        search_query: str,
        max_results: Optional[int] = None,
        since_id: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Search tweets

        This operation gets a list of relevant tweets matching the search
        query.
        """
        request_url = f"{self._connection_runtime_url}/searchtweets"
        query_params = []
        value = str(search_query)
        if isinstance(search_query, bool):
            value = value.lower()
        query_params.append(f"searchQuery={quote(value)}")
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if since_id is not None:
            value = str(since_id)
            if isinstance(since_id, bool):
                value = value.lower()
            query_params.append(f"sinceId={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def followers_async(
        self,
        user_name: str,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get followers

        This operation gets the list of users that follow a given user.
        """
        request_url = f"{self._connection_runtime_url}/followers"
        query_params = []
        value = str(user_name)
        if isinstance(user_name, bool):
            value = value.lower()
        query_params.append(f"userName={quote(value)}")
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def my_followers_async(
        self,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get my followers

        This operation gets the list of users who are following me.
        """
        request_url = f"{self._connection_runtime_url}/myfollowers"
        query_params = []
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def following_async(
        self,
        user_name: str,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get following

        The operation gets the list of people the given user follows.
        """
        request_url = f"{self._connection_runtime_url}/friends"
        query_params = []
        value = str(user_name)
        if isinstance(user_name, bool):
            value = value.lower()
        query_params.append(f"userName={quote(value)}")
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def my_following_async(
        self,
        max_results: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Get my following

        This operation gets the list of users that I am following.
        """
        request_url = f"{self._connection_runtime_url}/myfriends"
        query_params = []
        if max_results is not None:
            value = str(max_results)
            if isinstance(max_results, bool):
                value = value.lower()
            query_params.append(f"maxResults={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def user_async(
        self,
        user_name: str,
    ) -> dict[str, Any] | None:
        """
        Get user

        This operation gets the profile details for a given user, such as user
        name, description, followers count, and more.
        """
        request_url = f"{self._connection_runtime_url}/user"
        query_params = []
        value = str(user_name)
        if isinstance(user_name, bool):
            value = value.lower()
        query_params.append(f"userName={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def tweet_async(
        self,
        input: bytes,
        tweet_text: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Post a tweet

        This operation posts a new tweet.
        """
        request_url = f"{self._connection_runtime_url}/posttweet"
        query_params = []
        if tweet_text is not None:
            value = str(tweet_text)
            if isinstance(tweet_text, bool):
                value = value.lower()
            query_params.append(f"tweetText={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST",
            request_url,
            body=input,
            content_type="application/octet-stream",
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def retweet_async(
        self,
        tweet_id: str,
        trim_user: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Retweet

        This operation retweets a tweet.
        """
        request_url = f"{self._connection_runtime_url}/retweet"
        query_params = []
        value = str(tweet_id)
        if isinstance(tweet_id, bool):
            value = value.lower()
        query_params.append(f"tweetId={quote(value)}")
        if trim_user is not None:
            value = str(trim_user)
            if isinstance(trim_user, bool):
                value = value.lower()
            query_params.append(f"trimUser={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnNewTweet": {
        "operation_id": "OnNewTweet",
        "path": "/{connectionId}/onnewtweet",
        "method": "get",
        "required_parameters": ["searchQuery"],
        "callback_payload_type": "TriggerBatchResponseTweetModel",
    },
}
