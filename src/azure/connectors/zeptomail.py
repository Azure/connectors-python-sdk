# Copyright (c) Microsoft Corporation. All rights reserved.

# ZeptomailClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class GetMailAgentResponse:
    """
    Response for Get all Mail Agent
    """

    data: Optional[List[Dict[str, Any]]] = None
    """data"""
    message: Optional[str] = None
    """message"""


@dataclass
class GetProcessedEmailsResponse:
    """
    Response for Get processed email details
    """

    data: Optional[Dict[str, Any]] = None
    """data"""
    message: Optional[str] = None
    """message"""


@dataclass
class SendMailInput:
    """
    Send mail
    """

    mailagent_key: Optional[str] = None
    """Choose Mail Agent or enter the Mail Agent key."""
    bounce_address: Optional[str] = None
    """The email address to which bounced emails will be sent."""
    from_: Optional[FromAddress] = field(
        default=None,
        metadata={"wire_name": "from"},
    )
    to: Optional[List[EmailAddressItems]] = None
    """Recipient email address."""
    cc: Optional[List[EmailAddressItems]] = None
    """Email addresses of cc'd recipients."""
    bcc: Optional[List[EmailAddressItems]] = None
    """Email addresses of bcc'd recipients."""
    mailtype: Optional[str] = None
    """Format of the mail to be sent"""
    subject: Optional[str] = None
    """Subject line of your email"""
    htmlbody: Optional[str] = None
    """This is the body content for your email."""
    reply_to: Optional[List[ReplyToAddress]] = None
    """
    The email address to which the recipient's email responses will be
    addressed.
    """
    attachments: Optional[List[Dict[str, Any]]] = None
    """Attachments"""


@dataclass
class SuccessMessage:
    """
    Response for Send mail
    """

    data: Optional[List[Dict[str, Any]]] = None
    """data"""
    message: Optional[str] = None
    """The status of the request made."""
    request_id: Optional[str] = None
    """Unique reference ID of the request."""


@dataclass
class SendTemplateMailInput:
    """
    Send template mail
    """

    mailagent_key: Optional[str] = None
    """Choose Mail Agent or enter the Mail Agent key."""
    bounce_address: Optional[str] = None
    """the email address to which bounced emails will be sent."""
    mail_template_key: Optional[str] = None
    """Choose the Mail template"""
    from_: Optional[FromAddress] = field(
        default=None,
        metadata={"wire_name": "from"},
    )
    to: Optional[List[EmailAddressItems]] = None
    """Recipient email address."""
    cc: Optional[List[EmailAddressItems]] = None
    """Email addresses of cc'd recipients."""
    bcc: Optional[List[EmailAddressItems]] = None
    """Email addresses of bcc'd recipients."""
    merge_key_detail: Optional[List[Dict[str, Any]]] = None
    """
    You can use merge tags to replace the placeholders with multiple values for
    different recipients.
    """
    reply_to: Optional[List[ReplyToAddress]] = None
    """Reply To"""


@dataclass
class ProcessedMailStatsResponse:
    """
    Response for Processed emails analytics
    """

    hb: Optional[Dict[str, Any]] = None
    """Emails that have hard bounced"""
    sb: Optional[Dict[str, Any]] = None
    """sb"""
    sent: Optional[Dict[str, Any]] = None
    """Emails that have been sent"""


@dataclass
class FromAddress:
    """
    Definition: FromAddress
    """

    from_detail: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "from-detail"},
    )
    name: Optional[str] = None
    """Sender's user name"""


@dataclass
class EmailAddress:
    """
    Definition: EmailAddress
    """

    address: Optional[str] = None
    """Recipient's email address."""
    name: Optional[str] = None
    """Recipient's name."""


@dataclass
class ReplyToAddress:
    """
    Definition: ReplyToAddress
    """

    address: Optional[str] = None
    """
    A valid email address containing a domain that is verified in your Mail
    Agent.
    """
    name: Optional[str] = None
    """Name for the reply-to."""


@dataclass
class EmailAddressItems:
    """
    Definition: EmailAddressItems
    """

    email_address: Optional[EmailAddress] = None


# Client Class

class ZeptomailClient(ConnectorClientBase):
    """Typed client for zeptomail connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a ZeptomailClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "zeptomail"

    async def get_mail_agent_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get all Mail Agent

        This endpoint fetches details of all the Mail Agents in the account. It
        returns data including Mail Agent name, Mail Agent key, Created time,
        Status. \\n\\nData Fields Explained:\\n- mailagent_name: Name of the
        Mail Agent.\\n- mailagent_key: Mail Agent Key.\\n- mailagent_alias:
        Mail Agent Alias.\\n- created_time: Time at which the Mail Agent was
        created.\\n- status: Status of the Mail Agent.\\n\\nExample 1: To
        retrieve details of all Mail Agents in the ZeptoMail account. Sample
        prompt 1: Get me details of all the Mail Agents in the account.
        """
        request_url = f"{self._connection_runtime_url}/portal/v1.0/mailagents"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_processed_emails_async(
        self,
        mailagent_key: str,
        subject: Optional[str] = None,
        from_: Optional[str] = None,
        to: Optional[str] = None,
        date_from: Optional[str] = None,
        date_to: Optional[str] = None,
        request_id: Optional[str] = None,
        is_hb: Optional[bool] = None,
        is_sb: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get processed email details

        This endpoint fetches detailed information about emails processed by
        Mail Agent identified by its unique ID. It returns data including the
        Subject, From Address, Sent time and Request ID for emails sent for the
        time range. The mailagent_key parameter should match the Mail Agent key
        of the Mail Agent as listed in our system.
        """
        request_url = f"{self._connection_runtime_url}/v1.0/email"
        query_params = []
        value = str(mailagent_key)
        if isinstance(mailagent_key, bool):
            value = value.lower()
        query_params.append(f"mailagent_key={quote(value, safe='')}")
        if subject is not None:
            value = str(subject)
            if isinstance(subject, bool):
                value = value.lower()
            query_params.append(f"subject={quote(value, safe='')}")
        if from_ is not None:
            value = str(from_)
            if isinstance(from_, bool):
                value = value.lower()
            query_params.append(f"from={quote(value, safe='')}")
        if to is not None:
            value = str(to)
            if isinstance(to, bool):
                value = value.lower()
            query_params.append(f"to={quote(value, safe='')}")
        if date_from is not None:
            value = str(date_from)
            if isinstance(date_from, bool):
                value = value.lower()
            query_params.append(f"date_from={quote(value, safe='')}")
        if date_to is not None:
            value = str(date_to)
            if isinstance(date_to, bool):
                value = value.lower()
            query_params.append(f"date_to={quote(value, safe='')}")
        if request_id is not None:
            value = str(request_id)
            if isinstance(request_id, bool):
                value = value.lower()
            query_params.append(f"request_id={quote(value, safe='')}")
        if is_hb is not None:
            value = str(is_hb)
            if isinstance(is_hb, bool):
                value = value.lower()
            query_params.append(f"is_hb={quote(value, safe='')}")
        if is_sb is not None:
            value = str(is_sb)
            if isinstance(is_sb, bool):
                value = value.lower()
            query_params.append(f"is_sb={quote(value, safe='')}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_mail_async(
        self,
        input: SendMailInput,
    ) -> dict[str, Any] | None:
        """
        Send mail

        To send an email to the desired recipient.
        """
        request_url = f"{self._connection_runtime_url}/v1.0/email"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_template_mail_async(
        self,
        input: SendTemplateMailInput,
    ) -> dict[str, Any] | None:
        """
        Send template mail

        Sending a single email using Templates.
        """
        request_url = f"{self._connection_runtime_url}/v1.0/email/template"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def processed_mail_stats_async(
        self,
        mailagent: str,
        from_time: Optional[str] = None,
        to_time: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Processed emails analytics

        This endpoint fetches emails analytics for emails sent by Mail Agent
        identified by its Mail Agent name. It returns data including total
        count of Hardbounce, Softbounce and Sent statistics over a period of
        time and each day . The mailagent parameter should match the Mail Agent
        name as listed in our system.
        """
        request_url = f"{self._connection_runtime_url}/v1.0/stats/email"
        query_params = []
        value = str(mailagent)
        if isinstance(mailagent, bool):
            value = value.lower()
        query_params.append(f"mailagent={quote(value, safe='')}")
        if from_time is not None:
            value = str(from_time)
            if isinstance(from_time, bool):
                value = value.lower()
            query_params.append(f"from_time={quote(value, safe='')}")
        if to_time is not None:
            value = str(to_time)
            if isinstance(to_time, bool):
                value = value.lower()
            query_params.append(f"to_time={quote(value, safe='')}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
