# Copyright (c) Microsoft Corporation. All rights reserved.

# SendgridClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class AddGlobalSuppressRequestAndResponse:
    """
    Response for Add a global suppression
    """

    recipient_emails: Optional[List[str]] = None
    """Recipient email"""


@dataclass
class GetGlobalSuppressResponse:
    """
    Response for Get the global suppression
    """

    recipient_email: Optional[str] = None
    """Recipient email"""


@dataclass
class ObjectEntity:
    """
    Response for Add recipient to list
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class Bounces:
    """
    Response for Get bounce for an email
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class EmailIsUnsubscribedResponse:
    """
    Response for Check if email is in unsubscribed email list
    """

    is_unsubscribed: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isUnsubscribed"},
    )
    """Is Email Unsubscribed"""


@dataclass
class RecipientLists:
    """
    Response for List Recipient Lists
    """

    lists: Optional[List[RecipientList]] = None
    """List of recipient lists"""


@dataclass
class Recipients:
    """
    Response for List Recipients
    """

    recipients: Optional[List[Recipient]] = None
    """List of recipients"""


@dataclass
class EmailRequest:
    """
    Definition: EmailRequest
    """

    from_: Optional[str] = field(default=None, metadata={"wire_name": "from"})
    """
    Origination address of your email. Must be a valid email address from your
    domain
    """
    fromname: Optional[str] = None
    """Origination name of your email"""
    to: Optional[str] = None
    """
    Receiving address of your email. Must include one or more valid comma
    separated email addresses
    """
    toname: Optional[str] = None
    """
    Comma separated receiving names of your email. Must match the number of
    \"to\" email addresses if specified
    """
    subject: Optional[str] = None
    """Subject of your email"""
    body: Optional[str] = None
    """Content of your email. Supported formats include plain text and HTML"""
    ishtml: Optional[bool] = None
    """Specify whether the content of the email is HTML or plain text"""
    cc: Optional[str] = None
    """
    CC'ed addresses of your email. Must include one or more valid comma
    separated email addresses
    """
    ccname: Optional[str] = None
    """Comma separated names of the cc'ed recipients"""
    bcc: Optional[str] = None
    """
    Bcc addresses of your email. Must include one or more valid comma separated
    email addresses
    """
    bccname: Optional[str] = None
    """Comma separated names of the Bcc'ed recipients."""
    replyto: Optional[str] = None
    """Reply To address of your email. Must be a valid email address"""
    date: Optional[str] = None
    """
    Specify the date header of your email (Example: \"Thu, 21 Dec 2000 16:01:07
    +0200\"). Must be a valid date
    """
    headers: Optional[str] = None
    """Custom email headers in json format"""


@dataclass
class EmailResponse:
    """
    Definition: EmailResponse
    """

    message: Optional[str] = None
    """Status message from send email response"""


@dataclass
class GetGlobalSuppressEmptyResponse:
    """
    Definition: GetGlobalSuppressEmptyResponse
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class EmailAttachment:
    """
    Definition: EmailAttachment
    """

    content: Optional[str] = None
    """Attachment file content"""
    filename: Optional[str] = None
    """Attachment file name"""
    contenttype: Optional[str] = None
    """Type of content in the attachment."""


@dataclass
class RecipientList:
    """
    Definition: RecipientList
    """

    id: Optional[int] = None
    """Unique list id"""
    name: Optional[str] = None
    """Name"""
    recipient_count: Optional[int] = None
    """Recipient count"""


@dataclass
class Recipient:
    """
    Definition: Recipient
    """

    email: Optional[str] = None
    """Email address"""
    last_name: Optional[str] = None
    """Last name"""
    first_name: Optional[str] = None
    """First name"""
    id: Optional[str] = None
    """Unique recipient id"""


@dataclass
class ScopeList:
    """
    Definition: ScopeList
    """

    scopes: Optional[List[str]] = None
    """List of scopes"""


@dataclass
class Bounce:
    """
    Definition: Bounce
    """

    created: Optional[int] = None
    """Created"""
    email: Optional[str] = None
    """Email"""
    reason: Optional[str] = None
    """Reason"""
    status: Optional[str] = None
    """Status"""


# Client Class

class SendgridClient(ConnectorClientBase):
    """Typed client for sendgrid connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a SendgridClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "sendgrid"

    async def add_global_suppression_async(
        self,
        input: AddGlobalSuppressRequestAndResponse,
    ) -> dict[str, Any] | None:
        """
        Add a global suppression

        Add a global suppression
        """
        request_url = f"{self._connection_runtime_url}/suppressions/global"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_global_suppression_async(
        self,
        email: str,
    ) -> dict[str, Any] | None:
        """
        Get the global suppression

        Get the global suppression
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/suppressions/global/{quote(str(email), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_global_suppression_async(
        self,
        email: str,
    ) -> None:
        """
        Delete the global suppression

        Delete the global suppression
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/suppressions/global/{quote(str(email), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def add_recipient_to_list_async(
        self,
        list_id: str,
        recipient_id: str,
    ) -> dict[str, Any] | None:
        """
        Add recipient to list

        Add an individual recipient to a recipient list.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3"
            f"/contactdb"
            f"/lists"
            f"/{quote(str(list_id), safe='')}"
            f"/recipients"
            f"/{quote(str(recipient_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_bounce_async(
        self,
        email: str,
    ) -> dict[str, Any] | None:
        """
        Get bounce for an email

        Get a specific bounce for a given email address.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/suppression/bounces/{quote(str(email), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_bounce_async(
        self,
        email: str,
    ) -> None:
        """
        Delete an email from bounce list

        Delete an email address from your bounce list.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/suppression/bounces/{quote(str(email), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def check_email_is_in_unsubscribes_list_async(
        self,
        email: str,
    ) -> dict[str, Any] | None:
        """
        Check if email is in unsubscribed email list

        Check if email is in unsubscribed email list.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/unsubscribes/{quote(str(email), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_email_async(
        self,
        input: EmailRequest,
    ) -> dict[str, Any] | None:
        """
        Send email

        Sends an email (V4). Limited to 1000 recipients.
        """
        request_url = f"{self._connection_runtime_url}/v4/mail/send"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_recipient_lists_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List Recipient Lists

        Retrieves all the recipient lists.
        """
        request_url = f"{self._connection_runtime_url}/v3/contactdb/lists"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_recipients_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List Recipients

        Retrieves all registered recipients.
        """
        request_url = f"{self._connection_runtime_url}/v3/contactdb/recipients"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
