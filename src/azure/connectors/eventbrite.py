# Copyright (c) Microsoft Corporation. All rights reserved.

# EventbriteClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CreateEventResponse:
    """
    Response for Create event
    """

    name: Optional[Dict[str, Any]] = None
    description: Optional[Dict[str, Any]] = None
    id: Optional[str] = None
    """The id of the event."""
    url: Optional[str] = None
    """The URL of the event."""
    start: Optional[Dict[str, Any]] = None
    end: Optional[Dict[str, Any]] = None
    created: Optional[str] = None
    """The date-time the event was created at."""
    changed: Optional[str] = None
    """The date-time the event was changed at."""
    capacity: Optional[int] = None
    """The capacity of the event."""
    online_event: Optional[bool] = None
    """A boolean indicating whether or not the event is online."""
    organizer_id: Optional[str] = None
    """The id of the organizer of the event."""
    venue_id: Optional[str] = None
    """The id of the venue of the event."""
    category_id: Optional[str] = None
    """The id of the category of the event."""


GetEventsForOrganizationResponse = List[Dict[str, Any]]


GetOrdersResponse = List[Dict[str, Any]]


GetOrganizationsResponse = List[Dict[str, Any]]


@dataclass
class GetOrganizersResponse:
    """
    Response for Get organizers
    """

    organizers: Optional[List[Dict[str, Any]]] = None


@dataclass
class GetMyVenuesResponse:
    """
    Response for Get venues
    """

    venues: Optional[List[Dict[str, Any]]] = None


@dataclass
class GetCategoriesResponse:
    """
    Response for Get categories
    """

    categories: Optional[List[Dict[str, Any]]] = None


@dataclass
class GetMyEventsResponse:
    """
    Response for Get organization events
    """

    events: Optional[List[Dict[str, Any]]] = None


GetEventsForOrganizerResponse = List[Dict[str, Any]]


# Client Class

class EventbriteClient(ConnectorClientBase):
    """Typed client for eventbrite connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a EventbriteClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "eventbrite"

    async def create_event_async(
        self,
        organization_id: str,
        event_name_html: str,
        event_description_html: str,
        event_start_utc: str,
        event_end_utc: str,
        event_start_timezone: str,
        event_end_timezone: str,
        event_currency: str,
        event_organizer_id: Optional[str] = None,
        event_venue_id: Optional[str] = None,
        event_category_id: Optional[str] = None,
        event_password: Optional[str] = None,
        event_capacity: Optional[str] = None,
        event_shareable: Optional[str] = None,
        event_invite_only: Optional[str] = None,
        event_online_event: Optional[str] = None,
        event_listed: Optional[str] = None,
        event_hide_start_date: Optional[str] = None,
        event_hide_end_date: Optional[str] = None,
        event_show_remaining: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Create event

        Create a new Eventbrite event.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3/organizations/{quote(str(organization_id), safe='')}/events/"
        )
        query_params = []
        value = str(event_name_html)
        if isinstance(event_name_html, bool):
            value = value.lower()
        query_params.append(f"event.name.html={quote(value)}")
        value = str(event_description_html)
        if isinstance(event_description_html, bool):
            value = value.lower()
        query_params.append(f"event.description.html={quote(value)}")
        value = str(event_start_utc)
        if isinstance(event_start_utc, bool):
            value = value.lower()
        query_params.append(f"event.start.utc={quote(value)}")
        value = str(event_end_utc)
        if isinstance(event_end_utc, bool):
            value = value.lower()
        query_params.append(f"event.end.utc={quote(value)}")
        value = str(event_start_timezone)
        if isinstance(event_start_timezone, bool):
            value = value.lower()
        query_params.append(f"event.start.timezone={quote(value)}")
        value = str(event_end_timezone)
        if isinstance(event_end_timezone, bool):
            value = value.lower()
        query_params.append(f"event.end.timezone={quote(value)}")
        value = str(event_currency)
        if isinstance(event_currency, bool):
            value = value.lower()
        query_params.append(f"event.currency={quote(value)}")
        if event_organizer_id is not None:
            value = str(event_organizer_id)
            if isinstance(event_organizer_id, bool):
                value = value.lower()
            query_params.append(f"event.organizer_id={quote(value)}")
        if event_venue_id is not None:
            value = str(event_venue_id)
            if isinstance(event_venue_id, bool):
                value = value.lower()
            query_params.append(f"event.venue_id={quote(value)}")
        if event_category_id is not None:
            value = str(event_category_id)
            if isinstance(event_category_id, bool):
                value = value.lower()
            query_params.append(f"event.category_id={quote(value)}")
        if event_password is not None:
            value = str(event_password)
            if isinstance(event_password, bool):
                value = value.lower()
            query_params.append(f"event.password={quote(value)}")
        if event_capacity is not None:
            value = str(event_capacity)
            if isinstance(event_capacity, bool):
                value = value.lower()
            query_params.append(f"event.capacity={quote(value)}")
        if event_shareable is not None:
            value = str(event_shareable)
            if isinstance(event_shareable, bool):
                value = value.lower()
            query_params.append(f"event.shareable={quote(value)}")
        if event_invite_only is not None:
            value = str(event_invite_only)
            if isinstance(event_invite_only, bool):
                value = value.lower()
            query_params.append(f"event.invite_only={quote(value)}")
        if event_online_event is not None:
            value = str(event_online_event)
            if isinstance(event_online_event, bool):
                value = value.lower()
            query_params.append(f"event.online_event={quote(value)}")
        if event_listed is not None:
            value = str(event_listed)
            if isinstance(event_listed, bool):
                value = value.lower()
            query_params.append(f"event.listed={quote(value)}")
        if event_hide_start_date is not None:
            value = str(event_hide_start_date)
            if isinstance(event_hide_start_date, bool):
                value = value.lower()
            query_params.append(f"event.hide_start_date={quote(value)}")
        if event_hide_end_date is not None:
            value = str(event_hide_end_date)
            if isinstance(event_hide_end_date, bool):
                value = value.lower()
            query_params.append(f"event.hide_end_date={quote(value)}")
        if event_show_remaining is not None:
            value = str(event_show_remaining)
            if isinstance(event_show_remaining, bool):
                value = value.lower()
            query_params.append(f"event.show_remaining={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_event_async(
        self,
        id: str,
        organization_id: str,
        event_start_timezone: str,
        event_end_timezone: str,
        event_currency: str,
        event_name_html: Optional[str] = None,
        event_description_html: Optional[str] = None,
        event_start_utc: Optional[str] = None,
        event_end_utc: Optional[str] = None,
        event_organizer_id: Optional[str] = None,
        event_venue_id: Optional[str] = None,
        event_category_id: Optional[str] = None,
        event_password: Optional[str] = None,
        event_capacity: Optional[str] = None,
        event_shareable: Optional[str] = None,
        event_invite_only: Optional[str] = None,
        event_online_event: Optional[str] = None,
        event_listed: Optional[str] = None,
        event_hide_start_date: Optional[str] = None,
        event_hide_end_date: Optional[str] = None,
        event_show_remaining: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update event

        Update an existing event.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v2/v3/events/{quote(str(id), safe='')}/"
        )
        query_params = []
        value = str(organization_id)
        if isinstance(organization_id, bool):
            value = value.lower()
        query_params.append(f"organization_id={quote(value)}")
        if event_name_html is not None:
            value = str(event_name_html)
            if isinstance(event_name_html, bool):
                value = value.lower()
            query_params.append(f"event.name.html={quote(value)}")
        if event_description_html is not None:
            value = str(event_description_html)
            if isinstance(event_description_html, bool):
                value = value.lower()
            query_params.append(f"event.description.html={quote(value)}")
        if event_start_utc is not None:
            value = str(event_start_utc)
            if isinstance(event_start_utc, bool):
                value = value.lower()
            query_params.append(f"event.start.utc={quote(value)}")
        if event_end_utc is not None:
            value = str(event_end_utc)
            if isinstance(event_end_utc, bool):
                value = value.lower()
            query_params.append(f"event.end.utc={quote(value)}")
        value = str(event_start_timezone)
        if isinstance(event_start_timezone, bool):
            value = value.lower()
        query_params.append(f"event.start.timezone={quote(value)}")
        value = str(event_end_timezone)
        if isinstance(event_end_timezone, bool):
            value = value.lower()
        query_params.append(f"event.end.timezone={quote(value)}")
        value = str(event_currency)
        if isinstance(event_currency, bool):
            value = value.lower()
        query_params.append(f"event.currency={quote(value)}")
        if event_organizer_id is not None:
            value = str(event_organizer_id)
            if isinstance(event_organizer_id, bool):
                value = value.lower()
            query_params.append(f"event.organizer_id={quote(value)}")
        if event_venue_id is not None:
            value = str(event_venue_id)
            if isinstance(event_venue_id, bool):
                value = value.lower()
            query_params.append(f"event.venue_id={quote(value)}")
        if event_category_id is not None:
            value = str(event_category_id)
            if isinstance(event_category_id, bool):
                value = value.lower()
            query_params.append(f"event.category_id={quote(value)}")
        if event_password is not None:
            value = str(event_password)
            if isinstance(event_password, bool):
                value = value.lower()
            query_params.append(f"event.password={quote(value)}")
        if event_capacity is not None:
            value = str(event_capacity)
            if isinstance(event_capacity, bool):
                value = value.lower()
            query_params.append(f"event.capacity={quote(value)}")
        if event_shareable is not None:
            value = str(event_shareable)
            if isinstance(event_shareable, bool):
                value = value.lower()
            query_params.append(f"event.shareable={quote(value)}")
        if event_invite_only is not None:
            value = str(event_invite_only)
            if isinstance(event_invite_only, bool):
                value = value.lower()
            query_params.append(f"event.invite_only={quote(value)}")
        if event_online_event is not None:
            value = str(event_online_event)
            if isinstance(event_online_event, bool):
                value = value.lower()
            query_params.append(f"event.online_event={quote(value)}")
        if event_listed is not None:
            value = str(event_listed)
            if isinstance(event_listed, bool):
                value = value.lower()
            query_params.append(f"event.listed={quote(value)}")
        if event_hide_start_date is not None:
            value = str(event_hide_start_date)
            if isinstance(event_hide_start_date, bool):
                value = value.lower()
            query_params.append(f"event.hide_start_date={quote(value)}")
        if event_hide_end_date is not None:
            value = str(event_hide_end_date)
            if isinstance(event_hide_end_date, bool):
                value = value.lower()
            query_params.append(f"event.hide_end_date={quote(value)}")
        if event_show_remaining is not None:
            value = str(event_show_remaining)
            if isinstance(event_show_remaining, bool):
                value = value.lower()
            query_params.append(f"event.show_remaining={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organizations_async(
        self,
    ) -> dict[str, Any] | None:
        """
        GetOrganizations

        Get a list of organizations for your account.
        """
        request_url = (
            f"{self._connection_runtime_url}/v3/users/me/organizations/"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organizers_async(
        self,
        organization_id: str,
    ) -> dict[str, Any] | None:
        """
        Get organizers

        Get a list of organizers for organization.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3"
            f"/organizations"
            f"/{quote(str(organization_id), safe='')}"
            f"/organizers"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_my_venues_async(
        self,
        organization_id: str,
    ) -> dict[str, Any] | None:
        """
        Get venues

        Get venues for organization.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3/organizations/{quote(str(organization_id), safe='')}/venues/"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_categories_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get categories

        Get the Eventbrite event categories.
        """
        request_url = f"{self._connection_runtime_url}/v3/categories/"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organization_events_async(
        self,
        organization_id: str,
        order_by: str,
        status: str,
    ) -> dict[str, Any] | None:
        """
        Get organization events

        Get organization events.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3/organizations/{quote(str(organization_id), safe='')}/events/"
        )
        query_params = []
        value = str(order_by)
        if isinstance(order_by, bool):
            value = value.lower()
        query_params.append(f"order_by={quote(value)}")
        value = str(status)
        if isinstance(status, bool):
            value = value.lower()
        query_params.append(f"status={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnNewEventV2": {
        "operation_id": "OnNewEventV2",
        "path": "/{connectionId}/v2/trigger/v3/organizations/{organization_id}/events/",
        "method": "get",
        "required_parameters": ["organization_id", "organizer_filter"],
        "callback_payload_type": "GetEventsForOrganizationResponse",
    },
    "OnOrderChangedV2": {
        "operation_id": "OnOrderChangedV2",
        "path": "/{connectionId}/v2/trigger/v3/events/{id}/orders/",
        "method": "get",
        "required_parameters": ["organization_id", "id"],
        "callback_payload_type": "GetOrdersResponse",
    },
}
