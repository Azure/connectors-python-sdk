# Copyright (c) Microsoft Corporation. All rights reserved.

# InfusionsoftClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class TaskResponse:
    """
    Response for Create a task
    """

    id: Optional[int] = None
    """An integer that uniquely identifies this task."""
    title: Optional[str] = None
    """The actual task name (Do this thing)."""
    description: Optional[str] = None
    """A description of the task defined by the user."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """
    A string representing the type of Task as defined by the user from a list
    of types editable in the Infusionsoft application.
    """
    priority: Optional[int] = None
    """The priority of the task."""
    completed: Optional[bool] = None
    """A boolean indicating the completed status of the task."""
    url: Optional[str] = None
    """The link to the task."""
    due_date: Optional[str] = None
    """The datetime the task is due, given as GMT in ISO 8601 format."""


OnNewTaskResponse = List["TaskResponse"]


ListOrdersResponse = List[Dict[str, Any]]


@dataclass
class ListTasksResponse:
    """
    Response for List tasks
    """

    tasks: Optional[List[Dict[str, Any]]] = None
    """Collection of tasks."""


@dataclass
class CreateTaskRequest:
    """
    Definition: CreateTask_Request
    """

    title: Optional[str] = None
    """The title of the task."""
    description: Optional[str] = None
    """A description of the task title."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the task."""
    priority: Optional[int] = None
    """Priority of the task (1, 2, 3, etc.)"""
    due_date: Optional[str] = None
    """Due date of the task (Ex. 2014-06-17T17:12:52.000Z"""


# Client Class

class InfusionsoftClient(ConnectorClientBase):
    """Typed client for infusionsoft connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a InfusionsoftClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "infusionsoft"

    async def create_task_async(
        self,
        input: CreateTaskRequest,
    ) -> dict[str, Any] | None:
        """
        Create a task

        Create a new task.
        """
        request_url = f"{self._connection_runtime_url}/crm/rest/v1/tasks/"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_task_async(
        self,
        input: CreateTaskRequest,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Update a task

        Update an existing task.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/crm/rest/v1/tasks/{quote(str(id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_tasks_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List tasks

        Retrieve a page of tasks ordered by due date.
        """
        request_url = (
            f"{self._connection_runtime_url}/crm/rest/v1/tasks/search"
        )
        query_params = []
        query_params.append("order=" + quote("-due_date"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnNewTask": {
        "operation_id": "OnNewTask",
        "path": "/{connectionId}/trigger/crm/rest/v1/tasks/search",
        "method": "get",
        "required_parameters": [],
        "callback_payload_type": "OnNewTaskResponse",
    },
    "OnNewOrder": {
        "operation_id": "OnNewOrder",
        "path": "/{connectionId}/trigger/crm/rest/v1/orders",
        "method": "get",
        "required_parameters": [],
        "callback_payload_type": "ListOrdersResponse",
    },
}
