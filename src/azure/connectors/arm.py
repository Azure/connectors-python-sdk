# Copyright (c) Microsoft Corporation. All rights reserved.

# ArmClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class LocationListResult:
    """
    Response for Lists the subscription locations
    """

    value: Optional[List[Location]] = None
    """The locations."""


@dataclass
class Subscription:
    """
    Response for Read a subscription
    """

    id: Optional[str] = None
    """
    The fully qualified Id. For example,
    /subscriptions/00000000-0000-0000-0000-000000000000.
    """
    subscription_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "subscriptionId"},
    )
    """The subscription Id."""
    tenant_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tenantId"},
    )
    """The tenant Id."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """The subscription display name."""
    state: Optional[str] = None
    """The subscription state."""
    subscription_policies: Optional[SubscriptionPolicies] = field(
        default=None,
        metadata={"wire_name": "subscriptionPolicies"},
    )
    authorization_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "authorizationSource"},
    )
    """
    The authorization source of the request. Valid values are one or more
    combinations of Legacy, RoleBased, Bypassed, Direct and Management. For
    example, 'Legacy, RoleBased'.
    """


@dataclass
class SubscriptionListResult:
    """
    Response for List subscriptions
    """

    value: Optional[List[Subscription]] = None
    """The subscriptions."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class DeploymentExtended:
    """
    Response for Read a template deployment
    """

    id: Optional[str] = None
    """The ID of the deployment."""
    name: Optional[str] = None
    """The name of the deployment."""
    properties: Optional[DeploymentPropertiesExtended] = None


@dataclass
class DeploymentValidateResult:
    """
    Response for Validate a template deployment
    """

    error: Optional[ResourceManagementErrorWithDetails] = None
    properties: Optional[DeploymentPropertiesExtended] = None


@dataclass
class DeploymentExportResult:
    """
    Response for Export deployment template
    """

    template: Optional[Dict[str, Any]] = None
    """The template content."""


@dataclass
class DeploymentListResult:
    """
    Response for List template deployments
    """

    value: Optional[List[DeploymentExtended]] = None
    """The list of deployments."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class DeploymentOperation:
    """
    Response for Read a template deployment operation
    """

    id: Optional[str] = None
    """Full deployment operation id."""
    operation_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "operationId"},
    )
    """Deployment operation id."""
    properties: Optional[DeploymentOperationProperties] = None


@dataclass
class DeploymentOperationsListResult:
    """
    Response for Lists template deployment operations
    """

    value: Optional[List[DeploymentOperation]] = None
    """The list of deployments."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class Provider:
    """
    Response for Unregister resource provider
    """

    id: Optional[str] = None
    """The provider id."""
    namespace: Optional[str] = None
    """The namespace of the provider."""
    registration_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrationState"},
    )
    """The registration state of the provider."""
    resource_types: Optional[List[ProviderResourceType]] = field(
        default=None,
        metadata={"wire_name": "resourceTypes"},
    )
    """The collection of provider resource types."""


@dataclass
class ProviderListResult:
    """
    Response for List resource providers
    """

    value: Optional[List[Provider]] = None
    """The list of resource providers."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class ResourceListResult:
    """
    Response for List resources by resource group
    """

    value: Optional[List[GenericResource]] = None
    """The list of resources."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class ResourceGroup:
    """
    Response for Read a resource group
    """

    id: Optional[str] = None
    """
    The ID of the resource group (e.g. /subscriptions/XXX/resourceGroups/YYY).
    """
    name: Optional[str] = None
    """The Name of the resource group."""
    location: Optional[str] = None
    """
    The location of the resource group. It cannot be changed after the resource
    group has been created. Has to be one of the supported Azure Locations,
    such as West US, East US, West Europe, East Asia, etc.
    """
    managed_by: Optional[str] = field(
        default=None,
        metadata={"wire_name": "managedBy"},
    )
    """Id of the resource that manages this resource group."""
    tags: Optional[Dict[str, Any]] = None
    """The tags attached to the resource group."""
    properties: Optional[ResourceGroupProperties] = None


@dataclass
class ResourceGroupExportResult:
    """
    Response for Export a resource group template
    """

    template: Optional[Dict[str, Any]] = None
    """The template content."""
    error: Optional[ResourceManagementErrorWithDetails] = None


@dataclass
class ResourceGroupListResult:
    """
    Response for List resource groups
    """

    value: Optional[List[ResourceGroup]] = None
    """The list of resource groups."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class GenericResource:
    """
    Response for Read a resource
    """

    id: Optional[str] = None
    """Resource Id"""
    name: Optional[str] = None
    """Resource name"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Resource type"""
    location: Optional[str] = None
    """Resource location"""
    tags: Optional[Dict[str, Any]] = None
    """Resource tags"""
    plan: Optional[Plan] = None
    kind: Optional[str] = None
    """The kind of the resource."""
    managed_by: Optional[str] = field(
        default=None,
        metadata={"wire_name": "managedBy"},
    )
    """Id of the resource that manages this resource."""
    sku: Optional[Sku] = None
    identity: Optional[Identity] = None
    properties: Optional[Dict[str, Any]] = None
    """The resource properties."""


@dataclass
class ResourcesInvokeInput:
    """
    Invoke resource operation
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class ResourcesInvokeResponse:
    """
    Response for Invoke resource operation
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class ProviderResourcesInvokeInput:
    """
    Invoke resource operation in provider
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class ProviderResourcesInvokeResponse:
    """
    Response for Invoke resource operation in provider
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class TagValue:
    """
    Response for Create or update a subscription resource tag value
    """

    id: Optional[str] = None
    """The tag ID."""
    tag_value: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tagValue"},
    )
    """The tag value."""
    count: Optional[TagCount] = None


@dataclass
class TagDetails:
    """
    Response for Create or update a subscription resource tag name
    """

    id: Optional[str] = None
    """The tag ID."""
    tag_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tagName"},
    )
    """The tag name."""
    count: Optional[TagCount] = None
    values: Optional[List[TagValue]] = None
    """The list of tag values."""


@dataclass
class TagsListResult:
    """
    Response for List subscription resource tags
    """

    value: Optional[List[TagDetails]] = None
    """The list of tags."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """The URL to get the next set of results."""


@dataclass
class Location:
    """
    Definition: Location
    """

    id: Optional[str] = None
    """
    The fully qualified Id of the location. For example,
    /subscriptions/00000000-0000-0000-0000-000000000000/locations/westus.
    """
    subscription_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "subscriptionId"},
    )
    """The subscription Id."""
    name: Optional[str] = None
    """The location name."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """The display name of the location."""
    latitude: Optional[str] = None
    """The latitude of the location."""
    longitude: Optional[str] = None
    """The longitude of the location."""


@dataclass
class SubscriptionPolicies:
    """
    Definition: SubscriptionPolicies
    """

    location_placement_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "locationPlacementId"},
    )
    """
    The subscription location placement Id. The Id indicates which regions are
    visible for a subscription. For example, a subscription with a location
    placement Id of Public_2014-09-01 has access to Azure public regions.
    """
    quota_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "quotaId"},
    )
    """The subscription quota Id."""
    spending_limit: Optional[str] = field(
        default=None,
        metadata={"wire_name": "spendingLimit"},
    )
    """The subscription spending limit."""


@dataclass
class TemplateLink:
    """
    Definition: TemplateLink
    """

    uri: Optional[str] = None
    """URI referencing the template."""
    content_version: Optional[str] = field(
        default=None,
        metadata={"wire_name": "contentVersion"},
    )
    """If included it must match the ContentVersion in the template."""


@dataclass
class ParametersLink:
    """
    Definition: ParametersLink
    """

    uri: Optional[str] = None
    """URI referencing the template."""
    content_version: Optional[str] = field(
        default=None,
        metadata={"wire_name": "contentVersion"},
    )
    """If included it must match the ContentVersion in the template."""


@dataclass
class DeploymentProperties:
    """
    Definition: DeploymentProperties
    """

    template: Optional[Dict[str, Any]] = None
    """
    The template content. It can be a JObject or a well formed JSON string. Use
    only one of Template or TemplateLink.
    """
    template_link: Optional[TemplateLink] = field(
        default=None,
        metadata={"wire_name": "templateLink"},
    )
    parameters: Optional[Dict[str, Any]] = None
    """
    Deployment parameters. It can be a JObject or a well formed JSON string.
    Use only one of Parameters or ParametersLink.
    """
    parameters_link: Optional[ParametersLink] = field(
        default=None,
        metadata={"wire_name": "parametersLink"},
    )
    mode: Optional[str] = None
    """The deployment mode."""
    debug_setting: Optional[DebugSetting] = field(
        default=None,
        metadata={"wire_name": "debugSetting"},
    )


@dataclass
class DebugSetting:
    """
    Definition: DebugSetting
    """

    detail_level: Optional[str] = field(
        default=None,
        metadata={"wire_name": "detailLevel"},
    )
    """The debug detail level."""


@dataclass
class Deployment:
    """
    Definition: Deployment
    """

    properties: Optional[DeploymentProperties] = None


@dataclass
class ResourceManagementErrorWithDetails:
    """
    Definition: ResourceManagementErrorWithDetails
    """

    code: Optional[str] = None
    """The error code returned from the server."""
    message: Optional[str] = None
    """The error message returned from the server."""
    target: Optional[str] = None
    """The target of the error."""
    details: Optional[List[Dict[str, Any]]] = None
    """Validation error."""


@dataclass
class AliasPathType:
    """
    Definition: AliasPathType
    """

    path: Optional[str] = None
    """The path of an alias."""
    api_versions: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "apiVersions"},
    )
    """The api versions."""


@dataclass
class AliasType:
    """
    Definition: AliasType
    """

    name: Optional[str] = None
    """The alias name."""
    paths: Optional[List[AliasPathType]] = None
    """The paths for an alias."""


@dataclass
class ProviderResourceType:
    """
    Definition: ProviderResourceType
    """

    resource_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceType"},
    )
    """The resource type."""
    locations: Optional[List[ObjectWithoutType]] = None
    """
    The collection of locations where this resource type can be created in.
    """
    aliases: Optional[List[AliasType]] = None
    """The aliases that are supported by this resource type."""
    api_versions: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "apiVersions"},
    )
    """The api version."""
    properties: Optional[Dict[str, Any]] = None
    """The properties."""


@dataclass
class BasicDependency:
    """
    Definition: BasicDependency
    """

    id: Optional[str] = None
    """The ID of the dependency."""
    resource_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceType"},
    )
    """The dependency resource type."""
    resource_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceName"},
    )
    """The dependency resource name."""


@dataclass
class Dependency:
    """
    Definition: Dependency
    """

    depends_on: Optional[List[BasicDependency]] = field(
        default=None,
        metadata={"wire_name": "dependsOn"},
    )
    """The list of dependencies."""
    id: Optional[str] = None
    """The ID of the dependency."""
    resource_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceType"},
    )
    """The dependency resource type."""
    resource_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceName"},
    )
    """The dependency resource name."""


@dataclass
class DeploymentPropertiesExtended:
    """
    Definition: DeploymentPropertiesExtended
    """

    provisioning_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "provisioningState"},
    )
    """The state of the provisioning."""
    correlation_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "correlationId"},
    )
    """The correlation ID of the deployment."""
    timestamp: Optional[str] = None
    """The timestamp of the template deployment."""
    outputs: Optional[Dict[str, Any]] = None
    """Key/value pairs that represent deploymentoutput."""
    providers: Optional[List[Provider]] = None
    """The list of resource providers needed for the deployment."""
    dependencies: Optional[List[Dependency]] = None
    """The list of deployment dependencies."""
    template: Optional[Dict[str, Any]] = None
    """The template content. Use only one of Template or TemplateLink."""
    template_link: Optional[TemplateLink] = field(
        default=None,
        metadata={"wire_name": "templateLink"},
    )
    parameters: Optional[Dict[str, Any]] = None
    """Deployment parameters. Use only one of Parameters or ParametersLink."""
    parameters_link: Optional[ParametersLink] = field(
        default=None,
        metadata={"wire_name": "parametersLink"},
    )
    mode: Optional[str] = None
    """The deployment mode."""
    debug_setting: Optional[DebugSetting] = field(
        default=None,
        metadata={"wire_name": "debugSetting"},
    )


@dataclass
class Plan:
    """
    Definition: Plan
    """

    name: Optional[str] = None
    """The plan ID."""
    publisher: Optional[str] = None
    """The publisher ID."""
    product: Optional[str] = None
    """The offer ID."""
    promotion_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "promotionCode"},
    )
    """The promotion code."""


@dataclass
class Sku:
    """
    Definition: Sku
    """

    name: Optional[str] = None
    """The sku name."""
    tier: Optional[str] = None
    """The sku tier."""
    size: Optional[str] = None
    """The sku size."""
    family: Optional[str] = None
    """The sku family."""
    model: Optional[str] = None
    """The sku model."""
    capacity: Optional[int] = None
    """The sku capacity."""


@dataclass
class Identity:
    """
    Definition: Identity
    """

    principal_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "principalId"},
    )
    """The principal id of resource identity."""
    tenant_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tenantId"},
    )
    """The tenant id of resource."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """The identity type."""


@dataclass
class ResourceGroupProperties:
    """
    Definition: ResourceGroupProperties
    """

    provisioning_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "provisioningState"},
    )
    """The provisioning state."""


@dataclass
class ExportTemplateRequest:
    """
    Definition: ExportTemplateRequest
    """

    resources: Optional[List[str]] = None
    """
    The ids of the resources. The only supported string currently is '*' (all
    resources). Future api updates will support exporting specific resources.
    """
    options: Optional[str] = None
    """
    The export template options. Supported values include
    'IncludeParameterDefaultValue', 'IncludeComments' or
    'IncludeParameterDefaultValue, IncludeComments
    """


@dataclass
class TagCount:
    """
    Definition: TagCount
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of count."""
    value: Optional[int] = None
    """Value of count."""


@dataclass
class TargetResource:
    """
    Definition: TargetResource
    """

    id: Optional[str] = None
    """The ID of the resource."""
    resource_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceName"},
    )
    """The name of the resource."""
    resource_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "resourceType"},
    )
    """The type of the resource."""


@dataclass
class HttpMessage:
    """
    Definition: HttpMessage
    """

    content: Optional[Dict[str, Any]] = None
    """HTTP message content."""


@dataclass
class DeploymentOperationProperties:
    """
    Definition: DeploymentOperationProperties
    """

    provisioning_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "provisioningState"},
    )
    """The state of the provisioning."""
    timestamp: Optional[str] = None
    """The date and time of the operation."""
    service_request_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "serviceRequestId"},
    )
    """Deployment operation service request id."""
    status_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "statusCode"},
    )
    """Operation status code."""
    status_message: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "statusMessage"},
    )
    """Operation status message."""
    target_resource: Optional[TargetResource] = field(
        default=None,
        metadata={"wire_name": "targetResource"},
    )
    request: Optional[HttpMessage] = None
    response: Optional[HttpMessage] = None


@dataclass
class ObjectWithoutType:
    """
    Definition: ObjectWithoutType
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


# Client Class

class ArmClient(ConnectorClientBase):
    """Typed client for arm connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a ArmClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "arm"

    async def subscriptions_list_locations_async(
        self,
        subscription_id: str,
    ) -> dict[str, Any] | None:
        """
        Lists the subscription locations

        Lists the locations available for the subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions/{quote(str(subscription_id), safe='')}/locations"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def subscriptions_get_async(
        self,
        subscription_id: str,
    ) -> dict[str, Any] | None:
        """
        Read a subscription

        Reads the details for a particular subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions/{quote(str(subscription_id), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def subscriptions_list_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List subscriptions

        Gets a list of all the subscriptions to which the principal has access.
        """
        request_url = f"{self._connection_runtime_url}/subscriptions"
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployments_get_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
        wait: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Read a template deployment

        Reads a template deployment within a resource group.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if wait is not None:
            value = str(wait)
            if isinstance(wait, bool):
                value = value.lower()
            query_params.append(f"wait={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployments_create_or_update_async(
        self,
        input: Deployment,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
        wait: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Create or update a template deployment

        Create or update a named resource group template deployment. A template
        and parameters are expected for the request to succeed.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if wait is not None:
            value = str(wait)
            if isinstance(wait, bool):
                value = value.lower()
            query_params.append(f"wait={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployments_delete_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
    ) -> None:
        """
        Delete template deployment

        Deletes a resource group template deployment. The resources will not be
        deleted; only the metadata about the template deployment.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def deployments_cancel_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
    ) -> None:
        """
        Cancel a template deployment

        Cancel a currently running template deployment. All pending template
        operations will be suspended.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
            f"/cancel"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def deployments_validate_async(
        self,
        input: Deployment,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
    ) -> dict[str, Any] | None:
        """
        Validate a template deployment

        Validates a deployment template. This operation does not have side
        effects and can be used to test a template deployment for syntax or
        logical errors.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
            f"/validate"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployments_export_template_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
    ) -> dict[str, Any] | None:
        """
        Export deployment template

        Exports a template from a past resource group deployment.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
            f"/exportTemplate"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployments_list_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        filter: Optional[str] = None,
        top: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        List template deployments

        Lists all the resource group template deployments. This operation is
        useful to know what has been provisioned thus far.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/Microsoft.Resources"
            f"/deployments"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployment_operations_get_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
        operation_id: str,
    ) -> dict[str, Any] | None:
        """
        Read a template deployment operation

        Reads a particular resource group template deployment operation. This
        is useful for troubleshooting failed template deployments.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
            f"/operations"
            f"/{quote(str(operation_id), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def deployment_operations_list_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        deployment_name: str,
        top: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        Lists template deployment operations

        Lists all the template deployment operations. This is useful for
        troubleshooting failed template deployments.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/deployments"
            f"/{quote(str(deployment_name), safe='')}"
            f"/operations"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def providers_unregister_async(
        self,
        subscription_id: str,
        resource_provider_namespace: str,
    ) -> dict[str, Any] | None:
        """
        Unregister resource provider

        Unregisters provider from a subscription. This operation will fail if
        there are any resources from that resource provider in the
        subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/unregister"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def providers_register_async(
        self,
        subscription_id: str,
        resource_provider_namespace: str,
    ) -> dict[str, Any] | None:
        """
        Register resource provider

        Registers a resource provider to be used with a subscription. This will
        provision permissions for the service into your subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/register"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def providers_list_async(
        self,
        subscription_id: str,
        top: Optional[int] = None,
        expand: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List resource providers

        Lists the resource providers available for the subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions/{quote(str(subscription_id), safe='')}/providers"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if expand is not None:
            value = str(expand)
            if isinstance(expand, bool):
                value = value.lower()
            query_params.append(f"$expand={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def providers_get_async(
        self,
        subscription_id: str,
        resource_provider_namespace: str,
        expand: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Read resource provider

        Reads a particular resource provider within the subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if expand is not None:
            value = str(expand)
            if isinstance(expand, bool):
                value = value.lower()
            query_params.append(f"$expand={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_list_resources_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        filter: Optional[str] = None,
        expand: Optional[str] = None,
        top: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        List resources by resource group

        Lists all the resources under a resource group.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourceGroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/resources"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if expand is not None:
            value = str(expand)
            if isinstance(expand, bool):
                value = value.lower()
            query_params.append(f"$expand={quote(value)}")
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_get_async(
        self,
        subscription_id: str,
        resource_group_name: str,
    ) -> dict[str, Any] | None:
        """
        Read a resource group

        Reads a particular resource group within the subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_create_or_update_async(
        self,
        input: ResourceGroup,
        subscription_id: str,
        resource_group_name: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a resource group

        Creates or updates a resource group. The response code can be used to
        distinguish between a create (201) or update (200).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_delete_async(
        self,
        subscription_id: str,
        resource_group_name: str,
    ) -> None:
        """
        Delete a resource group

        Delete a particular resource group within the subscription.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def resource_groups_patch_async(
        self,
        input: ResourceGroup,
        subscription_id: str,
        resource_group_name: str,
    ) -> dict[str, Any] | None:
        """
        Update an existing resource group

        Updates an existing resource group. If the resource does not exist,
        this request will fail.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_export_template_async(
        self,
        input: ExportTemplateRequest,
        subscription_id: str,
        resource_group_name: str,
    ) -> dict[str, Any] | None:
        """
        Export a resource group template

        Exports a deployment template from an existing resource group. This can
        only be successful if the underlying resources have a schema defined by
        Microsoft.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/exportTemplate"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resource_groups_list_async(
        self,
        subscription_id: str,
        filter: Optional[str] = None,
        top: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        List resource groups

        Lists all the resource groups within the subscription. The results are
        paginated at 1,000+ records.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resources_list_async(
        self,
        subscription_id: str,
        filter: Optional[str] = None,
        expand: Optional[str] = None,
        top: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        List resources by subscription

        Reads all of the resources under a particular subscription. The results
        are paginated at 1,000+ records.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions/{quote(str(subscription_id), safe='')}/resources"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if expand is not None:
            value = str(expand)
            if isinstance(expand, bool):
                value = value.lower()
            query_params.append(f"$expand={quote(value)}")
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resources_get_by_id_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        x_ms_api_version: str,
    ) -> dict[str, Any] | None:
        """
        Read a resource

        Reads a resource object.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resources_create_or_update_by_id_async(
        self,
        input: GenericResource,
        subscription_id: str,
        resource_group_name: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        x_ms_api_version: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a resource

        Creates or updates a resource. The response code can be used to
        distinguish between a create (201) or update (200).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def resources_delete_by_id_async(
        self,
        subscription_id: str,
        resource_group_name: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        x_ms_api_version: str,
    ) -> None:
        """
        Delete a resource

        Deletes a resource.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def resources_invoke_async(
        self,
        input: ResourcesInvokeInput,
        subscription_id: str,
        resource_group_name: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        action_name: str,
        x_ms_api_version: str,
    ) -> dict[str, Any] | None:
        """
        Invoke resource operation

        Invokes an operation on an Azure resource.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/resourcegroups"
            f"/{quote(str(resource_group_name), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
            f"/{quote(str(action_name), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def provider_resources_get_by_id_async(
        self,
        subscription_id: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        x_ms_api_version: str,
    ) -> dict[str, Any] | None:
        """
        Read a resource in provider

        Reads a resource object.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def provider_resources_invoke_async(
        self,
        input: ProviderResourcesInvokeInput,
        subscription_id: str,
        resource_provider_namespace: str,
        short_resource_id: str,
        x_ms_api_version: str,
    ) -> dict[str, Any] | None:
        """
        Invoke resource operation in provider

        Invokes an operation on an Azure resource.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/providers"
            f"/{quote(str(resource_provider_namespace), safe='')}"
            f"/{quote(str(short_resource_id), safe='')}"
        )
        query_params = []
        value = str(x_ms_api_version)
        if isinstance(x_ms_api_version, bool):
            value = value.lower()
        query_params.append(f"x-ms-api-version={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def tags_create_or_update_value_async(
        self,
        subscription_id: str,
        tag_name: str,
        tag_value: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a subscription resource tag value

        Create or update a subscription resource tag value.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/tagNames"
            f"/{quote(str(tag_name), safe='')}"
            f"/tagValues"
            f"/{quote(str(tag_value), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def tags_delete_value_async(
        self,
        subscription_id: str,
        tag_name: str,
        tag_value: str,
    ) -> None:
        """
        Delete a subscription resource tag value

        Delete a subscription resource tag value.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/tagNames"
            f"/{quote(str(tag_name), safe='')}"
            f"/tagValues"
            f"/{quote(str(tag_value), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def tags_create_or_update_async(
        self,
        subscription_id: str,
        tag_name: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a subscription resource tag name

        Create or update a subscription resource tag name.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/tagNames"
            f"/{quote(str(tag_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def tags_delete_async(
        self,
        subscription_id: str,
        tag_name: str,
    ) -> None:
        """
        Delete a subscription resource tag name

        Delete a subscription resource tag name.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions"
            f"/{quote(str(subscription_id), safe='')}"
            f"/tagNames"
            f"/{quote(str(tag_name), safe='')}"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def tags_list_async(
        self,
        subscription_id: str,
    ) -> dict[str, Any] | None:
        """
        List subscription resource tags

        Lists all the subscription resource tags.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/subscriptions/{quote(str(subscription_id), safe='')}/tagNames"
        )
        query_params = []
        query_params.append("x-ms-api-version=" + quote("2016-06-01"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
