# Copyright (c) Microsoft Corporation. All rights reserved.

# TicketmasterClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class EventsGetResponse:
    """
    Response for Event search
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    page: Optional[Dict[str, Any]] = None


@dataclass
class EventGetResponse:
    """
    Response for Get event details
    """

    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    classifications: Optional[List[Dict[str, Any]]] = None
    dates: Optional[Dict[str, Any]] = None
    id: Optional[str] = None
    """The identifier."""
    images: Optional[List[Dict[str, Any]]] = None
    locale: Optional[str] = None
    """The locale."""
    name: Optional[str] = None
    """The name."""
    please_note: Optional[str] = field(
        default=None,
        metadata={"wire_name": "pleaseNote"},
    )
    """The please note."""
    price_ranges: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "priceRanges"},
    )
    promoter: Optional[Dict[str, Any]] = None
    sales: Optional[Dict[str, Any]] = None
    test: Optional[bool] = None
    """Whether test."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """The type."""
    url: Optional[str] = None
    """The URL address."""


@dataclass
class EventImagesGetResponse:
    """
    Response for Get event images
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """The type."""
    id: Optional[str] = None
    """The identifier."""
    images: Optional[List[Dict[str, Any]]] = None
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )


@dataclass
class AttractionsGetResponse:
    """
    Response for Attraction search
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    page: Optional[Dict[str, Any]] = None


@dataclass
class AttractionGetResponse:
    """
    Response for Get attraction details
    """

    name: Optional[str] = None
    """The name."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """The type."""
    id: Optional[str] = None
    """The identifier."""
    test: Optional[bool] = None
    """Whether test."""
    locale: Optional[str] = None
    """The locale."""
    images: Optional[List[Dict[str, Any]]] = None
    classifications: Optional[List[Dict[str, Any]]] = None
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )


@dataclass
class ClassificationsGetResponse:
    """
    Response for Classification search
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    page: Optional[Dict[str, Any]] = None


@dataclass
class ClassificationGetResponse:
    """
    Response for Get classification details
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    segment: Optional[Dict[str, Any]] = None


@dataclass
class GenreGetResponse:
    """
    Response for Get genre details
    """

    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    id: Optional[str] = None
    """The identifier."""
    name: Optional[str] = None
    """The name."""


@dataclass
class SegmentGetResponse:
    """
    Response for Get segment details
    """

    id: Optional[str] = None
    """The identifier."""
    name: Optional[str] = None
    """The name."""
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )


@dataclass
class SubGenreGetResponse:
    """
    Response for Get sub-genre details
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    id: Optional[str] = None
    """The identifier."""
    name: Optional[str] = None
    """The name."""
    locale: Optional[str] = None
    """The locale."""


@dataclass
class VenuesGetResponse:
    """
    Response for Venue search
    """

    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    page: Optional[Dict[str, Any]] = None


@dataclass
class VenueGetResponse:
    """
    Response for Get venue details
    """

    name: Optional[str] = None
    """The name."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """The type."""
    id: Optional[str] = None
    """The identifier."""
    test: Optional[bool] = None
    """Whether test."""
    locale: Optional[str] = None
    """The locale."""
    postal_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "postalCode"},
    )
    """The postal code."""
    timezone: Optional[str] = None
    """The timezone."""
    city: Optional[Dict[str, Any]] = None
    state: Optional[Dict[str, Any]] = None
    country: Optional[Dict[str, Any]] = None
    address: Optional[Dict[str, Any]] = None
    location: Optional[Dict[str, Any]] = None
    markets: Optional[List[Dict[str, Any]]] = None
    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )


@dataclass
class SuggestionsGetResponse:
    """
    Response for Find suggestions
    """

    links: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_links"},
    )
    embedded: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "_embedded"},
    )


# Client Class

class TicketmasterClient(ConnectorClientBase):
    """Typed client for ticketmaster connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a TicketmasterClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "ticketmaster"

    async def events_get_async(
        self,
        size: Optional[int] = None,
        id: Optional[str] = None,
        keyword: Optional[str] = None,
        attraction_id: Optional[str] = None,
        venue_id: Optional[str] = None,
        postal_code: Optional[str] = None,
        latlong: Optional[str] = None,
        radius: Optional[str] = None,
        unit: Optional[str] = None,
        source: Optional[str] = None,
        locale: Optional[str] = None,
        market_id: Optional[str] = None,
        start_date_time: Optional[str] = None,
        end_date_time: Optional[str] = None,
        include_t_b_a: Optional[str] = None,
        include_t_b_d: Optional[str] = None,
        include_test: Optional[str] = None,
        page: Optional[str] = None,
        sort: Optional[str] = None,
        onsale_start_date_time: Optional[str] = None,
        onsale_end_date_time: Optional[str] = None,
        city: Optional[List[str]] = None,
        country_code: Optional[str] = None,
        state_code: Optional[str] = None,
        classification_name: Optional[List[str]] = None,
        classification_id: Optional[List[str]] = None,
        dma_id: Optional[str] = None,
        local_start_date_time: Optional[List[str]] = None,
        local_start_end_date_time: Optional[List[str]] = None,
        start_end_date_time: Optional[List[str]] = None,
        public_visibility_start_date_time: Optional[List[str]] = None,
        pre_sale_date_time: Optional[List[str]] = None,
        onsale_on_start_date: Optional[str] = None,
        onsale_on_after_start_date: Optional[str] = None,
        collection_id: Optional[List[str]] = None,
        segment_id: Optional[List[str]] = None,
        segment_name: Optional[List[str]] = None,
        include_family: Optional[str] = None,
        promoter_id: Optional[str] = None,
        genre_id: Optional[List[str]] = None,
        sub_genre_id: Optional[List[str]] = None,
        type_id: Optional[List[str]] = None,
        sub_type_id: Optional[List[str]] = None,
        geo_point: Optional[str] = None,
        preferred_country: Optional[str] = None,
        include_spellcheck: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Event search

        Find events and filter your search by location, date, availability, and
        much more.
        """
        request_url = (
            f"{self._connection_runtime_url}/discovery/v2/events.json"
        )
        query_params = []
        if size is not None:
            value = str(size)
            if isinstance(size, bool):
                value = value.lower()
            query_params.append(f"size={quote(value)}")
        if id is not None:
            value = str(id)
            if isinstance(id, bool):
                value = value.lower()
            query_params.append(f"id={quote(value)}")
        if keyword is not None:
            value = str(keyword)
            if isinstance(keyword, bool):
                value = value.lower()
            query_params.append(f"keyword={quote(value)}")
        if attraction_id is not None:
            value = str(attraction_id)
            if isinstance(attraction_id, bool):
                value = value.lower()
            query_params.append(f"attractionId={quote(value)}")
        if venue_id is not None:
            value = str(venue_id)
            if isinstance(venue_id, bool):
                value = value.lower()
            query_params.append(f"venueId={quote(value)}")
        if postal_code is not None:
            value = str(postal_code)
            if isinstance(postal_code, bool):
                value = value.lower()
            query_params.append(f"postalCode={quote(value)}")
        if latlong is not None:
            value = str(latlong)
            if isinstance(latlong, bool):
                value = value.lower()
            query_params.append(f"latlong={quote(value)}")
        if radius is not None:
            value = str(radius)
            if isinstance(radius, bool):
                value = value.lower()
            query_params.append(f"radius={quote(value)}")
        if unit is not None:
            value = str(unit)
            if isinstance(unit, bool):
                value = value.lower()
            query_params.append(f"unit={quote(value)}")
        if source is not None:
            value = str(source)
            if isinstance(source, bool):
                value = value.lower()
            query_params.append(f"source={quote(value)}")
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if market_id is not None:
            value = str(market_id)
            if isinstance(market_id, bool):
                value = value.lower()
            query_params.append(f"marketId={quote(value)}")
        if start_date_time is not None:
            value = str(start_date_time)
            if isinstance(start_date_time, bool):
                value = value.lower()
            query_params.append(f"startDateTime={quote(value)}")
        if end_date_time is not None:
            value = str(end_date_time)
            if isinstance(end_date_time, bool):
                value = value.lower()
            query_params.append(f"endDateTime={quote(value)}")
        if include_t_b_a is not None:
            value = str(include_t_b_a)
            if isinstance(include_t_b_a, bool):
                value = value.lower()
            query_params.append(f"includeTBA={quote(value)}")
        if include_t_b_d is not None:
            value = str(include_t_b_d)
            if isinstance(include_t_b_d, bool):
                value = value.lower()
            query_params.append(f"includeTBD={quote(value)}")
        if include_test is not None:
            value = str(include_test)
            if isinstance(include_test, bool):
                value = value.lower()
            query_params.append(f"includeTest={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if sort is not None:
            value = str(sort)
            if isinstance(sort, bool):
                value = value.lower()
            query_params.append(f"sort={quote(value)}")
        if onsale_start_date_time is not None:
            value = str(onsale_start_date_time)
            if isinstance(onsale_start_date_time, bool):
                value = value.lower()
            query_params.append(f"onsaleStartDateTime={quote(value)}")
        if onsale_end_date_time is not None:
            value = str(onsale_end_date_time)
            if isinstance(onsale_end_date_time, bool):
                value = value.lower()
            query_params.append(f"onsaleEndDateTime={quote(value)}")
        if city is not None:
            value = str(city)
            if isinstance(city, bool):
                value = value.lower()
            query_params.append(f"city={quote(value)}")
        if country_code is not None:
            value = str(country_code)
            if isinstance(country_code, bool):
                value = value.lower()
            query_params.append(f"countryCode={quote(value)}")
        if state_code is not None:
            value = str(state_code)
            if isinstance(state_code, bool):
                value = value.lower()
            query_params.append(f"stateCode={quote(value)}")
        if classification_name is not None:
            value = str(classification_name)
            if isinstance(classification_name, bool):
                value = value.lower()
            query_params.append(f"classificationName={quote(value)}")
        if classification_id is not None:
            value = str(classification_id)
            if isinstance(classification_id, bool):
                value = value.lower()
            query_params.append(f"classificationId={quote(value)}")
        if dma_id is not None:
            value = str(dma_id)
            if isinstance(dma_id, bool):
                value = value.lower()
            query_params.append(f"dmaId={quote(value)}")
        if local_start_date_time is not None:
            value = str(local_start_date_time)
            if isinstance(local_start_date_time, bool):
                value = value.lower()
            query_params.append(f"localStartDateTime={quote(value)}")
        if local_start_end_date_time is not None:
            value = str(local_start_end_date_time)
            if isinstance(local_start_end_date_time, bool):
                value = value.lower()
            query_params.append(f"localStartEndDateTime={quote(value)}")
        if start_end_date_time is not None:
            value = str(start_end_date_time)
            if isinstance(start_end_date_time, bool):
                value = value.lower()
            query_params.append(f"startEndDateTime={quote(value)}")
        if public_visibility_start_date_time is not None:
            value = str(public_visibility_start_date_time)
            if isinstance(public_visibility_start_date_time, bool):
                value = value.lower()
            query_params.append(f"publicVisibilityStartDateTime={quote(value)}")
        if pre_sale_date_time is not None:
            value = str(pre_sale_date_time)
            if isinstance(pre_sale_date_time, bool):
                value = value.lower()
            query_params.append(f"preSaleDateTime={quote(value)}")
        if onsale_on_start_date is not None:
            value = str(onsale_on_start_date)
            if isinstance(onsale_on_start_date, bool):
                value = value.lower()
            query_params.append(f"onsaleOnStartDate={quote(value)}")
        if onsale_on_after_start_date is not None:
            value = str(onsale_on_after_start_date)
            if isinstance(onsale_on_after_start_date, bool):
                value = value.lower()
            query_params.append(f"onsaleOnAfterStartDate={quote(value)}")
        if collection_id is not None:
            value = str(collection_id)
            if isinstance(collection_id, bool):
                value = value.lower()
            query_params.append(f"collectionId={quote(value)}")
        if segment_id is not None:
            value = str(segment_id)
            if isinstance(segment_id, bool):
                value = value.lower()
            query_params.append(f"segmentId={quote(value)}")
        if segment_name is not None:
            value = str(segment_name)
            if isinstance(segment_name, bool):
                value = value.lower()
            query_params.append(f"segmentName={quote(value)}")
        if include_family is not None:
            value = str(include_family)
            if isinstance(include_family, bool):
                value = value.lower()
            query_params.append(f"includeFamily={quote(value)}")
        if promoter_id is not None:
            value = str(promoter_id)
            if isinstance(promoter_id, bool):
                value = value.lower()
            query_params.append(f"promoterId={quote(value)}")
        if genre_id is not None:
            value = str(genre_id)
            if isinstance(genre_id, bool):
                value = value.lower()
            query_params.append(f"genreId={quote(value)}")
        if sub_genre_id is not None:
            value = str(sub_genre_id)
            if isinstance(sub_genre_id, bool):
                value = value.lower()
            query_params.append(f"subGenreId={quote(value)}")
        if type_id is not None:
            value = str(type_id)
            if isinstance(type_id, bool):
                value = value.lower()
            query_params.append(f"typeId={quote(value)}")
        if sub_type_id is not None:
            value = str(sub_type_id)
            if isinstance(sub_type_id, bool):
                value = value.lower()
            query_params.append(f"subTypeId={quote(value)}")
        if geo_point is not None:
            value = str(geo_point)
            if isinstance(geo_point, bool):
                value = value.lower()
            query_params.append(f"geoPoint={quote(value)}")
        if preferred_country is not None:
            value = str(preferred_country)
            if isinstance(preferred_country, bool):
                value = value.lower()
            query_params.append(f"preferredCountry={quote(value)}")
        if include_spellcheck is not None:
            value = str(include_spellcheck)
            if isinstance(include_spellcheck, bool):
                value = value.lower()
            query_params.append(f"includeSpellcheck={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def event_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get event details

        Get details for a specific event using the unique identifier for the
        event. This includes the venue and location, the attraction(s), and the
        Ticketmaster Website URL for purchasing tickets for the event
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/events/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def event_images_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get event images

        Get images for a specific event using the unique identifier for the
        event.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/events/{quote(str(id), safe='')}/images"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def attractions_get_async(
        self,
        id: Optional[str] = None,
        keyword: Optional[str] = None,
        source: Optional[str] = None,
        locale: Optional[str] = None,
        include_test: Optional[str] = None,
        size: Optional[str] = None,
        page: Optional[str] = None,
        sort: Optional[str] = None,
        classification_name: Optional[List[str]] = None,
        classification_id: Optional[List[str]] = None,
        include_family: Optional[str] = None,
        segment_id: Optional[List[str]] = None,
        genre_id: Optional[List[str]] = None,
        sub_genre_id: Optional[List[str]] = None,
        type_id: Optional[List[str]] = None,
        sub_type_id: Optional[List[str]] = None,
        country_code: Optional[str] = None,
        preferred_country: Optional[str] = None,
        include_spellcheck: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Attraction search

        Find attractions (artists, sports, packages, plays and so on) and
        filter your search by name, and much more.
        """
        request_url = (
            f"{self._connection_runtime_url}/discovery/v2/attractions"
        )
        query_params = []
        if id is not None:
            value = str(id)
            if isinstance(id, bool):
                value = value.lower()
            query_params.append(f"id={quote(value)}")
        if keyword is not None:
            value = str(keyword)
            if isinstance(keyword, bool):
                value = value.lower()
            query_params.append(f"keyword={quote(value)}")
        if source is not None:
            value = str(source)
            if isinstance(source, bool):
                value = value.lower()
            query_params.append(f"source={quote(value)}")
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if include_test is not None:
            value = str(include_test)
            if isinstance(include_test, bool):
                value = value.lower()
            query_params.append(f"includeTest={quote(value)}")
        if size is not None:
            value = str(size)
            if isinstance(size, bool):
                value = value.lower()
            query_params.append(f"size={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if sort is not None:
            value = str(sort)
            if isinstance(sort, bool):
                value = value.lower()
            query_params.append(f"sort={quote(value)}")
        if classification_name is not None:
            value = str(classification_name)
            if isinstance(classification_name, bool):
                value = value.lower()
            query_params.append(f"classificationName={quote(value)}")
        if classification_id is not None:
            value = str(classification_id)
            if isinstance(classification_id, bool):
                value = value.lower()
            query_params.append(f"classificationId={quote(value)}")
        if include_family is not None:
            value = str(include_family)
            if isinstance(include_family, bool):
                value = value.lower()
            query_params.append(f"includeFamily={quote(value)}")
        if segment_id is not None:
            value = str(segment_id)
            if isinstance(segment_id, bool):
                value = value.lower()
            query_params.append(f"segmentId={quote(value)}")
        if genre_id is not None:
            value = str(genre_id)
            if isinstance(genre_id, bool):
                value = value.lower()
            query_params.append(f"genreId={quote(value)}")
        if sub_genre_id is not None:
            value = str(sub_genre_id)
            if isinstance(sub_genre_id, bool):
                value = value.lower()
            query_params.append(f"subGenreId={quote(value)}")
        if type_id is not None:
            value = str(type_id)
            if isinstance(type_id, bool):
                value = value.lower()
            query_params.append(f"typeId={quote(value)}")
        if sub_type_id is not None:
            value = str(sub_type_id)
            if isinstance(sub_type_id, bool):
                value = value.lower()
            query_params.append(f"subTypeId={quote(value)}")
        if country_code is not None:
            value = str(country_code)
            if isinstance(country_code, bool):
                value = value.lower()
            query_params.append(f"countryCode={quote(value)}")
        if preferred_country is not None:
            value = str(preferred_country)
            if isinstance(preferred_country, bool):
                value = value.lower()
            query_params.append(f"preferredCountry={quote(value)}")
        if include_spellcheck is not None:
            value = str(include_spellcheck)
            if isinstance(include_spellcheck, bool):
                value = value.lower()
            query_params.append(f"includeSpellcheck={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def attraction_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get attraction details

        Get details for a specific attraction using the unique identifier for
        the attraction.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/attractions/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def classifications_get_async(
        self,
        id: Optional[str] = None,
        keyword: Optional[str] = None,
        source: Optional[str] = None,
        locale: Optional[str] = None,
        include_test: Optional[str] = None,
        size: Optional[str] = None,
        page: Optional[str] = None,
        sort: Optional[str] = None,
        country_code: Optional[str] = None,
        preferred_country: Optional[str] = None,
        include_spellcheck: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Classification search

        Find classifications and filter your search by name, and much more.
        Classifications help define the nature of attractions and events.
        """
        request_url = (
            f"{self._connection_runtime_url}/discovery/v2/classifications"
        )
        query_params = []
        if id is not None:
            value = str(id)
            if isinstance(id, bool):
                value = value.lower()
            query_params.append(f"id={quote(value)}")
        if keyword is not None:
            value = str(keyword)
            if isinstance(keyword, bool):
                value = value.lower()
            query_params.append(f"keyword={quote(value)}")
        if source is not None:
            value = str(source)
            if isinstance(source, bool):
                value = value.lower()
            query_params.append(f"source={quote(value)}")
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if include_test is not None:
            value = str(include_test)
            if isinstance(include_test, bool):
                value = value.lower()
            query_params.append(f"includeTest={quote(value)}")
        if size is not None:
            value = str(size)
            if isinstance(size, bool):
                value = value.lower()
            query_params.append(f"size={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if sort is not None:
            value = str(sort)
            if isinstance(sort, bool):
                value = value.lower()
            query_params.append(f"sort={quote(value)}")
        if country_code is not None:
            value = str(country_code)
            if isinstance(country_code, bool):
                value = value.lower()
            query_params.append(f"countryCode={quote(value)}")
        if preferred_country is not None:
            value = str(preferred_country)
            if isinstance(preferred_country, bool):
                value = value.lower()
            query_params.append(f"preferredCountry={quote(value)}")
        if include_spellcheck is not None:
            value = str(include_spellcheck)
            if isinstance(include_spellcheck, bool):
                value = value.lower()
            query_params.append(f"includeSpellcheck={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def classification_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get classification details

        Get details for a specific segment, genre, or sub-genre using its
        unique identifier.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/classifications/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def genre_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get genre details

        Get details for a specific genre using its unique identifier.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/classifications/genres/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def segment_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get segment details

        Get details for a specific segment using its unique identifier.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/classifications/segments/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def sub_genre_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get sub-genre details

        Get details for a specific sub-genre using its unique identifier.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery"
            f"/v2"
            f"/classifications"
            f"/subgenres"
            f"/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def venues_get_async(
        self,
        id: Optional[str] = None,
        keyword: Optional[str] = None,
        latlong: Optional[str] = None,
        radius: Optional[str] = None,
        unit: Optional[str] = None,
        source: Optional[str] = None,
        locale: Optional[str] = None,
        include_test: Optional[str] = None,
        size: Optional[str] = None,
        page: Optional[str] = None,
        sort: Optional[str] = None,
        country_code: Optional[str] = None,
        state_code: Optional[str] = None,
        geo_point: Optional[str] = None,
        preferred_country: Optional[str] = None,
        include_spellcheck: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Venue search

        Find venues and filter your search by name, and much more.
        """
        request_url = f"{self._connection_runtime_url}/discovery/v2/venues"
        query_params = []
        if id is not None:
            value = str(id)
            if isinstance(id, bool):
                value = value.lower()
            query_params.append(f"id={quote(value)}")
        if keyword is not None:
            value = str(keyword)
            if isinstance(keyword, bool):
                value = value.lower()
            query_params.append(f"keyword={quote(value)}")
        if latlong is not None:
            value = str(latlong)
            if isinstance(latlong, bool):
                value = value.lower()
            query_params.append(f"latlong={quote(value)}")
        if radius is not None:
            value = str(radius)
            if isinstance(radius, bool):
                value = value.lower()
            query_params.append(f"radius={quote(value)}")
        if unit is not None:
            value = str(unit)
            if isinstance(unit, bool):
                value = value.lower()
            query_params.append(f"unit={quote(value)}")
        if source is not None:
            value = str(source)
            if isinstance(source, bool):
                value = value.lower()
            query_params.append(f"source={quote(value)}")
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if include_test is not None:
            value = str(include_test)
            if isinstance(include_test, bool):
                value = value.lower()
            query_params.append(f"includeTest={quote(value)}")
        if size is not None:
            value = str(size)
            if isinstance(size, bool):
                value = value.lower()
            query_params.append(f"size={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if sort is not None:
            value = str(sort)
            if isinstance(sort, bool):
                value = value.lower()
            query_params.append(f"sort={quote(value)}")
        if country_code is not None:
            value = str(country_code)
            if isinstance(country_code, bool):
                value = value.lower()
            query_params.append(f"countryCode={quote(value)}")
        if state_code is not None:
            value = str(state_code)
            if isinstance(state_code, bool):
                value = value.lower()
            query_params.append(f"stateCode={quote(value)}")
        if geo_point is not None:
            value = str(geo_point)
            if isinstance(geo_point, bool):
                value = value.lower()
            query_params.append(f"geoPoint={quote(value)}")
        if preferred_country is not None:
            value = str(preferred_country)
            if isinstance(preferred_country, bool):
                value = value.lower()
            query_params.append(f"preferredCountry={quote(value)}")
        if include_spellcheck is not None:
            value = str(include_spellcheck)
            if isinstance(include_spellcheck, bool):
                value = value.lower()
            query_params.append(f"includeSpellcheck={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def venue_get_async(
        self,
        id: str,
        locale: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Get venue details

        Get details for a specific venue using the unique identifier for the
        venue.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/discovery/v2/venues/{quote(str(id), safe='')}"
        )
        query_params = []
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def suggestions_get_async(
        self,
        keyword: Optional[str] = None,
        latlong: Optional[str] = None,
        radius: Optional[str] = None,
        unit: Optional[str] = None,
        source: Optional[str] = None,
        locale: Optional[str] = None,
        include_t_b_a: Optional[str] = None,
        include_t_b_d: Optional[str] = None,
        include_test: Optional[str] = None,
        size: Optional[str] = None,
        country_code: Optional[str] = None,
        segment_id: Optional[List[str]] = None,
        geo_point: Optional[str] = None,
        resource: Optional[List[str]] = None,
        preferred_country: Optional[str] = None,
        start_end_date_time: Optional[List[str]] = None,
        local_start_end_date_time: Optional[List[str]] = None,
        include_spellcheck: Optional[str] = None,
        domain: Optional[List[str]] = None,
    ) -> dict[str, Any] | None:
        """
        Find suggestions

        Find search suggestions and filter your suggestions by location,
        source, etc.
        """
        request_url = f"{self._connection_runtime_url}/discovery/v2/suggest"
        query_params = []
        if keyword is not None:
            value = str(keyword)
            if isinstance(keyword, bool):
                value = value.lower()
            query_params.append(f"keyword={quote(value)}")
        if latlong is not None:
            value = str(latlong)
            if isinstance(latlong, bool):
                value = value.lower()
            query_params.append(f"latlong={quote(value)}")
        if radius is not None:
            value = str(radius)
            if isinstance(radius, bool):
                value = value.lower()
            query_params.append(f"radius={quote(value)}")
        if unit is not None:
            value = str(unit)
            if isinstance(unit, bool):
                value = value.lower()
            query_params.append(f"unit={quote(value)}")
        if source is not None:
            value = str(source)
            if isinstance(source, bool):
                value = value.lower()
            query_params.append(f"source={quote(value)}")
        if locale is not None:
            value = str(locale)
            if isinstance(locale, bool):
                value = value.lower()
            query_params.append(f"locale={quote(value)}")
        if include_t_b_a is not None:
            value = str(include_t_b_a)
            if isinstance(include_t_b_a, bool):
                value = value.lower()
            query_params.append(f"includeTBA={quote(value)}")
        if include_t_b_d is not None:
            value = str(include_t_b_d)
            if isinstance(include_t_b_d, bool):
                value = value.lower()
            query_params.append(f"includeTBD={quote(value)}")
        if include_test is not None:
            value = str(include_test)
            if isinstance(include_test, bool):
                value = value.lower()
            query_params.append(f"includeTest={quote(value)}")
        if size is not None:
            value = str(size)
            if isinstance(size, bool):
                value = value.lower()
            query_params.append(f"size={quote(value)}")
        if country_code is not None:
            value = str(country_code)
            if isinstance(country_code, bool):
                value = value.lower()
            query_params.append(f"countryCode={quote(value)}")
        if segment_id is not None:
            value = str(segment_id)
            if isinstance(segment_id, bool):
                value = value.lower()
            query_params.append(f"segmentId={quote(value)}")
        if geo_point is not None:
            value = str(geo_point)
            if isinstance(geo_point, bool):
                value = value.lower()
            query_params.append(f"geoPoint={quote(value)}")
        if resource is not None:
            value = str(resource)
            if isinstance(resource, bool):
                value = value.lower()
            query_params.append(f"resource={quote(value)}")
        if preferred_country is not None:
            value = str(preferred_country)
            if isinstance(preferred_country, bool):
                value = value.lower()
            query_params.append(f"preferredCountry={quote(value)}")
        if start_end_date_time is not None:
            value = str(start_end_date_time)
            if isinstance(start_end_date_time, bool):
                value = value.lower()
            query_params.append(f"startEndDateTime={quote(value)}")
        if local_start_end_date_time is not None:
            value = str(local_start_end_date_time)
            if isinstance(local_start_end_date_time, bool):
                value = value.lower()
            query_params.append(f"localStartEndDateTime={quote(value)}")
        if include_spellcheck is not None:
            value = str(include_spellcheck)
            if isinstance(include_spellcheck, bool):
                value = value.lower()
            query_params.append(f"includeSpellcheck={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"domain={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
