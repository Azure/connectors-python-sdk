# Copyright (c) Microsoft Corporation. All rights reserved.

# CampfireClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CreateMessageResponse:
    """
    Response for Create a message
    """

    message: Optional[Dict[str, Any]] = None
    """A Campfire message"""


@dataclass
class RoomsResponse:
    """
    Response for When a room is created
    """

    rooms: Optional[List[Dict[str, Any]]] = None
    """Collection of Rooms"""


@dataclass
class MessagesResponse:
    """
    Response for When a new message is received
    """

    messages: Optional[List[Dict[str, Any]]] = None
    """Collection Of messages"""


@dataclass
class UploadResponse:
    """
    Response for When a file is uploaded
    """

    uploads: Optional[List[Dict[str, Any]]] = None
    """Collection of Uploads"""


@dataclass
class UserResponse:
    """
    Response for Get user by ID
    """

    user: Optional[Dict[str, Any]] = None


@dataclass
class AccountsResponse:
    """
    Response for List accounts
    """

    accounts: Optional[List[Dict[str, Any]]] = None
    """Collection Of Accounts"""


@dataclass
class Account:
    """
    Definition: Account
    """

    id: Optional[int] = None
    """Id of the account"""
    name: Optional[str] = None
    """Name of the account"""
    app_href: Optional[str] = None
    """Link to the account"""
    account_url: Optional[str] = None
    """Account Url"""


@dataclass
class Room:
    """
    Definition: Room
    """

    name: Optional[str] = None
    """Room name."""
    id: Optional[int] = None
    """Room Id."""
    locked: Optional[bool] = None
    """Boolean value, True if a room is locked."""
    topic: Optional[str] = None
    """Room topic or description."""


@dataclass
class Message:
    """
    Definition: Message
    """

    body: Optional[str] = None
    """Message Body."""
    id: Optional[int] = None
    """Message Id."""
    starred: Optional[bool] = None
    """Boolean value, True if a message is starred."""


@dataclass
class Upload:
    """
    Definition: Upload
    """

    full_url: Optional[str] = None
    """File Url."""
    room_id: Optional[int] = None
    """Room Id."""
    id: Optional[int] = None
    """File Id."""
    content_type: Optional[str] = None
    """Format of the uploaded file."""
    name: Optional[str] = None
    """File name."""
    created_at: Optional[str] = None
    """Timestamp of upload."""


@dataclass
class CreateUploadResponse:
    """
    Definition: CreateUploadResponse
    """

    upload: Optional[Upload] = None


# Client Class

class CampfireClient(ConnectorClientBase):
    """Typed client for campfire connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a CampfireClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "campfire"

    async def create_message_async(
        self,
        room_id: str,
        account: str,
        message: str,
    ) -> dict[str, Any] | None:
        """
        Create a message

        Sends a message to the given room.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/room/{quote(str(room_id), safe='')}/speak.json"
        )
        query_params = []
        value = str(account)
        if isinstance(account, bool):
            value = value.lower()
        query_params.append(f"account={quote(value)}")
        value = str(message)
        if isinstance(message, bool):
            value = value.lower()
        query_params.append(f"message={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_user_async(
        self,
        user_id: int,
        account: str,
    ) -> dict[str, Any] | None:
        """
        Get user by ID

        Retrieves information about a user by given ID
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/users/{quote(str(user_id), safe='')}.json"
        )
        query_params = []
        value = str(account)
        if isinstance(account, bool):
            value = value.lower()
        query_params.append(f"account={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_accounts_async(
        self,
        parent_operation: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List accounts

        Retrieves the list of accounts this user has access too
        """
        request_url = f"{self._connection_runtime_url}/authorization.json"
        query_params = []
        if parent_operation is not None:
            value = str(parent_operation)
            if isinstance(parent_operation, bool):
                value = value.lower()
            query_params.append(f"parentOperation={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_rooms_async(
        self,
        account: str,
    ) -> dict[str, Any] | None:
        """
        List Rooms

        Retrieves the list of rooms in an account
        """
        request_url = f"{self._connection_runtime_url}/rooms.json"
        query_params = []
        value = str(account)
        if isinstance(account, bool):
            value = value.lower()
        query_params.append(f"account={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnNewRoom": {
        "operation_id": "OnNewRoom",
        "path": "/{connectionId}/OnNewRoom_trigger/rooms.json",
        "method": "get",
        "required_parameters": ["account"],
        "callback_payload_type": "RoomsResponse",
    },
    "OnNewMessage": {
        "operation_id": "OnNewMessage",
        "path": "/{connectionId}/OnNewMessage_trigger/room/{roomId}/recent.json",
        "method": "get",
        "required_parameters": ["account", "roomId"],
        "callback_payload_type": "MessagesResponse",
    },
    "OnNewUpload": {
        "operation_id": "OnNewUpload",
        "path": "/{connectionId}/OnNewUpload_trigger/room/{roomId}/uploads.json",
        "method": "get",
        "required_parameters": ["account", "roomId"],
        "callback_payload_type": "UploadResponse",
    },
}
