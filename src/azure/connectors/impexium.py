# Copyright (c) Microsoft Corporation. All rights reserved.

# ImpexiumClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class GetAbandonedCheckoutsResponse:
    """
    Response for Get Abandoned Checkouts
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[AbandonedCheckoutData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListAllExhibitorsResponse:
    """
    Response for List All Exhibitors
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[ExhibitorData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListOfExamsResponse:
    """
    Response for List of Exams
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[ExamData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListRegistrantsResponse:
    """
    Response for List Registrants
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RegistrantData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetCourseAttendeesResponse:
    """
    Response for Get Course Attendees
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CourseAttendeeData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


AddExamScoresInput = List["ExamScoreData"]


@dataclass
class ExamScoreResultData:
    """
    Response for Add Exam Scores
    """

    individual_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "individualRecordNumber"},
    )
    """Individual Record Number."""
    status: Optional[str] = None
    """Status."""
    message: Optional[str] = None
    """Message."""


@dataclass
class FindMembersByNameResponse:
    """
    Response for Find Members by Name
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[ContactData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetPurchasesForAnIndividualResponse:
    """
    Response for Get Purchases for an Individual
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[PurchasedItemData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


AddOrUpdateAListOfCustomFieldsPerOrganizationInput = List["CustomFieldValueData"]


@dataclass
class CustomFieldResultData:
    """
    Response for Add or Update a List of Custom Fields Per Organization
    """

    status: Optional[str] = None
    """Status."""
    message: Optional[str] = None
    """Message."""
    custom_field_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customFieldName"},
    )
    """Custom Field Name."""


@dataclass
class CustomFieldData:
    """
    Response for Get Individual Custom Field Values
    """

    name: Optional[str] = None
    """Name."""
    caption: Optional[str] = None
    """Caption."""
    value: Optional[str] = None
    """Custom Field Value. For multi-selects, use \"|\" as delimiter."""


@dataclass
class ListAllEventCancellationsByEventResponse:
    """
    Response for List All Event Cancellations by Event
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RegistrantCancellationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetAllOpenOrdersForAnIndividualResponse:
    """
    Response for Get All Open Orders for an Individual
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[PayableOrderData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListCompletedUserTasksByUserIdOrEmailResponse:
    """
    Response for List Completed User Tasks by User ID or Email
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[UserTaskData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListPendingUserTasksByUserIdOrEmailResponse:
    """
    Response for List Pending User Tasks by User ID or Email
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[UserTaskData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class TaskData:
    """
    Response for Update Task by Task Number
    """

    task_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "taskNumber"},
    )
    """Task Number."""
    title: Optional[str] = None
    """Title."""
    category: Optional[str] = None
    """Category."""
    description: Optional[str] = None
    """Description."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    due_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dueDate"},
    )
    """Due Date."""


@dataclass
class ListAllCountriesResponse:
    """
    Response for List All Countries
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[CountryData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetAllStatesByCountryResponse:
    """
    Response for Get All States by Country
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[StateProvinceData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListAllExhibitsResponse:
    """
    Response for List All Exhibits
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[ExhibitData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class RequestUpdateData:
    """
    Response for Update Customer Request
    """

    request_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestNumber"},
    )
    """Request Number."""
    closed_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "closedDate"},
    )
    """Closed Date."""


AddCategoriesForAnOrganizationInput = List["SaveCategoryBasicData"]


@dataclass
class ListOfCustomerRelationshipsResponse:
    """
    Response for List of Customer Relationships
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RelationshipTypeData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListAllOpenCustomerRequestResponse:
    """
    Response for List All Open Customer Request
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RequestData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class MembershipData:
    """
    Response for Get Organization Inactive Memberships
    """

    membership_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "membershipType"},
    )
    """Membership Type."""
    membership_type_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "membershipTypeId"},
    )
    """Membership Type ID."""
    code: Optional[str] = None
    """Code."""
    expire_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "expireDate"},
    )
    """Expire Date."""
    grace_expire_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "graceExpireDate"},
    )
    """Grace Expire Date."""
    inherited_membership_benefits: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "inheritedMembershipBenefits"},
    )
    """Inherited Membership Benefits."""
    renewal_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "renewalUrl"},
    )
    """Renewal URL."""
    join_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "joinDate"},
    )
    """Join Date."""
    terminate_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "terminateDate"},
    )
    """Terminate Date."""
    effective_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "effectiveDate"},
    )
    """Effective Date."""
    membership_unique_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "membershipUniqueId"},
    )
    """Membership Unique ID."""
    terms_list: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "termsList"},
    )
    """Terms List"""


@dataclass
class UserTaskData:
    """
    Response for Update User Task Progress or Mark as Completed
    """

    task_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "taskNumber"},
    )
    """Task Number."""
    title: Optional[str] = None
    """Title."""
    category: Optional[str] = None
    """Category."""
    description: Optional[str] = None
    """Description."""
    assigned_by: Optional[str] = field(
        default=None,
        metadata={"wire_name": "assignedBy"},
    )
    """Assigned By."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    due_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dueDate"},
    )
    """Due Date."""
    progress: Optional[str] = None
    """Progress."""
    completed_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "completedDate"},
    )
    """Completed Date."""


@dataclass
class IndividualData:
    """
    Response for Find Members (Individuals) by First Name
    """

    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix."""
    gender: Optional[str] = None
    """Gender."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    committees: Optional[List[Dict[str, Any]]] = None
    """Committees."""
    security_roles: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "securityRoles"},
    )
    """Security Roles."""
    user: Optional[Dict[str, Any]] = None
    designation_data: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "designationData"},
    )
    """Designations."""
    relationships: Optional[List[Dict[str, Any]]] = None
    """Relationships."""
    job_roles: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "jobRoles"},
    )
    """Job Roles."""
    tags: Optional[List[str]] = None
    """Tags."""
    id: Optional[str] = None
    """Individual Id."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    title: Optional[str] = None
    """Title."""
    name: Optional[str] = None
    """Name."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    email: Optional[str] = None
    """Email."""
    image_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "imageUri"},
    )
    """Image URI."""
    twitter: Optional[str] = None
    """Twitter."""
    linked_in: Optional[str] = field(
        default=None,
        metadata={"wire_name": "linkedIn"},
    )
    """LinkedIn."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails."""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""
    web_site: Optional[str] = field(
        default=None,
        metadata={"wire_name": "webSite"},
    )
    """Website."""
    memberships: Optional[List[Dict[str, Any]]] = None
    """Memberships."""
    category: Optional[str] = None
    """Category."""
    categories: Optional[List[Dict[str, Any]]] = None
    """Categories."""
    custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "customFields"},
    )
    """Custom Fields."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    links: Optional[List[Dict[str, Any]]] = None
    """Links."""
    old_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "oldId"},
    )
    """Old Id."""
    is_deceased: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isDeceased"},
    )
    """Is Deceased."""


AddCategoriesForAnIndividualInput = List["SaveCategoryBasicData"]


@dataclass
class ListOfAllOrganizationMembersResponse:
    """
    Response for List of All Organization Members
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[OrganizationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListOfAllIndividualMembersResponse:
    """
    Response for List of All Individual Members
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetListOfActiveCertificationsForAnOrganizationResponse:
    """
    Response for Get List of Active Certifications for an Organization
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CertificationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetListOfActiveCertificationsForAnIndividualResponse:
    """
    Response for Get List of Active Certifications for an Individual
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CertificationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


RegisterAnIndividualForAFreeSessionInput = List["SessionRegistrationData"]


@dataclass
class GetAListOfLicensesResponse:
    """
    Response for Get a List of Licenses
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[LicenseData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListAllAwardsResponse:
    """
    Response for List All Awards
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[AwardData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class FindMembersOrIndividualsByNameResponse:
    """
    Response for Find Members (Individuals) by Name
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetAListOfAllServicesOfAnOrganizationResponse:
    """
    Response for Get a List of All Services of an Organization
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[ServiceData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ServiceData:
    """
    Response for Add a Service to an Organization
    """

    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""


@dataclass
class PhoneDataSet:
    """
    Response for Update Phone for an Individual
    """

    id: Optional[str] = None
    """ID."""
    primary: Optional[bool] = None
    """Is Primary."""
    type_: Optional[int] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    country: Optional[Dict[str, Any]] = None
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    number: Optional[str] = None
    """Number."""
    extension: Optional[str] = None
    """Extension."""
    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""


@dataclass
class DeleteAnIndividualWebLinkInput:
    """
    Delete an Individual Web Link
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    url: Optional[str] = None
    """Url."""


@dataclass
class AddWebLinkForIndividualInput:
    """
    Add Web Link for Individual
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    url: Optional[str] = None
    """Url."""


@dataclass
class GetNomineesByCommitteeResponse:
    """
    Response for Get Nominees by Committee
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CommitteeNomineeData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List"""


@dataclass
class GetOrganizationsActiveSubscriptionsResponse:
    """
    Response for Get Organization's Active Subscriptions
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[SubscriptionData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class DeleteAnOrganizationWebLinkInput:
    """
    Delete an Organization Web Link
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    url: Optional[str] = None
    """Url."""


@dataclass
class AddWebLinkForOrganizationInput:
    """
    Add Web Link for Organization
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    url: Optional[str] = None
    """Url."""


@dataclass
class GetOrganizationsRelationshipsResponse:
    """
    Response for Get Organization's Relationships
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RelationshipData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """DataList"""


@dataclass
class AddressSaveData:
    """
    Response for Add or Update Address to Organization
    """

    id: Optional[str] = None
    """ID."""
    primary: Optional[bool] = None
    """Primary."""
    line1: Optional[str] = None
    """Line 1."""
    line2: Optional[str] = None
    """Line 2."""
    line3: Optional[str] = None
    """Line 3."""
    city: Optional[str] = None
    """City."""
    state: Optional[str] = None
    """State."""
    zipcode: Optional[str] = None
    """Zipcode."""
    country: Optional[str] = None
    """Country."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    is_preferred_shipping: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPreferredShipping"},
    )
    """Is Preferred Shipping."""
    is_preferred_billing: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPreferredBilling"},
    )
    """Is Preferred Billing."""
    is_bad_address: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isBadAddress"},
    )
    """Is Bad Address."""


AddOrUpdateAListOfCustomFieldsPerIndividualInput = List[Dict[str, Any]]


@dataclass
class GetUpcomingEventsResponse:
    """
    Response for Get Upcoming Events
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[EventData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """DataList"""


@dataclass
class GetCommitteeInformationForAnIndividualResponse:
    """
    Response for Get Committee Information for an Individual
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number"""
    data_list: Optional[List[CommitteeMemberData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class GetAllEventsResponse:
    """
    Response for Get All Events
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[EventData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """DataList"""


@dataclass
class GetAllEventRegistrationsInformationForAnIndividualResponse:
    """
    Response for Get All Event Registrations Information for an Individual
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RegistrationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetIndividualsRelationshipsResponse:
    """
    Response for Get Individual's Relationships
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[RelationshipData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class UpdateAnIndividualEmailInput:
    """
    Update an Individual Email
    """

    primary: Optional[bool] = None
    """Primary."""
    address: Optional[str] = None
    """Address."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory"""


@dataclass
class SaveRelationshipForOrganizationInput:
    """
    Save Relationship for Organization
    """

    relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relationshipName"},
    )
    """Relationship Name."""
    related_to_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relatedToCustomerRecordNumber"},
    )
    """Related To Customer Record Number."""
    reciprocal_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "reciprocalRelationshipName"},
    )
    """Reciprocal Relationship Name."""
    is_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPrimary"},
    )
    """Is Primary."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    is_reciprocal_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isReciprocalPrimary"},
    )
    """Is Reciprocal Primary."""
    title: Optional[str] = None
    """Title."""


@dataclass
class AddIndividualInput:
    """
    Add Individual
    """

    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    email: Optional[str] = None
    """Email Address."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""


@dataclass
class AddEmailToIndividualInput:
    """
    Add Email to Individual
    """

    primary: Optional[bool] = None
    """Primary"""
    address: Optional[str] = None
    """Address."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory"""


@dataclass
class EmailData:
    """
    Response for Add Email to Individual
    """

    primary: Optional[bool] = None
    """Is Primary Email."""
    address: Optional[str] = None
    """Address."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""


@dataclass
class FindMembersOrIndividualsByFirstAndLastNameResponse:
    """
    Response for Find Members (Individuals) by First Name and Last Name
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class AddActivityInput:
    """
    Add Activity
    """

    category: Optional[str] = None
    """Category."""
    text: Optional[str] = None
    """Text."""
    url: Optional[str] = None
    """URL."""
    activity_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "activityDate"},
    )
    """Activity Date."""


@dataclass
class FindIndividualIdOrEmailResponse:
    """
    Response for Find Individual by Id or Email or Record Number
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class AddRelationshipToIndividualInput:
    """
    Add Relationship to Individual
    """

    relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relationshipName"},
    )
    """Relationship Name."""
    related_to_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relatedToCustomerRecordNumber"},
    )
    """Related To Customer Record Number."""
    reciprocal_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "reciprocalRelationshipName"},
    )
    """Reciprocal Relationship Name."""
    is_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPrimary"},
    )
    """Is Primary."""
    is_reciprocal_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isReciprocalPrimary"},
    )
    """Is Reciprocal Primary."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""


@dataclass
class IndividualsLookupByNameResponse:
    """
    Response for Lookup Individuals
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualLookupBasicData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class AddToCommitteeResponse:
    """
    Response for Add Individual to Committee
    """

    id: Optional[str] = None
    """Member Id."""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    code: Optional[str] = None
    """Code."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    position: Optional[str] = None
    """Position."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """Position Code."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    organization_representing: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "organizationRepresenting"},
    )
    nominated_by_customer: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomer"},
    )
    addresses: Optional[List[Dict[str, Any]]] = None
    phones: Optional[List[Dict[str, Any]]] = None
    emails: Optional[List[Dict[str, Any]]] = None
    rank: Optional[int] = None
    """Rank."""


@dataclass
class AddOrganizationInput:
    """
    Add Organization
    """

    id: Optional[str] = None
    """Id."""
    name: Optional[str] = None
    """Name."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    branch_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "branchName"},
    )
    """Branch Name."""
    website: Optional[str] = None
    """Website."""


@dataclass
class OrganizationData:
    """
    Response for Add Organization
    """

    contact_ids: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "contactIds"},
    )
    """Contact Ids."""
    parent_company_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentCompanyId"},
    )
    """Parent Company Id."""
    description: Optional[str] = None
    """Description."""
    branch_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "branchName"},
    )
    """Branch Name."""
    tags: Optional[List[str]] = None
    """Tags."""
    id: Optional[str] = None
    """Organization Id."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    title: Optional[str] = None
    """Title."""
    name: Optional[str] = None
    """Organization Name."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    email: Optional[str] = None
    """Email."""
    image_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "imageUri"},
    )
    """Image URI."""
    twitter: Optional[str] = None
    """Twitter."""
    linked_in: Optional[str] = field(
        default=None,
        metadata={"wire_name": "linkedIn"},
    )
    """LinkedIn."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails."""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""
    web_site: Optional[str] = field(
        default=None,
        metadata={"wire_name": "webSite"},
    )
    """Website."""
    memberships: Optional[List[Dict[str, Any]]] = None
    """Memberships."""
    category: Optional[str] = None
    """Category."""
    categories: Optional[List[Dict[str, Any]]] = None
    """Categories."""
    custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "customFields"},
    )
    """Custom Fields."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    links: Optional[List[Dict[str, Any]]] = None
    """Links."""
    old_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "oldId"},
    )
    """Old Id."""


@dataclass
class UpdateOrganizationInput:
    """
    Update Organization
    """

    name: Optional[str] = None
    """Name."""
    description: Optional[str] = None
    """Description."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    branch_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "branchName"},
    )
    """Branch Name."""
    website: Optional[str] = None
    """Website."""


@dataclass
class OrganizationLookupByNameResponse:
    """
    Response for Lookup Organizations
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[OrganizationLookupBasicData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class GetAllCommitteesResponse:
    """
    Response for Get All Committees
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CommitteeData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetPositionsByCommitteeResponse:
    """
    Response for Get Positions by Committee
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[CommitteePositionData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetSubCommitteesResponse:
    """
    Response for Get Sub-Committees
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """pageNumber"""
    data_list: Optional[List[CommitteeData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class GetIndividualsActiveSubscriptionsResponse:
    """
    Response for Get Individual's Active Subscriptions
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[SubscriptionData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """dataList"""


@dataclass
class ListAllIndividualsResponse:
    """
    Response for List all Individuals
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[IndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )


@dataclass
class FindCustomerPhoneResponse:
    """
    Response for Find Customer by Phone Number
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )


MarkRegistrantAttendedInput = List[Dict[str, Any]]


@dataclass
class AwardsGetIndividualAwardRecipientsResponse:
    """
    Response for List Award Individual Recipients
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[AwardRecipientIndividualData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class AwardsGetOrganizationAwardRecipientsResponse:
    """
    Response for List Award Organization Recipients
    """

    page_number: Optional[int] = field(
        default=None,
        metadata={"wire_name": "pageNumber"},
    )
    """Page Number."""
    data_list: Optional[List[AwardRecipientOrganizationData]] = field(
        default=None,
        metadata={"wire_name": "dataList"},
    )
    """Data List."""


@dataclass
class WhenIndividualCreatedInput:
    """
    When an individual is created
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenIndividualDeletedInput:
    """
    When an individual is deleted
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenIndividualRequestForgottenInput:
    """
    When an individual requests to be forgotten
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenProductPurchasedInput:
    """
    When a product is purchased
    """

    id: Optional[str] = None
    """Id."""
    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    filters: Optional[List[str]] = None
    """Filters."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCommitteeMemberUpdatedInput:
    """
    When a committee member is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenPurchaseCancelledInput:
    """
    When a purchase is canceled
    """

    id: Optional[str] = None
    """Id."""
    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    filters: Optional[List[str]] = None
    """Filters."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenRequestUpdatedInput:
    """
    When a customer request is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenEmailUpdatedInput:
    """
    When a customer email is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCustomerCustomFieldValueUpdatedInput:
    """
    When a customer custom field value is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    filters: Optional[List[str]] = None
    """Filters."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCustomerIsMergedInput:
    """
    When a customer is merged
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCustomerRelationshipUpdatedInput:
    """
    When a customer relationship is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCustomerPhoneUpdatedInput:
    """
    When a customer phone is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenCustomerAddressUpdatedInput:
    """
    When a customer address is updated
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenEventRegistrationSubstitutedInput:
    """
    When an event registration is substituted
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    filters: Optional[List[str]] = None
    """Filters."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenPurchasePaidInput:
    """
    When a purchase is paid
    """

    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    filters: Optional[List[str]] = None
    """Filters."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class WhenMembershipTerminatedInput:
    """
    When a membership is terminated
    """

    id: Optional[str] = None
    """Id."""
    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""
    secret: Optional[str] = None
    """Secret."""
    call_back_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callBackUrl"},
    )
    """Callback URL."""
    description: Optional[str] = None
    """Description."""


@dataclass
class AbandonedCheckoutData:
    """
    Definition: AbandonedCheckoutData
    """

    created_on: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdOn"},
    )
    """Created On"""
    created_by_email_address: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdByEmailAddress"},
    )
    """Created By Email Address"""
    currency: Optional[str] = None
    """Currency"""
    customer: Optional[Dict[str, Any]] = None
    line_items: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "lineItems"},
    )
    """Line Items"""


@dataclass
class ExhibitorData:
    """
    Definition: ExhibitorData
    """

    organization: Optional[Dict[str, Any]] = None
    """Organization"""
    booths: Optional[List[Dict[str, Any]]] = None
    """Booths"""


@dataclass
class ExamData:
    """
    Definition: ExamData
    """

    exam_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "examStartDate"},
    )
    """Exam Start Date"""
    exam_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "examEndDate"},
    )
    """Exam End Date"""
    exam_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "examNumber"},
    )
    """Exam Number"""
    education_credits: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "educationCredits"},
    )
    """Education Credits"""
    categories: Optional[List[Dict[str, Any]]] = None
    """categories"""
    active_prices: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "activePrices"},
    )
    """Active Prices"""
    related_products: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "relatedProducts"},
    )
    """Related Products"""
    tags: Optional[List[str]] = None
    """Tags."""
    id: Optional[str] = None
    """ID."""
    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""
    short_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortDescription"},
    )
    """Short Description."""
    long_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "longDescription"},
    )
    """Long Description."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    url: Optional[str] = None
    """Url."""
    image_url: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "imageUrl"},
    )
    """Image Url."""
    is_public: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPublic"},
    )
    """Is Public."""
    available_from: Optional[str] = field(
        default=None,
        metadata={"wire_name": "availableFrom"},
    )
    """Available From."""
    available_until: Optional[str] = field(
        default=None,
        metadata={"wire_name": "availableUntil"},
    )
    """Available Until."""


@dataclass
class RegistrantData:
    """
    Definition: RegistrantData
    """

    title: Optional[str] = None
    """Title."""
    badge_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeName"},
    )
    """Badge Name"""
    badge_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeOrganization"},
    )
    """Badge Organization."""
    badge_city: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeCity"},
    )
    """Badge City."""
    badge_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeState"},
    )
    """Badge State."""
    attended_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "attendedDate"},
    )
    """Attended Date."""
    sessions: Optional[List[Dict[str, Any]]] = None
    itemized_custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "itemizedCustomFields"},
    )
    """Itemized Custom Fields"""
    guest_of_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "guestOfRecordNumber"},
    )
    """Guest Of Record Number."""
    registrant_type_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrantTypeCode"},
    )
    """Registrant Type Code."""
    registrant_type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrantTypeName"},
    )
    """Registrant Type Name."""
    registration_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrationNumber"},
    )
    """Registration Number."""
    bought_together_with: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "boughtTogetherWith"},
    )
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix."""
    gender: Optional[str] = None
    """Gender."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    id: Optional[str] = None
    """ID."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type"""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    addresses: Optional[List[Dict[str, Any]]] = None
    phones: Optional[List[Dict[str, Any]]] = None
    emails: Optional[List[Dict[str, Any]]] = None


@dataclass
class CourseAttendeeData:
    """
    Definition: CourseAttendeeData
    """

    title: Optional[str] = None
    """Title."""
    registered_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registeredDate"},
    )
    """Registered Date."""
    attended_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "attendedDate"},
    )
    """Attended Date."""
    itemized_custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "itemizedCustomFields"},
    )
    """Itemized Custom Fields"""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix"""
    gender: Optional[str] = None
    """Gender."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    id: Optional[str] = None
    """ID."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails."""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""


@dataclass
class ExamScoreData:
    """
    Definition: ExamScoreData
    """

    individual_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "individualRecordNumber"},
    )
    """Individual Record Number."""
    score: Optional[str] = None
    """Score."""


@dataclass
class ContactData:
    """
    Definition: ContactData
    """

    id: Optional[str] = None
    """ID."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    title: Optional[str] = None
    """Title."""
    name: Optional[str] = None
    """Name."""
    addresses: Optional[List[Dict[str, Any]]] = None
    image_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "imageUri"},
    )
    """Image Uri."""
    twitter: Optional[str] = None
    """Twitter."""
    linked_in: Optional[str] = field(
        default=None,
        metadata={"wire_name": "linkedIn"},
    )
    """LinkedIn."""
    emails: Optional[List[Dict[str, Any]]] = None
    """emails"""
    phones: Optional[List[Dict[str, Any]]] = None
    web_site: Optional[str] = field(
        default=None,
        metadata={"wire_name": "webSite"},
    )
    """Website."""
    memberships: Optional[List[Dict[str, Any]]] = None
    """Memberships."""
    category: Optional[str] = None
    """Category."""
    categories: Optional[List[Dict[str, Any]]] = None
    """Categories"""
    custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "customFields"},
    )
    """Custom Fields"""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    links: Optional[List[Dict[str, Any]]] = None
    """Links"""
    old_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "oldId"},
    )
    """Old ID."""


@dataclass
class PurchasedItemData:
    """
    Definition: PurchasedItemData
    """

    order_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "orderNumber"},
    )
    """Order Number."""
    product_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "productCode"},
    )
    """Product Code."""
    product_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "productName"},
    )
    """Product Name."""
    product_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "productType"},
    )
    """Product Type."""
    purchase_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "purchaseDate"},
    )
    """Purchase Date."""
    additional_info: Optional[str] = field(
        default=None,
        metadata={"wire_name": "additionalInfo"},
    )
    """Additional Info."""
    quantity: Optional[int] = None
    """Quantity."""
    itemized_custom_fields: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "itemizedCustomFields"},
    )
    """Itemized Custom Fields"""
    product_categories: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "productCategories"},
    )
    """Product Categories"""


@dataclass
class CustomFieldValueData:
    """
    Definition: CustomFieldValueData
    """

    name: Optional[str] = None
    """Name."""
    value: Optional[str] = None
    """Value."""


@dataclass
class CommitteeNomineeSaveData:
    """
    Definition: CommitteeNomineeSaveData
    """

    nominee_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nomineeRecordNumber"},
    )
    """Nominee Record Number."""
    code: Optional[str] = None
    """Code."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """Position Code."""
    status: Optional[str] = None
    """Status."""
    representing_organization_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRecordNumber"},
    )
    """Representing Organization Record Number."""
    representing_organization_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRelationshipName"},
    )
    """Representing Organization Relationship Name."""
    nominated_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomerRecordNumber"},
    )
    """Nominated By Customer Record Number."""
    committee_term: Optional[int] = field(
        default=None,
        metadata={"wire_name": "committeeTerm"},
    )
    """Committee Term."""
    rank: Optional[int] = None
    """Rank."""


@dataclass
class RegistrantCancellationData:
    """
    Definition: RegistrantCancellationData
    """

    title: Optional[str] = None
    """Title."""
    registration_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrationNumber"},
    )
    """Registration Number."""
    event_cancellation_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventCancellationDate"},
    )
    """Event Cancellation Date."""
    guest_of_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "guestOfRecordNumber"},
    )
    """Guest Of Record Number."""
    cancelled_sessions: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "cancelledSessions"},
    )
    """Cancelled Sessions."""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix."""
    gender: Optional[str] = None
    """Gender."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    """primaryOrganization"""
    id: Optional[str] = None
    """ID."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails"""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""


@dataclass
class PayableOrderData:
    """
    Definition: PayableOrderData
    """

    number: Optional[str] = None
    """Number."""
    date: Optional[str] = None
    """Date."""
    balance: Optional[float] = None
    """Balance."""
    line_items: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "lineItems"},
    )
    """lineItems"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type."""
    id: Optional[str] = None
    """ID."""
    customer_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerId"},
    )
    """Customer Id."""
    total_amount: Optional[float] = field(
        default=None,
        metadata={"wire_name": "totalAmount"},
    )
    """Total Amount."""
    title: Optional[str] = None
    """Title."""


@dataclass
class BaseNoteData:
    """
    Definition: BaseNoteData
    """

    content: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Content"},
    )
    """Content."""
    category: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Category"},
    )
    """Category."""
    is_internal: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isInternal"},
    )
    """Is Internal."""


@dataclass
class TaskSaveData:
    """
    Definition: TaskSaveData
    """

    title: Optional[str] = None
    """Title."""
    category: Optional[str] = None
    """Category."""
    description: Optional[str] = None
    """Description."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    due_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dueDate"},
    )
    """Due Date."""


@dataclass
class CountryData:
    """
    Definition: CountryData
    """

    id: Optional[str] = None
    """ID."""
    name: Optional[str] = None
    """Name."""
    two_letter_iso_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "twoLetterIsoCode"},
    )
    """Two Letter Iso Code."""
    three_letter_iso_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "threeLetterIsoCode"},
    )
    """Three Letter Iso Code."""
    code: Optional[int] = None
    """Code."""


@dataclass
class StateProvinceData:
    """
    Definition: StateProvinceData
    """

    id: Optional[str] = None
    """ID."""
    name: Optional[str] = None
    """Name."""
    abbreviation: Optional[str] = None
    """Abbreviation."""
    iso_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "isoCode"},
    )
    """Iso Code."""


@dataclass
class ExhibitData:
    """
    Definition: ExhibitData
    """

    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    start_time: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startTime"},
    )
    """Start Time."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    end_time: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endTime"},
    )
    """End Time."""
    categories: Optional[List[Dict[str, Any]]] = None
    """Categories"""
    short_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortDescription"},
    )
    """Short Description."""
    long_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "longDescription"},
    )
    """Long Description."""
    featured_listing: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "featuredListing"},
    )


@dataclass
class RequestSaveData:
    """
    Definition: RequestSaveData
    """

    requested_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestedByCustomerRecordNumber"},
    )
    """Requested By Customer Record Number."""
    request_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestType"},
    )
    """Request Type."""
    description: Optional[str] = None
    """Description."""
    source: Optional[str] = None
    """Source."""


@dataclass
class RelationshipTypeData:
    """
    Definition: RelationshipTypeData
    """

    name: Optional[str] = None
    """Name."""
    reciprocal_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "reciprocalRelationshipName"},
    )
    """Reciprocal Relationship Name."""
    relationship_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relationshipType"},
    )
    """Relationship Type."""
    allow_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowPrimary"},
    )
    """Allow Primary."""
    can_purchase_for_organization: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canPurchaseForOrganization"},
    )
    """Can Purchase For Organization."""
    can_manage_p_a_c: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canManagePAC"},
    )
    """Can Manage PAC."""
    can_manage_organization: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canManageOrganization"},
    )
    """Can Manage Organization."""


@dataclass
class RequestData:
    """
    Definition: RequestData
    """

    request_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestNumber"},
    )
    """Request Number."""
    request_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestDate"},
    )
    """Request Date."""
    requested_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestedByCustomerRecordNumber"},
    )
    """Requested By Customer Record Number."""
    request_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "requestType"},
    )
    """Request Type."""
    description: Optional[str] = None
    """Description."""
    source: Optional[str] = None
    """Source."""
    closed_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "closedDate"},
    )
    """Closed Date."""


@dataclass
class NotificationData:
    """
    Definition: NotificationData
    """

    title: Optional[str] = None
    """Title."""
    message: Optional[str] = None
    """Message."""
    url: Optional[str] = None
    """Url."""
    date: Optional[str] = None
    """Date."""
    is_read: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isRead"},
    )
    """Is Read."""


@dataclass
class SaveCategoryBasicData:
    """
    Definition: SaveCategoryBasicData
    """

    code: Optional[str] = None
    """Code."""
    is_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPrimary"},
    )
    """Is Primary."""


@dataclass
class CertificationData:
    """
    Definition: CertificationData
    """

    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""
    expire_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "expireDate"},
    )
    """Expire Date."""


@dataclass
class ActivityData:
    """
    Definition: ActivityData
    """

    category: Optional[str] = None
    """Category."""
    text: Optional[str] = None
    """Text."""
    url: Optional[str] = None
    """Url."""
    activity_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "activityDate"},
    )
    """Activity Date."""


@dataclass
class SessionRegistrationData:
    """
    Definition: SessionRegistrationData
    """

    session_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "sessionCode"},
    )
    """Session Code."""


@dataclass
class AwardData:
    """
    Definition: AwardData
    """

    id: Optional[str] = None
    """ID."""
    name: Optional[str] = None
    """Name."""
    description: Optional[str] = None
    """Description."""
    year: Optional[int] = None
    """Year."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    allow_public_nomination: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "allowPublicNomination"},
    )
    """Allow Public Nomination."""
    nomination_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominationStartDate"},
    )
    """Nomination Start Date."""
    nomination_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominationEndDate"},
    )
    """Nomination End Date."""


@dataclass
class LicenseData:
    """
    Definition: LicenseData
    """

    id: Optional[str] = None
    """ID."""
    license_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "licenseType"},
    )
    """License Type."""
    number: Optional[str] = None
    """Number."""
    state: Optional[str] = None
    """State."""
    status: Optional[str] = None
    """Status."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    expiration_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "expirationDate"},
    )
    """Experation Date."""


@dataclass
class CommitteeNomineeData:
    """
    Definition: CommitteeNomineeData
    """

    id: Optional[str] = None
    """ID."""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    code: Optional[str] = None
    """Code."""
    position: Optional[str] = None
    """Position."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """Position Code."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    organization_representing: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "organizationRepresenting"},
    )
    nominated_by_customer: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomer"},
    )
    addresses: Optional[List[Dict[str, Any]]] = None
    phones: Optional[List[Dict[str, Any]]] = None
    emails: Optional[List[Dict[str, Any]]] = None
    rank: Optional[int] = None
    """Rank."""


@dataclass
class RelationshipData:
    """
    Definition: RelationshipData
    """

    relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "relationshipName"},
    )
    """Relationship Name."""
    related_to_customer: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "relatedToCustomer"},
    )
    """Related To Customer."""
    reciprocal_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "reciprocalRelationshipName"},
    )
    """Reciprocal Relationship Name."""
    is_primary: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isPrimary"},
    )
    """Is Primary."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""


@dataclass
class RegistrationData:
    """
    Definition: RegistrationData
    """

    individual_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "individualId"},
    )
    """Individual ID."""
    event: Optional[Dict[str, Any]] = None
    """Event."""
    sessions: Optional[List[Dict[str, Any]]] = None
    """Sessions."""
    badge_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeName"},
    )
    """Badge Name."""
    badge_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeOrganization"},
    )
    """Badge Organization."""
    badge_city: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeCity"},
    )
    """Badge City."""
    badge_state: Optional[str] = field(
        default=None,
        metadata={"wire_name": "badgeState"},
    )
    """Badge State."""
    bought_together_with: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "boughtTogetherWith"},
    )
    registrant_type_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrantTypeCode"},
    )
    """Registrant Type Code."""
    registrant_type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrantTypeName"},
    )
    """Registrant Type Name."""
    registration_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "registrationNumber"},
    )
    """Registration Number."""


@dataclass
class CommitteeMemberData:
    """
    Definition: CommitteeMemberData
    """

    committee: Optional[Dict[str, Any]] = None
    position: Optional[str] = None
    """Position."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """Position Code."""
    code: Optional[str] = None
    """Code."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""


@dataclass
class EventData:
    """
    Definition: EventData
    """

    event_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventId"},
    )
    """Event ID."""
    event_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventCode"},
    )
    """Event Code."""
    event_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventName"},
    )
    """Event Name."""
    event_start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventStartDate"},
    )
    """Event Start Date."""
    event_start_time: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventStartTime"},
    )
    """Event Start Time."""
    event_end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventEndDate"},
    )
    """Event End Date."""
    event_end_time: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventEndTime"},
    )
    """Event End Time."""
    event_timezone: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventTimezone"},
    )
    """Event Time Zone."""
    event_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventDescription"},
    )
    """Event Description."""
    short_description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortDescription"},
    )
    """Short Description."""
    public_event: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "publicEvent"},
    )
    """Public Event."""
    image_url: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "imageUrl"},
    )
    """Image Url."""
    external_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "externalUrl"},
    )
    """External Url."""
    external_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "externalCode"},
    )
    """External Code."""
    event_locations: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "eventLocations"},
    )
    """Event Locations."""
    categories: Optional[List[Dict[str, Any]]] = None
    """Event Locations - Categories."""
    education_credits: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "educationCredits"},
    )
    """Event Locations - Education Credits."""
    uploads_data: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "uploadsData"},
    )
    """Event Locations - Uploads Data"""
    related_products: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "relatedProducts"},
    )
    """Event Locations - Related Products."""
    tags: Optional[List[str]] = None
    """Event Locations - Tags"""


@dataclass
class PhoneSaveData:
    """
    Definition: PhoneSaveData
    """

    country_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "countryName"},
    )
    """Country Name."""
    primary: Optional[bool] = None
    """Primary."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show in Directory."""
    number: Optional[str] = None
    """Number."""
    extension: Optional[str] = None
    """Extension."""
    type_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "typeName"},
    )
    """Type Name."""


@dataclass
class IndividualPayload:
    """
    Definition: IndividualPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )
    """Notifications."""


@dataclass
class CommitteeMemberPayload:
    """
    Definition: CommitteeMemberPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )
    """Notifications."""


@dataclass
class PurchaseCancelledPayload:
    """
    Definition: PurchaseCancelledPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )
    """Notifications."""


@dataclass
class CustomerRequestPayload:
    """
    Definition: CustomerRequestPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id,."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )
    """Notifications."""


@dataclass
class EmailPayload:
    """
    Definition: EmailPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )
    """Notifications."""


@dataclass
class CommitteeMemberCreateData:
    """
    Definition: CommitteeMemberCreateData
    """

    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    code: Optional[str] = None
    """Member Code."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """Position Code."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    representing_organization_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRecordNumber"},
    )
    """Representing Organization - Record Number."""
    representing_organization_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRelationshipName"},
    )
    """Representing Organization - Relationship Name."""
    nominated_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomerRecordNumber"},
    )
    """Nominated By - Record Number"""
    committee_term: Optional[int] = field(
        default=None,
        metadata={"wire_name": "committeeTerm"},
    )
    """Committee Term."""
    rank: Optional[int] = None
    """Rank."""


@dataclass
class SubscriptionData:
    """
    Definition: SubscriptionData
    """

    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""
    expire_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "expireDate"},
    )
    """Expire Date."""
    quantity: Optional[int] = None
    """Quantity."""


@dataclass
class CommitteePositionData:
    """
    Definition: CommitteePositionData
    """

    code: Optional[str] = None
    """Code."""
    name: Optional[str] = None
    """Name."""
    term: Optional[int] = None
    """Term."""
    description: Optional[str] = None
    """Description."""
    is_admin: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isAdmin"},
    )
    """Is Admin."""


@dataclass
class CommitteeData:
    """
    Definition: CommitteeData
    """

    name: Optional[str] = None
    """Name."""
    code: Optional[str] = None
    """Code."""
    website: Optional[str] = None
    """Website."""
    id: Optional[str] = None
    """Id."""
    parent_committee_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentCommitteeId"},
    )
    """Parent Committee Id."""
    category_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "categoryName"},
    )
    """Category Name."""
    category_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "categoryCode"},
    )
    """Category Code."""
    description: Optional[str] = None
    """Description."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    term: Optional[Dict[str, Any]] = None
    """Term."""
    tags: Optional[List[str]] = None
    """Tags."""


@dataclass
class CommitteeMemberUpdateData:
    """
    Definition: CommitteeMemberUpdateData
    """

    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    code: Optional[str] = None
    """Member Code."""
    position_code: Optional[str] = field(
        default=None,
        metadata={"wire_name": "positionCode"},
    )
    """New Position Code."""
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    """Start Date."""
    end_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "endDate"},
    )
    """End Date."""
    representing_organization_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRecordNumber"},
    )
    """Representing Organization - Record Number."""
    representing_organization_relationship_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "representingOrganizationRelationshipName"},
    )
    """Representing Organization - Relationship Name."""
    nominated_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomerRecordNumber"},
    )
    """Nominated By - Record Number"""
    committee_term: Optional[int] = field(
        default=None,
        metadata={"wire_name": "committeeTerm"},
    )
    """Committee Term."""
    rank: Optional[int] = None
    """Rank."""


@dataclass
class EducationCreditData:
    """
    Definition: EducationCreditData
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Education Credit Type."""
    description: Optional[str] = None
    """Description."""
    number_of_credits: Optional[float] = field(
        default=None,
        metadata={"wire_name": "numberOfCredits"},
    )
    """Number of Credits Earned."""
    date_earned: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateEarned"},
    )
    """Earned Date."""
    provider_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "providerName"},
    )
    """Provider Name."""
    state_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "stateName"},
    )
    """State or Province Name."""
    reference: Optional[str] = None
    """Reference."""
    is_self_reported: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isSelfReported"},
    )
    """Is Self Reported."""


@dataclass
class NoteData:
    """
    Definition: NoteData
    """

    is_alert: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isAlert"},
    )
    """Is Alert."""
    title: Optional[str] = None
    """Title."""
    follow_up_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "followUpDate"},
    )
    """Follow Up Date."""
    content: Optional[str] = None
    """Content."""
    category: Optional[str] = None
    """Category."""
    is_internal: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isInternal"},
    )
    """Is Internal."""


@dataclass
class AwardNominationData:
    """
    Definition: AwardNominationData
    """

    nominee_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nomineeRecordNumber"},
    )
    """Nominee Record Number."""
    nominated_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomerRecordNumber"},
    )
    """Nominated By Record Number."""
    nomination_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominationDate"},
    )
    """Nomination Date."""
    awarded_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "awardedDate"},
    )
    """Awarded Date."""
    status: Optional[str] = None
    """Status."""
    description: Optional[str] = None
    """An explanation why the nominee should be considered for the award."""


@dataclass
class UpdateAwardNominationData:
    """
    Definition: UpdateAwardNominationData
    """

    nominee_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nomineeRecordNumber"},
    )
    """Nominee Record Number."""
    nominated_by_customer_record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominatedByCustomerRecordNumber"},
    )
    """Nominated By Record Number."""
    nomination_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nominationDate"},
    )
    """Nomination Date."""
    awarded_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "awardedDate"},
    )
    """Awarded Date."""
    status: Optional[str] = None
    """Status."""
    description: Optional[str] = None
    """An explanation why the nominee should be considered for the award."""


@dataclass
class AwardRecipientIndividualData:
    """
    Definition: AwardRecipientIndividualData
    """

    awarded_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "awardedDate"},
    )
    """Awarded Date."""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix."""
    gender: Optional[str] = None
    """Gender."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    id: Optional[str] = None
    """Individual Id."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    title: Optional[str] = None
    """Title."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails."""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""


@dataclass
class AwardRecipientOrganizationData:
    """
    Definition: AwardRecipientOrganizationData
    """

    awarded_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "awardedDate"},
    )
    """Awarded Date."""
    name: Optional[str] = None
    """Organization Name."""
    acronym: Optional[str] = None
    """Acronym."""
    branch_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "branchName"},
    )
    """Branch Name."""
    annual_sales: Optional[float] = field(
        default=None,
        metadata={"wire_name": "annualSales"},
    )
    """Annual Sales."""
    employee_range_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "employeeRangeId"},
    )
    """Employee Range Id."""
    id: Optional[str] = None
    """Organization Id."""
    customer_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "customerType"},
    )
    """Customer Type."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    show_in_directory: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "showInDirectory"},
    )
    """Show In Directory."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""
    emails: Optional[List[Dict[str, Any]]] = None
    """Emails."""
    phones: Optional[List[Dict[str, Any]]] = None
    """Phones."""


@dataclass
class OrganizationLookupBasicData:
    """
    Definition: OrganizationLookupBasicData
    """

    name: Optional[str] = None
    """Organization Name."""
    acronym: Optional[str] = None
    """Acronym."""
    branch_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "branchName"},
    )
    """Branch Name."""
    employee_range_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "employeeRangeId"},
    )
    """Employee Range Id."""
    id: Optional[str] = None
    """Organization Id."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""
    addresses: Optional[List[Dict[str, Any]]] = None
    """Addresses."""


@dataclass
class IndividualLookupBasicData:
    """
    Definition: IndividualLookupBasicData
    """

    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """First Name."""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """Last Name."""
    middle_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "middleName"},
    )
    """Middle Name."""
    preferred_first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "preferredFirstName"},
    )
    """Preferred First Name."""
    second_last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "secondLastName"},
    )
    """Second Last Name."""
    prefix: Optional[str] = None
    """Prefix."""
    suffix: Optional[str] = None
    """Suffix."""
    gender: Optional[str] = None
    """Gender."""
    title: Optional[str] = None
    """Title."""
    primary_organization: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "primaryOrganization"},
    )
    id: Optional[str] = None
    """Individual Id."""
    record_number: Optional[str] = field(
        default=None,
        metadata={"wire_name": "recordNumber"},
    )
    """Record Number."""


@dataclass
class CustomerCustomFieldValuePayload:
    """
    Definition: CustomerCustomFieldValuePayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class CustomerMergedPayload:
    """
    Definition: CustomerMergedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class CustomerRelationshipUpdatedPayload:
    """
    Definition: CustomerRelationshipUpdatedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class CustomerPhoneUpdatedPayload:
    """
    Definition: CustomerPhoneUpdatedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class CustomerAddressUpdatedPayload:
    """
    Definition: CustomerAddressUpdatedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class EventRegistrationSubstitutedPayload:
    """
    Definition: EventRegistrationSubstitutedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class IndividualRequestToBeForgottenPayload:
    """
    Definition: IndividualRequestToBeForgottenPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class ProductPurchasePayload:
    """
    Definition: ProductPurchasePayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


@dataclass
class MembershipTypeTerminatedPayload:
    """
    Definition: MembershipTypeTerminatedPayload
    """

    webhook_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebhookId"},
    )
    """Webhook Id."""
    properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "Properties"},
    )
    """Properties."""
    notifications: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "Notifications"},
    )


# Client Class

class ImpexiumClient(ConnectorClientBase):
    """Typed client for impexium connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a ImpexiumClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "impexium"

    async def get_abandoned_checkouts_async(
        self,
        page_number: int,
        abandoned_from: str,
        product_code: Optional[str] = None,
        customer_record_number: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Abandoned Checkouts

        Get Abandoned Checkouts
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Shopping"
            f"/AbandonedCheckOuts"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        value = str(abandoned_from)
        if isinstance(abandoned_from, bool):
            value = value.lower()
        query_params.append(f"abandonedFrom={quote(value)}")
        if product_code is not None:
            value = str(product_code)
            if isinstance(product_code, bool):
                value = value.lower()
            query_params.append(f"productCode={quote(value)}")
        if customer_record_number is not None:
            value = str(customer_record_number)
            if isinstance(customer_record_number, bool):
                value = value.lower()
            query_params.append(f"customerRecordNumber={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_exhibitors_async(
        self,
        exhibit_code: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List All Exhibitors

        List All Exhibitors
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Exhibits"
            f"/{quote(str(exhibit_code), safe='')}"
            f"/Exhibitors"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_of_exams_async(
        self,
        page_number: int,
        code: Optional[str] = None,
        category_name: Optional[str] = None,
        is_public: Optional[bool] = None,
        changed_since: Optional[str] = None,
        tag: Optional[str] = None,
        include_prices: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        List of Exams

        List of Exams
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Products/Exams/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if code is not None:
            value = str(code)
            if isinstance(code, bool):
                value = value.lower()
            query_params.append(f"Code={quote(value)}")
        if category_name is not None:
            value = str(category_name)
            if isinstance(category_name, bool):
                value = value.lower()
            query_params.append(f"categoryName={quote(value)}")
        if is_public is not None:
            value = str(is_public)
            if isinstance(is_public, bool):
                value = value.lower()
            query_params.append(f"isPublic={quote(value)}")
        if changed_since is not None:
            value = str(changed_since)
            if isinstance(changed_since, bool):
                value = value.lower()
            query_params.append(f"changedSince={quote(value)}")
        if tag is not None:
            value = str(tag)
            if isinstance(tag, bool):
                value = value.lower()
            query_params.append(f"Tag={quote(value)}")
        if include_prices is not None:
            value = str(include_prices)
            if isinstance(include_prices, bool):
                value = value.lower()
            query_params.append(f"includePrices={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_registrants_async(
        self,
        event_code: str,
        page_number: int,
        session_code: Optional[str] = None,
        include_details: Optional[bool] = None,
        registered_since: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List Registrants

        List Registrants
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Events"
            f"/{quote(str(event_code), safe='')}"
            f"/Registrations"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if session_code is not None:
            value = str(session_code)
            if isinstance(session_code, bool):
                value = value.lower()
            query_params.append(f"sessionCode={quote(value)}")
        if include_details is not None:
            value = str(include_details)
            if isinstance(include_details, bool):
                value = value.lower()
            query_params.append(f"includeDetails={quote(value)}")
        if registered_since is not None:
            value = str(registered_since)
            if isinstance(registered_since, bool):
                value = value.lower()
            query_params.append(f"registeredSince={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_course_attendees_async(
        self,
        code: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get Course Attendees

        Get Course Attendees
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Courses"
            f"/{quote(str(code), safe='')}"
            f"/Attendees"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_exam_scores_async(
        self,
        input: AddExamScoresInput,
        exam_code: str,
    ) -> dict[str, Any] | None:
        """
        Add Exam Scores

        Add Exam Scores
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Exams/{quote(str(exam_code), safe='')}/Scores"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_members_by_name_async(
        self,
        name: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Find Members by Name

        Find Members by Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Customers"
            f"/Members"
            f"/FindByName"
            f"/{quote(str(name), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_purchases_for_an_individual_async(
        self,
        id_or_record_number: str,
        page_number: int,
        product_code: Optional[str] = None,
        purchased_since: Optional[str] = None,
        product_category_code: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Purchases for an Individual

        Get Purchases for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Purchases"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if product_code is not None:
            value = str(product_code)
            if isinstance(product_code, bool):
                value = value.lower()
            query_params.append(f"productCode={quote(value)}")
        if purchased_since is not None:
            value = str(purchased_since)
            if isinstance(purchased_since, bool):
                value = value.lower()
            query_params.append(f"purchasedSince={quote(value)}")
        if product_category_code is not None:
            value = str(product_category_code)
            if isinstance(product_category_code, bool):
                value = value.lower()
            query_params.append(f"productCategoryCode={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_or_update_a_list_of_custom_fields_per_organization_async(
        self,
        input: AddOrUpdateAListOfCustomFieldsPerOrganizationInput,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add or Update a List of Custom Fields Per Organization

        Add or Update a List of Custom Fields Per Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/CustomFieldsList"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_nominee_async(
        self,
        input: CommitteeNomineeSaveData,
        code: str,
    ) -> None:
        """
        Add Nominee

        Add Nominee
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Committees/{quote(str(code), safe='')}/Nominations"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_individual_custom_field_values_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Individual Custom Field Values

        Get Individual Custom Field Values
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/CustomFields"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_custom_field_value_async(
        self,
        input: CustomFieldData,
        id: str,
    ) -> None:
        """
        Update Individual Custom Field Values

        Update Individual Custom Field Values.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/CustomFields"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def list_all_event_cancellations_by_event_async(
        self,
        event_code: str,
        page_number: int,
        include_details: Optional[bool] = None,
        cancelled_since: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List All Event Cancellations by Event

        List All Event Cancellations by Event
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Events"
            f"/{quote(str(event_code), safe='')}"
            f"/Cancellations"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_details is not None:
            value = str(include_details)
            if isinstance(include_details, bool):
                value = value.lower()
            query_params.append(f"includeDetails={quote(value)}")
        if cancelled_since is not None:
            value = str(cancelled_since)
            if isinstance(cancelled_since, bool):
                value = value.lower()
            query_params.append(f"cancelledSince={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_all_open_orders_for_an_individual_async(
        self,
        id_or_record_number: str,
        page_number: int,
        include_line_items: Optional[bool] = None,
        from_date: Optional[str] = None,
        to_date: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get All Open Orders for an Individual

        Get All Open Orders for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Orders"
            f"/Open"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_line_items is not None:
            value = str(include_line_items)
            if isinstance(include_line_items, bool):
                value = value.lower()
            query_params.append(f"includeLineItems={quote(value)}")
        if from_date is not None:
            value = str(from_date)
            if isinstance(from_date, bool):
                value = value.lower()
            query_params.append(f"fromDate={quote(value)}")
        if to_date is not None:
            value = str(to_date)
            if isinstance(to_date, bool):
                value = value.lower()
            query_params.append(f"toDate={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_completed_user_tasks_by_user_id_or_email_async(
        self,
        user_id_or_email: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List Completed User Tasks by User ID or Email

        List Completed User Tasks by User ID or Email
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/tasks"
            f"/Users"
            f"/{quote(str(user_id_or_email), safe='')}"
            f"/Completed"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_pending_user_tasks_by_user_id_or_email_async(
        self,
        user_id_or_email: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List Pending User Tasks by User ID or Email

        List Pending User Tasks by User ID or Email
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/tasks"
            f"/Users"
            f"/{quote(str(user_id_or_email), safe='')}"
            f"/Pending"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_note_to_sales_opportunity_async(
        self,
        input: BaseNoteData,
        id: str,
    ) -> None:
        """
        Add Note to Sales Opportunity

        Add Note to Sales Opportunity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Sales/Opportunities/{quote(str(id), safe='')}/Notes"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_activity_to_sales_opportunity_async(
        self,
        input: ActivityData,
        id: str,
    ) -> None:
        """
        Add Activity to Sales Opportunity

        Add Activity to Sales Opportunity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Sales/Opportunities/{quote(str(id), safe='')}/Activities"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def update_task_by_task_number_async(
        self,
        input: TaskSaveData,
        task_number: str,
    ) -> dict[str, Any] | None:
        """
        Update Task by Task Number

        Update Task by Task Number
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/tasks/{quote(str(task_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_countries_async(
        self,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List All Countries

        List All Countries
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Countries/All/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_all_states_by_country_async(
        self,
        country_id: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get All States by Country

        Get All States by Country
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Countries"
            f"/{quote(str(country_id), safe='')}"
            f"/States"
            f"/All"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_exhibits_async(
        self,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List All Exhibits

        List All Exhibits
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Exhibits/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_a_category_for_an_organization_async(
        self,
        record_number: str,
        category_code: str,
    ) -> None:
        """
        Delete a Category for an Organization

        Delete a Category for an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(record_number), safe='')}"
            f"/Categories"
            f"/{quote(str(category_code), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def add_customer_request_async(
        self,
        input: RequestSaveData,
    ) -> None:
        """
        Add Customer Request

        Add Customer Request
        """
        request_url = f"{self._connection_runtime_url}/api/v1/Requests"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def update_customer_request_async(
        self,
        input: RequestUpdateData,
    ) -> dict[str, Any] | None:
        """
        Update Customer Request

        Update Customer Request
        """
        request_url = f"{self._connection_runtime_url}/api/v1/Requests"

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_categories_for_an_organization_async(
        self,
        input: AddCategoriesForAnOrganizationInput,
        record_number: str,
    ) -> None:
        """
        Add Categories for an Organization

        Add Categories for an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(record_number), safe='')}"
            f"/Categories"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def list_of_customer_relationships_async(
        self,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List of Customer Relationships

        List of Customer Relationships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Customers"
            f"/RelationshipTypes"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_open_customer_request_async(
        self,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        List All Open Customer Request

        List All Open Customer Request
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Requests/Open/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organization_inactive_memberships_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Organization Inactive Memberships

        Get Organization Inactive Memberships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id), safe='')}"
            f"/Memberships"
            f"/Inactive"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_record_from_custom_data_table_async(
        self,
        table_name: str,
        id: str,
    ) -> None:
        """
        Delete Record From Custom Data Table

        Delete Record From Custom Data Table
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/CustomData"
            f"/{quote(str(table_name), safe='')}"
            f"/{quote(str(id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def update_user_task_progress_or_mark_as_completed_async(
        self,
        input: UserTaskData,
        user_id_or_email: str,
    ) -> dict[str, Any] | None:
        """
        Update User Task Progress or Mark as Completed

        Update User Task Progress or Mark as Completed
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/tasks/Users/{quote(str(user_id_or_email), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_members_or_individuals_by_first_name_async(
        self,
        first_name: str,
        page_number: int,
        include_email: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Find Members (Individuals) by First Name

        Find Members (Individuals) by First Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Members"
            f"/FindByFirstName"
            f"/{quote(str(first_name), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_members_or_individuals_by_last_name_async(
        self,
        last_name: str,
        page_number: int,
        include_email: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Find Members (Individuals) by Last Name

        Find Members (Individuals) by Last Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Members"
            f"/FindByLastName"
            f"/{quote(str(last_name), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def assign_task_to_a_user_async(
        self,
        input: UserTaskData,
        user_id_or_email: str,
    ) -> None:
        """
        Create a new task or Assign Task to a User

        Create a new task or Assign Task to a User
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/tasks/Users/{quote(str(user_id_or_email), safe='')}/Task"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def delete_a_category_for_an_individual_async(
        self,
        record_number: str,
        category_code: str,
    ) -> None:
        """
        Delete a Category for an Individual

        Delete a Category for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(record_number), safe='')}"
            f"/Categories"
            f"/{quote(str(category_code), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def add_notification_to_individual_async(
        self,
        input: NotificationData,
        id: str,
    ) -> None:
        """
        Add Notification to Individual

        Add Notification to Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/Notifications"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_categories_for_an_individual_async(
        self,
        input: AddCategoriesForAnIndividualInput,
        record_number: str,
    ) -> None:
        """
        Add Categories for an Individual

        Add Categories for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(record_number), safe='')}"
            f"/Categories"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_a_new_task_async(
        self,
        input: TaskSaveData,
    ) -> dict[str, Any] | None:
        """
        Add a New Task

        Add a New Task
        """
        request_url = f"{self._connection_runtime_url}/api/v1/tasks"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_of_all_organization_members_async(
        self,
        page_number: int,
        zip_code: Optional[str] = None,
        radius: Optional[float] = None,
        state_abbreviation: Optional[str] = None,
        congressional_district: Optional[float] = None,
        membership_type_code: Optional[str] = None,
        membership_type_category: Optional[str] = None,
        city: Optional[str] = None,
        name: Optional[str] = None,
        tag: Optional[str] = None,
        latitude: Optional[float] = None,
        longitude: Optional[float] = None,
        domain: Optional[str] = None,
        include_membership: Optional[bool] = None,
        include_address: Optional[bool] = None,
        include_phone: Optional[bool] = None,
        include_email: Optional[bool] = None,
        include_custom_fields: Optional[bool] = None,
        expiring_from: Optional[str] = None,
        expiring_to: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List of All Organization Members

        List of All Organization Members
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/Members"
            f"/All"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if zip_code is not None:
            value = str(zip_code)
            if isinstance(zip_code, bool):
                value = value.lower()
            query_params.append(f"zipCode={quote(value)}")
        if radius is not None:
            value = str(radius)
            if isinstance(radius, bool):
                value = value.lower()
            query_params.append(f"Radius={quote(value)}")
        if state_abbreviation is not None:
            value = str(state_abbreviation)
            if isinstance(state_abbreviation, bool):
                value = value.lower()
            query_params.append(f"stateAbbreviation={quote(value)}")
        if congressional_district is not None:
            value = str(congressional_district)
            if isinstance(congressional_district, bool):
                value = value.lower()
            query_params.append(f"congressionalDistrict={quote(value)}")
        if membership_type_code is not None:
            value = str(membership_type_code)
            if isinstance(membership_type_code, bool):
                value = value.lower()
            query_params.append(f"membershipTypeCode={quote(value)}")
        if membership_type_category is not None:
            value = str(membership_type_category)
            if isinstance(membership_type_category, bool):
                value = value.lower()
            query_params.append(f"membershipTypeCategory={quote(value)}")
        if city is not None:
            value = str(city)
            if isinstance(city, bool):
                value = value.lower()
            query_params.append(f"City={quote(value)}")
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"Name={quote(value)}")
        if tag is not None:
            value = str(tag)
            if isinstance(tag, bool):
                value = value.lower()
            query_params.append(f"Tag={quote(value)}")
        if latitude is not None:
            value = str(latitude)
            if isinstance(latitude, bool):
                value = value.lower()
            query_params.append(f"Latitude={quote(value)}")
        if longitude is not None:
            value = str(longitude)
            if isinstance(longitude, bool):
                value = value.lower()
            query_params.append(f"Longitude={quote(value)}")
        if domain is not None:
            value = str(domain)
            if isinstance(domain, bool):
                value = value.lower()
            query_params.append(f"Domain={quote(value)}")
        if include_membership is not None:
            value = str(include_membership)
            if isinstance(include_membership, bool):
                value = value.lower()
            query_params.append(f"includeMembership={quote(value)}")
        if include_address is not None:
            value = str(include_address)
            if isinstance(include_address, bool):
                value = value.lower()
            query_params.append(f"includeAddress={quote(value)}")
        if include_phone is not None:
            value = str(include_phone)
            if isinstance(include_phone, bool):
                value = value.lower()
            query_params.append(f"includePhone={quote(value)}")
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if include_custom_fields is not None:
            value = str(include_custom_fields)
            if isinstance(include_custom_fields, bool):
                value = value.lower()
            query_params.append(f"includeCustomFields={quote(value)}")
        if expiring_from is not None:
            value = str(expiring_from)
            if isinstance(expiring_from, bool):
                value = value.lower()
            query_params.append(f"expiringFrom={quote(value)}")
        if expiring_to is not None:
            value = str(expiring_to)
            if isinstance(expiring_to, bool):
                value = value.lower()
            query_params.append(f"expiringTo={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_of_all_individual_members_async(
        self,
        page_number: int,
        zip_code: Optional[str] = None,
        radius: Optional[float] = None,
        membership_type_code: Optional[str] = None,
        membership_type_category: Optional[str] = None,
        tag: Optional[str] = None,
        include_membership: Optional[bool] = None,
        include_address: Optional[bool] = None,
        include_phone: Optional[bool] = None,
        include_email: Optional[bool] = None,
        include_link: Optional[bool] = None,
        include_custom_fields: Optional[bool] = None,
        include_categories: Optional[bool] = None,
        include_membership_renewal_url: Optional[bool] = None,
        expiring_from: Optional[str] = None,
        expiring_to: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List of All Individual Members

        List of All Individual Members
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Members"
            f"/All"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if zip_code is not None:
            value = str(zip_code)
            if isinstance(zip_code, bool):
                value = value.lower()
            query_params.append(f"zipCode={quote(value)}")
        if radius is not None:
            value = str(radius)
            if isinstance(radius, bool):
                value = value.lower()
            query_params.append(f"Radius={quote(value)}")
        if membership_type_code is not None:
            value = str(membership_type_code)
            if isinstance(membership_type_code, bool):
                value = value.lower()
            query_params.append(f"membershipTypeCode={quote(value)}")
        if membership_type_category is not None:
            value = str(membership_type_category)
            if isinstance(membership_type_category, bool):
                value = value.lower()
            query_params.append(f"membershipTypeCategory={quote(value)}")
        if tag is not None:
            value = str(tag)
            if isinstance(tag, bool):
                value = value.lower()
            query_params.append(f"Tag={quote(value)}")
        if include_membership is not None:
            value = str(include_membership)
            if isinstance(include_membership, bool):
                value = value.lower()
            query_params.append(f"includeMembership={quote(value)}")
        if include_address is not None:
            value = str(include_address)
            if isinstance(include_address, bool):
                value = value.lower()
            query_params.append(f"includeAddress={quote(value)}")
        if include_phone is not None:
            value = str(include_phone)
            if isinstance(include_phone, bool):
                value = value.lower()
            query_params.append(f"includePhone={quote(value)}")
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if include_link is not None:
            value = str(include_link)
            if isinstance(include_link, bool):
                value = value.lower()
            query_params.append(f"includeLink={quote(value)}")
        if include_custom_fields is not None:
            value = str(include_custom_fields)
            if isinstance(include_custom_fields, bool):
                value = value.lower()
            query_params.append(f"includeCustomFields={quote(value)}")
        if include_categories is not None:
            value = str(include_categories)
            if isinstance(include_categories, bool):
                value = value.lower()
            query_params.append(f"includeCategories={quote(value)}")
        if include_membership_renewal_url is not None:
            value = str(include_membership_renewal_url)
            if isinstance(include_membership_renewal_url, bool):
                value = value.lower()
            query_params.append(f"includeMembershipRenewalUrl={quote(value)}")
        if expiring_from is not None:
            value = str(expiring_from)
            if isinstance(expiring_from, bool):
                value = value.lower()
            query_params.append(f"expiringFrom={quote(value)}")
        if expiring_to is not None:
            value = str(expiring_to)
            if isinstance(expiring_to, bool):
                value = value.lower()
            query_params.append(f"expiringTo={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_list_of_active_certifications_for_an_organization_async(
        self,
        id_or_record_number: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get List of Active Certifications for an Organization

        Get List of Active Certifications for an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Certifications"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_list_of_active_certifications_for_an_individual_async(
        self,
        id_or_record_number: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get List of Active Certifications for an Individual

        Get List of Active Certifications for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Certifications"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_individual_inactive_memberships_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Individual Inactive Memberships

        Get Individual Inactive Memberships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id), safe='')}"
            f"/Memberships"
            f"/Inactive"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_activity_to_organization_async(
        self,
        input: ActivityData,
        id: str,
    ) -> None:
        """
        Add Activity to Organization

        Add Activity to Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Activities"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def register_an_individual_for_a_free_session_async(
        self,
        input: RegisterAnIndividualForAFreeSessionInput,
        event_code: str,
        customer_id_or_record_number: str,
        registration_number: Optional[str] = None,
    ) -> None:
        """
        Register an Individual for a Free Session

        Register an Individual for a Free Session
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Events"
            f"/{quote(str(event_code), safe='')}"
            f"/Sessions"
            f"/Register"
            f"/{quote(str(customer_id_or_record_number), safe='')}"
        )
        query_params = []
        if registration_number is not None:
            value = str(registration_number)
            if isinstance(registration_number, bool):
                value = value.lower()
            query_params.append(f"registrationNumber={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_a_list_of_licenses_async(
        self,
        id: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get a List of Licenses

        Get a List of Licenses
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id), safe='')}"
            f"/Licenses"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_awards_async(
        self,
        page_number: int,
        year: Optional[int] = None,
    ) -> dict[str, Any] | None:
        """
        List All Awards

        List All Awards
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Awards/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if year is not None:
            value = str(year)
            if isinstance(year, bool):
                value = value.lower()
            query_params.append(f"Year={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_members_or_individuals_by_name_async(
        self,
        name: str,
        page_number: int,
        include_email: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Find Members (Individuals) by Name

        Find Members (Individuals) by Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Members"
            f"/FindByName"
            f"/{quote(str(name), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_a_list_of_all_services_of_an_organization_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get a List of All Services of an Organization

        Get a List of All Services of an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Services"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_a_service_to_an_organization_async(
        self,
        input: ServiceData,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Add a Service to an Organization

        Add a Service to an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Services"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_phone_for_an_individual_async(
        self,
        input: PhoneSaveData,
        id_or_record_number: str,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Update Phone for an Individual

        Update Phone for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Phones"
            f"/{quote(str(id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_phone_for_an_organization_async(
        self,
        input: PhoneSaveData,
        id_or_record_number: str,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Update Phone for an Organization

        Update Phone for an Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Phones"
            f"/{quote(str(id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_an_individual_web_link_async(
        self,
        input: DeleteAnIndividualWebLinkInput,
        id_or_record_number: str,
    ) -> None:
        """
        Delete an Individual Web Link

        Delete an Individual Web Link
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Links"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def add_web_link_for_individual_async(
        self,
        input: AddWebLinkForIndividualInput,
        id_or_record_number: str,
    ) -> None:
        """
        Add Web Link for Individual

        Add Web Link for Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Links"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_phone_to_organization_async(
        self,
        input: PhoneSaveData,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add Phone to Organization

        Add Phone to Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Phones"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_nominees_by_committee_async(
        self,
        code: str,
        page_number: int,
        term: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Nominees by Committee

        Get Nominees by Committee
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Committees"
            f"/{quote(str(code), safe='')}"
            f"/Nominations"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if term is not None:
            value = str(term)
            if isinstance(term, bool):
                value = value.lower()
            query_params.append(f"Term={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organizations_active_subscriptions_async(
        self,
        id: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get Organization's Active Subscriptions

        Get Organization's Active Subscriptions
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id), safe='')}"
            f"/Subscriptions"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_an_organization_web_link_async(
        self,
        input: DeleteAnOrganizationWebLinkInput,
        id_or_record_number: str,
    ) -> None:
        """
        Delete an Organization Web Link

        Delete an Organization Web Link
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Links"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def add_web_link_for_organization_async(
        self,
        input: AddWebLinkForOrganizationInput,
        id_or_record_number: str,
    ) -> None:
        """
        Add Web Link for Organization

        Add Web Link for Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Links"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_email_to_organization_async(
        self,
        input: EmailData,
        id: str,
    ) -> None:
        """
        Add Email to Organization

        Add Email to Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Emails"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_organizations_relationships_async(
        self,
        id: str,
        page_number: int,
        relationship_name: Optional[str] = None,
        includes_details: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get Organization's Relationships

        Get Organization's Relationships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id), safe='')}"
            f"/Relationships"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if relationship_name is not None:
            value = str(relationship_name)
            if isinstance(relationship_name, bool):
                value = value.lower()
            query_params.append(f"relationshipName.={quote(value)}")
        if includes_details is not None:
            value = str(includes_details)
            if isinstance(includes_details, bool):
                value = value.lower()
            query_params.append(f"includesDetails={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_or_update_address_to_organization_async(
        self,
        input: AddressSaveData,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add or Update Address to Organization

        Add or Update Address to Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Addresses"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_or_update_a_list_of_custom_fields_per_individual_async(
        self,
        input: AddOrUpdateAListOfCustomFieldsPerIndividualInput,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add or Update a List of Custom Fields Per Individual

        Add or Update a List of Custom Fields Per Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/CustomFieldsList"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_upcoming_events_async(
        self,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get Upcoming Events

        Get Upcoming Events
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Events/Upcoming/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_phone_to_individual_async(
        self,
        input: PhoneSaveData,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add Phone to Individual

        Add Phone to Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Phones"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_committee_information_for_an_individual_async(
        self,
        id: str,
        page_number: int,
        include_inactive: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get Committee Information for an Individual

        Get Committee Information for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id), safe='')}"
            f"/Committees"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_inactive is not None:
            value = str(include_inactive)
            if isinstance(include_inactive, bool):
                value = value.lower()
            query_params.append(f"includeInactive={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organization_custom_field_values_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Organization Custom Field Values

        Get Organization Custom Field Values
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/CustomFields"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_organization_active_memberships_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Organization Active Memberships

        Get Organization Active Memberships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/{quote(str(id), safe='')}"
            f"/Memberships"
            f"/Active"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_all_events_async(
        self,
        page_number: int,
        code: Optional[str] = None,
        name: Optional[str] = None,
        tag: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get All Events

        Get All Events
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Events/All/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if code is not None:
            value = str(code)
            if isinstance(code, bool):
                value = value.lower()
            query_params.append(f"Code={quote(value)}")
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"Name={quote(value)}")
        if tag is not None:
            value = str(tag)
            if isinstance(tag, bool):
                value = value.lower()
            query_params.append(f"Tag={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_individual_active_memberships_async(
        self,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get Individual Active Memberships

        Get Individual Active Memberships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/Memberships/Active"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_all_event_registrations_information_for_an_individual_async(
        self,
        id_or_record_number: str,
        page_number: int,
        event_code: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get All Event Registrations Information for an Individual

        Get All Event Registrations Information for an Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Registrations"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if event_code is not None:
            value = str(event_code)
            if isinstance(event_code, bool):
                value = value.lower()
            query_params.append(f"eventCode={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_individuals_relationships_async(
        self,
        id: str,
        page_number: int,
        relationship_name: Optional[str] = None,
        include_details: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get Individual's Relationships

        Get Individual's Relationships
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id), safe='')}"
            f"/Relationships"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if relationship_name is not None:
            value = str(relationship_name)
            if isinstance(relationship_name, bool):
                value = value.lower()
            query_params.append(f"relationshipName={quote(value)}")
        if include_details is not None:
            value = str(include_details)
            if isinstance(include_details, bool):
                value = value.lower()
            query_params.append(f"includeDetails={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_an_individual_email_async(
        self,
        input: UpdateAnIndividualEmailInput,
        id_or_record_number: str,
        current_email_address: str,
    ) -> None:
        """
        Update an Individual Email

        Update an Individual Email
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Emails"
            f"/{quote(str(current_email_address), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def save_relationship_for_organization_async(
        self,
        input: SaveRelationshipForOrganizationInput,
        id: str,
    ) -> None:
        """
        Save Relationship for Organization

        Save Relationship for Organization
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Relationships"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def add_individual_async(
        self,
        input: AddIndividualInput,
        create_user: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Add Individual

        Adds an Individual.
        """
        request_url = f"{self._connection_runtime_url}/api/v1/Individuals"
        query_params = []
        if create_user is not None:
            value = str(create_user)
            if isinstance(create_user, bool):
                value = value.lower()
            query_params.append(f"createUser={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_email_to_individual_async(
        self,
        input: AddEmailToIndividualInput,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add Email to Individual

        Add Email to Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Emails"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_or_update_address_to_individual_async(
        self,
        input: AddressSaveData,
        id_or_record_number: str,
    ) -> dict[str, Any] | None:
        """
        Add or Update Address to Individual

        Add or Update Address to Individual
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/Addresses"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_members_or_individuals_by_first_and_last_name_async(
        self,
        first_name: str,
        last_name: str,
        page_number: int,
        include_email: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Find Members (Individuals) by First Name and Last Name

        Find Members (Individuals) by First Name and Last Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Members"
            f"/FindByName"
            f"/{quote(str(first_name), safe='')}"
            f"/{quote(str(last_name), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if include_email is not None:
            value = str(include_email)
            if isinstance(include_email, bool):
                value = value.lower()
            query_params.append(f"includeEmail={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_committee_members_by_committee_id_or_code_async(
        self,
        id_or_code: str,
        page_number: int,
        term: Optional[int] = None,
        position_codes: Optional[str] = None,
    ) -> None:
        """
        Get Committee Members by committee ID or code

        Get Committee Members by Committee ID or Code
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Committees"
            f"/{quote(str(id_or_code), safe='')}"
            f"/Members"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if term is not None:
            value = str(term)
            if isinstance(term, bool):
                value = value.lower()
            query_params.append(f"Term={quote(value)}")
        if position_codes is not None:
            value = str(position_codes)
            if isinstance(position_codes, bool):
                value = value.lower()
            query_params.append(f"positionCodes={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def add_activity_async(
        self,
        input: AddActivityInput,
        id: str,
    ) -> None:
        """
        Add Activity

        Add Activity.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/Activities"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def find_individual_id_or_email_async(
        self,
        id_or_record_number_or_email: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Find Individual by Id or Email or Record Number

        Find Individual by Id or Email.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/Profile"
            f"/{quote(str(id_or_record_number_or_email), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("IncludeDetails=" + quote("true"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_relationship_to_individual_async(
        self,
        input: AddRelationshipToIndividualInput,
        id: str,
    ) -> None:
        """
        Add Relationship to Individual

        Add Relationship to Individual.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/Relationships"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def individual_add_education_credit_async(
        self,
        input: EducationCreditData,
        id_or_record_number: str,
    ) -> None:
        """
        Add Education Credits to Individual

        Add a new Education Credit to an Individual.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id_or_record_number), safe='')}"
            f"/EducationCredits"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def individual_add_note_async(
        self,
        input: NoteData,
        id: str,
    ) -> None:
        """
        Add Note to Individual

        Add a Note to an Individual.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(id), safe='')}/Notes"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def individuals_lookup_by_name_async(
        self,
        page_number: int,
        name: str,
    ) -> dict[str, Any] | None:
        """
        Lookup Individuals

        Lookup Individuals by Name
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/Lookup/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeOrgAddresses=" + quote("true"))
        value = str(name)
        if isinstance(name, bool):
            value = value.lower()
        query_params.append(f"name={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_to_committee_async(
        self,
        input: CommitteeMemberCreateData,
        code: str,
    ) -> dict[str, Any] | None:
        """
        Add Individual to Committee

        Add Individual to Committee.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Committees/{quote(str(code), safe='')}/Members"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_committee_member_async(
        self,
        input: CommitteeMemberUpdateData,
        code: str,
        member_record_number: str,
        current_position_code: str,
    ) -> None:
        """
        Update Committee Member

        Update Committee Member.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Committees"
            f"/{quote(str(code), safe='')}"
            f"/Members"
            f"/{quote(str(member_record_number), safe='')}"
            f"/{quote(str(current_position_code), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def add_organization_async(
        self,
        input: AddOrganizationInput,
    ) -> dict[str, Any] | None:
        """
        Add Organization

        Add Organization.
        """
        request_url = f"{self._connection_runtime_url}/api/v1/Organizations"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_organization_async(
        self,
        input: UpdateOrganizationInput,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Update Organization

        Update Organization.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organization_get_profile_async(
        self,
        id_or_recordnumber: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Find Organization by Id or Record Number

        Get organization's profile information by ID or Record Number
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Organizations"
            f"/Profile"
            f"/{quote(str(id_or_recordnumber), safe='')}"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeDescription=" + quote("true"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organization_add_note_async(
        self,
        input: NoteData,
        id: str,
    ) -> None:
        """
        Add Note to Organization

        Add a Note to an Organization.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/{quote(str(id), safe='')}/Notes"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def organization_lookup_by_name_async(
        self,
        page_number: int,
        name: str,
    ) -> dict[str, Any] | None:
        """
        Lookup Organizations

        Lookup Organizations by Name or Acronym
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Organizations/Lookup/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeAddresses=" + quote("true"))
        value = str(name)
        if isinstance(name, bool):
            value = value.lower()
        query_params.append(f"name={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_all_committees_async(
        self,
        page_number: int,
        code: Optional[str] = None,
        name: Optional[str] = None,
        term: Optional[int] = None,
        active_only: Optional[bool] = None,
    ) -> dict[str, Any] | None:
        """
        Get All Committees

        Get All Committees
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Committees/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if code is not None:
            value = str(code)
            if isinstance(code, bool):
                value = value.lower()
            query_params.append(f"Code={quote(value)}")
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"Name={quote(value)}")
        if term is not None:
            value = str(term)
            if isinstance(term, bool):
                value = value.lower()
            query_params.append(f"Term={quote(value)}")
        if active_only is not None:
            value = str(active_only)
            if isinstance(active_only, bool):
                value = value.lower()
            query_params.append(f"activeOnly={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_positions_by_committee_async(
        self,
        code: str,
    ) -> dict[str, Any] | None:
        """
        Get Positions by Committee

        Get Positions by Committee
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Committees/{quote(str(code), safe='')}/Positions"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_sub_committees_async(
        self,
        code: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get Sub-Committees

        Get Sub-Committees
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Committees"
            f"/{quote(str(code), safe='')}"
            f"/subcommittees"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_individuals_active_subscriptions_async(
        self,
        id: str,
        page_number: int,
    ) -> dict[str, Any] | None:
        """
        Get Individual's Active Subscriptions

        Get Individual's Active Subscriptions
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Individuals"
            f"/{quote(str(id), safe='')}"
            f"/Subscriptions"
            f"/All"
            f"/{quote(str(page_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_all_individuals_async(
        self,
        page_number: int,
        name: Optional[str] = None,
        include_details: Optional[bool] = None,
        old_id: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List all Individuals

        List all Individuals
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Individuals/{quote(str(page_number), safe='')}"
        )
        query_params = []
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"Name={quote(value)}")
        if include_details is not None:
            value = str(include_details)
            if isinstance(include_details, bool):
                value = value.lower()
            query_params.append(f"includeDetails={quote(value)}")
        if old_id is not None:
            value = str(old_id)
            if isinstance(old_id, bool):
                value = value.lower()
            query_params.append(f"oldID={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def find_customer_phone_async(
        self,
        page_number: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Find Customer by Phone Number

        Find Customer by Phone Number.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Customers/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeAddress=" + quote("true"))
        query_params.append("includePhone=" + quote("true"))
        query_params.append("includeEmail=" + quote("true"))
        value = str(phone_number)
        if isinstance(phone_number, bool):
            value = value.lower()
        query_params.append(f"phoneNumber={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def mark_registrant_attended_async(
        self,
        input: MarkRegistrantAttendedInput,
        record_number: str,
    ) -> None:
        """
        Mark Registrant as Attended

        Mark the Registrant as Attended for the specified Events.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Events"
            f"/Registrants"
            f"/{quote(str(record_number), safe='')}"
            f"/Attended"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def awards_add_award_nomination_async(
        self,
        input: AwardNominationData,
        id: str,
    ) -> None:
        """
        Add Award Nomination

        Add a new Award Nomination.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/Awards/{quote(str(id), safe='')}/Nominations"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def awards_update_award_nomination_async(
        self,
        input: UpdateAwardNominationData,
        id: str,
        nominee_record_number: str,
    ) -> None:
        """
        Update Award Nomination

        Update an existing Award Nomination.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Awards"
            f"/{quote(str(id), safe='')}"
            f"/Nominations"
            f"/{quote(str(nominee_record_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def awards_get_individual_award_recipients_async(
        self,
        page_number: int,
        id: str,
    ) -> dict[str, Any] | None:
        """
        List Award Individual Recipients

        List all Individuals that are Award Recipients.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Awards"
            f"/{quote(str(id), safe='')}"
            f"/Recipients"
            f"/Individuals"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeDetails=" + quote("true"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def awards_get_organization_award_recipients_async(
        self,
        page_number: int,
        id: str,
    ) -> dict[str, Any] | None:
        """
        List Award Organization Recipients

        List all Organizations that are Award Recipients.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/Awards"
            f"/{quote(str(id), safe='')}"
            f"/Recipients"
            f"/Organizations"
            f"/{quote(str(page_number), safe='')}"
        )
        query_params = []
        query_params.append("includeDetails=" + quote("true"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "When-Individual-Created": {
        "operation_id": "When-Individual-Created",
        "path": "/{connectionId}/api/v1/Webhooks/IndividualCreated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Individual-Deleted": {
        "operation_id": "When-Individual-Deleted",
        "path": "/{connectionId}/api/v1/Webhooks/Individual.Deleted",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Individual-Request-Forgotten": {
        "operation_id": "When-Individual-Request-Forgotten",
        "path": "/{connectionId}/api/v1/Webhooks/Individual.RequestToBeForgotten",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-product-purchased": {
        "operation_id": "When-product-purchased",
        "path": "/{connectionId}/api/v1/Webhooks/ProductPurchased",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Committee-Member-Updated": {
        "operation_id": "When-Committee-Member-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/CommitteeMemberUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-purchase-cancelled": {
        "operation_id": "When-purchase-cancelled",
        "path": "/{connectionId}/api/v1/Webhooks/PurchaseCancelled",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Request-Updated": {
        "operation_id": "When-Request-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/RequestUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Email-Updated": {
        "operation_id": "When-Email-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/EmailUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Customer-Custom-Field-Value-Updated": {
        "operation_id": "When-Customer-Custom-Field-Value-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/Customer.CustomFieldValueUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Customer-Is-Merged": {
        "operation_id": "When-Customer-Is-Merged",
        "path": "/{connectionId}/api/v1/Webhooks/Customer.Merged",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Customer-Relationship-Updated": {
        "operation_id": "When-Customer-Relationship-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/Customer.RelationshipUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Customer-Phone-Updated": {
        "operation_id": "When-Customer-Phone-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/Customer.PhoneUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Customer-Address-Updated": {
        "operation_id": "When-Customer-Address-Updated",
        "path": "/{connectionId}/api/v1/Webhooks/Customer.AddressUpdated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Event-Registration-Substituted": {
        "operation_id": "When-Event-Registration-Substituted",
        "path": "/{connectionId}/api/v1/Webhooks/Event.Registration.Substituted",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Purchase-Paid": {
        "operation_id": "When-Purchase-Paid",
        "path": "/{connectionId}/api/v1/Webhooks/Purchase.Paid",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
    "When-Membership-Terminated": {
        "operation_id": "When-Membership-Terminated",
        "path": "/{connectionId}/api/v1/Webhooks/Membership.Terminated",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
}
