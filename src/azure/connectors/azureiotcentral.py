# Copyright (c) Microsoft Corporation. All rights reserved.

# AzureiotcentralClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class DeviceGroupCollection:
    """
    Response for List device groups
    """

    value: Optional[List[DeviceGroup]] = None
    """The collection of device groups."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of device groups."""


@dataclass
class DeviceGroup:
    """
    Response for Get a device group
    """

    id: Optional[str] = None
    """Unique ID of the device group."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the device group."""
    filter: Optional[str] = None
    """Query defining which devices should be in this group."""
    description: Optional[str] = None
    """Short summary of device group."""
    etag: Optional[str] = None
    """ETag used to prevent conflict in device group updates."""
    organizations: Optional[List[str]] = None
    """
    List of organizations of the device group, only one organization is
    supported today, multiple organizations will be supported soon.
    """


@dataclass
class DeviceGroupDeviceCollection:
    """
    Response for Get devices by device group ID
    """

    value: Optional[List[Device]] = None
    """The collection of devices belonging to the device group."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of devices in the group."""


@dataclass
class DeviceCloudProperties:
    """
    Response for Get device cloud properties (deprecated)
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class DeviceCommand:
    """
    Response for Execute a device command (deprecated)
    """

    id: Optional[str] = None
    """The request ID of the device command execution."""
    request: Optional[Any] = None
    """The payload for the device command."""
    response: Optional[Any] = None
    """The payload of the device command response."""
    connection_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "connectionTimeout"},
    )
    """
    Connection timeout in seconds to wait for a disconnected device to come
    online. Defaults to 0 seconds.
    """
    response_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseTimeout"},
    )
    """
    Response timeout in seconds to wait for a command completion on a device.
    Defaults to 30 seconds.
    """
    response_code: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseCode"},
    )
    """The status code of the device command response."""


@dataclass
class DeviceRelationshipCollection:
    """
    Response for List relationships
    """

    value: Optional[List[DeviceRelationshipStatic]] = None
    """The collection of device relationships."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of device relationships."""


@dataclass
class DeviceRelationshipStatic:
    """
    Response for Get device relationship
    """

    id: Optional[str] = None
    """Unique ID of the device relationship."""
    source: Optional[str] = None
    """The device ID of the source (parent) device."""
    target: Optional[str] = None
    """The device ID of the source (child) device."""


@dataclass
class JobCollection:
    """
    Response for List jobs
    """

    value: Optional[List[JobStatic]] = None
    """The collection of jobs."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of jobs."""


@dataclass
class JobStatic:
    """
    Response for Get a job
    """

    id: Optional[str] = None
    """Unique ID of the job."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the job."""
    description: Optional[str] = None
    """Detailed description of the job."""
    group: Optional[str] = None
    """The ID of the device group on which to execute the job."""
    batch: Optional[JobBatch] = None
    """The batching configuration for the job."""
    cancellation_threshold: Optional[JobCancellationThreshold] = field(
        default=None,
        metadata={"wire_name": "cancellationThreshold"},
    )
    """The cancellation threshold for the job."""
    data: Optional[List[Dict[str, Any]]] = None
    """
    Data related to the operation being performed by this job. All entries must
    be of the same type.
    """
    organizations: Optional[List[str]] = None
    """
    List of organizations of the job, only one organization is supported today,
    multiple organizations will be supported soon.
    """
    scheduled_job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "scheduledJobId"},
    )
    """Id of the scheduled job definition that created this job."""


@dataclass
class Job:
    """
    Response for Create a job
    """

    id: Optional[str] = None
    """Unique ID of the job."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the job."""
    description: Optional[str] = None
    """Detailed description of the job."""
    group: Optional[str] = None
    """The ID of the device group on which to execute the job."""
    batch: Optional[JobBatch] = None
    """The batching configuration for the job."""
    cancellation_threshold: Optional[JobCancellationThreshold] = field(
        default=None,
        metadata={"wire_name": "cancellationThreshold"},
    )
    """The cancellation threshold for the job."""
    organizations: Optional[List[str]] = None
    """
    List of organizations of the job, only one organization is supported today,
    multiple organizations will be supported soon.
    """
    scheduled_job_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "scheduledJobId"},
    )
    """Id of the scheduled job definition that created this job."""


@dataclass
class JobDeviceStatusCollection:
    """
    Response for Get device statuses
    """

    value: Optional[List[JobDeviceStatus]] = None
    """The collection of job device statuses."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of job device statuses."""


@dataclass
class OrganizationCollection:
    """
    Response for List organizations
    """

    value: Optional[List[Organization]] = None
    """The collection of organizations."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of organizations."""


@dataclass
class Organization:
    """
    Response for Get an organization
    """

    id: Optional[str] = None
    """Unique ID of the organization."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the organization."""
    parent: Optional[str] = None
    """Display name of the parent organization."""


@dataclass
class ScheduledJobCollection:
    """
    Response for List scheduled jobs
    """

    value: Optional[List[ScheduledJob]] = None
    """The collection of scheduled jobs."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of scheduled jobs."""


@dataclass
class ScheduledJob:
    """
    Response for Get a scheduled job
    """

    etag: Optional[str] = None
    """ETag used to prevent conflict in scheduled job updates."""
    id: Optional[str] = None
    """Unique ID of the scheduled job."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the scheduled job."""
    description: Optional[str] = None
    """Detailed description of the scheduled job."""
    group: Optional[str] = None
    """The ID of the device group on which to execute the job."""
    batch: Optional[JobBatch] = None
    """The batching configuration for the scheduled job."""
    cancellation_threshold: Optional[JobCancellationThreshold] = field(
        default=None,
        metadata={"wire_name": "cancellationThreshold"},
    )
    """The cancellation threshold for the scheduled job."""
    organizations: Optional[List[str]] = None
    """
    List of organizations of the job, only one organization is supported today,
    multiple organizations will be supported soon.
    """
    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""
    enabled: Optional[bool] = None
    """Whether the scheduled job is enabled."""
    completed: Optional[bool] = None
    """Whether the scheduled job has completed."""


@dataclass
class ScheduledJobStatic:
    """
    Response for Update a scheduled job
    """

    etag: Optional[str] = None
    """ETag used to prevent conflict in scheduled job updates."""
    id: Optional[str] = None
    """Unique ID of the scheduled job."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the scheduled job."""
    description: Optional[str] = None
    """Detailed description of the scheduled job."""
    group: Optional[str] = None
    """The ID of the device group on which to execute the job."""
    batch: Optional[JobBatch] = None
    """The batching configuration for the scheduled job."""
    cancellation_threshold: Optional[JobCancellationThreshold] = field(
        default=None,
        metadata={"wire_name": "cancellationThreshold"},
    )
    """The cancellation threshold for the scheduled job."""
    data: Optional[List[Dict[str, Any]]] = None
    """
    Data related to the operation being performed by this job. All entries must
    be of the same type.
    """
    organizations: Optional[List[str]] = None
    """
    List of organizations of the job, only one organization is supported today,
    multiple organizations will be supported soon.
    """
    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""
    enabled: Optional[bool] = None
    """Whether the scheduled job is enabled."""
    completed: Optional[bool] = None
    """Whether the scheduled job has completed."""


@dataclass
class ScheduledJobJobCollection:
    """
    Response for Get jobs by scheduled job ID
    """

    value: Optional[List[JobStatic]] = None
    """The collection of jobs."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of jobs."""


@dataclass
class WorkflowTrigger:
    """
    Response for When a rule is fired
    """

    id: Optional[str] = None
    """Unique ID of the workflow trigger."""
    rule: Optional[str] = None
    """Unique ID of the rule that is tied to the workflow trigger."""
    url: Optional[str] = None
    """Callback URL of the workflow Trigger."""


@dataclass
class Device:
    """
    Response for Get a device by ID
    """

    id: Optional[str] = None
    """Unique ID of the device."""
    etag: Optional[str] = None
    """ETag used to prevent conflict in device updates."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the device."""
    template: Optional[str] = None
    """The device template definition for the device."""
    simulated: Optional[bool] = None
    """Whether the device is simulated."""
    enabled: Optional[bool] = None
    """Whether the device connection to IoT Central has been enabled."""
    provisioned: Optional[bool] = None
    """Whether resources have been allocated for the device."""


@dataclass
class DeviceComponentCommand:
    """
    Response for Get component command response
    """

    id: Optional[str] = None
    """The request ID of the device command execution."""
    request: Optional[Any] = None
    """The payload for the device command."""
    response: Optional[Any] = None
    """The payload of the device command response."""
    connection_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "connectionTimeout"},
    )
    """
    Connection timeout in seconds to wait for a disconnected device to come
    online. Defaults to 0 seconds.
    """
    response_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseTimeout"},
    )
    """
    Response timeout in seconds to wait for a command completion on a device.
    Defaults to 30 seconds.
    """
    response_code: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseCode"},
    )
    """The status code of the device command response."""


@dataclass
class DeviceComponentTelemetry:
    """
    Response for Get component telemetry value
    """

    value: Optional[Any] = None
    """The last known value of this device telemetry."""
    timestamp: Optional[str] = None
    """
    String-formatted date representing the time when the telemetry value was
    sent.
    """


@dataclass
class DeviceModuleCommand:
    """
    Response for Get module command response
    """

    id: Optional[str] = None
    """The request ID of the device command execution."""
    request: Optional[Any] = None
    """The payload for the device command."""
    response: Optional[Any] = None
    """The payload of the device command response."""
    connection_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "connectionTimeout"},
    )
    """
    Connection timeout in seconds to wait for a disconnected device to come
    online. Defaults to 0 seconds.
    """
    response_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseTimeout"},
    )
    """
    Response timeout in seconds to wait for a command completion on a device.
    Defaults to 30 seconds.
    """
    response_code: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseCode"},
    )
    """The status code of the device command response."""


@dataclass
class DeviceModuleComponentCommand:
    """
    Response for Get module component command response
    """

    id: Optional[str] = None
    """The request ID of the device command execution."""
    request: Optional[Any] = None
    """The payload for the device command."""
    response: Optional[Any] = None
    """The payload of the device command response."""
    connection_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "connectionTimeout"},
    )
    """
    Connection timeout in seconds to wait for a disconnected device to come
    online. Defaults to 0 seconds.
    """
    response_timeout: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseTimeout"},
    )
    """
    Response timeout in seconds to wait for a command completion on a device.
    Defaults to 30 seconds.
    """
    response_code: Optional[int] = field(
        default=None,
        metadata={"wire_name": "responseCode"},
    )
    """The status code of the device command response."""


@dataclass
class DeviceModuleComponentTelemetry:
    """
    Response for Get module component telemetry value
    """

    value: Optional[Any] = None
    """The last known value of this device telemetry."""
    timestamp: Optional[str] = None
    """
    String-formatted date representing the time when the telemetry value was
    sent.
    """


@dataclass
class ModuleProperties:
    """
    Response for Get module properties
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class DeviceModuleTelemetry:
    """
    Response for Get module telemetry value
    """

    value: Optional[Any] = None
    """The last known value of this device telemetry."""
    timestamp: Optional[str] = None
    """
    String-formatted date representing the time when the telemetry value was
    sent.
    """


@dataclass
class DeviceProperties:
    """
    Response for Get device properties
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class DeviceTelemetry:
    """
    Response for Get device telemetry value
    """

    value: Optional[Any] = None
    """The last known value of this device telemetry."""
    timestamp: Optional[str] = None
    """
    String-formatted date representing the time when the telemetry value was
    sent.
    """


@dataclass
class DeviceCollection:
    """
    Response for List devices
    """

    value: Optional[List[Device]] = None
    """The collection of devices."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of devices."""


@dataclass
class DeviceTemplate:
    """
    Response for Get a device template by ID
    """

    id: Optional[str] = field(default=None, metadata={"wire_name": "@id"})
    """Unique ID of the device template."""
    etag: Optional[str] = None
    """ETag used to prevent conflict in device template updates."""
    type_: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "@type"},
    )
    """The JSON-LD types of this device template."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the device template."""
    description: Optional[str] = None
    """Detailed description of the device template."""
    capability_model: Optional[Any] = field(
        default=None,
        metadata={"wire_name": "capabilityModel"},
    )
    """The capability model utilized by this device template."""


@dataclass
class DeviceTemplateCollection:
    """
    Response for List device templates
    """

    value: Optional[List[DeviceTemplate]] = None
    """The collection of device templates."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of device templates."""


@dataclass
class Role:
    """
    Response for Get role
    """

    id: Optional[str] = None
    """Unique ID of the role."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the role."""


@dataclass
class RoleCollection:
    """
    Response for List roles
    """

    value: Optional[List[Role]] = None
    """The collection of roles."""


@dataclass
class User:
    """
    Response for Create user
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class UserStatic:
    """
    Response for Get user
    """

    id: Optional[str] = None
    """Unique ID of the user."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the user."""
    roles: Optional[List[RoleAssignment]] = None
    """
    List of role assignments that specify the permissions to access the
    application.
    """


@dataclass
class UserCollection:
    """
    Response for List users
    """

    value: Optional[List[UserStatic]] = None
    """The collection of users."""


@dataclass
class UserPatch:
    """
    Response for Update user
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class ApplicationCollection:
    """
    Response for Get the list of applications accessible to the signed-in user
    """

    value: Optional[List[Application]] = None
    """The collection of applications."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of applications."""


@dataclass
class DynamicValues:
    """
    Response for Workflow_GetComponents
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaDeviceCloudPropertiesResponse:
    """
    Response for Schema_DeviceCloudProperties
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaDeviceCommandResponse:
    """
    Response for Schema_DeviceCommand
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaDevicePropertiesResponse:
    """
    Response for Schema_DeviceProperties
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaDeviceTelemetryResponse:
    """
    Response for Schema_DeviceTelemetry
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaJobResponse:
    """
    Response for Schema_Job
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaScheduledJobResponse:
    """
    Response for Schema_ScheduledJob
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class SchemaUserResponse:
    """
    Response for Schema_User_V1
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class Action:
    """
    Definition: Action
    """

    id: Optional[str] = None
    """Unique ID of the action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the action."""


@dataclass
class EmailAction:
    """
    Definition: EmailAction
    """

    id: Optional[str] = None
    """Unique ID of the email action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the email action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the email action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the email action."""


@dataclass
class WebhookAction:
    """
    Definition: WebhookAction
    """

    id: Optional[str] = None
    """Unique ID of the webhook action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the webhook action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the webhook action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the webhook action."""
    url: Optional[str] = None
    """Callback URL of the webhook."""


@dataclass
class PowerAutomateAction:
    """
    Definition: PowerAutomateAction
    """

    id: Optional[str] = None
    """Unique ID of the Power Automate action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the Power Automate action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the Power Automate action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the Power Automate action."""


@dataclass
class LogicAppsAction:
    """
    Definition: LogicAppsAction
    """

    id: Optional[str] = None
    """Unique ID of the Logic Apps action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the Logic Apps action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the Logic Apps action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the Logic Apps action."""


@dataclass
class ActionGroupsAction:
    """
    Definition: ActionGroupsAction
    """

    id: Optional[str] = None
    """Unique ID of the Action Groups action."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the Action Groups action."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the Action Groups action."""
    rules: Optional[List[str]] = None
    """Unique IDs of the rules that are tied to the Action Groups action."""


@dataclass
class Application:
    """
    Definition: Application
    """

    id: Optional[str] = None
    """Unique ID of the application."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the application."""
    subdomain: Optional[str] = None
    """The URL subdomain of the application."""
    host: Optional[str] = None
    """The URL host of the application."""


@dataclass
class DeviceRelationship:
    """
    Definition: DeviceRelationship
    """

    id: Optional[str] = None
    """Unique ID of the device relationship."""
    target: Optional[str] = None
    """The device ID of the source (child) device."""


@dataclass
class PropertyJobData:
    """
    Definition: PropertyJobData
    """

    data: Optional[List[Dict[str, Any]]] = None
    """
    Data related to the operation being performed by this job. All entries must
    be of the same type.
    """


@dataclass
class CommandJobData:
    """
    Definition: CommandJobData
    """

    data: Optional[List[Dict[str, Any]]] = None
    """
    Data related to the operation being performed by this job. All entries must
    be of the same type.
    """


@dataclass
class CloudPropertyJobData:
    """
    Definition: CloudPropertyJobData
    """

    data: Optional[List[Dict[str, Any]]] = None
    """The collection of job data."""


@dataclass
class DeviceTemplateMigrationJobData:
    """
    Definition: DeviceTemplateMigrationJobData
    """

    data: Optional[List[Dict[str, Any]]] = None
    """The collection of job data."""


@dataclass
class JobDeviceStatus:
    """
    Definition: JobDeviceStatus
    """

    id: Optional[str] = None
    """ID of the device whose job status is being provided."""
    status: Optional[str] = None
    """
    Indicates whether the job is starting, running, etc. for the given device.
    """


@dataclass
class JobBatch:
    """
    Definition: JobBatch
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """
    Whether batching is done on a specified number of devices or a percentage
    of the total devices.
    """
    value: Optional[float] = None
    """The number or percentage of devices on which batching is done."""


@dataclass
class JobCancellationThreshold:
    """
    Definition: JobCancellationThreshold
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """
    Whether the cancellation threshold is per a specified number of devices or
    a percentage of the total devices.
    """
    value: Optional[float] = None
    """
    The number or percentage of devices on which the cancellation threshold is
    applied.
    """
    batch: Optional[bool] = None
    """
    Whether the cancellation threshold applies per-batch or to the overall job.
    """


@dataclass
class OrganizationStatic:
    """
    Definition: OrganizationStatic
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class Rule:
    """
    Definition: Rule
    """

    id: Optional[str] = None
    """Unique ID of the rule."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the rule."""


@dataclass
class RuleCollection:
    """
    Definition: RuleCollection
    """

    value: Optional[List[Rule]] = None
    """The collection of rules."""
    next_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nextLink"},
    )
    """URL to get the next page of rules."""


@dataclass
class JobSchedule:
    """
    Definition: JobSchedule
    """

    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""


@dataclass
class DateJobSchedule:
    """
    Definition: DateJobSchedule
    """

    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""


@dataclass
class OccurrencesJobSchedule:
    """
    Definition: OccurrencesJobSchedule
    """

    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""


@dataclass
class JobScheduleEnd:
    """
    Definition: JobScheduleEnd
    """

    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the job schedule end."""
    date: Optional[str] = None
    """The date when to end the scheduled job (YYYY-MM-DDTHH:MM:SSZ)."""
    occurrences: Optional[int] = None
    """The number of occurrences after which to end the scheduled job."""


@dataclass
class ScheduledJobPatch:
    """
    Definition: ScheduledJobPatch
    """

    etag: Optional[str] = None
    """ETag used to prevent conflict in scheduled job updates."""
    id: Optional[str] = None
    """Unique ID of the scheduled job."""
    schedule: Optional[Dict[str, Any]] = None
    """The schedule at which to execute the job."""
    enabled: Optional[bool] = None
    """Whether the scheduled job is enabled."""
    completed: Optional[bool] = None
    """Whether the scheduled job has completed."""


@dataclass
class EmailUser:
    """
    Definition: EmailUserV1
    """

    id: Optional[str] = None
    """Unique ID of the user."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the user."""
    roles: Optional[List[RoleAssignment]] = None
    """
    List of role assignments that specify the permissions to access the
    application.
    """
    email: Optional[str] = None
    """Email address of the user."""


@dataclass
class ServicePrincipalUser:
    """
    Definition: ServicePrincipalUserV1
    """

    id: Optional[str] = None
    """Unique ID of the user."""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """Type of the user."""
    roles: Optional[List[RoleAssignment]] = None
    """
    List of role assignments that specify the permissions to access the
    application.
    """
    tenant_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tenantId"},
    )
    """The Microsoft Entra ID tenant ID of the service principal."""
    object_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "objectId"},
    )
    """The Microsoft Entra ID object ID of the service principal."""


@dataclass
class RoleAssignment:
    """
    Definition: RoleAssignmentV1
    """

    role: Optional[str] = None
    """ID of the role for this role assignment."""


@dataclass
class WebhookActionDevice:
    """
    Definition: WebhookActionDevice
    """

    id: Optional[str] = None
    """Unique ID of the device."""
    etag: Optional[str] = None
    """ETag used to prevent conflict in device updates."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Display name of the device."""
    instance_of: Optional[str] = field(
        default=None,
        metadata={"wire_name": "instanceOf"},
    )
    """The device template definition for the device."""
    simulated: Optional[bool] = None
    """Whether the device is simulated."""
    approved: Optional[bool] = None
    """Whether the device has been approved to connect to IoT Central."""
    provisioned: Optional[bool] = None
    """Whether resources have been allocated for the device."""
    cloud_properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "cloudProperties"},
    )
    """Cloud properties that triggered the webhook."""
    properties: Optional[Dict[str, Any]] = None
    """Device properties that triggered the webhook."""
    telemetry: Optional[Dict[str, Any]] = None
    """Device telemetry that triggered the webhook."""


@dataclass
class WebhookActionBody:
    """
    Definition: WebhookActionBody
    """

    action: Optional[Action] = None
    """Action that triggered the webhook."""
    application: Optional[Application] = None
    """Application that triggered the webhook."""
    device: Optional[WebhookActionDevice] = None
    """Device that triggered the webhook."""
    rule: Optional[Rule] = None
    """Rule that triggered the webhook."""
    timestamp: Optional[str] = None
    """Timestamp at which the webhook was triggered."""


# Client Class

class AzureiotcentralClient(ConnectorClientBase):
    """Typed client for azureiotcentral connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a AzureiotcentralClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "azureiotcentral"

    async def device_groups_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List device groups

        Get the list of device groups in an application.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/ga_2022_07_31/deviceGroups"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_groups_get_async(
        self,
        device_group_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get a device group

        Get details about an existing device group by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/deviceGroups/{str(device_group_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_groups_set_async(
        self,
        input: DeviceGroup,
        device_group_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Create a device group

        Create or update a device group.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/deviceGroups/{str(device_group_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_groups_remove_async(
        self,
        device_group_id: str,
        application: str,
    ) -> None:
        """
        Delete device group

        Delete an existing device group by ID
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/deviceGroups/{str(device_group_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def device_groups_get_devices_async(
        self,
        device_group_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get devices by device group ID

        Get the list of devices in a device group in an application.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/deviceGroups/{str(device_group_id)}/devices"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_cloud_properties_async(
        self,
        device_id: str,
        application: str,
        instance_of: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get device cloud properties (deprecated)

        Get all cloud property values of a device by device ID (deprecated).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/devices/{str(device_id)}/cloudProperties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_update_cloud_properties_async(
        self,
        input: DeviceCloudProperties,
        device_id: str,
        application: str,
        instance_of: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update device cloud properties (deprecated)

        Update all cloud property values of a device by device ID (deprecated).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/devices/{str(device_id)}/cloudProperties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_execute_component_command_async(
        self,
        input: DeviceCommand,
        device_id: str,
        component_name: str,
        command_name: str,
        application: str,
        instance_of: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Execute a device command (deprecated)

        Execute a command on a device (deprecated).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/preview"
            f"/devices"
            f"/{str(device_id)}"
            f"/components"
            f"/{str(component_name)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_relationships_list_async(
        self,
        device_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List relationships

        List all relationships based on device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/devices/{str(device_id)}/relationships"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_relationships_get_async(
        self,
        device_id: str,
        relationship_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get device relationship

        Get details about an existing device relationship by device ID and by
        relationship ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/ga_2022_07_31"
            f"/devices"
            f"/{str(device_id)}"
            f"/relationships"
            f"/{str(relationship_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_relationships_set_async(
        self,
        input: DeviceRelationship,
        relationship_id: str,
        device_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Create a device relationship

        Create a new device relationship between the given device and a new
        device given a device ID and a relationship ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/ga_2022_07_31"
            f"/devices"
            f"/{str(device_id)}"
            f"/relationships"
            f"/{str(relationship_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_relationships_update_async(
        self,
        input: DeviceRelationship,
        device_id: str,
        relationship_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Update a device relationship

        Update an existing device relationship between the given device and a
        new device given a device ID and a relationship ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/ga_2022_07_31"
            f"/devices"
            f"/{str(device_id)}"
            f"/relationships"
            f"/{str(relationship_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_relationships_remove_async(
        self,
        device_id: str,
        relationship_id: str,
        application: str,
    ) -> None:
        """
        Delete a device relationship

        Delete an existing device relationship by device ID and relationship
        ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/ga_2022_07_31"
            f"/devices"
            f"/{str(device_id)}"
            f"/relationships"
            f"/{str(relationship_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def jobs_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List jobs

        Get the list of jobs in an application.
        """
        request_url = f"{self._connection_runtime_url}/api/ga_2022_07_31/jobs"
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def jobs_get_async(
        self,
        job_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get a job

        Get details about an existing job by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def jobs_set_async(
        self,
        input: Job,
        job_id: str,
        application: str,
        job_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Create a job

        Create and execute a new job via its job definition.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if job_type is not None:
            value = str(job_type)
            if isinstance(job_type, bool):
                value = value.lower()
            query_params.append(f"job_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def jobs_get_devices_async(
        self,
        job_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get device statuses

        Get the list of individual device statuses by job ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}/devices"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def jobs_stop_async(
        self,
        job_id: str,
        application: str,
    ) -> None:
        """
        Stop a running job

        Stop execution of a job that is currently running.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}/stop"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def jobs_resume_async(
        self,
        job_id: str,
        application: str,
    ) -> None:
        """
        Resume a stopped job

        Resume execution of an existing stopped job.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}/resume"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def jobs_rerun_async(
        self,
        job_id: str,
        rerun_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Resume a job on failed devices

        Execute a rerun of an existing job on all failed devices.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/jobs/{str(job_id)}/rerun/{str(rerun_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organizations_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List organizations

        Get the list of organizations in an application.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/ga_2022_07_31/organizations"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organizations_get_async(
        self,
        organization_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get an organization

        Get details about an existing organization by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/organizations/{str(organization_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organizations_set_async(
        self,
        input: Organization,
        organization_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Create an organization

        Create a new organization.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/organizations/{str(organization_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def organizations_remove_async(
        self,
        organization_id: str,
        application: str,
    ) -> None:
        """
        Delete organization

        Delete an existing organization by ID
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/organizations/{str(organization_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def scheduled_jobs_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List scheduled jobs

        Get the list of scheduled jobs in an application.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/ga_2022_07_31/scheduledJobs"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def scheduled_jobs_get_async(
        self,
        scheduled_job_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get a scheduled job

        Get details about an existing scheduled job by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/scheduledJobs/{str(scheduled_job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def scheduled_jobs_set_async(
        self,
        input: ScheduledJob,
        scheduled_job_id: str,
        application: str,
        job_type: Optional[str] = None,
        scheduled_job_end_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Create a scheduled job

        Create and execute a new scheduled job via its job definition.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/scheduledJobs/{str(scheduled_job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if job_type is not None:
            value = str(job_type)
            if isinstance(job_type, bool):
                value = value.lower()
            query_params.append(f"job_type={quote(value)}")
        if scheduled_job_end_type is not None:
            value = str(scheduled_job_end_type)
            if isinstance(scheduled_job_end_type, bool):
                value = value.lower()
            query_params.append(f"scheduled_job_end_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def scheduled_jobs_update_async(
        self,
        input: ScheduledJobPatch,
        scheduled_job_id: str,
        application: str,
        scheduled_job_end_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update a scheduled job

        Update an existing scheduled job by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/scheduledJobs/{str(scheduled_job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if scheduled_job_end_type is not None:
            value = str(scheduled_job_end_type)
            if isinstance(scheduled_job_end_type, bool):
                value = value.lower()
            query_params.append(f"scheduled_job_end_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def scheduled_jobs_remove_async(
        self,
        scheduled_job_id: str,
        application: str,
    ) -> None:
        """
        Delete a scheduled job

        Delete an existing scheduled job by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/scheduledJobs/{str(scheduled_job_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def scheduled_jobs_list_jobs_async(
        self,
        scheduled_job_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get jobs by scheduled job ID

        Get the list of jobs for a scheduled job definition.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/scheduledJobs/{str(scheduled_job_id)}/jobs"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_async(
        self,
        device_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get a device by ID

        Get details about an existing device by device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/devices/{str(device_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_command_response_async(
        self,
        device_id: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get device command response

        Get the last response for a device command.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/devices/{str(device_id)}/commands/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_component_command_response_async(
        self,
        device_id: str,
        component_name: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get component command response

        Get the last response for a device component command.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/components"
            f"/{str(component_name)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_component_telemetry_value_async(
        self,
        device_id: str,
        component_name: str,
        telemetry_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get component telemetry value

        Get the last telemetry value from a component.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/components"
            f"/{str(component_name)}"
            f"/telemetry"
            f"/{str(telemetry_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_module_command_response_async(
        self,
        device_id: str,
        module: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get module command response

        Get the last response for a device module command
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_module_component_command_response_async(
        self,
        device_id: str,
        module: str,
        component_name: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get module component command response

        Get the last response for a module component command.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/components"
            f"/{str(component_name)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_module_component_telemetry_value_async(
        self,
        device_id: str,
        module: str,
        component_name: str,
        telemetry_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get module component telemetry value

        Get the last telemetry value from a module component.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/components"
            f"/{str(component_name)}"
            f"/telemetry"
            f"/{str(telemetry_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_module_properties_async(
        self,
        device_id: str,
        module: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get module properties

        Get all property values of a module.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/properties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_module_telemetry_value_async(
        self,
        device_id: str,
        module: str,
        telemetry_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get module telemetry value

        Get the last telemetry value from a module.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/telemetry"
            f"/{str(telemetry_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_properties_async(
        self,
        device_id: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get device properties

        Get all property values of a device by device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/devices/{str(device_id)}/properties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_get_telemetry_value_async(
        self,
        device_id: str,
        telemetry_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get device telemetry value

        Get the last telemetry value from a device.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/devices/{str(device_id)}/telemetry/{str(telemetry_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List devices

        Get the list of devices in an application.
        """
        request_url = f"{self._connection_runtime_url}/api/v1/devices"
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_remove_async(
        self,
        device_id: str,
        application: str,
    ) -> None:
        """
        Delete a device

        Delete an existing device by device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/devices/{str(device_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def devices_run_command_async(
        self,
        input: DeviceCommand,
        device_id: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Run a device command

        Run a command on a device.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/devices/{str(device_id)}/commands/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_run_component_command_async(
        self,
        input: DeviceComponentCommand,
        device_id: str,
        component_name: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Run a component command

        Run a command on a component.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/components"
            f"/{str(component_name)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_run_module_command_async(
        self,
        input: DeviceModuleCommand,
        device_id: str,
        module: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Run a module command

        Run a command on a module.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_run_module_component_command_async(
        self,
        input: DeviceModuleComponentCommand,
        device_id: str,
        module: str,
        component_name: str,
        command_name: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Run a module component command

        Run a command on a module component.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/components"
            f"/{str(component_name)}"
            f"/commands"
            f"/{str(command_name)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_set_async(
        self,
        input: Device,
        device_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a device

        Create a new device or update an existing one by device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/devices/{str(device_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_update_module_properties_async(
        self,
        input: ModuleProperties,
        device_id: str,
        module: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update module properties

        Update all property values of a module.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api"
            f"/v1"
            f"/devices"
            f"/{str(device_id)}"
            f"/modules"
            f"/{str(module)}"
            f"/properties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def devices_update_properties_async(
        self,
        input: DeviceProperties,
        device_id: str,
        application: str,
        template: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update device properties

        Update all property values of a device by device ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/devices/{str(device_id)}/properties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_templates_get_async(
        self,
        template_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get a device template by ID

        Get details about an existing device template by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/deviceTemplates/{str(template_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_templates_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List device templates

        Get the list of device templates in an application.
        """
        request_url = f"{self._connection_runtime_url}/api/v1/deviceTemplates"
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def device_templates_remove_async(
        self,
        template_id: str,
        application: str,
    ) -> None:
        """
        Delete a device template

        Delete an existing device template by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/deviceTemplates/{str(template_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def roles_get_async(
        self,
        role_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get role

        Get a role by ID.
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/roles/{str(role_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def roles_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List roles

        Get the list of roles in an application.
        """
        request_url = f"{self._connection_runtime_url}/api/v1/roles"
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def users_create_async(
        self,
        input: User,
        user_id: str,
        application: str,
        user_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Create user

        Create a user in the application
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/users/{str(user_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if user_type is not None:
            value = str(user_type)
            if isinstance(user_type, bool):
                value = value.lower()
            query_params.append(f"user_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def users_get_async(
        self,
        user_id: str,
        application: str,
    ) -> dict[str, Any] | None:
        """
        Get user

        Get a user by ID
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/users/{str(user_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def users_list_async(
        self,
        application: str,
    ) -> dict[str, Any] | None:
        """
        List users

        Get the list of users in an application
        """
        request_url = f"{self._connection_runtime_url}/api/v1/users"
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def users_remove_async(
        self,
        user_id: str,
        application: str,
    ) -> None:
        """
        Delete user

        Delete a user
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/users/{str(user_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def users_update_async(
        self,
        input: UserPatch,
        user_id: str,
        application: str,
        user_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update user

        Update a user in the application via patch
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/users/{str(user_id)}"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if user_type is not None:
            value = str(user_type)
            if isinstance(user_type, bool):
                value = value.lower()
            query_params.append(f"user_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def applications_list_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get the list of applications accessible to the signed-in user
        """
        request_url = (
            f"{self._connection_runtime_url}/api/preview/applications"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def workflow_get_components_async(
        self,
        application: str,
        template: str,
        module: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Workflow_GetComponents
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/components"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        value = str(template)
        if isinstance(template, bool):
            value = value.lower()
        query_params.append(f"template={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def workflow_get_capabilities_async(
        self,
        application: str,
        template: str,
        component: Optional[str] = None,
        module: Optional[str] = None,
        type_: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Workflow_GetCapabilities
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/capabilities"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        value = str(template)
        if isinstance(template, bool):
            value = value.lower()
        query_params.append(f"template={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if type_ is not None:
            value = str(type_)
            if isinstance(type_, bool):
                value = value.lower()
            query_params.append(f"type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def workflow_get_capabilities_v1_async(
        self,
        application: str,
        template: str,
        component: Optional[str] = None,
        module: Optional[str] = None,
        type_: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Workflow_GetCapabilities_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/capabilities"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        value = str(template)
        if isinstance(template, bool):
            value = value.lower()
        query_params.append(f"template={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if type_ is not None:
            value = str(type_)
            if isinstance(type_, bool):
                value = value.lower()
            query_params.append(f"type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def workflow_get_components_v1_async(
        self,
        application: str,
        template: str,
        module: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Workflow_GetComponents_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/components"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        value = str(template)
        if isinstance(template, bool):
            value = value.lower()
        query_params.append(f"template={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def workflow_get_modules_async(
        self,
        application: str,
        template: str,
    ) -> dict[str, Any] | None:
        """
        Workflow_GetModules_V1
        """
        request_url = (
            f"{self._connection_runtime_url}/api/v1/_internal/workflow/modules"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        value = str(template)
        if isinstance(template, bool):
            value = value.lower()
        query_params.append(f"template={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_cloud_properties_async(
        self,
        application: str,
        instance_of: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceCloudProperties
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/schema/DeviceCloudProperties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_command_async(
        self,
        application: str,
        instance_of: Optional[str] = None,
        component: Optional[str] = None,
        capability: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceCommand
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/schema/DeviceCommand"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if capability is not None:
            value = str(capability)
            if isinstance(capability, bool):
                value = value.lower()
            query_params.append(f"capability={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_command_v1_async(
        self,
        application: str,
        template: Optional[str] = None,
        module: Optional[str] = None,
        component: Optional[str] = None,
        capability: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceCommand_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/schema/DeviceCommand"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if capability is not None:
            value = str(capability)
            if isinstance(capability, bool):
                value = value.lower()
            query_params.append(f"capability={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_properties_async(
        self,
        application: str,
        instance_of: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceProperties
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/schema/DeviceProperties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_properties_v1_async(
        self,
        application: str,
        template: Optional[str] = None,
        module: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceProperties_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/schema/DeviceProperties"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_telemetry_async(
        self,
        application: str,
        instance_of: Optional[str] = None,
        component: Optional[str] = None,
        capability: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceTelemetry
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/schema/DeviceTelemetry"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if instance_of is not None:
            value = str(instance_of)
            if isinstance(instance_of, bool):
                value = value.lower()
            query_params.append(f"instanceOf={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if capability is not None:
            value = str(capability)
            if isinstance(capability, bool):
                value = value.lower()
            query_params.append(f"capability={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_device_telemetry_v1_async(
        self,
        application: str,
        template: Optional[str] = None,
        module: Optional[str] = None,
        component: Optional[str] = None,
        capability: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_DeviceTelemetry_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/schema/DeviceTelemetry"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if template is not None:
            value = str(template)
            if isinstance(template, bool):
                value = value.lower()
            query_params.append(f"template={quote(value)}")
        if module is not None:
            value = str(module)
            if isinstance(module, bool):
                value = value.lower()
            query_params.append(f"module={quote(value)}")
        if component is not None:
            value = str(component)
            if isinstance(component, bool):
                value = value.lower()
            query_params.append(f"component={quote(value)}")
        if capability is not None:
            value = str(capability)
            if isinstance(capability, bool):
                value = value.lower()
            query_params.append(f"capability={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_job_async(
        self,
        application: str,
        job_type: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_Job
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/_internal/workflow/schema/Job"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if job_type is not None:
            value = str(job_type)
            if isinstance(job_type, bool):
                value = value.lower()
            query_params.append(f"job_type={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_scheduled_job_async(
        self,
        application: str,
        scheduled_job_end_type: Optional[str] = None,
        job_type: Optional[str] = None,
        patch: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_ScheduledJob
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/ga_2022_07_31/_internal/workflow/schema/ScheduledJob"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if scheduled_job_end_type is not None:
            value = str(scheduled_job_end_type)
            if isinstance(scheduled_job_end_type, bool):
                value = value.lower()
            query_params.append(f"scheduled_job_end_type={quote(value)}")
        if job_type is not None:
            value = str(job_type)
            if isinstance(job_type, bool):
                value = value.lower()
            query_params.append(f"job_type={quote(value)}")
        if patch is not None:
            value = str(patch)
            if isinstance(patch, bool):
                value = value.lower()
            query_params.append(f"patch={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_user_async(
        self,
        application: str,
        user_type: Optional[str] = None,
        patch: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Schema_User_V1
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/v1/_internal/workflow/schema/User"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if user_type is not None:
            value = str(user_type)
            if isinstance(user_type, bool):
                value = value.lower()
            query_params.append(f"user_type={quote(value)}")
        if patch is not None:
            value = str(patch)
            if isinstance(patch, bool):
                value = value.lower()
            query_params.append(f"patch={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def schema_webhook_action_body_async(
        self,
        application: str,
        rule: Optional[str] = None,
    ) -> None:
        """
        Schema_WebhookActionBody
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/api/preview/_internal/workflow/schema/WebhookActionBody"
        )
        query_params = []
        value = str(application)
        if isinstance(application, bool):
            value = value.lower()
        query_params.append(f"application={quote(value)}")
        if rule is not None:
            value = str(rule)
            if isinstance(rule, bool):
                value = value.lower()
            query_params.append(f"rule={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "Workflow_CreateTrigger": {
        "operation_id": "Workflow_CreateTrigger",
        "path": "/{connectionId}/api/preview/_internal/workflow/triggers",
        "method": "post",
        "required_parameters": ["application", "body"],
        "callback_payload_type": "WorkflowTrigger",
    },
}
