# Copyright (c) Microsoft Corporation. All rights reserved.

# DynamicsaxClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class BusinessEventSubscriptionResponse:
    """
    Response for When a Business Event occurs
    """

    id: Optional[str] = None
    """Id of the subscription"""
    resource: Optional[str] = None
    """Resource of the subscription request"""
    notification_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "notificationType"},
    )
    """Notification Type"""
    notification_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "notificationUrl"},
    )
    """Notification Url"""


@dataclass
class ExecuteProcedureInput:
    """
    Execute action
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class AxOnlineProcedureResult:
    """
    Response for Execute action
    """

    value: Optional[str] = None
    """Output parameter values"""
    output_parameters: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "OutputParameters"},
    )
    """Output parameter values"""


@dataclass
class ItemsList:
    """
    Response for Lists items present in table
    """

    value: Optional[List[Item]] = None
    """List of Items"""


@dataclass
class PostItemInput:
    """
    Create record
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class PostItemResponse:
    """
    Response for Create record
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetItemResponse:
    """
    Response for Get a record
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class PatchItemInput:
    """
    Update a record
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class PatchItemResponse:
    """
    Response for Update a record
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class TablesList:
    """
    Response for Get list of entities
    """

    value: Optional[List[Table]] = None
    """List of Tables"""


@dataclass
class DataSetsList:
    """
    Response for Get instances
    """

    value: Optional[List[DataSet]] = None
    """List of datasets"""


@dataclass
class TriggerFieldDataList:
    """
    Response for Gets business events categories
    """

    value: Optional[List[TriggerFieldData]] = None
    """List of business event categories"""


@dataclass
class BusinessEventsList:
    """
    Response for Gets business events
    """

    value: Optional[List[BusinessEventData]] = None
    """List of business events"""


@dataclass
class ProceduresList:
    """
    Response for Get actions
    """

    value: Optional[List[Procedure]] = None
    """List of Procedures"""


@dataclass
class ProcedureMetadata:
    """
    Response for Get actions metadata
    """

    name: Optional[str] = None
    """Procedure name"""
    title: Optional[str] = None
    """Procedure title"""
    schema: Optional[ObjectEntity] = None


@dataclass
class TableMetadata:
    """
    Response for Get entity metadata
    """

    name: Optional[str] = None
    """Table name"""
    title: Optional[str] = None
    """Table title"""
    x_ms_permission: Optional[str] = field(
        default=None,
        metadata={"wire_name": "x-ms-permission"},
    )
    """Table permission"""
    x_ms_capabilities: Optional[TableCapabilitiesMetadata] = field(
        default=None,
        metadata={"wire_name": "x-ms-capabilities"},
    )
    schema: Optional[ObjectEntity] = None
    referenced_entities: Optional[ObjectEntity] = field(
        default=None,
        metadata={"wire_name": "referencedEntities"},
    )
    web_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "webUrl"},
    )
    """Url link"""


@dataclass
class ObjectEntity:
    """
    Definition: Object
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class DataSetsMetadata:
    """
    Definition: DataSetsMetadata
    """

    tabular: Optional[TabularDataSetsMetadata] = None
    blob: Optional[BlobDataSetsMetadata] = None


@dataclass
class TabularDataSetsMetadata:
    """
    Definition: TabularDataSetsMetadata
    """

    source: Optional[str] = None
    """Dataset source"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Dataset display name"""
    url_encoding: Optional[str] = field(
        default=None,
        metadata={"wire_name": "urlEncoding"},
    )
    """Dataset url encoding"""
    table_display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tableDisplayName"},
    )
    """Table display name"""
    table_plural_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "tablePluralName"},
    )
    """Table plural display name"""


@dataclass
class BlobDataSetsMetadata:
    """
    Definition: BlobDataSetsMetadata
    """

    source: Optional[str] = None
    """Blob dataset source"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """Blob dataset display name"""
    url_encoding: Optional[str] = field(
        default=None,
        metadata={"wire_name": "urlEncoding"},
    )
    """Blob dataset url encoding"""


@dataclass
class TableCapabilitiesMetadata:
    """
    Definition: TableCapabilitiesMetadata
    """

    sort_restrictions: Optional[TableSortRestrictionsMetadata] = field(
        default=None,
        metadata={"wire_name": "sortRestrictions"},
    )
    filter_restrictions: Optional[TableFilterRestrictionsMetadata] = field(
        default=None,
        metadata={"wire_name": "filterRestrictions"},
    )
    select_restrictions: Optional[TableSelectRestrictionsMetadata] = field(
        default=None,
        metadata={"wire_name": "selectRestrictions"},
    )
    is_only_server_pagable: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isOnlyServerPagable"},
    )
    """Server paging restrictions"""
    filter_function_support: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "filterFunctionSupport"},
    )
    """List of supported filter capabilities"""
    server_paging_options: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "serverPagingOptions"},
    )
    """List of supported server-driven paging capabilities"""


@dataclass
class TableSortRestrictionsMetadata:
    """
    Definition: TableSortRestrictionsMetadata
    """

    sortable: Optional[bool] = None
    """Indicates whether this table has sortable columns"""
    unsortable_properties: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "unsortableProperties"},
    )
    """List of unsortable properties"""
    ascending_only_properties: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "ascendingOnlyProperties"},
    )
    """List of properties which support ascending order only"""


@dataclass
class TableFilterRestrictionsMetadata:
    """
    Definition: TableFilterRestrictionsMetadata
    """

    filterable: Optional[bool] = None
    """Indicates whether this table has filterable columns"""
    non_filterable_properties: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "nonFilterableProperties"},
    )
    """List of non filterable properties"""
    required_properties: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "requiredProperties"},
    )
    """List of required properties"""


@dataclass
class TableSelectRestrictionsMetadata:
    """
    Definition: TableSelectRestrictionsMetadata
    """

    selectable: Optional[bool] = None
    """Indicates whether this table has selectable columns"""


@dataclass
class BusinessEventSubscription:
    """
    Definition: BusinessEventSubscription
    """

    notification_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "NotificationUrl"},
    )
    """
    Callback URL to the flow engine. Expected as part of the request and
    provided by Flow.
    """


@dataclass
class BusinessEventData:
    """
    Definition: BusinessEventData
    """

    id: Optional[str] = field(default=None, metadata={"wire_name": "Id"})
    """Business event Id"""
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """Business event data name"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DisplayName"},
    )
    """Business event data display name"""


@dataclass
class TriggerFieldData:
    """
    Definition: TriggerFieldData
    """

    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """Business event data name"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DisplayName"},
    )
    """Business event data display name"""


@dataclass
class Procedure:
    """
    Definition: Procedure
    """

    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """Procedure name"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DisplayName"},
    )
    """Procedure display name"""


@dataclass
class DataSet:
    """
    Definition: DataSet
    """

    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """Dataset name"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DisplayName"},
    )
    """Dataset display name"""
    query: Optional[List[PassThroughNativeQuery]] = None
    """Pass-through Native Queries"""


@dataclass
class Table:
    """
    Definition: Table
    """

    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    """The name of the table. The name is used at runtime."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "DisplayName"},
    )
    """The display name of the table."""
    dynamic_properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "DynamicProperties"},
    )
    """Additional table properties provided by the connector to the clients."""


@dataclass
class PassThroughNativeQuery:
    """
    Definition: PassThroughNativeQuery
    """

    language: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Language"},
    )
    """Query language"""


@dataclass
class Item:
    """
    Definition: Item
    """

    dynamic_properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "dynamicProperties"},
    )


@dataclass
class ExportableItemsList:
    """
    Definition: ExportableItemsList
    """

    value: Optional[List[ExportableItem]] = None
    """List of Items."""


@dataclass
class ExportableItem:
    """
    Definition: ExportableItem
    """

    dynamic_properties: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "dynamicProperties"},
    )


# Client Class

class DynamicsaxClient(ConnectorClientBase):
    """Typed client for dynamicsax connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a DynamicsaxClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "dynamicsax"

    async def execute_procedure_async(
        self,
        input: ExecuteProcedureInput,
        dataset: str,
        procedure: str,
    ) -> dict[str, Any] | None:
        """
        Execute action

        Execute action
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/procedures"
            f"/{str(procedure)}"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_items_async(
        self,
        dataset: str,
        table: str,
        apply: Optional[str] = None,
        filter: Optional[str] = None,
        orderby: Optional[str] = None,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        select: Optional[str] = None,
        cross_company: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Lists items present in table

        Lists items present in table
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
            f"/items"
        )
        query_params = []
        if apply is not None:
            value = str(apply)
            if isinstance(apply, bool):
                value = value.lower()
            query_params.append(f"$apply={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if orderby is not None:
            value = str(orderby)
            if isinstance(orderby, bool):
                value = value.lower()
            query_params.append(f"$orderby={quote(value)}")
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if select is not None:
            value = str(select)
            if isinstance(select, bool):
                value = value.lower()
            query_params.append(f"$select={quote(value)}")
        if cross_company is not None:
            value = str(cross_company)
            if isinstance(cross_company, bool):
                value = value.lower()
            query_params.append(f"cross-company={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def post_item_async(
        self,
        input: PostItemInput,
        dataset: str,
        table: str,
    ) -> dict[str, Any] | None:
        """
        Create record

        Create a new record in an entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
            f"/items"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Get a record

        Retrieves a single record
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
            f"/items"
            f"/{str(id)}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ) -> None:
        """
        Delete record

        Deletes a single record in an entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
            f"/items"
            f"/{str(id)}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

    async def patch_item_async(
        self,
        input: PatchItemInput,
        dataset: str,
        table: str,
        id: str,
    ) -> dict[str, Any] | None:
        """
        Update a record

        Updates a single record in an entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
            f"/items"
            f"/{str(id)}"
        )

        response = await self.http_client.send_async(
            "PATCH", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PATCH",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_tables_async(
        self,
        dataset: str,
    ) -> dict[str, Any] | None:
        """
        Get list of entities

        Retrieves a list of entities
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets/{quote(str(dataset), safe='')}/tables"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_data_sets_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get instances

        Retrieves a list of accessable Dynamics 365 for Fin & Ops instances
        """
        request_url = f"{self._connection_runtime_url}/datasets"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_business_event_categories_async(
        self,
        dataset: str,
    ) -> dict[str, Any] | None:
        """
        Gets business events categories

        Gets business events categories.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets/{quote(str(dataset), safe='')}/businesseventcategories"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_business_events_async(
        self,
        dataset: str,
        businesseventcategory: str,
    ) -> dict[str, Any] | None:
        """
        Gets business events

        Gets business events.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/businesseventcategories"
            f"/{str(businesseventcategory)}"
            f"/businessevents"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_legal_entities_async(
        self,
        dataset: str,
        businesseventcategory: str,
        businessevent: str,
    ) -> dict[str, Any] | None:
        """
        Gets legal entities

        Gets legal entities.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets/{quote(str(dataset), safe='')}/legalentities"
        )
        query_params = []
        value = str(businesseventcategory)
        if isinstance(businesseventcategory, bool):
            value = value.lower()
        query_params.append(f"businesseventcategory={quote(value)}")
        value = str(businessevent)
        if isinstance(businessevent, bool):
            value = value.lower()
        query_params.append(f"businessevent={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_procedures_async(
        self,
        dataset: str,
    ) -> dict[str, Any] | None:
        """
        Get actions

        Get actions
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/datasets/{quote(str(dataset), safe='')}/procedures"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_procedure_async(
        self,
        dataset: str,
        procedure: str,
    ) -> dict[str, Any] | None:
        """
        Get actions metadata

        Get actions metadata.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/$metadata.json"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/procedures"
            f"/{str(procedure)}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_table_async(
        self,
        dataset: str,
        table: str,
    ) -> dict[str, Any] | None:
        """
        Get entity metadata

        Retrieves metadata for an entity
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/$metadata.json"
            f"/datasets"
            f"/{quote(str(dataset), safe='')}"
            f"/tables"
            f"/{str(table)}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "SubscribeOnABusinessEvent": {
        "operation_id": "SubscribeOnABusinessEvent",
        "path": "/{connectionId}/datasets/{dataset}/subscribebusinessevent/{businessevent}",
        "method": "post",
        "required_parameters": ["dataset", "businesseventcategory", "businessevent"],
        "callback_payload_type": "BusinessEventSubscriptionResponse",
    },
}
