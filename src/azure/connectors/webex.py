# Copyright (c) Microsoft Corporation. All rights reserved.

# WebexClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class CreateSpaceMemberInput:
    """
    Add Member to Space
    """

    is_moderator: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isModerator"},
    )
    """Is moderator?"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """Email address"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """Person ID of the new member"""
    room_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomId"},
    )
    """Select a space"""


@dataclass
class CreateSpaceMemberResponse:
    """
    Response for Add Member to Space
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    is_moderator: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isModerator"},
    )
    """isModerator"""
    is_monitor: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isMonitor"},
    )
    """isMonitor"""
    person_display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personDisplayName"},
    )
    """personDisplayName"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """personEmail"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """personId"""
    person_org_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personOrgId"},
    )
    """personOrgId"""
    room_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomId"},
    )
    """roomId"""


@dataclass
class GetMessagesResponse:
    """
    Response for Get Messages
    """

    items: Optional[List[Dict[str, Any]]] = None
    """items"""


@dataclass
class SendMessageInput:
    """
    Send a Message
    """

    files: Optional[List[str]] = None
    """files"""
    markdown: Optional[str] = None
    """Message, in markdown format"""
    room_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomId"},
    )
    """Select a recipient space"""
    text: Optional[str] = None
    """
    Message Text, in plain text, used for clients that do not support markdown
    """
    to_person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "toPersonEmail"},
    )
    """The email address of the recipient when sending a private 1:1 message"""
    to_person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "toPersonId"},
    )
    """The ID of the recipient when sending a 1:1 message"""


@dataclass
class SendMessageResponse:
    """
    Response for Send a Message
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """personEmail"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """personId"""
    room_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomId"},
    )
    """roomId"""
    room_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomType"},
    )
    """roomType"""
    text: Optional[str] = None
    """text"""
    to_person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "toPersonEmail"},
    )
    """toPersonEmail"""


@dataclass
class GetMessageDetailsResponse:
    """
    Response for Get Message Details
    """

    created: Optional[str] = None
    """created"""
    files: Optional[List[str]] = None
    """files"""
    id: Optional[str] = None
    """id"""
    markdown: Optional[str] = None
    """markdown"""
    mentioned_groups: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "mentionedGroups"},
    )
    """mentionedGroups"""
    mentioned_people: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "mentionedPeople"},
    )
    """mentionedPeople"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """personEmail"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """personId"""
    room_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomId"},
    )
    """roomId"""
    room_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "roomType"},
    )
    """roomType"""
    text: Optional[str] = None
    """text"""
    to_person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "toPersonEmail"},
    )
    """toPersonEmail"""
    to_person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "toPersonId"},
    )
    """toPersonId"""


@dataclass
class GetPeopleResponse:
    """
    Response for Get People
    """

    items: Optional[List[Dict[str, Any]]] = None
    """items"""
    not_found_ids: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "notFoundIds"},
    )
    """notFoundIds"""


@dataclass
class GetMyOwnDetailsResponse:
    """
    Response for Get My Own Details
    """

    avatar: Optional[str] = None
    """avatar"""
    created: Optional[str] = None
    """created"""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """displayName"""
    emails: Optional[List[str]] = None
    """emails"""
    first_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstName"},
    )
    """firstName"""
    id: Optional[str] = None
    """id"""
    last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastActivity"},
    )
    """lastActivity"""
    last_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastName"},
    )
    """lastName"""
    nick_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "nickName"},
    )
    """nickName"""
    org_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "orgId"},
    )
    """orgId"""
    phone_numbers: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "phoneNumbers"},
    )
    """phoneNumbers"""
    status: Optional[str] = None
    """status"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """type"""


@dataclass
class GetSpacesResponse:
    """
    Response for Get Spaces List
    """

    items: Optional[List[Dict[str, Any]]] = None
    """items"""


@dataclass
class CreateSpaceInput:
    """
    Create Space
    """

    team_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "teamId"},
    )
    """Optionally select a team"""
    title: Optional[str] = None
    """Space Title"""


@dataclass
class CreateSpaceResponse:
    """
    Response for Create Space
    """

    created: Optional[str] = None
    """created"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """creatorId"""
    id: Optional[str] = None
    """id"""
    is_locked: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isLocked"},
    )
    """isLocked"""
    last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastActivity"},
    )
    """lastActivity"""
    title: Optional[str] = None
    """title"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """type"""


@dataclass
class GetSpaceDetailResponse:
    """
    Response for Get Space Details
    """

    created: Optional[str] = None
    """created"""
    creator_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "creatorId"},
    )
    """creatorId"""
    id: Optional[str] = None
    """id"""
    is_locked: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isLocked"},
    )
    """isLocked"""
    last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "lastActivity"},
    )
    """lastActivity"""
    title: Optional[str] = None
    """title"""
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    """type"""


@dataclass
class CreateTeamMemberInput:
    """
    Add Member to Team
    """

    is_moderator: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isModerator"},
    )
    """Is Moderator?"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """Email address"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """Person ID of the new member"""
    team_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "teamId"},
    )
    """Select a team"""


@dataclass
class CreateTeamMemberResponse:
    """
    Response for Add Member to Team
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    is_moderator: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "isModerator"},
    )
    """isModerator"""
    person_display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personDisplayName"},
    )
    """personDisplayName"""
    person_email: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personEmail"},
    )
    """personEmail"""
    person_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personId"},
    )
    """personId"""
    person_org_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "personOrgId"},
    )
    """personOrgId"""
    team_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "teamId"},
    )
    """teamId"""


@dataclass
class MembershipsUpdatedInput:
    """
    When a membership is updated
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class MembershipsUpdatedResponse:
    """
    Response for When a membership is updated
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class MembershipsDeletedInput:
    """
    When a membership is deleted
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class MembershipsDeletedResponse:
    """
    Response for When a membership is deleted
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class MembershipsCreatedInput:
    """
    When a membership is created
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class MembershipsCreatedResponse:
    """
    Response for When a membership is created
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class MessagesCreatedInput:
    """
    When a message is created
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class MessagesCreatedResponse:
    """
    Response for When a message is created
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class MessagesDeletedInput:
    """
    When a message is deleted
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class MessagesDeletedResponse:
    """
    Response for When a message is deleted
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class SpaceCreatedInput:
    """
    When a space is created
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class SpaceCreatedResponse:
    """
    Response for When a space is created
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


@dataclass
class SpaceUpdatedInput:
    """
    When a space is updated
    """

    event: Optional[str] = None
    """event"""
    name: Optional[str] = None
    """name"""
    resource: Optional[str] = None
    """resource"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""


@dataclass
class SpaceUpdatedResponse:
    """
    Response for When a space is updated
    """

    created: Optional[str] = None
    """created"""
    id: Optional[str] = None
    """id"""
    name: Optional[str] = None
    """name"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """targetUrl"""
    resource: Optional[str] = None
    """resource"""
    event: Optional[str] = None
    """event"""
    filter: Optional[str] = None
    """filter"""
    secret: Optional[str] = None
    """secret"""
    status: Optional[str] = None
    """status"""


# Client Class

class WebexClient(ConnectorClientBase):
    """Typed client for webex connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a WebexClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "webex"

    async def create_space_member_async(
        self,
        input: CreateSpaceMemberInput,
    ) -> dict[str, Any] | None:
        """
        Add Member to Space

        Add a member to an existing space
        """
        request_url = f"{self._connection_runtime_url}/v1/memberships"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_messages_async(
        self,
        room_id: str,
        mentioned_people: Optional[str] = None,
        before_message: Optional[str] = None,
        before: Optional[str] = None,
        max: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Messages

        Get a list of recent messages for a space
        """
        request_url = f"{self._connection_runtime_url}/v1/messages"
        query_params = []
        value = str(room_id)
        if isinstance(room_id, bool):
            value = value.lower()
        query_params.append(f"roomId={quote(value)}")
        if mentioned_people is not None:
            value = str(mentioned_people)
            if isinstance(mentioned_people, bool):
                value = value.lower()
            query_params.append(f"mentionedPeople={quote(value)}")
        if before_message is not None:
            value = str(before_message)
            if isinstance(before_message, bool):
                value = value.lower()
            query_params.append(f"beforeMessage={quote(value)}")
        if before is not None:
            value = str(before)
            if isinstance(before, bool):
                value = value.lower()
            query_params.append(f"before={quote(value)}")
        if max is not None:
            value = str(max)
            if isinstance(max, bool):
                value = value.lower()
            query_params.append(f"max={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_message_async(
        self,
        input: SendMessageInput,
    ) -> dict[str, Any] | None:
        """
        Send a Message

        Send a message to webex
        """
        request_url = f"{self._connection_runtime_url}/v1/messages"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_message_details_async(
        self,
        message_id: str,
    ) -> dict[str, Any] | None:
        """
        Get Message Details

        Shows details for a message, by message ID.
        """
        request_url = (
            f"{self._connection_runtime_url}/v1/messages/{str(message_id)}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_people_async(
        self,
        id: Optional[str] = None,
        email: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get People

        Lookup details and presence information for people in your organization
        """
        request_url = f"{self._connection_runtime_url}/v1/people"
        query_params = []
        if id is not None:
            value = str(id)
            if isinstance(id, bool):
                value = value.lower()
            query_params.append(f"id={quote(value)}")
        if email is not None:
            value = str(email)
            if isinstance(email, bool):
                value = value.lower()
            query_params.append(f"email={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_my_own_details_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get My Own Details

        Lookup your own details
        """
        request_url = f"{self._connection_runtime_url}/v1/people/me"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_spaces_async(
        self,
        max: Optional[str] = None,
        type_: Optional[str] = None,
        sort_by: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Spaces List

        This action retrieves a list of spaces
        """
        request_url = f"{self._connection_runtime_url}/v1/rooms"
        query_params = []
        if max is not None:
            value = str(max)
            if isinstance(max, bool):
                value = value.lower()
            query_params.append(f"max={quote(value)}")
        if type_ is not None:
            value = str(type_)
            if isinstance(type_, bool):
                value = value.lower()
            query_params.append(f"type={quote(value)}")
        if sort_by is not None:
            value = str(sort_by)
            if isinstance(sort_by, bool):
                value = value.lower()
            query_params.append(f"sortBy={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_space_async(
        self,
        input: CreateSpaceInput,
    ) -> dict[str, Any] | None:
        """
        Create Space

        Create space or team space
        """
        request_url = f"{self._connection_runtime_url}/v1/rooms"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_space_detail_async(
        self,
        room_id: str,
    ) -> dict[str, Any] | None:
        """
        Get Space Details

        Return details of a given space
        """
        request_url = f"{self._connection_runtime_url}/v1/rooms/{str(room_id)}"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_team_member_async(
        self,
        input: CreateTeamMemberInput,
    ) -> dict[str, Any] | None:
        """
        Add Member to Team

        Add a member to a team
        """
        request_url = f"{self._connection_runtime_url}/v1/team/memberships"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "MembershipsUpdated": {
        "operation_id": "MembershipsUpdated",
        "path": "/{connectionId}/v1/webhooks/1",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "MembershipsUpdatedResponse",
    },
    "MembershipsDeleted": {
        "operation_id": "MembershipsDeleted",
        "path": "/{connectionId}/v1/webhooks/2",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "MembershipsDeletedResponse",
    },
    "MembershipsCreated": {
        "operation_id": "MembershipsCreated",
        "path": "/{connectionId}/v1/webhooks/3",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "MembershipsCreatedResponse",
    },
    "MessagesCreated": {
        "operation_id": "MessagesCreated",
        "path": "/{connectionId}/v1/webhooks/4",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "MessagesCreatedResponse",
    },
    "MessagesDeleted": {
        "operation_id": "MessagesDeleted",
        "path": "/{connectionId}/v1/webhooks/5",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "MessagesDeletedResponse",
    },
    "SpaceCreated": {
        "operation_id": "SpaceCreated",
        "path": "/{connectionId}/v1/webhooks/6",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "SpaceCreatedResponse",
    },
    "SpaceUpdated": {
        "operation_id": "SpaceUpdated",
        "path": "/{connectionId}/v1/webhooks/7",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": "SpaceUpdatedResponse",
    },
}
