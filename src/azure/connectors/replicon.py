# Copyright (c) Microsoft Corporation. All rights reserved.

# RepliconClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class BulkGetProjectDetails3Input:
    """
    Get Project Details
    """

    projects: Optional[List[Dict[str, Any]]] = None
    """projects"""


@dataclass
class ProjectDetailsResponse:
    """
    Response for Get Project Details
    """

    d: Optional[List[Dict[str, Any]]] = None
    """d"""


@dataclass
class CreateProjectOrApplyModificationsInput:
    """
    Create Project Or Apply Modifications
    """

    target: Optional[Dict[str, Any]] = None
    """project target"""
    modifications: Optional[Dict[str, Any]] = None
    """modifications"""
    project_modification_option_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectModificationOptionUri"},
    )
    """project modification option uri"""
    unit_of_work_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "unitOfWorkId"},
    )
    """unit of work id"""


@dataclass
class CreateProjectOrApplyModificationsResponse:
    """
    Response for Create Project Or Apply Modifications
    """

    d: Optional[Dict[str, Any]] = None
    """d"""


@dataclass
class UserListServiceGetDataInput:
    """
    Get User List
    """

    page: Optional[str] = None
    """page"""
    pagesize: Optional[str] = None
    """page size"""
    column_uris: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "columnUris"},
    )
    """column uris"""
    sort: Optional[List[Any]] = None
    """sort"""
    filter_expression: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "filterExpression"},
    )
    """filter expression"""


@dataclass
class UserListServiceGetDataResponse:
    """
    Response for Get User List
    """

    d: Optional[Dict[str, Any]] = None
    """d"""


@dataclass
class GetDescendantTaskDetailsInput:
    """
    Get Descendant Task Details
    """

    parent_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "parentUri"},
    )
    """parent uri"""


@dataclass
class GetDescendantTaskDetailsResponse:
    """
    Response for Get Descendant Task Details
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class CreateTaskHierarchyOrApplyModificationsInput:
    """
    Create Task Hierarchy Or Apply Modifications
    """

    project: Optional[Dict[str, Any]] = None
    """project"""
    task_hierarchy: Optional[List[Dict[str, Any]]] = field(
        default=None,
        metadata={"wire_name": "taskHierarchy"},
    )
    """task hierarchy"""
    task_modification_option_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "taskModificationOptionUri"},
    )
    """task modification option uri"""
    unit_of_work_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "unitOfWorkId"},
    )
    """unit of work id"""


@dataclass
class CreateTaskHierarchyOrApplyModificationsResponse:
    """
    Response for Create Task Hierarchy Or Apply Modifications
    """

    d: Optional[List[Dict[str, Any]]] = None
    """d"""


@dataclass
class MoveTaskInput:
    """
    Move Task
    """

    task_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "taskUri"},
    )
    """task uri"""
    target_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUri"},
    )
    """target uri"""
    move_task_method_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "moveTaskMethodUri"},
    )
    """move task method uri"""


@dataclass
class TaskListServiceGetDataInput:
    """
    Task List Service Get Data
    """

    page: Optional[str] = None
    """page"""
    pagesize: Optional[str] = None
    """page size"""
    column_uris: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "columnUris"},
    )
    """column uris"""
    sort: Optional[List[Any]] = None
    """sort"""
    filter_expression: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "filterExpression"},
    )
    """filter expression"""


@dataclass
class TaskListServiceGetDataResponse:
    """
    Response for Task List Service Get Data
    """

    d: Optional[Dict[str, Any]] = None
    """d"""


@dataclass
class GetTimesheetSummaryInput:
    """
    Get Timesheet Summary
    """

    timesheet_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "timesheetUri"},
    )
    """timesheet uri"""


@dataclass
class GetTimesheetSummaryResponse:
    """
    Response for Get Timesheet Summary
    """

    d: Optional[Dict[str, Any]] = None
    """d"""


@dataclass
class BulkGetTimeEnteredSummaryInput:
    """
    Get Time Entered Summary
    """

    task_uris: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "taskUris"},
    )
    """task uris"""


@dataclass
class BulkGetTimeEnteredSummaryResponse:
    """
    Response for Get Time Entered Summary
    """

    d: Optional[List[Dict[str, Any]]] = None
    """d"""


@dataclass
class WebhookSubscriptionsRestAPIInput:
    """
    Subscribe to Event triggers
    """

    event_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventType"},
    )
    """Event type to subscribe to"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """target url"""


@dataclass
class WebhookSubscriptionResponse:
    """
    Response for Subscribe to Event triggers
    """

    id: Optional[str] = None
    """identity of the webhook event"""
    created_timestamp: Optional[str] = field(
        default=None,
        metadata={"wire_name": "createdTimestamp"},
    )
    """created time stamp of the webhook event"""
    updated_timestamp: Optional[str] = field(
        default=None,
        metadata={"wire_name": "updatedTimestamp"},
    )
    """updated time stamp of the webhook event"""
    event_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "eventType"},
    )
    """event Type of the webhook event"""
    target_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "targetUrl"},
    )
    """target Url of the webhook event"""
    shared_secret: Optional[str] = field(
        default=None,
        metadata={"wire_name": "sharedSecret"},
    )
    """shared Secret of the webhook event"""
    auth_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "authType"},
    )
    """auth Type of the webhook event"""


@dataclass
class PutProjectTeamMemberAssignmentsInput:
    """
    Put Project Team Member Assignments
    """

    project_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "projectUri"},
    )
    """project uri"""
    resource_uris: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "resourceUris"},
    )
    """resource uris"""


@dataclass
class PutResourceAssignmentsInput:
    """
    Rewrite Resource Assignment of a task
    """

    task_uri: Optional[str] = field(
        default=None,
        metadata={"wire_name": "taskUri"},
    )
    """task uri"""
    resource_uris: Optional[List[str]] = field(
        default=None,
        metadata={"wire_name": "resourceUris"},
    )
    """resource uris"""


@dataclass
class TenantEndpointDetails:
    """
    Response for Get Tenant End Point Details
    """

    d: Optional[Dict[str, Any]] = None
    """d"""


@dataclass
class Date:
    """
    Definition: Date
    """

    day: Optional[int] = None
    """day"""
    month: Optional[int] = None
    """month"""
    year: Optional[int] = None
    """year"""


@dataclass
class Duration:
    """
    Definition: Duration
    """

    hours: Optional[int] = None
    """hours"""
    microseconds: Optional[int] = None
    """microseconds"""
    milliseconds: Optional[int] = None
    """milliseconds"""
    minutes: Optional[int] = None
    """minutes"""
    seconds: Optional[int] = None
    """seconds"""


# Client Class

class RepliconClient(ConnectorClientBase):
    """Typed client for replicon connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a RepliconClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "replicon"

    async def bulk_get_project_details3_async(
        self,
        input: BulkGetProjectDetails3Input,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Get Project Details

        Get the project details from Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/ProjectService1.svc/BulkGetProjectDetails3"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_project_or_apply_modifications_async(
        self,
        input: CreateProjectOrApplyModificationsInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Create Project Or Apply Modifications

        Create Project Or Apply Modifications in Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/ProjectService1.svc/CreateProjectOrApplyModifications"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def user_list_service_get_data_async(
        self,
        input: UserListServiceGetDataInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Get User List

        Get User List Data from Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/UserListService1.svc/GetData"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_descendant_task_details_async(
        self,
        input: GetDescendantTaskDetailsInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Get Descendant Task Details

        Get Hierarchial Task Details from Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TaskService1.svc/GetDescendantTaskDetails"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_task_hierarchy_or_apply_modifications_async(
        self,
        input: CreateTaskHierarchyOrApplyModificationsInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Create Task Hierarchy Or Apply Modifications

        Create Task Hierarchy Or Apply Modifications to a Replicon Task
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services"
            f"/TaskService1.svc"
            f"/CreateTaskHierarchyOrApplyModifications"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def move_task_async(
        self,
        input: MoveTaskInput,
        host_url: str,
    ) -> None:
        """
        Move Task

        Move Task to a new Parent
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TaskService1.svc/MoveTask"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def task_list_service_get_data_async(
        self,
        input: TaskListServiceGetDataInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Task List Service Get Data

        Get Task List Service Data From Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TaskListService1.svc/GetData"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_timesheet_summary_async(
        self,
        input: GetTimesheetSummaryInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Get Timesheet Summary

        Get Timesheet Summary from Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TimesheetService1.svc/GetTimesheetSummary"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def bulk_get_time_entered_summary_async(
        self,
        input: BulkGetTimeEnteredSummaryInput,
        host_url: str,
    ) -> dict[str, Any] | None:
        """
        Get Time Entered Summary

        Get Time Entered by Task Uri from Replicon
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TaskService1.svc/BulkGetTimeEnteredSummary"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def put_project_team_member_assignments_async(
        self,
        input: PutProjectTeamMemberAssignmentsInput,
        host_url: str,
    ) -> None:
        """
        Put Project Team Member Assignments

        Rewrite Team Member assignment of Project
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/ProjectService1.svc/PutProjectTeamMemberAssignments"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def put_resource_assignments_async(
        self,
        input: PutResourceAssignmentsInput,
        host_url: str,
    ) -> None:
        """
        Rewrite Resource Assignment of a task

        Update the Resource assignment to a specified task
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/services/TaskService1.svc/PutResourceAssignments"
        )
        query_params = []
        value = str(host_url)
        if isinstance(host_url, bool):
            value = value.lower()
        query_params.append(f"hostUrl={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def get_my_tenant_endpoint_details_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get Tenant End Point Details

        Get End Point Details of the Tenant
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/DiscoveryService1.svc/GetMyTenantEndpointDetails"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "WebhookSubscriptionsRestAPI": {
        "operation_id": "WebhookSubscriptionsRestAPI",
        "path": "/{connectionId}/webhook-api/api/subscription",
        "method": "post",
        "required_parameters": ["hostUrl", "body"],
        "callback_payload_type": "WebhookSubscriptionResponse",
    },
}
