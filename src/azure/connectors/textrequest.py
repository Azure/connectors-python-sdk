# Copyright (c) Microsoft Corporation. All rights reserved.

# TextrequestClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class GetMessagesByContactPhoneResponse:
    """
    Response for Get a conversation's messages by a contact's phone number
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of message entities gotten from Text Request."""


@dataclass
class SendMessageByPhoneNumberInput:
    """
    Send a message to the contact with the given phone number
    """

    body: Optional[str] = None
    """Content of the message to send."""
    sender_name: Optional[str] = None
    """
    The name of the message sender. This is shown in the TextRequest web app
    and api when retrieving this message.
    """
    status_callback: Optional[str] = None
    """
    Url for the web app to hit with a POST command whenever this message's
    status goes from undelivered to sent (or error).
    """
    location_callback: Optional[str] = None
    """
    Location callback url to hit with a POST command when the user accesses and
    approves sharing their location with the sender. To include the link, add
    [Location Request] to the message body. This callback is not required for
    location requests, but it can be useful for APIs.
    """
    mms_media: Optional[List[str]] = None
    """
    Array of MMS Media attachments for this message. Attachments must be urls
    gotten from the post /mms endpoint (not included in this connector).
    External attachment links will not be accepted.
    """


@dataclass
class SendMessageByPhoneNumberResponse:
    """
    Response for Send a message to the contact with the given phone number
    """

    message_id: Optional[str] = None
    """Id of the message. This value is unique for each message."""
    body: Optional[str] = None
    """Content of this message."""
    message_direction: Optional[str] = None
    """
    Singular character, either R or S, denoting whether this message was
    received from the contact or sent to them, respectively.
    """
    response_by_username: Optional[str] = None
    """
    Username of the Text Request user that last sent a message to this user.
    """
    message_timestamp_utc: Optional[str] = None
    """Timestamp of when the message was sent or received by Text Request."""
    delivery_status: Optional[str] = None
    """
    Current status of this message. This can be 'sending', 'delivered',
    'error', or 'send'.
    """
    delivery_error: Optional[str] = None
    """
    Error that caused this message to not be delivered. If the delivery_status
    is not 'error', this value will be null.
    """
    mms_media: Optional[List[str]] = None
    """
    List of urls pointing to this message's attachments. All urls will be
    text-request generated urls.
    """


@dataclass
class GetContactByPhoneNumberResponse:
    """
    Response for Gets the contact with the specified phone number
    """

    phone_number: Optional[str] = None
    """Phone number of this contact."""
    first_name: Optional[str] = None
    """First name of this contact."""
    last_name: Optional[str] = None
    """Last name of this contact."""
    display_name: Optional[str] = None
    """
    Full name of this contact. This is the default name displayed in the web
    app when viewing contacts.
    """
    is_suppressed: Optional[bool] = None
    """
    Whether the conversation with this contact are suppressed, and therefore
    not pushed to the top of the message queue when received.
    """
    is_archived: Optional[bool] = None
    """
    Whether the conversation with this contact are archived, and therefore
    cannot be seen when querying conversations (by default).
    """
    is_blocked: Optional[bool] = None
    """
    Whether the conversation with this contact are blocked, and therefore not
    received or stored.
    """
    suppressed_reason: Optional[str] = None
    """
    Phrase describing the reason for suppressing contact. If is_suppressed is
    false, this will be null.
    """
    note: Optional[str] = None
    """
    Short phrase describing contact. Used for convenience. This is never shown
    to the contact.
    """
    groups: Optional[List[int]] = None
    """Array of integer ids referring to groups this contact belongs to."""
    contact_tags: Optional[List[str]] = None
    """Array of guids referring to tags attached to this contact."""
    custom_fields: Optional[List[Dict[str, Any]]] = None
    """Array of custom field ids and values defined for this contact."""
    is_resolved: Optional[bool] = None
    """
    Whether the conversation with this contact is resolved. Any new texts
    received by this contact will set this false.
    """
    first_contact_utc: Optional[str] = None
    """Timestamp of when the first message was sent to this contact."""
    opted_out_utc: Optional[str] = None
    """Timestamp of when this contact opted out to receiving messages."""
    last_msg_sent_utc: Optional[str] = None
    """Timestamp of when the last message was sent to this contact."""
    last_msg_received_utc: Optional[str] = None
    """Timestamp of when the last message was received from this contact."""
    total_msgs_sent: Optional[int] = None
    """The number of total messages sent to this contact."""
    total_msgs_received: Optional[int] = None
    """The number of total messages received from this contact."""
    response_count: Optional[int] = None
    """The total number of response messages from the contact."""
    date_created_utc: Optional[str] = None
    """The timestamp of when ths contact was created."""
    last_contact_date_utc: Optional[str] = None
    """
    The timestamp of when the last message was sent or received from this
    contact.
    """
    last_message: Optional[Dict[str, Any]] = None
    """Last message sent to or from this contact."""


@dataclass
class CreateContactInput:
    """
    Create or update a contact
    """

    first_name: Optional[str] = None
    """The new first name of the contact."""
    last_name: Optional[str] = None
    """The new last name of the contact."""
    display_name: Optional[str] = None
    """
    The new full name of the contact. This is the default name displayed in the
    web app when viewing contacts.
    """
    is_suppressed: Optional[bool] = None
    """
    Determines whether conversations with this contact are suppressed, and
    therefore not pushed to the top of the message queue when received.
    """
    is_archived: Optional[bool] = None
    """
    Determines whether conversations with this contact are archived, and
    therefore cannot be seen when querying conversations (by default).
    """
    is_blocked: Optional[bool] = None
    """
    Determines whether conversations with this contact are blocked, and
    therefore not received or stored.
    """
    suppressed_reason: Optional[str] = None
    """Phrase describing the reason for suppressing contact."""
    note: Optional[str] = None
    """
    Short phrase describing contact. Used for convenience. This is never shown
    to the contact.
    """
    groups: Optional[List[int]] = None
    """Array of integer ids referring to groups this contact will belong to."""
    contact_tags: Optional[List[str]] = None
    """Array of guids referring to tags attached to this contact."""
    custom_fields: Optional[List[Dict[str, Any]]] = None
    """Array of custom field ids and values defined for this contact."""
    is_resolved: Optional[bool] = None
    """
    Determines whether the current conversation with this contact is resolved.
    Any new texts received by this contact will set this false.
    """


@dataclass
class CreateContactResponse:
    """
    Response for Create or update a contact
    """

    phone_number: Optional[str] = None
    """Phone number of this contact."""
    first_name: Optional[str] = None
    """First name of this contact."""
    last_name: Optional[str] = None
    """Last name of this contact."""
    display_name: Optional[str] = None
    """
    Full name of this contact. This is the default name displayed in the web
    app when viewing contacts.
    """
    is_suppressed: Optional[bool] = None
    """
    Whether the conversation with this contact are suppressed, and therefore
    not pushed to the top of the message queue when received.
    """
    is_archived: Optional[bool] = None
    """
    Whether the conversation with this contact are archived, and therefore
    cannot be seen when querying conversations (by default).
    """
    is_blocked: Optional[bool] = None
    """
    Whether the conversation with this contact are blocked, and therefore not
    received or stored.
    """
    suppressed_reason: Optional[str] = None
    """
    Phrase describing the reason for suppressing contact. If is_suppressed is
    false, this will be null.
    """
    note: Optional[str] = None
    """
    Short phrase describing contact. Used for convenience. This is never shown
    to the contact.
    """
    groups: Optional[List[int]] = None
    """Array of integer ids referring to groups this contact belongs to."""
    contact_tags: Optional[List[str]] = None
    """Array of guids referring to tags attached to this contact."""
    custom_fields: Optional[List[Dict[str, Any]]] = None
    """Array of custom field ids and values defined for this contact."""
    is_resolved: Optional[bool] = None
    """
    Whether the conversation with this contact is resolved. Any new texts
    received by this contact will set this false.
    """
    first_contact_utc: Optional[str] = None
    """Timestamp of when the first message was sent to this contact."""
    opted_out_utc: Optional[str] = None
    """Timestamp of when this contact opted out to receiving messages."""
    last_msg_sent_utc: Optional[str] = None
    """Timestamp of when the last message was sent to this contact."""
    last_msg_received_utc: Optional[str] = None
    """Timestamp of when the last message was received from this contact."""
    total_msgs_sent: Optional[int] = None
    """The number of total messages sent to this contact."""
    total_msgs_received: Optional[int] = None
    """The number of total messages received from this contact."""
    response_count: Optional[int] = None
    """Total number of response messages from the contact."""
    date_created_utc: Optional[str] = None
    """Timestamp of when ths contact was created."""
    last_contact_date_utc: Optional[str] = None
    """
    Timestamp of when the last message was sent or received from this contact.
    """
    last_message: Optional[Dict[str, Any]] = None
    """Last message sent to or from this contact."""


@dataclass
class GetContactsResponse:
    """
    Response for Get all contacts that match the specified filtering criterion
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of contact entities gotten from Text Request."""


@dataclass
class BulkUpdateContactsInput:
    """
    Bulk update contacts
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetGroupByIdResponse:
    """
    Response for Get a group by its id
    """

    id: Optional[int] = None
    """The numerical id of the group."""
    group_member_count: Optional[int] = None
    """The number of members of the group."""
    is_keyword: Optional[bool] = None
    """
    Wheter this group is a keyword group. Such groups are autogenerated when a
    user sets up a keyword. Contacts are automatically added when they enroll.
    """
    name: Optional[str] = None
    """The name of this group."""
    notes: Optional[str] = None
    """Any notes on this group."""
    last_message_sent_utc: Optional[str] = None
    """Timestamp of when the last group message was sent for this group."""


@dataclass
class UpdateGroupInput:
    """
    Updates a group with the given id
    """

    name: Optional[str] = None
    """The new name of the group."""
    notes: Optional[str] = None
    """Short phrase describing group. Used for convenience."""


@dataclass
class UpdateGroupResponse:
    """
    Response for Updates a group with the given id
    """

    id: Optional[int] = None
    """The numerical id of the group."""
    group_member_count: Optional[int] = None
    """The number of members of the group."""
    is_keyword: Optional[bool] = None
    """
    Wheter this group is a keyword group. Such groups are autogenerated when a
    user sets up a keyword. Contacts are automatically added when they enroll.
    """
    name: Optional[str] = None
    """The name of this group."""
    notes: Optional[str] = None
    """Any notes on this group."""
    last_message_sent_utc: Optional[str] = None
    """Timestamp of when the last group message was sent for this group."""


@dataclass
class GetGroupsResponse:
    """
    Response for Gets all groups
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of group entities gotten from Text Request."""


@dataclass
class CreateGroupInput:
    """
    Creates a new group
    """

    name: Optional[str] = None
    """The new name of the group."""
    notes: Optional[str] = None
    """Short phrase describing group. Used for convenience."""


@dataclass
class CreateGroupResponse:
    """
    Response for Creates a new group
    """

    id: Optional[int] = None
    """The numerical id of the group."""
    group_member_count: Optional[int] = None
    """The number of members of the group."""
    is_keyword: Optional[bool] = None
    """
    Wheter this group is a keyword group. Such groups are autogenerated when a
    user sets up a keyword. Contacts are automatically added when they enroll.
    """
    name: Optional[str] = None
    """The name of this group."""
    notes: Optional[str] = None
    """Any notes on this group."""
    last_message_sent_utc: Optional[str] = None
    """Timestamp of when the last group message was sent for this group."""


@dataclass
class GetTagsResponse:
    """
    Response for Gets all tags
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of tag entities gotten from Text Request."""


@dataclass
class GetPaymentResponse:
    """
    Response for Gets the payment with the specified id
    """

    payment_id: Optional[int] = None
    """The numerical id of the payment."""
    request_date: Optional[str] = None
    """
    The timestamp of when this payment's initial message was queued to be sent.
    """
    recipient: Optional[str] = None
    """
    The name of the payment recipient. This may be null if the contact does not
    have a name associated with their phone number.
    """
    description: Optional[str] = None
    """
    The description of the payment. This is not shown to the contact. It is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """The phone number of the contact who is being sent this payment."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment."""
    is_past_due: Optional[bool] = None
    """Whether this payment is past the request date."""
    reminder_was_sent: Optional[bool] = None
    """Whether a text reminder has already been sent for this payment."""
    transaction_status: Optional[str] = None
    """
    The status of the transaction in authvia. This value starts as Unknown.
    """
    textrequest_payment_status: Optional[str] = None
    """
    The status of the transaction in Text Request. This represents that status
    shown in the payments page on the Text Request app. It starts as Sent.
    """
    reference_number: Optional[str] = None
    """
    The user-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class MarkPaymentPaidResponse:
    """
    Response for Mark a payment as paid
    """

    payment_id: Optional[int] = None
    """The numerical id of the payment."""
    request_date: Optional[str] = None
    """
    The timestamp of when this payment's initial message was queued to be sent.
    """
    recipient: Optional[str] = None
    """
    The name of the payment recipient. This may be null if the contact does not
    have a name associated with their phone number.
    """
    description: Optional[str] = None
    """
    The description of the payment. This is not shown to the contact. It is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """The phone number of the contact who is being sent this payment."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment."""
    is_past_due: Optional[bool] = None
    """Whether this payment is past the request date."""
    reminder_was_sent: Optional[bool] = None
    """Whether a text reminder has already been sent for this payment."""
    transaction_status: Optional[str] = None
    """
    The status of the transaction in authvia. This value starts as Unknown.
    """
    textrequest_payment_status: Optional[str] = None
    """
    The status of the transaction in Text Request. This represents that status
    shown in the payments page on the Text Request app. It starts as Sent.
    """
    reference_number: Optional[str] = None
    """
    The user-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class SendPaymentReminderResponse:
    """
    Response for Send a follow-up text reminding the user to pay the specified payment
    """

    payment_id: Optional[int] = None
    """The numerical id of the payment."""
    request_date: Optional[str] = None
    """
    The timestamp of when this payment's initial message was queued to be sent.
    """
    recipient: Optional[str] = None
    """
    The name of the payment recipient. This may be null if the contact does not
    have a name associated with their phone number.
    """
    description: Optional[str] = None
    """
    The description of the payment. This is not shown to the contact. It is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """The phone number of the contact who is being sent this payment."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment."""
    is_past_due: Optional[bool] = None
    """Whether this payment is past the request date."""
    reminder_was_sent: Optional[bool] = None
    """Whether a text reminder has already been sent for this payment."""
    transaction_status: Optional[str] = None
    """
    The status of the transaction in authvia. This value starts as Unknown.
    """
    textrequest_payment_status: Optional[str] = None
    """
    The status of the transaction in Text Request. This represents that status
    shown in the payments page on the Text Request app. It starts as Sent.
    """
    reference_number: Optional[str] = None
    """
    The user-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class CancelPaymentResponse:
    """
    Response for Cancels the specified payment
    """

    payment_id: Optional[int] = None
    """The numerical id of the payment."""
    request_date: Optional[str] = None
    """
    The timestamp of when this payment's initial message was queued to be sent.
    """
    recipient: Optional[str] = None
    """
    The name of the payment recipient. This may be null if the contact does not
    have a name associated with their phone number.
    """
    description: Optional[str] = None
    """
    The description of the payment. This is not shown to the contact. It is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """The phone number of the contact who is being sent this payment."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment."""
    is_past_due: Optional[bool] = None
    """Whether this payment is past the request date."""
    reminder_was_sent: Optional[bool] = None
    """Whether a text reminder has already been sent for this payment."""
    transaction_status: Optional[str] = None
    """
    The status of the transaction in authvia. This value starts as Unknown.
    """
    textrequest_payment_status: Optional[str] = None
    """
    The status of the transaction in Text Request. This represents that status
    shown in the payments page on the Text Request app. It starts as Sent.
    """
    reference_number: Optional[str] = None
    """
    The user-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class GetPaymentsResponse:
    """
    Response for Gets all payments
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of payment entities gotten from Text Request."""


@dataclass
class CreatePaymentInput:
    """
    Creates a new payment
    """

    description: Optional[str] = None
    """
    Description of the payment. Description is not shown to the contact and is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """Phone number of the contact this payment will be sent to."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment. """
    message: Optional[str] = None
    """Message text to send to contact along with payment link."""
    reference_number: Optional[str] = None
    """
    User-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class CreatePaymentResponse:
    """
    Response for Creates a new payment
    """

    payment_id: Optional[int] = None
    """The numerical id of the payment."""
    request_date: Optional[str] = None
    """
    The timestamp of when this payment's initial message was queued to be sent.
    """
    recipient: Optional[str] = None
    """
    The name of the payment recipient. This may be null if the contact does not
    have a name associated with their phone number.
    """
    description: Optional[str] = None
    """
    The description of the payment. This is not shown to the contact. It is
    only used by internal users for context about payments.
    """
    customer_phone: Optional[str] = None
    """The phone number of the contact who is being sent this payment."""
    amount_requested: Optional[float] = None
    """The amount in dollars requested by this payment."""
    is_past_due: Optional[bool] = None
    """Whether this payment is past the request date."""
    reminder_was_sent: Optional[bool] = None
    """Whether a text reminder has already been sent for this payment."""
    transaction_status: Optional[str] = None
    """
    The status of the transaction in authvia. This value starts as Unknown.
    """
    textrequest_payment_status: Optional[str] = None
    """
    The status of the transaction in Text Request. This represents that status
    shown in the payments page on the Text Request app. It starts as Sent.
    """
    reference_number: Optional[str] = None
    """
    The user-defined id defined when payments are created that can be used to
    quickly find payments. Reference numbers are not unique; many payments can
    have the same reference.
    """


@dataclass
class GetDashboardResponse:
    """
    Response for Get info on this specific dashboard
    """

    id: Optional[int] = None
    """The numerical id of the dashboard."""
    name: Optional[str] = None
    """The name of the dashboard."""
    phone: Optional[str] = None
    """The texting number associated with this dashboard."""


@dataclass
class UpdateDashboardsNameInput:
    """
    Update a specific dashboard's name
    """

    name: Optional[str] = None
    """The new name of the dashboard."""


@dataclass
class UpdateDashboardsNameResponse:
    """
    Response for Update a specific dashboard's name
    """

    id: Optional[int] = None
    """The numerical id of the dashboard."""
    name: Optional[str] = None
    """The name of the dashboard."""
    phone: Optional[str] = None
    """The texting number associated with this dashboard."""


@dataclass
class GetConversationsResponse:
    """
    Response for Gets all conversations for this dashboard
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of conversation entities gotten from Text Request."""


@dataclass
class GetDashboardsResponse:
    """
    Response for Get all dashboards in an account
    """

    meta: Optional[Dict[str, Any]] = None
    """
    Metadata about the data returned by this request. This data is returned by
    all bulk GET requests.
    """
    items: Optional[List[Dict[str, Any]]] = None
    """List of dashboard entities gotten from Text Request."""


@dataclass
class CreateDashboardInput:
    """
    Create a new dashboard with the given name and phone number
    """

    name: Optional[str] = None
    """The name of the dashboard."""
    phone: Optional[str] = None
    """The texting number associated with this dashboard."""


@dataclass
class CreateDashboardResponse:
    """
    Response for Create a new dashboard with the given name and phone number
    """

    id: Optional[int] = None
    """The numerical id of the dashboard."""
    name: Optional[str] = None
    """The name of the dashboard."""
    phone: Optional[str] = None
    """The texting number associated with this dashboard."""


@dataclass
class TextingWebhookInput:
    """
    Triggers when a text is sent or received
    """

    target_url: Optional[str] = None
    """Url the webhook will redirect to"""
    event: Optional[str] = None
    """event"""
    http_verb: Optional[str] = field(
        default=None,
        metadata={"wire_name": "httpVerb"},
    )
    """httpVerb"""


@dataclass
class TextingWebhookResponse:
    """
    Response for Triggers when a text is sent or received
    """

    id: Optional[int] = None
    """The numerical id of the dashboard."""
    target_url: Optional[str] = None
    """The url the webhook will post to"""
    event: Optional[str] = None
    """The webhook's associated trigger event"""


# Client Class

class TextrequestClient(ConnectorClientBase):
    """Typed client for textrequest connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a TextrequestClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "textrequest"

    async def get_messages_by_contact_phone_async(
        self,
        dashboard_id: str,
        phone_number: str,
        page: str,
        page_size: str,
    ) -> dict[str, Any] | None:
        """
        Get a conversation's messages by a contact's phone number

        Get the conversation between the specified dashboard and phone number.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
            f"/messages"
        )
        query_params = []
        value = str(page)
        if isinstance(page, bool):
            value = value.lower()
        query_params.append(f"page={quote(value)}")
        value = str(page_size)
        if isinstance(page_size, bool):
            value = value.lower()
        query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_message_by_phone_number_async(
        self,
        input: SendMessageByPhoneNumberInput,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Send a message to the contact with the given phone number

        Sends a message (starting a new conversation if the given phone number
        has no message history).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
            f"/messages"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def archive_conversation_async(
        self,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Archive a Conversation

        Archives a conversation, removing it from the results of all
        conversation retrieval endpoints. A conversation will be automatically
        unarchived if the customer sends a text to your Text Request number, or
        if you send the customer a text from your Text Request number.
        Archiving conversations is a good way to remove clutter from customers
        you don't plan on talking to in the future. Note that if a customer
        texts in \"STOP\" to prevent you from sending them any further SMS
        messages, the conversation is automatically archived.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
            f"/conversations"
            f"/archive"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def unarchive_conversation_async(
        self,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Unarchive a Conversation

        Unarchives a conversation. Once a conversation is unarchived, it will
        once again appear in the queue. it will also appear in conversations
        returned by the /conversations endpoint. If a conversation is already
        unarchived, this method has no effect.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
            f"/conversations"
            f"/unarchive"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_contact_by_phone_number_async(
        self,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Gets the contact with the specified phone number

        Gets the contact with the specified phone number
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_contact_async(
        self,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Deletes the contact with the specified phone number

        Deletes the contact with the specified phone number
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_contact_async(
        self,
        input: CreateContactInput,
        dashboard_id: str,
        phone_number: str,
    ) -> dict[str, Any] | None:
        """
        Create or update a contact

        Creates or updates a contact (depending on whether one already exists
        with the given phone number).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/contacts"
            f"/{quote(str(phone_number), safe='')}"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_contacts_async(
        self,
        dashboard_id: str,
        page: str,
        page_size: str,
        contact_phone_number: Optional[str] = None,
        last_message_timestamp_before_utc: Optional[str] = None,
        last_message_timestamp_after_utc: Optional[str] = None,
        contact_created_before: Optional[str] = None,
        contact_created_after: Optional[str] = None,
        is_resolved: Optional[str] = None,
        is_blocked: Optional[str] = None,
        is_archived: Optional[str] = None,
        is_suppressed: Optional[str] = None,
        has_opted_out: Optional[str] = None,
        last_message_sent_before: Optional[str] = None,
        last_message_sent_after: Optional[str] = None,
        last_message_received_before: Optional[str] = None,
        last_message_received_after: Optional[str] = None,
        tags: Optional[str] = None,
        groups: Optional[str] = None,
        custom_field_id1: Optional[str] = None,
        custom_field_value1: Optional[str] = None,
        custom_field_id2: Optional[str] = None,
        custom_field_value2: Optional[str] = None,
        custom_field_id3: Optional[str] = None,
        custom_field_value3: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get all contacts that match the specified filtering criterion

        Gets all contacts that match the specified filtering criterion. Only
        the dashboard specified will be searched; there is no account level
        search for a number available.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/contacts"
        )
        query_params = []
        if contact_phone_number is not None:
            value = str(contact_phone_number)
            if isinstance(contact_phone_number, bool):
                value = value.lower()
            query_params.append(f"contact_phone_number={quote(value)}")
        if last_message_timestamp_before_utc is not None:
            value = str(last_message_timestamp_before_utc)
            if isinstance(last_message_timestamp_before_utc, bool):
                value = value.lower()
            query_params.append(f"last_message_timestamp_before_utc={quote(value)}")
        if last_message_timestamp_after_utc is not None:
            value = str(last_message_timestamp_after_utc)
            if isinstance(last_message_timestamp_after_utc, bool):
                value = value.lower()
            query_params.append(f"last_message_timestamp_after_utc={quote(value)}")
        if contact_created_before is not None:
            value = str(contact_created_before)
            if isinstance(contact_created_before, bool):
                value = value.lower()
            query_params.append(f"contact_created_before={quote(value)}")
        if contact_created_after is not None:
            value = str(contact_created_after)
            if isinstance(contact_created_after, bool):
                value = value.lower()
            query_params.append(f"contact_created_after={quote(value)}")
        if is_resolved is not None:
            value = str(is_resolved)
            if isinstance(is_resolved, bool):
                value = value.lower()
            query_params.append(f"is_resolved={quote(value)}")
        if is_blocked is not None:
            value = str(is_blocked)
            if isinstance(is_blocked, bool):
                value = value.lower()
            query_params.append(f"is_blocked={quote(value)}")
        if is_archived is not None:
            value = str(is_archived)
            if isinstance(is_archived, bool):
                value = value.lower()
            query_params.append(f"is_archived={quote(value)}")
        if is_suppressed is not None:
            value = str(is_suppressed)
            if isinstance(is_suppressed, bool):
                value = value.lower()
            query_params.append(f"is_suppressed={quote(value)}")
        if has_opted_out is not None:
            value = str(has_opted_out)
            if isinstance(has_opted_out, bool):
                value = value.lower()
            query_params.append(f"has_opted_out={quote(value)}")
        if last_message_sent_before is not None:
            value = str(last_message_sent_before)
            if isinstance(last_message_sent_before, bool):
                value = value.lower()
            query_params.append(f"last_message_sent_before={quote(value)}")
        if last_message_sent_after is not None:
            value = str(last_message_sent_after)
            if isinstance(last_message_sent_after, bool):
                value = value.lower()
            query_params.append(f"last_message_sent_after={quote(value)}")
        if last_message_received_before is not None:
            value = str(last_message_received_before)
            if isinstance(last_message_received_before, bool):
                value = value.lower()
            query_params.append(f"last_message_received_before={quote(value)}")
        if last_message_received_after is not None:
            value = str(last_message_received_after)
            if isinstance(last_message_received_after, bool):
                value = value.lower()
            query_params.append(f"last_message_received_after={quote(value)}")
        if tags is not None:
            value = str(tags)
            if isinstance(tags, bool):
                value = value.lower()
            query_params.append(f"tags={quote(value)}")
        if groups is not None:
            value = str(groups)
            if isinstance(groups, bool):
                value = value.lower()
            query_params.append(f"groups={quote(value)}")
        if custom_field_id1 is not None:
            value = str(custom_field_id1)
            if isinstance(custom_field_id1, bool):
                value = value.lower()
            query_params.append(f"custom_field_id_1={quote(value)}")
        if custom_field_value1 is not None:
            value = str(custom_field_value1)
            if isinstance(custom_field_value1, bool):
                value = value.lower()
            query_params.append(f"custom_field_value_1={quote(value)}")
        if custom_field_id2 is not None:
            value = str(custom_field_id2)
            if isinstance(custom_field_id2, bool):
                value = value.lower()
            query_params.append(f"custom_field_id_2={quote(value)}")
        if custom_field_value2 is not None:
            value = str(custom_field_value2)
            if isinstance(custom_field_value2, bool):
                value = value.lower()
            query_params.append(f"custom_field_value_2={quote(value)}")
        if custom_field_id3 is not None:
            value = str(custom_field_id3)
            if isinstance(custom_field_id3, bool):
                value = value.lower()
            query_params.append(f"custom_field_id_3={quote(value)}")
        if custom_field_value3 is not None:
            value = str(custom_field_value3)
            if isinstance(custom_field_value3, bool):
                value = value.lower()
            query_params.append(f"custom_field_value_3={quote(value)}")
        value = str(page)
        if isinstance(page, bool):
            value = value.lower()
        query_params.append(f"page={quote(value)}")
        value = str(page_size)
        if isinstance(page_size, bool):
            value = value.lower()
        query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def bulk_update_contacts_async(
        self,
        input: BulkUpdateContactsInput,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Bulk update contacts

        Only the dashboard specified will have contacts added; contacts are
        organized by dashboard, and there is no account level search for a
        number available.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/contacts"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_group_by_id_async(
        self,
        dashboard_id: str,
        group_id: str,
    ) -> dict[str, Any] | None:
        """
        Get a group by its id

        Gets the group with the specified id.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/groups"
            f"/{quote(str(group_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_group_async(
        self,
        dashboard_id: str,
        group_id: str,
    ) -> dict[str, Any] | None:
        """
        Deletes the group with the specified id

        Deletes the group with the specified id. This will succeed even if
        there are contacts that are members of the group (the contacts will not
        be deleted).
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/groups"
            f"/{quote(str(group_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_group_async(
        self,
        input: UpdateGroupInput,
        dashboard_id: str,
        group_id: str,
    ) -> dict[str, Any] | None:
        """
        Updates a group with the given id

        Updates a group's name using the group's numerical id. If the passed in
        group id does not exist, the call will fail.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/groups"
            f"/{quote(str(group_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_groups_async(
        self,
        dashboard_id: str,
        page: str,
        page_size: str,
    ) -> dict[str, Any] | None:
        """
        Gets all groups

        Gets all groups for the user's account.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/groups"
        )
        query_params = []
        value = str(page)
        if isinstance(page, bool):
            value = value.lower()
        query_params.append(f"page={quote(value)}")
        value = str(page_size)
        if isinstance(page_size, bool):
            value = value.lower()
        query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_group_async(
        self,
        input: CreateGroupInput,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Creates a new group

        Creates a new group with the provided name.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/groups"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_tags_async(
        self,
        dashboard_id: str,
        page: str,
        page_size: str,
    ) -> dict[str, Any] | None:
        """
        Gets all tags

        Gets all the tags for this dashboard. Tags are unique between
        dashboards.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/tags"
        )
        query_params = []
        value = str(page)
        if isinstance(page, bool):
            value = value.lower()
        query_params.append(f"page={quote(value)}")
        value = str(page_size)
        if isinstance(page_size, bool):
            value = value.lower()
        query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_custom_fields_async(
        self,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Gets all custom fields

        Gets all custom fields for this dashboard. Custom fields are unique
        between dashboards.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/fields"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_payment_async(
        self,
        dashboard_id: str,
        payment_id: str,
    ) -> dict[str, Any] | None:
        """
        Gets the payment with the specified id

        Gets the payment with the specified id.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/payments"
            f"/{quote(str(payment_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def mark_payment_paid_async(
        self,
        dashboard_id: str,
        payment_id: str,
    ) -> dict[str, Any] | None:
        """
        Mark a payment as paid

        Closes a payment as \"paid\". Use this endpoint for when the payment is
        paid through a means other than the payment request portal. This will
        close the payment and add its payment amount to your statistics. This
        endpoint is not required for payments paid through the payments-portal
        sent through Text Request; that will automatically mark a payment as
        paid.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/payments"
            f"/{quote(str(payment_id), safe='')}"
            f"/mark_as_paid"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_payment_reminder_async(
        self,
        dashboard_id: str,
        payment_id: str,
    ) -> dict[str, Any] | None:
        """
        Send a follow-up text reminding the user to pay the specified payment

        Sends a reminder to the contact to pay the specified payment. The
        content of the message is auto-generated. To avoid spam, only one
        reminder can be sent per payment
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/payments"
            f"/{quote(str(payment_id), safe='')}"
            f"/resend"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def cancel_payment_async(
        self,
        dashboard_id: str,
        payment_id: str,
    ) -> dict[str, Any] | None:
        """
        Cancels the specified payment

        Cancels the specified payment, texting the user that the payment has
        been canceled. Payments cannot be edited, so canceling and issuing a
        new payment is the best way to correct a payment.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards"
            f"/{quote(str(dashboard_id), safe='')}"
            f"/payments"
            f"/{quote(str(payment_id), safe='')}"
            f"/cancel"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_payments_async(
        self,
        dashboard_id: str,
        page: str,
        page_size: str,
        reference_number: Optional[str] = None,
        phone_number: Optional[str] = None,
        sort_type: Optional[str] = None,
        sort_direction: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Gets all payments

        Gets all payments, including canceled and paid payments. These can be
        filtered using the query parameters.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/payments"
        )
        query_params = []
        if reference_number is not None:
            value = str(reference_number)
            if isinstance(reference_number, bool):
                value = value.lower()
            query_params.append(f"reference_number={quote(value)}")
        if phone_number is not None:
            value = str(phone_number)
            if isinstance(phone_number, bool):
                value = value.lower()
            query_params.append(f"phone_number={quote(value)}")
        if sort_type is not None:
            value = str(sort_type)
            if isinstance(sort_type, bool):
                value = value.lower()
            query_params.append(f"sort_type={quote(value)}")
        if sort_direction is not None:
            value = str(sort_direction)
            if isinstance(sort_direction, bool):
                value = value.lower()
            query_params.append(f"sort_direction={quote(value)}")
        value = str(page)
        if isinstance(page, bool):
            value = value.lower()
        query_params.append(f"page={quote(value)}")
        value = str(page_size)
        if isinstance(page_size, bool):
            value = value.lower()
        query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_payment_async(
        self,
        input: CreatePaymentInput,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Creates a new payment

        Creates a new payment with the provided message to the provided
        contact.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/payments"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_dashboard_async(
        self,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Get info on this specific dashboard

        Returns the name and phone of the dashboard.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_dashboard_async(
        self,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Deletes the specified dashboard

        Deletes the specified dashboard.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_dashboards_name_async(
        self,
        input: UpdateDashboardsNameInput,
        dashboard_id: str,
    ) -> dict[str, Any] | None:
        """
        Update a specific dashboard's name

        Updates a dashboard. This does not change a dashboard's phone number.
        If you want a new number with another dashboard's contacts, you will
        have to create a new dashboard with a POST and add the contacts with a
        bulk export/import.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_conversations_async(
        self,
        dashboard_id: str,
        tags: Optional[str] = None,
        show_unresolved_only: Optional[str] = None,
        include_archived: Optional[str] = None,
        search: Optional[str] = None,
        page: Optional[str] = None,
        page_size: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Gets all conversations for this dashboard

        Gets all conversations for this dashboard, and includes info on the
        last message for each conversation.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/dashboards/{quote(str(dashboard_id), safe='')}/conversations"
        )
        query_params = []
        if tags is not None:
            value = str(tags)
            if isinstance(tags, bool):
                value = value.lower()
            query_params.append(f"tags={quote(value)}")
        if show_unresolved_only is not None:
            value = str(show_unresolved_only)
            if isinstance(show_unresolved_only, bool):
                value = value.lower()
            query_params.append(f"show_unresolved_only={quote(value)}")
        if include_archived is not None:
            value = str(include_archived)
            if isinstance(include_archived, bool):
                value = value.lower()
            query_params.append(f"include_archived={quote(value)}")
        if search is not None:
            value = str(search)
            if isinstance(search, bool):
                value = value.lower()
            query_params.append(f"search={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if page_size is not None:
            value = str(page_size)
            if isinstance(page_size, bool):
                value = value.lower()
            query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_dashboards_async(
        self,
        page: Optional[str] = None,
        page_size: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get all dashboards in an account

        Gets all dashboards in an account.
        """
        request_url = f"{self._connection_runtime_url}/dashboards"
        query_params = []
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if page_size is not None:
            value = str(page_size)
            if isinstance(page_size, bool):
                value = value.lower()
            query_params.append(f"page_size={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_dashboard_async(
        self,
        input: CreateDashboardInput,
    ) -> dict[str, Any] | None:
        """
        Create a new dashboard with the given name and phone number

        Creates a new dashboard in the account. Once the dashboard is created,
        admins will be emailed about the change and can begin assigning users.
        This endpoint can make a dashboard for any provisioned numbers. You can
        search for provisioned numbers in a specific area code using GET
        /dashboards/provisioning. If the phone number is already in use, a Bad
        Request exception will be thrown.
        """
        request_url = f"{self._connection_runtime_url}/dashboards"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "TextingWebhook": {
        "operation_id": "TextingWebhook",
        "path": "/{connectionId}/dashboards/{dashboard_id}/hooks",
        "method": "post",
        "required_parameters": ["dashboard_id", "body"],
        "callback_payload_type": "TextingWebhookResponse",
    },
}
