# Copyright (c) Microsoft Corporation. All rights reserved.

# WordpressClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class ListPostsResponse:
    """
    Response for When a post is created
    """

    posts: Optional[List[PostResponse]] = None
    """Posts"""


@dataclass
class SiteStatsModel:
    """
    Response for Get site statistics
    """

    visitors_today: Optional[int] = None
    visitors_yesterday: Optional[int] = None
    visitors: Optional[int] = None
    views_today: Optional[int] = None
    views_yesterday: Optional[int] = None
    views_best_day: Optional[str] = None
    views_best_day_total: Optional[int] = None
    views: Optional[int] = None
    comments: Optional[int] = None
    posts: Optional[int] = None
    followers_blog: Optional[int] = None
    followers_comments: Optional[int] = None
    comments_per_month: Optional[int] = None
    comments_most_active_recent_day: Optional[str] = None
    comments_most_active_time: Optional[str] = None
    comments_spam: Optional[int] = None
    categories: Optional[int] = None
    tags: Optional[int] = None
    shares: Optional[int] = None
    shares_twitter: Optional[int] = None
    shares_facebook: Optional[int] = None
    shares_press_this: Optional[int] = field(
        default=None,
        metadata={"wire_name": "shares_press-this"},
    )


@dataclass
class PostModel:
    """
    Response for Get post
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "ID"})
    site_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "site_ID"},
    )
    date: Optional[str] = None
    modified: Optional[str] = None
    title: Optional[str] = None
    u_r_l: Optional[str] = field(default=None, metadata={"wire_name": "URL"})
    short_u_r_l: Optional[str] = field(
        default=None,
        metadata={"wire_name": "short_URL"},
    )
    content: Optional[str] = None
    excerpt: Optional[str] = None
    slug: Optional[str] = None
    guid: Optional[str] = None
    status: Optional[str] = None
    sticky: Optional[bool] = None
    password: Optional[str] = None
    parent: Optional[Any] = None
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    likes_enabled: Optional[bool] = None
    sharing_enabled: Optional[bool] = None
    like_count: Optional[int] = None
    i_like: Optional[bool] = None
    is_reblogged: Optional[bool] = None
    is_following: Optional[bool] = None
    global_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "global_ID"},
    )
    featured_image: Optional[str] = None
    post_thumbnail: Optional[Dict[str, Any]] = None
    format: Optional[str] = None
    geo: Optional[bool] = None


@dataclass
class SiteList:
    """
    Response for List sites
    """

    sites: Optional[List[Site]] = None
    """List of sites"""


@dataclass
class Site:
    """
    Definition: Site
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "ID"})
    """Site ID"""
    name: Optional[str] = None
    """Site Name"""
    description: Optional[str] = None
    """Site Description"""
    u_r_l: Optional[str] = field(default=None, metadata={"wire_name": "URL"})
    """Site URL"""
    is_multisite: Optional[bool] = None
    """
    Whether the site is a Multisite site or not. Always true for WP.com sites.
    """
    post_count: Optional[int] = None
    """The number of posts the site has"""
    subscribers_count: Optional[int] = None
    """The number of subscribers the site has"""
    lang: Optional[str] = None
    """Language"""
    visible: Optional[bool] = None
    """Site visibility"""
    is_private: Optional[bool] = None
    """Is site private"""
    single_user_site: Optional[bool] = None
    """Is single user site"""
    is_vip: Optional[bool] = None
    """Is VIP"""
    is_following: Optional[bool] = None
    """Is Following"""


SiteModel = List[Dict[str, Any]]


SiteStatsChartModel = List[Dict[str, Any]]


@dataclass
class SiteInsightsModel:
    """
    Definition: SiteInsightsModel
    """

    highest_hour: Optional[int] = None
    highest_hour_percent: Optional[float] = None
    highest_day_of_week: Optional[int] = None
    highest_day_percent: Optional[float] = None


TopPostModel = List[Dict[str, Any]]


@dataclass
class PostResponse:
    """
    Definition: PostResponse
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "ID"})
    comment_count: Optional[int] = None
    site_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "site_ID"},
    )
    date: Optional[str] = None
    modified: Optional[str] = None
    title: Optional[str] = None
    u_r_l: Optional[str] = field(default=None, metadata={"wire_name": "URL"})
    slug: Optional[str] = None
    guid: Optional[str] = None
    type_: Optional[str] = field(default=None, metadata={"wire_name": "type"})
    like_count: Optional[int] = None


PostsModel = List[Dict[str, Any]]


@dataclass
class CreatePostModel:
    """
    Definition: CreatePostModel
    """

    title: Optional[str] = None
    """The title of the post"""
    content: Optional[str] = None
    """The content of the post"""
    status: Optional[str] = None
    """The status of the post"""
    tags: Optional[str] = None
    """The tags for post"""


@dataclass
class Error:
    """
    Definition: Error
    """

    code: Optional[int] = None
    message: Optional[str] = None
    fields: Optional[str] = None


# Client Class

class WordpressClient(ConnectorClientBase):
    """Typed client for wordpress connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a WordpressClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "wordpress"

    async def site_stats_async(
        self,
        site_id: str,
    ) -> dict[str, Any] | None:
        """
        Get site statistics

        Get statistics for a specified site
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/sites/{quote(str(site_id), safe='')}/stats"
        )
        query_params = []
        query_params.append("fields=" + quote("stats"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_async(
        self,
        site_id: str,
        post_id: str,
    ) -> dict[str, Any] | None:
        """
        Get post

        Get post
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/sites"
            f"/{quote(str(site_id), safe='')}"
            f"/posts"
            f"/{quote(str(post_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_async(
        self,
        input: CreatePostModel,
        site_id: str,
    ) -> dict[str, Any] | None:
        """
        Create post

        Create post
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/sites/{quote(str(site_id), safe='')}/posts/new"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_sites_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List sites

        List sites
        """
        request_url = f"{self._connection_runtime_url}/me/sites"
        query_params = []
        query_params.append(
            "fields=" + quote(
                "ID, name, description, URL,  is_multisite, post_count,subscribers_count,"
                " lang,visible,is_private,single_user_site,is_vip,is_following"
            )
        )
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnTriggerNewPost": {
        "operation_id": "OnTriggerNewPost",
        "path": "/{connectionId}/trigger/me/posts",
        "method": "get",
        "required_parameters": [],
        "callback_payload_type": "ListPostsResponse",
    },
}
