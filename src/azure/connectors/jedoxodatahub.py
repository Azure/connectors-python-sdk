# Copyright (c) Microsoft Corporation. All rights reserved.

# JedoxodatahubClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class DatabasesResponse:
    """
    Response for Get databases
    """

    value: Optional[DatabaseArray] = None


@dataclass
class Database:
    """
    Response for Get database by id
    """

    cube_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "CubeCount"},
    )
    dimension_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "DimensionCount"},
    )
    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})


@dataclass
class CubesResponse:
    """
    Response for Get cubes
    """

    value: Optional[CubeArray] = None


@dataclass
class Cube:
    """
    Response for Get cube by ID
    """

    filled_cell_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "FilledCellCount"},
    )
    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})


@dataclass
class CubeCellsResponse:
    """
    Response for Get cube cells
    """

    value: Optional[CellArray] = None


@dataclass
class DimensionsResponse:
    """
    Response for Get dimensions
    """

    value: Optional[DimensionArray] = None


@dataclass
class Dimension:
    """
    Response for Get dimension by ID
    """

    element_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "ElementCount"},
    )
    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})


@dataclass
class ElementsResponse:
    """
    Response for Get elements
    """

    value: Optional[ElementArray] = None


@dataclass
class Element:
    """
    Response for Get element by ID
    """

    id: Optional[int] = field(default=None, metadata={"wire_name": "Id"})
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    position: Optional[int] = field(
        default=None,
        metadata={"wire_name": "Position"},
    )
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})
    weight: Optional[float] = field(
        default=None,
        metadata={"wire_name": "Weight"},
    )


@dataclass
class ViewsResponse:
    """
    Response for Get stored views
    """

    value: Optional[ViewArray] = None


@dataclass
class View:
    """
    Response for Get stored view by ID
    """

    creation_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CreationDate"},
    )
    cube_id: Optional[int] = field(
        default=None,
        metadata={"wire_name": "CubeId"},
    )
    cube_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "CubeName"},
    )
    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    friendly_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FriendlyName"},
    )
    global_: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Global"},
    )
    id: Optional[str] = field(default=None, metadata={"wire_name": "Id"})
    user_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "UserName"},
    )


@dataclass
class ViewCellsResponse:
    """
    Response for Get stored view cells
    """

    value: Optional[ViewCellArray] = None


@dataclass
class IntegratorProjectGroupsResponse:
    """
    Response for Get Integrator project groups
    """

    value: Optional[IntegratorProjectGroupArray] = None


@dataclass
class IntegratorProjectGroup:
    """
    Response for Get Integrator project group by identifier
    """

    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    developer: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Developer"},
    )
    friendly_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "FriendlyName"},
    )
    id: Optional[str] = field(default=None, metadata={"wire_name": "Id"})
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    namespace: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Namespace"},
    )
    version: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Version"},
    )


@dataclass
class IntegratorProjectsResponse:
    """
    Response for Get Integrator projects
    """

    value: Optional[IntegratorProjectArray] = None


@dataclass
class IntegratorProject:
    """
    Response for Get Integrator project by name
    """

    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    modification_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ModificationDate"},
    )
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    version: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Version"},
    )


@dataclass
class ExtractsResponse:
    """
    Response for Get extracts
    """

    value: Optional[IntegratorComponentArray] = None


@dataclass
class IntegratorComponent:
    """
    Response for Get extract by Name
    """

    description: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    modification_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ModificationDate"},
    )
    name: Optional[str] = field(default=None, metadata={"wire_name": "Name"})
    type_: Optional[str] = field(default=None, metadata={"wire_name": "Type"})


@dataclass
class ExtractRowsResponse:
    """
    Response for Get extract rows
    """

    value: Optional[IntegratorExtractComponentRowArray] = None


@dataclass
class JobsResponse:
    """
    Response for Get jobs
    """

    value: Optional[IntegratorComponentArray] = None


@dataclass
class IntegratorRunResult:
    """
    Response for Run job
    """

    errors: Optional[int] = None
    execution_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "executionType"},
    )
    id: Optional[int] = None
    start_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "startDate"},
    )
    status: Optional[str] = None
    trace_available: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "traceAvailable"},
    )
    warnings: Optional[int] = None


@dataclass
class LoadsResponse:
    """
    Response for Get loads
    """

    value: Optional[IntegratorComponentArray] = None


@dataclass
class TransformsResponse:
    """
    Response for Get transforms
    """

    value: Optional[IntegratorComponentArray] = None


@dataclass
class TransformRowsResponse:
    """
    Response for Get transform Rows
    """

    value: Optional[IntegratorTransformComponentRowArray] = None


@dataclass
class GetCubeCellResultSchemaResponse:
    """
    Response for Get Cube Cell Result Schema
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetExtractRowsResultSchemaResponse:
    """
    Response for Get Extract Rows Result Schema
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetTransformRowsResultSchemaResponse:
    """
    Response for Get Transform Rows Result Schema
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class GetViewCellResultSchemaResponse:
    """
    Response for Get View Cell Result Schema
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class Cell:
    """
    Definition: Cell
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


CellArray = List["Cell"]


CubeArray = List["Cube"]


DatabaseArray = List["Database"]


DimensionArray = List["Dimension"]


ElementArray = List["Element"]


@dataclass
class Error:
    """
    Definition: Error
    """

    error: Optional[str] = None


IntegratorComponentArray = List["IntegratorComponent"]


@dataclass
class IntegratorExtractComponentRow:
    """
    Definition: IntegratorExtractComponentRow
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


IntegratorExtractComponentRowArray = List["IntegratorExtractComponentRow"]


IntegratorProjectArray = List["IntegratorProject"]


IntegratorProjectGroupArray = List["IntegratorProjectGroup"]


@dataclass
class IntegratorTransformComponentRow:
    """
    Definition: IntegratorTransformComponentRow
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


IntegratorTransformComponentRowArray = List["IntegratorTransformComponentRow"]


ViewArray = List["View"]


@dataclass
class ViewCell:
    """
    Definition: ViewCell
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


ViewCellArray = List["ViewCell"]


# Client Class

class JedoxodatahubClient(ConnectorClientBase):
    """Typed client for jedoxodatahub connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a JedoxodatahubClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "jedoxodatahub"

    async def databases_async(
        self,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get databases

        Get a list of databases found in the server. System and config
        databases are excluded from the list, but can be requested by providing
        the ID.
        """
        request_url = f"{self._connection_runtime_url}/Databases"
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def database_by_id_async(
        self,
        database_id: str,
    ) -> dict[str, Any] | None:
        """
        Get database by id

        Get the database with the given ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def cubes_async(
        self,
        database_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get cubes

        Get a list of cubes in the given database. To prevent issues with the
        URL encodings, Attribute cubes will be renamed, e.g. \\#_Years to
        ATT_Years.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})/Cubes"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def cube_by_id_async(
        self,
        database_id: str,
        cube_id: str,
    ) -> dict[str, Any] | None:
        """
        Get cube by ID

        Get the cube with the given ID in the given database. To prevent issues
        with the URL encodings, Attribute cubes will be renamed, e.g. \\#_Years
        to ATT_Years.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Cubes({quote(str(cube_id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def cube_cells_async(
        self,
        database_id: str,
        cube_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
        baseonly: Optional[str] = None,
        userules: Optional[str] = None,
        zerosupression: Optional[str] = None,
        disablepaging: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get cube cells

        Get the cells from a cube. This returns the cells' values and element
        names. If the cell has a string value, the value is instead stored in
        the stringValue field. Element names are stored in dynamic properties.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Cubes({quote(str(cube_id), safe='')})"
            f"/Cells"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if baseonly is not None:
            value = str(baseonly)
            if isinstance(baseonly, bool):
                value = value.lower()
            query_params.append(f"baseonly={quote(value)}")
        if userules is not None:
            value = str(userules)
            if isinstance(userules, bool):
                value = value.lower()
            query_params.append(f"userules={quote(value)}")
        if zerosupression is not None:
            value = str(zerosupression)
            if isinstance(zerosupression, bool):
                value = value.lower()
            query_params.append(f"zerosupression={quote(value)}")
        if disablepaging is not None:
            value = str(disablepaging)
            if isinstance(disablepaging, bool):
                value = value.lower()
            query_params.append(f"disablepaging={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def dimensions_async(
        self,
        database_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get dimensions

        Get a list of dimensions in the given database.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})/Dimensions"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def dimension_by_id_async(
        self,
        database_id: str,
        dimension_id: str,
    ) -> dict[str, Any] | None:
        """
        Get dimension by ID

        Get the dimension with the given ID in the given database.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Dimensions({quote(str(dimension_id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def elements_async(
        self,
        database_id: str,
        dimension_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get elements

        Get a list of elements in the given dimension.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Dimensions({quote(str(dimension_id), safe='')})"
            f"/Elements"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def element_by_id_async(
        self,
        database_id: str,
        dimension_id: str,
        element_id: str,
    ) -> dict[str, Any] | None:
        """
        Get element by ID

        Returns the element with the given ID in the given dimension.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Dimensions({quote(str(dimension_id), safe='')})"
            f"/Elements({quote(str(element_id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def views_async(
        self,
        database_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get stored views

        Get a list of stored views in the given database.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})/Views"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def view_by_id_async(
        self,
        database_id: str,
        view_id: str,
    ) -> dict[str, Any] | None:
        """
        Get stored view by ID

        Get the view with the given ID in the given database.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Views({quote(str(view_id), safe='')})"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def view_cells_async(
        self,
        database_id: str,
        view_id: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
        baseonly: Optional[str] = None,
        userules: Optional[str] = None,
        zerosupression: Optional[str] = None,
        disablepaging: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get stored view cells

        Get all cells from a view. This returns the cells' values and element
        names. If the cell has a string value, the value is instead stored in
        the stringValue field. Element names are stored in dynamic properties.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Databases({quote(str(database_id), safe='')})"
            f"/Views({quote(str(view_id), safe='')})"
            f"/Cells"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if baseonly is not None:
            value = str(baseonly)
            if isinstance(baseonly, bool):
                value = value.lower()
            query_params.append(f"baseonly={quote(value)}")
        if userules is not None:
            value = str(userules)
            if isinstance(userules, bool):
                value = value.lower()
            query_params.append(f"userules={quote(value)}")
        if zerosupression is not None:
            value = str(zerosupression)
            if isinstance(zerosupression, bool):
                value = value.lower()
            query_params.append(f"zerosupression={quote(value)}")
        if disablepaging is not None:
            value = str(disablepaging)
            if isinstance(disablepaging, bool):
                value = value.lower()
            query_params.append(f"disablepaging={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def integrator_project_groups_async(
        self,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Integrator project groups

        Get a list of integrator project groups found in the server.
        """
        request_url = f"{self._connection_runtime_url}/Integrator"
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def integrator_projects_by_id_async(
        self,
        group_identifier: str,
    ) -> dict[str, Any] | None:
        """
        Get Integrator project group by identifier

        Get the project group with the given ID.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def integrator_projects_async(
        self,
        group_identifier: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get Integrator projects

        Get a list of integrator projects found in the server.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')/Projects"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def integrator_projects_by_name_async(
        self,
        group_identifier: str,
        project_name: str,
    ) -> dict[str, Any] | None:
        """
        Get Integrator project by name

        Get the integrator project with the given name.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def extracts_async(
        self,
        group_identifier: str,
        project_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get extracts

        Get a list of extracts in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Extracts"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def extract_by_name_async(
        self,
        group_identifier: str,
        project_name: str,
        extract_name: str,
    ) -> dict[str, Any] | None:
        """
        Get extract by Name

        Get the extract with the given name in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Extracts('{quote(str(extract_name), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def extract_rows_async(
        self,
        group_identifier: str,
        project_name: str,
        extract_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get extract rows

        Stream the rows of the extract with the given name in the given
        integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Extracts('{quote(str(extract_name), safe='')}')"
            f"/Rows"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def jobs_async(
        self,
        group_identifier: str,
        project_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get jobs

        Get a list of jobs in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Jobs"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def job_by_name_async(
        self,
        group_identifier: str,
        project_name: str,
        job_name: str,
    ) -> dict[str, Any] | None:
        """
        Get job by name

        Get the jobs with the given name in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Jobs('{quote(str(job_name), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def run_job_async(
        self,
        group_identifier: str,
        project_name: str,
        job_name: str,
    ) -> dict[str, Any] | None:
        """
        Run job

        Run the job with the given name in the given integrator project. The
        execution will be added to the queue.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Jobs('{quote(str(job_name), safe='')}')"
            f"/Run"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def run_job_with_variables_async(
        self,
        group_identifier: str,
        project_name: str,
        job_name: str,
        variables: str,
    ) -> dict[str, Any] | None:
        """
        Run job with variables

        Run the job with the given name in the given integrator project. The
        execution will be added to the queue.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Jobs('{quote(str(job_name), safe='')}')"
            f"/Run(Variables='{quote(quote(str(variables), safe=''), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def loads_async(
        self,
        group_identifier: str,
        project_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get loads

        Get a list of loads in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Loads"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def load_by_name_async(
        self,
        group_identifier: str,
        project_name: str,
        load_name: str,
    ) -> dict[str, Any] | None:
        """
        Get load by name

        Get the transform with the given name in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Loads('{quote(str(load_name), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def run_load_async(
        self,
        group_identifier: str,
        project_name: str,
        load_name: str,
    ) -> dict[str, Any] | None:
        """
        Run load

        Run the load with the given name in the given integrator project. The
        execution will be added to the queue.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Loads('{quote(str(load_name), safe='')}')"
            f"/Run()"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def run_load_with_variables_async(
        self,
        group_identifier: str,
        project_name: str,
        load_name: str,
        variables: str,
    ) -> dict[str, Any] | None:
        """
        Run load with variables

        Run the load with the given name in the given integrator project. The
        execution will be added to the queue.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Loads('{quote(str(load_name), safe='')}')"
            f"/Run(Variables='{quote(quote(str(variables), safe=''), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def transforms_async(
        self,
        group_identifier: str,
        project_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get transforms

        Get a list of transforms in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Transforms"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def transform_by_name_async(
        self,
        group_identifier: str,
        project_name: str,
        transform_name: str,
    ) -> dict[str, Any] | None:
        """
        Get transform by name

        Get the transform with the given name in the given integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Transforms('{quote(str(transform_name), safe='')}')"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def transform_rows_async(
        self,
        group_identifier: str,
        project_name: str,
        transform_name: str,
        top: Optional[str] = None,
        skip: Optional[str] = None,
        filter: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get transform Rows

        Stream the rows of the transform with the given name in the given
        integrator project.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/Integrator('{quote(str(group_identifier), safe='')}')"
            f"/Projects('{quote(str(project_name), safe='')}')"
            f"/Transforms('{quote(str(transform_name), safe='')}')"
            f"/Rows"
        )
        query_params = []
        if top is not None:
            value = str(top)
            if isinstance(top, bool):
                value = value.lower()
            query_params.append(f"$top={quote(value)}")
        if skip is not None:
            value = str(skip)
            if isinstance(skip, bool):
                value = value.lower()
            query_params.append(f"$skip={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"$filter={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_cube_cell_result_schema_async(
        self,
        database_id: str,
        cube_id: str,
    ) -> dict[str, Any] | None:
        """
        Get Cube Cell Result Schema

        Service request for getting the schema used for the result of cube
        cells.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/service"
            f"/powerapps"
            f"/schema"
            f"/databases"
            f"/{quote(str(database_id), safe='')}"
            f"/cubes"
            f"/{quote(str(cube_id), safe='')}"
            f"/cells"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_extract_rows_result_schema_async(
        self,
        group_identifier: str,
        project_name: str,
        extract_name: str,
    ) -> dict[str, Any] | None:
        """
        Get Extract Rows Result Schema

        Service request for getting the schema used for the result of extract
        rows.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/service"
            f"/powerapps"
            f"/schema"
            f"/integrator"
            f"/{quote(str(group_identifier), safe='')}"
            f"/{quote(str(project_name), safe='')}"
            f"/extracts"
            f"/{quote(str(extract_name), safe='')}"
            f"/rows"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_transform_rows_result_schema_async(
        self,
        group_identifier: str,
        project_name: str,
        transform_name: str,
    ) -> dict[str, Any] | None:
        """
        Get Transform Rows Result Schema

        Service request for getting the schema used for the result of transform
        rows.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/service"
            f"/powerapps"
            f"/schema"
            f"/integrator"
            f"/{quote(str(group_identifier), safe='')}"
            f"/{quote(str(project_name), safe='')}"
            f"/transforms"
            f"/{quote(str(transform_name), safe='')}"
            f"/rows"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_view_cell_result_schema_async(
        self,
        database_id: str,
        view_id: str,
    ) -> dict[str, Any] | None:
        """
        Get View Cell Result Schema

        Service request for getting the schema used for the result of view
        cells.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/service"
            f"/powerapps"
            f"/schema"
            f"/databases"
            f"/{quote(str(database_id), safe='')}"
            f"/views"
            f"/{quote(str(view_id), safe='')}"
            f"/cells"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)
