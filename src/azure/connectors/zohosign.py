# Copyright (c) Microsoft Corporation. All rights reserved.

# ZohosignClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class InvokeAPIInput:
    """
    Invoke API
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class InvokeAPIResponse:
    """
    Response for Invoke API
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class ZohoSignTriggersInput:
    """
    Zoho Sign Triggers
    """

    webhook_url: Optional[str] = None
    """Webhook url"""
    webhook_actions: Optional[str] = None
    """Webhook actions"""
    purpose: Optional[str] = None
    """Purpose"""
    appname: Optional[str] = None
    """appname"""


@dataclass
class GetFormDataResponse:
    """
    Response for Get data of completed request
    """

    code: Optional[int] = None
    """code"""
    document_form_data: Optional[Dict[str, Any]] = None
    """Document form data"""
    status: Optional[str] = None
    """status"""


@dataclass
class GetDocumentResponse:
    """
    Response for Get a document
    """

    code: Optional[int] = None
    """code"""
    requests: Optional[Dict[str, Any]] = None
    """requests"""
    message: Optional[str] = None
    """message"""
    status: Optional[str] = None
    """status"""


@dataclass
class UpdateDocumentInput:
    """
    Update the properties of a created request
    """

    requests: Optional[Dict[str, Any]] = None
    """requests"""


@dataclass
class UpdateDocumentResponse:
    """
    Response for Update the properties of a created request
    """

    code: Optional[int] = None
    """code"""
    message: Optional[str] = None
    """message"""
    status: Optional[str] = None
    """status"""


@dataclass
class SendSignRequestResponse:
    """
    Response for SendSignRequest
    """

    code: Optional[int] = None
    """code"""
    message: Optional[str] = None
    """message"""
    status: Optional[str] = None
    """status"""


@dataclass
class GetTemplatesResponse:
    """
    Response for Get list of templates
    """

    templates: Optional[List[Dict[str, Any]]] = None


# Client Class

class ZohosignClient(ConnectorClientBase):
    """Typed client for zohosign connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a ZohosignClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "zohosign"

    async def invoke_a_p_i_async(
        self,
        input: InvokeAPIInput,
        url: str,
        method: str,
    ) -> dict[str, Any] | None:
        """
        Invoke API

        Invoke API
        """
        request_url = (
            f"{self._connection_runtime_url}/{quote(str(url), safe='')}"
        )
        query_params = []
        value = str(method)
        if isinstance(method, bool):
            value = value.lower()
        query_params.append(f"method={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def download_completion_certificate_async(
        self,
        request_id: str,
    ) -> bytes:
        """
        Download Completion Certificate

        Downloads the completion certificate of a specific envelope
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests"
            f"/{quote(str(request_id), safe='')}"
            f"/completioncertificate"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def download_document_async(
        self,
        request_id: str,
    ) -> bytes:
        """
        Download Document

        Downloads a specific envelope
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/pdf"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def download_file_async(
        self,
        request_id: str,
        document_id: str,
    ) -> bytes:
        """
        Download File

        Downloads a specific document or file
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests"
            f"/{quote(str(request_id), safe='')}"
            f"/documents"
            f"/{quote(str(document_id), safe='')}"
            f"/pdf"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        return response.content

    async def get_form_data_async(
        self,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        Get data of completed request

        Shows the details of a signed envelope
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/fielddata"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def recall_document_async(
        self,
        request_id: str,
    ) -> None:
        """
        Recall a particular document

        Recall a particular document
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/recall"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def remind_document_recipients_async(
        self,
        request_id: str,
    ) -> None:
        """
        Reminder for recipients

        Send a reminder to recipients of a particular document.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/remind"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

    async def delete_document_async(
        self,
        request_id: str,
    ) -> None:
        """
        Delete Document

        Delete a particular document
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/delete"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

    async def get_document_async(
        self,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        Get a document

        Shows the details of a specific envelope
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_document_async(
        self,
        input: UpdateDocumentInput,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        Update the properties of a created request

        Update the properties of a created request
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def send_sign_request_async(
        self,
        request_id: str,
    ) -> dict[str, Any] | None:
        """
        SendSignRequest

        Send the uploaded document out for signature
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/requests/{quote(str(request_id), safe='')}/submit"
        )

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_template_details_async(
        self,
        template_id: str,
    ) -> None:
        """
        Get particular template details

        Shows the details of a specific document template
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/templates/{quote(str(template_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

    async def get_templates_async(
        self,
    ) -> dict[str, Any] | None:
        """
        Get list of templates

        Shows a list of all the document templates created
        """
        request_url = f"{self._connection_runtime_url}/templates"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "zoho-sign-triggers": {
        "operation_id": "zoho-sign-triggers",
        "path": "/{connectionId}/internal/accounts/webhooks",
        "method": "post",
        "required_parameters": ["body"],
        "callback_payload_type": None,
    },
}
