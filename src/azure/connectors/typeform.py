# Copyright (c) Microsoft Corporation. All rights reserved.

# TypeformClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class NewResponseWebhookInput:
    """
    When a response is submitted
    """

    url: Optional[str] = None
    """Webhook URL."""
    enabled: Optional[bool] = None
    """
    Yes if you want to send responses to the webhook immediately. Otherwise,
    no.
    """


@dataclass
class WebhookCreationResponse:
    """
    Response for When a response is submitted
    """

    id: Optional[str] = None
    """Unique ID of the webhook"""
    form_id: Optional[str] = None
    """Unique ID of the typeform"""
    tag: Optional[str] = None
    """Unique name you want to use for the webhook"""
    url: Optional[str] = None
    """Webhook URL"""
    enabled: Optional[bool] = None
    """
    True if you want to send responses to the webhook immediately. Otherwise,
    false.
    """
    created_at: Optional[str] = None
    """
    Date and time when webhook was created. In ISO 8601 format, UTC time, to
    the second, with T as a delimiter between the date and time.
    """
    updated_at: Optional[str] = None
    """
    Date of last update to webhook. In ISO 8601 format, UTC time, to the
    second, with T as a delimeter between the date and time.
    """


@dataclass
class ListFormsResponse:
    """
    Response for List forms
    """

    total_items: Optional[int] = None
    """Total number of forms."""
    page_count: Optional[int] = None
    """Number of pages of results."""
    items: Optional[List[Dict[str, Any]]] = None
    """Forms in response."""


ListFormsResponseOld = List[Dict[str, Any]]


@dataclass
class ResponseList:
    """
    Definition: ResponseList
    """

    responses: Optional[List[Item]] = None


@dataclass
class Item:
    """
    Definition: Item
    """

    item_internal_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ItemInternalId"},
    )


@dataclass
class ItemNew:
    """
    Definition: Item-New
    """

    item_internal_id: Optional[str] = field(
        default=None,
        metadata={"wire_name": "ItemInternalId"},
    )


@dataclass
class WebhookPayload:
    """
    Definition: WebhookPayload
    """

    event_id: Optional[str] = None
    """Unique ID for the webhook. Automatically assigned by Typeform."""
    event_type: Optional[str] = None
    """Reason the webhook is being sent."""
    form_response: Optional[Dict[str, Any]] = None
    """
    Object that contains information about the typeform and account associated
    with the webhook.
    """


# Client Class

class TypeformClient(ConnectorClientBase):
    """Typed client for typeform connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a TypeformClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "typeform"

    async def list_forms_async(
        self,
        search: Optional[str] = None,
        page: Optional[str] = None,
        page_size: Optional[str] = None,
        workspace_id: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List forms

        Retrieves a list of JSON descriptions for all forms in your Typeform
        account (public and private).
        """
        request_url = f"{self._connection_runtime_url}/forms"
        query_params = []
        if search is not None:
            value = str(search)
            if isinstance(search, bool):
                value = value.lower()
            query_params.append(f"search={quote(value)}")
        if page is not None:
            value = str(page)
            if isinstance(page, bool):
                value = value.lower()
            query_params.append(f"page={quote(value)}")
        if page_size is not None:
            value = str(page_size)
            if isinstance(page_size, bool):
                value = value.lower()
            query_params.append(f"page_size={quote(value)}")
        if workspace_id is not None:
            value = str(workspace_id)
            if isinstance(workspace_id, bool):
                value = value.lower()
            query_params.append(f"workspace_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_schema_async(
        self,
        form_id: str,
    ) -> dict[str, Any] | None:
        """
        Get schema

        Get schema
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v3/forms/{quote(str(form_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "NewResponseWebhook_V2": {
        "operation_id": "NewResponseWebhook_V2",
        "path": "/{connectionId}/v2/forms/{form_id}/webhooks/{tag}",
        "method": "post",
        "required_parameters": ["form_id", "tag", "body"],
        "callback_payload_type": "WebhookCreationResponse",
    },
}
