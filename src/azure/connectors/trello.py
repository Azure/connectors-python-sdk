# Copyright (c) Microsoft Corporation. All rights reserved.

# TrelloClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, Any, Dict, List as TypingList
from urllib.parse import quote
import json

from azure.connectors.sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class Card:
    """
    Response for List the cards in a board
    """

    id: Optional[str] = None
    """The unique id of the card."""
    check_item_states: Optional[TypingList[CheckItemState]] = field(
        default=None,
        metadata={"wire_name": "checkItemStates"},
    )
    """The check item states in the card."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the card is
    closed.
    """
    date_last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastActivity"},
    )
    """The datetime of last activity on the card."""
    desc: Optional[str] = None
    """The description of the card."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board which the card belongs to."""
    id_list: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idList"},
    )
    """The unique Id of the list which the card belongs to."""
    id_members_voted: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idMembersVoted"},
    )
    """The unique Ids of the voting members."""
    id_short: Optional[int] = field(
        default=None,
        metadata={"wire_name": "idShort"},
    )
    """The short Id of the card."""
    id_attachment_cover: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idAttachmentCover"},
    )
    """The unique Id of the attachment cover."""
    manual_cover_attachment: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "manualCoverAttachment"},
    )
    """
    A boolean value (true, false) determining whether or not the card has a
    manual cover attachment.
    """
    id_labels: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idLabels"},
    )
    """A list of label Ids on the card."""
    name: Optional[str] = None
    """The name of the card."""
    pos: Optional[float] = None
    """The position of the card."""
    short_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortLink"},
    )
    """A short link to the card."""
    badges: Optional[Badges] = None
    due: Optional[str] = None
    """The due date of the card."""
    short_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortUrl"},
    )
    """The short url link to the card."""
    subscribed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the card is
    subscribed to.
    """
    url: Optional[str] = None
    """The full url to the card."""
    checklists: Optional[TypingList[Checklist]] = None
    """The set of board checklists."""


@dataclass
class CardWithChecklists:
    """
    Response for Get a card by id
    """

    id: Optional[str] = None
    """The unique id of the card."""
    check_item_states: Optional[TypingList[CheckItemState]] = field(
        default=None,
        metadata={"wire_name": "checkItemStates"},
    )
    """The check item states in the card."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the card is
    closed.
    """
    date_last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastActivity"},
    )
    """The datetime of last activity on the card."""
    desc: Optional[str] = None
    """The description of the card."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board which the card belongs to."""
    id_list: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idList"},
    )
    """The unique Id of the list which the card belongs to."""
    id_members_voted: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idMembersVoted"},
    )
    """The unique Ids of the voting members."""
    id_short: Optional[int] = field(
        default=None,
        metadata={"wire_name": "idShort"},
    )
    """The short Id of the card."""
    id_attachment_cover: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idAttachmentCover"},
    )
    """The unique Id of the attachment cover."""
    manual_cover_attachment: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "manualCoverAttachment"},
    )
    """
    A boolean value (true, false) determining whether or not the card has a
    manual cover attachment.
    """
    id_labels: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idLabels"},
    )
    """A list of label Ids on the card."""
    name: Optional[str] = None
    """The name of the card."""
    pos: Optional[float] = None
    """The position of the card."""
    short_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortLink"},
    )
    """A short link to the card."""
    badges: Optional[Badges] = None
    due: Optional[str] = None
    """The due date of the card."""
    short_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortUrl"},
    )
    """The short url link to the card."""
    subscribed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the card is
    subscribed to.
    """
    url: Optional[str] = None
    """The full url to the card."""
    checklists: Optional[TypingList[Checklist]] = None
    """The set of card checklists."""


@dataclass
class ObjectEntity:
    """
    Response for Delete a card
    """

    additional_properties: Dict[str, Any] = field(default_factory=dict)
    """
    Dynamic properties determined at runtime
    (similar to .NET [JsonExtensionData])
    """


@dataclass
class Board:
    """
    Response for List boards
    """

    id: Optional[str] = None
    """The unique Id of the board."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the board is
    closed.
    """
    date_last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastActivity"},
    )
    """Date of the last activity on the board."""
    date_last_view: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastView"},
    )
    """Date of the last time the board is viewed."""
    desc: Optional[str] = None
    """The description of the board."""
    id_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idOrganization"},
    )
    """The id of the organization."""
    invitations: Optional[TypingList[str]] = None
    """The list of invitations on the board."""
    invited: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the account is
    invited to the board.
    """
    label_names: Optional[Label] = field(
        default=None,
        metadata={"wire_name": "labelNames"},
    )
    memberships: Optional[TypingList[Membership]] = None
    """The set of board memberships."""
    name: Optional[str] = None
    """The name of the board."""
    pinned: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the board is
    pinned.
    """
    power_ups: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "powerUps"},
    )
    """The set of power ups on the board."""
    perfs: Optional[Perfs] = None
    short_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortLink"},
    )
    """The short link of the board."""
    short_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortUrl"},
    )
    """The short url to the board."""
    starred: Optional[bool] = None
    """A value determining whether or not the board is starred."""
    subscribed: Optional[bool] = None
    """
    A value determining whether or not the account is subscribed to the board.
    """
    url: Optional[str] = None
    """The full url of the board."""


@dataclass
class BoardWithChecklists:
    """
    Response for Get a board by Id
    """

    id: Optional[str] = None
    """The unique Id of the board."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the board is
    closed.
    """
    date_last_activity: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastActivity"},
    )
    """Date of the last activity on the board."""
    date_last_view: Optional[str] = field(
        default=None,
        metadata={"wire_name": "dateLastView"},
    )
    """Date of the last time the board is viewed."""
    desc: Optional[str] = None
    """The description of the board."""
    id_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idOrganization"},
    )
    """The id of the organization."""
    invitations: Optional[TypingList[str]] = None
    """The list of invitations on the board."""
    invited: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the account is
    invited to the board.
    """
    label_names: Optional[Label] = field(
        default=None,
        metadata={"wire_name": "labelNames"},
    )
    memberships: Optional[TypingList[Membership]] = None
    """The set of board memberships."""
    name: Optional[str] = None
    """The name of the board."""
    pinned: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the board is
    pinned.
    """
    power_ups: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "powerUps"},
    )
    """The set of power ups on the board."""
    perfs: Optional[Perfs] = None
    short_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortLink"},
    )
    """The short link of the board."""
    short_url: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortUrl"},
    )
    """The short url to the board."""
    starred: Optional[bool] = None
    """A value determining whether or not the board is starred."""
    subscribed: Optional[bool] = None
    """
    A value determining whether or not the account is subscribed to the board.
    """
    url: Optional[str] = None
    """The full url of the board."""
    checklists: Optional[TypingList[Checklist]] = None
    """The set of board checklists."""


@dataclass
class List:
    """
    Response for List the card lists in a board
    """

    id: Optional[str] = None
    """The unique Id of the list."""
    name: Optional[str] = None
    """The name of the list."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the list is
    closed.
    """
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board which the list belongs to."""
    pos: Optional[float] = None
    """The position of the list."""
    subscribed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not to the account is
    subscribed to the list.
    """
    cards: Optional[TypingList[Card]] = None
    """The cards inside the list."""
    board: Optional[Board] = None


@dataclass
class CreateListResponse:
    """
    Response for Update a list
    """

    id: Optional[str] = None
    """The unique Id of the list."""
    name: Optional[str] = None
    """The name of the list."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board the list belongs to."""
    pos: Optional[float] = None
    """The position of the list."""


@dataclass
class Team:
    """
    Response for List teams you are a member of
    """

    id: Optional[str] = None
    """The unique ID of the team."""
    desc: Optional[str] = None
    """The description of the team."""
    display_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "displayName"},
    )
    """The display name of the team."""
    id_boards: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idBoards"},
    )
    """The boards assigned to this team."""
    billable_member_count: Optional[int] = field(
        default=None,
        metadata={"wire_name": "billableMemberCount"},
    )
    """The number of members in this team."""
    memberships: Optional[TypingList[Membership]] = None
    """The memberships for this team."""
    web_site: Optional[str] = field(
        default=None,
        metadata={"wire_name": "WebSite"},
    )
    """The team website."""
    prefs: Optional[TeamPreferences] = None


@dataclass
class Member:
    """
    Response for List members of a team
    """

    id: Optional[str] = None
    """The unique Id of the member."""
    full_name: Optional[str] = field(
        default=None,
        metadata={"wire_name": "fullName"},
    )
    """The full name of the member."""
    username: Optional[str] = None
    """The username of the member."""
    initials: Optional[str] = None
    """The initials of the member."""


@dataclass
class BoardLabel:
    """
    Response for List labels of a board
    """

    id: Optional[str] = None
    """The Label Id"""
    name: Optional[str] = None
    """The Label name."""
    color: Optional[str] = None
    """The Label color."""


@dataclass
class Comment:
    """
    Response for List comments for a card
    """

    member_creator: Optional[Member] = field(
        default=None,
        metadata={"wire_name": "memberCreator"},
    )
    text: Optional[str] = None
    """The text of the comment."""
    date: Optional[str] = None
    """The datetime the comment was left."""


@dataclass
class CardInAction:
    """
    Response for When a new card is added to a board
    """

    id: Optional[str] = None
    """The unique id of the card."""
    id_short: Optional[int] = field(
        default=None,
        metadata={"wire_name": "idShort"},
    )
    """The short Id of the card."""
    name: Optional[str] = None
    """The name of the card."""
    short_link: Optional[str] = field(
        default=None,
        metadata={"wire_name": "shortLink"},
    )
    """A short link to the card."""
    due: Optional[str] = None
    """The due date of the card."""


@dataclass
class CheckItemState:
    """
    Definition: CheckItemState
    """

    id_check_item: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idCheckItem"},
    )
    """ID of the check item."""
    state: Optional[str] = None
    """State of the check item."""


@dataclass
class Badges:
    """
    Definition: Badges
    """

    votes: Optional[int] = field(default=None, metadata={"wire_name": "Votes"})
    """The number of votes on the card badges."""
    viewing_member_voted: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "ViewingMemberVoted"},
    )
    """
    A boolean value (true, false) determining whether or not viewing members
    voted on the badge.
    """
    subscribed: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Subscribed"},
    )
    """
    A boolean value (true, false) determining whether or not the account is
    subscribed to the card badges.
    """
    fogbugz: Optional[str] = field(
        default=None,
        metadata={"wire_name": "Fogbugz"},
    )
    """The fogbugz of the badges."""
    check_items: Optional[int] = field(
        default=None,
        metadata={"wire_name": "CheckItems"},
    )
    """The number of check items in the badge."""
    check_items_checked: Optional[int] = field(
        default=None,
        metadata={"wire_name": "CheckItemsChecked"},
    )
    """The number of checked check items in the badge."""
    comments: Optional[int] = field(
        default=None,
        metadata={"wire_name": "Comments"},
    )
    """The comments on the badge."""
    attachments: Optional[int] = field(
        default=None,
        metadata={"wire_name": "Attachments"},
    )
    """The attachments on the badge."""
    description: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "Description"},
    )
    """The description of the badge."""
    due: Optional[str] = field(default=None, metadata={"wire_name": "Due"})
    """The due date of the badge."""


@dataclass
class UpdateCard:
    """
    Definition: UpdateCard
    """

    name: Optional[str] = None
    """The updated name of the new card."""
    desc: Optional[str] = None
    """The updated description of the new card."""
    closed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the card is
    closed.
    """
    id_members_array: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idMembersArray"},
    )
    """An array of member Ids to attach to the card."""
    id_attachment_cover: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idAttachmentCover"},
    )
    """The id of the attachment to attach to the card."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The id of the board to attach the card to."""
    id_list: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idList"},
    )
    """The id of the list to attach the card to."""
    pos: Optional[str] = None
    """
    The position of the card on the page. Specify top, bottom, or a positive
    number. Note this is used in relation to existing card locations so the
    resulting location may not match the exact value provided.
    """
    due: Optional[str] = None
    """The due date of the card."""
    subscribed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not to subscribe to
    the card.
    """


@dataclass
class CreateCard:
    """
    Definition: CreateCard
    """

    id_list: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idList"},
    )
    """The id of the list that the card should be added to."""
    name: Optional[str] = None
    """The name of the new card."""
    desc: Optional[str] = None
    """The description of the new card."""
    pos: Optional[str] = None
    """
    The position of the card. Specify top, bottom, or a positive number. Note
    this is used in relation to existing card locations so the resulting
    location may not match the exact value provided.
    """
    id_members_array: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idMembersArray"},
    )
    """An array of member Ids to attach to the card."""
    id_labels_array: Optional[TypingList[str]] = field(
        default=None,
        metadata={"wire_name": "idLabelsArray"},
    )
    """An array of label Ids to attach to the card."""
    url_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "urlSource"},
    )
    """
    Source url of the card. Has to be a URL starting with http:// or https://
    or null.
    """
    file_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "fileSource"},
    )
    """Source file of the card."""
    id_card_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idCardSource"},
    )
    """The Id of the card to copy into a new card."""
    keep_from_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "keepFromSource"},
    )
    """Properties of the card to copy over from the source."""
    due: Optional[str] = None
    """The due date of the card."""


@dataclass
class Label:
    """
    Definition: Label
    """

    green: Optional[str] = None
    """The value of the green label."""
    yellow: Optional[str] = None
    """The value of the yellow label."""
    orange: Optional[str] = None
    """The value of the orange label."""
    red: Optional[str] = None
    """The value of the red label."""
    purple: Optional[str] = None
    """The value of the purple label."""
    blue: Optional[str] = None
    """The value of the blue label."""
    sky: Optional[str] = None
    """The value of the sky blue label."""
    lime: Optional[str] = None
    """The value of the lime green label."""
    pink: Optional[str] = None
    """The value of the pink label."""
    black: Optional[str] = None
    """The value of the black label."""


@dataclass
class Membership:
    """
    Definition: Membership
    """

    id: Optional[str] = None
    """The unique Id of the membership."""
    id_member: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idMember"},
    )
    """The unique Id of the member."""
    member_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "memberType"},
    )
    """The type of member."""
    unconfirmed: Optional[bool] = None
    """
    A boolean value (true, false) determining whether or not the membership is
    unconfirmed.
    """


@dataclass
class Perfs:
    """
    Definition: Perfs
    """

    permission_level: Optional[str] = field(
        default=None,
        metadata={"wire_name": "permissionLevel"},
    )
    """The permission level of the object."""
    voting: Optional[str] = None
    """Who can vote on the object."""
    comments: Optional[str] = None
    """Who can comment on the object."""
    invitations: Optional[str] = None
    """Who can invite people to the object."""
    self_join: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "selfJoin"},
    )
    """
    A boolean value (true, false) determining whether or not the object can
    self join.
    """
    card_covers: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "cardCovers"},
    )
    """
    A boolean value (true, false) determining whether or not the object has
    access to card covers.
    """
    calendar_feed_enabled: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "calendarFeedEnabled"},
    )
    """
    A boolean value (true, false) determining whether or not the calendar feed
    enabled.
    """
    background: Optional[str] = None
    """The background of the object."""
    background_color: Optional[str] = field(
        default=None,
        metadata={"wire_name": "backgroundColor"},
    )
    """The background color of the object, in hex notation."""
    background_image: Optional[str] = field(
        default=None,
        metadata={"wire_name": "backgroundImage"},
    )
    """The background image of the object."""
    background_image_scaled: Optional[str] = field(
        default=None,
        metadata={"wire_name": "backgroundImageScaled"},
    )
    """The scaled version of the background image."""
    background_tile: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "backgroundTile"},
    )
    """The background tile of the object."""
    background_brightness: Optional[str] = field(
        default=None,
        metadata={"wire_name": "backgroundBrightness"},
    )
    """The brightness of the object background."""
    can_be_public: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canBePublic"},
    )
    """
    A boolean value (true, false) determining whether or not the object can be
    public.
    """
    can_be_org: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canBeOrg"},
    )
    """
    A boolean value (true, false) determining whether or not the object can be
    part of the organization
    """
    can_be_private: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canBePrivate"},
    )
    """
    A boolean value (true, false) determining whether or not the object can be
    private.
    """
    can_invite: Optional[bool] = field(
        default=None,
        metadata={"wire_name": "canInvite"},
    )
    """
    A boolean value (true, false) determining whether or not the object can
    have invititations.
    """


@dataclass
class UpdateBoard:
    """
    Definition: UpdateBoard
    """

    name: Optional[str] = None
    """The name of the board."""
    default_lists: Optional[str] = field(
        default=None,
        metadata={"wire_name": "defaultLists"},
    )
    """
    A string (true, false) determining whether or not to create default lists.
    """
    desc: Optional[str] = None
    """The description of the board."""
    id_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idOrganization"},
    )
    """The id of the team."""
    prefs_permission_level: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_permissionLevel"},
    )
    """The permission level of the board."""
    prefs_comments: Optional[str] = None
    """
    The comment preferences on the board. Valid options are 'disabled',
    'members', 'observers', 'org' or 'public'.
    """
    prefs_invitations: Optional[str] = None
    """
    The invitation preferences of the board. Valid options are 'admins' or
    'members'.
    """
    prefs_card_covers: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_cardCovers"},
    )
    """A string (true, false) determining whether or not to use card covers."""
    prefs_background: Optional[str] = None
    """The background color of the board."""
    prefs_voting: Optional[str] = None
    """The voting power-up preferences of the board."""
    prefs_card_aging: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_cardAging"},
    )
    """The card aging power-up preferences of the board."""
    enable_calendar: Optional[str] = None
    """
    A string (true, false) determining whether or not to enable calendar
    power-up
    """


@dataclass
class TeamPreferences:
    """
    Definition: TeamPreferences
    """

    permission_level: Optional[str] = field(
        default=None,
        metadata={"wire_name": "permissionLevel"},
    )
    """The permission level of the team."""


@dataclass
class Webhook:
    """
    Definition: Webhook
    """

    id: Optional[str] = None
    """The unique Id of the webhook."""
    description: Optional[str] = None
    """Brief Description of the webhook"""
    id_model: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idModel"},
    )
    """ID of the model to be monitored (i.e. card,board, etc.)"""
    callback_u_r_l: Optional[str] = field(
        default=None,
        metadata={"wire_name": "callbackURL"},
    )
    """The callbackURL to receive webhook notifications"""
    active: Optional[bool] = None
    """Determines whether the webhook is active and sending POST requests."""
    consecutive_failures: Optional[float] = field(
        default=None,
        metadata={"wire_name": "consecutiveFailures"},
    )
    """The number of consecutive Failures in posting webhook notifications."""
    first_consecutive_fail_date: Optional[str] = field(
        default=None,
        metadata={"wire_name": "firstConsecutiveFailDate"},
    )
    """The date of First Consecutive Failure."""


@dataclass
class CommentPost:
    """
    Definition: CommentPost
    """

    text: Optional[str] = None
    """The text of the comment."""


@dataclass
class CreateBoard:
    """
    Definition: CreateBoard
    """

    name: Optional[str] = None
    """The name of the board."""
    default_lists: Optional[str] = field(
        default=None,
        metadata={"wire_name": "defaultLists"},
    )
    """
    A string (true, false) determining whether or not to create default lists.
    """
    desc: Optional[str] = None
    """The description of the board."""
    id_organization: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idOrganization"},
    )
    """The id of the team."""
    prefs_permission_level: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_permissionLevel"},
    )
    """
    The permission level of the board. Valid options are 'org', 'private', or
    'public'.
    """
    prefs_comments: Optional[str] = None
    """The comment preferences of the board."""
    prefs_invitations: Optional[str] = None
    """The invitation preferences of the board."""
    prefs_card_covers: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_cardCovers"},
    )
    """A string (true, false) determining whether or not to use card covers."""
    prefs_background: Optional[str] = None
    """The background color of the board."""
    prefs_voting: Optional[str] = None
    """The voting power-up preferences of the board."""
    prefs_card_aging: Optional[str] = field(
        default=None,
        metadata={"wire_name": "prefs_cardAging"},
    )
    """The card aging power-up preferences of the board."""
    enable_calendar: Optional[str] = None
    """
    A string (true, false) determining whether or not to enable calendar
    power-up.
    """


@dataclass
class CreateList:
    """
    Definition: CreateList
    """

    name: Optional[str] = None
    """The name of the list."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board to create the list in."""
    pos: Optional[str] = None
    """The position of the list. Specify top, bottom, or a positive number."""
    id_list_source: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idListSource"},
    )
    """The id of the list to copy into the new list."""


@dataclass
class Checklist:
    """
    Definition: Checklist
    """

    id: Optional[str] = None
    """The unique Id of the checklist."""
    name: Optional[str] = None
    """The name of the checklist."""
    pos: Optional[int] = None
    """The position of the checklist."""
    id_board: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idBoard"},
    )
    """The unique Id of the board."""
    id_card: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idCard"},
    )
    """The unique Id of the card."""
    check_items: Optional[TypingList[Checkitem]] = field(
        default=None,
        metadata={"wire_name": "checkItems"},
    )
    """The list of the checklist items"""


@dataclass
class Checkitem:
    """
    Definition: Checkitem
    """

    id: Optional[str] = None
    """The unique Id of the checkitem."""
    name: Optional[str] = None
    """The name of them checkitem."""
    name_data: Optional[Dict[str, Any]] = field(
        default=None,
        metadata={"wire_name": "nameData"},
    )
    """The nameData of them checkitem."""
    pos: Optional[float] = None
    """The position of the checkitem."""
    state: Optional[str] = None
    """The state of the checkitem."""
    due: Optional[str] = None
    """The due date of the checkitem."""
    id_member: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idMember"},
    )
    """The unique Id of the member."""
    id_checklist: Optional[str] = field(
        default=None,
        metadata={"wire_name": "idChecklist"},
    )
    """The unique Id of the checklist."""


@dataclass
class ClientSubscription:
    """
    Definition: ClientSubscription
    """

    notification_u_r_l: Optional[str] = field(
        default=None,
        metadata={"wire_name": "NotificationURL"},
    )
    """
    Callback url to the flow engine. Expected as part of the request and
    provided by Flow.
    """


@dataclass
class SubscriptionResponse:
    """
    Definition: SubscriptionResponse
    """

    id: Optional[str] = None
    """Id of the subscription"""
    notification_u_r_l: Optional[str] = field(
        default=None,
        metadata={"wire_name": "NotificationURL"},
    )
    """Notification Url"""
    resource: Optional[str] = None
    """Resource of the subscription request"""
    notification_type: Optional[str] = field(
        default=None,
        metadata={"wire_name": "notificationType"},
    )
    """Notification Type"""


@dataclass
class SubscriptionPayload:
    """
    Definition: SubscriptionPayload
    """

    action: Optional[Dict[str, Any]] = None
    """webhook action"""


# Client Class

class TrelloClient(ConnectorClientBase):
    """Typed client for trello connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a TrelloClient.

        Args:
            connection_runtime_url: The connection runtime
                URL from Azure Portal.
            token_provider: Optional token provider.
                Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "trello"

    async def list_cards_async(
        self,
        board_id: str,
        actions: Optional[str] = None,
        attachments: Optional[bool] = None,
        attachment_fields: Optional[str] = None,
        stickers: Optional[bool] = None,
        members: Optional[bool] = None,
        memeber_fields: Optional[str] = None,
        check_item_states: Optional[bool] = None,
        checklists: Optional[str] = None,
        limit: Optional[int] = None,
        since: Optional[str] = None,
        before: Optional[str] = None,
        filter: Optional[str] = None,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List the cards in a board

        List the cards matching the query parameters in a specific board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/cards"
        )
        query_params = []
        if actions is not None:
            value = str(actions)
            if isinstance(actions, bool):
                value = value.lower()
            query_params.append(f"actions={quote(value)}")
        if attachments is not None:
            value = str(attachments)
            if isinstance(attachments, bool):
                value = value.lower()
            query_params.append(f"attachments={quote(value)}")
        if attachment_fields is not None:
            value = str(attachment_fields)
            if isinstance(attachment_fields, bool):
                value = value.lower()
            query_params.append(f"attachment_fields={quote(value)}")
        if stickers is not None:
            value = str(stickers)
            if isinstance(stickers, bool):
                value = value.lower()
            query_params.append(f"stickers={quote(value)}")
        if members is not None:
            value = str(members)
            if isinstance(members, bool):
                value = value.lower()
            query_params.append(f"members={quote(value)}")
        if memeber_fields is not None:
            value = str(memeber_fields)
            if isinstance(memeber_fields, bool):
                value = value.lower()
            query_params.append(f"memeber_fields={quote(value)}")
        if check_item_states is not None:
            value = str(check_item_states)
            if isinstance(check_item_states, bool):
                value = value.lower()
            query_params.append(f"checkItemStates={quote(value)}")
        if checklists is not None:
            value = str(checklists)
            if isinstance(checklists, bool):
                value = value.lower()
            query_params.append(f"checklists={quote(value)}")
        if limit is not None:
            value = str(limit)
            if isinstance(limit, bool):
                value = value.lower()
            query_params.append(f"limit={quote(value)}")
        if since is not None:
            value = str(since)
            if isinstance(since, bool):
                value = value.lower()
            query_params.append(f"since={quote(value)}")
        if before is not None:
            value = str(before)
            if isinstance(before, bool):
                value = value.lower()
            query_params.append(f"before={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"filter={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_cards_simple_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        A simple version of the list cards

        A simple version of the list cards API method used by dynamic values.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/simple/boards/{quote(str(board_id), safe='')}/cards"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_card_async(
        self,
        card_id: str,
        board_id: str,
        actions: Optional[str] = None,
        actions_entities: Optional[bool] = None,
        actions_display: Optional[bool] = None,
        actions_limit: Optional[int] = None,
        action_fields: Optional[str] = None,
        action_member_creator_fields: Optional[str] = None,
        attachments: Optional[bool] = None,
        attachment_fields: Optional[str] = None,
        members: Optional[bool] = None,
        member_fields: Optional[str] = None,
        members_voted: Optional[bool] = None,
        member_voted_fields: Optional[str] = None,
        check_item_states: Optional[bool] = None,
        check_item_state_fields: Optional[str] = None,
        checklists: Optional[str] = None,
        checklist_fields: Optional[str] = None,
        board: Optional[bool] = None,
        board_fields: Optional[str] = None,
        list: Optional[bool] = None,
        list_fields: Optional[str] = None,
        stickers: Optional[bool] = None,
        sticker_fields: Optional[str] = None,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get a card by id

        Get a specific card by its unique id.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if actions is not None:
            value = str(actions)
            if isinstance(actions, bool):
                value = value.lower()
            query_params.append(f"actions={quote(value)}")
        if actions_entities is not None:
            value = str(actions_entities)
            if isinstance(actions_entities, bool):
                value = value.lower()
            query_params.append(f"actions_entities={quote(value)}")
        if actions_display is not None:
            value = str(actions_display)
            if isinstance(actions_display, bool):
                value = value.lower()
            query_params.append(f"actions_display={quote(value)}")
        if actions_limit is not None:
            value = str(actions_limit)
            if isinstance(actions_limit, bool):
                value = value.lower()
            query_params.append(f"actions_limit={quote(value)}")
        if action_fields is not None:
            value = str(action_fields)
            if isinstance(action_fields, bool):
                value = value.lower()
            query_params.append(f"action_fields={quote(value)}")
        if action_member_creator_fields is not None:
            value = str(action_member_creator_fields)
            if isinstance(action_member_creator_fields, bool):
                value = value.lower()
            query_params.append(f"action_memberCreator_fields={quote(value)}")
        if attachments is not None:
            value = str(attachments)
            if isinstance(attachments, bool):
                value = value.lower()
            query_params.append(f"attachments={quote(value)}")
        if attachment_fields is not None:
            value = str(attachment_fields)
            if isinstance(attachment_fields, bool):
                value = value.lower()
            query_params.append(f"attachment_fields={quote(value)}")
        if members is not None:
            value = str(members)
            if isinstance(members, bool):
                value = value.lower()
            query_params.append(f"members={quote(value)}")
        if member_fields is not None:
            value = str(member_fields)
            if isinstance(member_fields, bool):
                value = value.lower()
            query_params.append(f"member_fields={quote(value)}")
        if members_voted is not None:
            value = str(members_voted)
            if isinstance(members_voted, bool):
                value = value.lower()
            query_params.append(f"membersVoted={quote(value)}")
        if member_voted_fields is not None:
            value = str(member_voted_fields)
            if isinstance(member_voted_fields, bool):
                value = value.lower()
            query_params.append(f"memberVoted_fields={quote(value)}")
        if check_item_states is not None:
            value = str(check_item_states)
            if isinstance(check_item_states, bool):
                value = value.lower()
            query_params.append(f"checkItemStates={quote(value)}")
        if check_item_state_fields is not None:
            value = str(check_item_state_fields)
            if isinstance(check_item_state_fields, bool):
                value = value.lower()
            query_params.append(f"checkItemState_fields={quote(value)}")
        if checklists is not None:
            value = str(checklists)
            if isinstance(checklists, bool):
                value = value.lower()
            query_params.append(f"checklists={quote(value)}")
        if checklist_fields is not None:
            value = str(checklist_fields)
            if isinstance(checklist_fields, bool):
                value = value.lower()
            query_params.append(f"checklist_fields={quote(value)}")
        if board is not None:
            value = str(board)
            if isinstance(board, bool):
                value = value.lower()
            query_params.append(f"board={quote(value)}")
        if board_fields is not None:
            value = str(board_fields)
            if isinstance(board_fields, bool):
                value = value.lower()
            query_params.append(f"board_fields={quote(value)}")
        if list is not None:
            value = str(list)
            if isinstance(list, bool):
                value = value.lower()
            query_params.append(f"list={quote(value)}")
        if list_fields is not None:
            value = str(list_fields)
            if isinstance(list_fields, bool):
                value = value.lower()
            query_params.append(f"list_fields={quote(value)}")
        if stickers is not None:
            value = str(stickers)
            if isinstance(stickers, bool):
                value = value.lower()
            query_params.append(f"stickers={quote(value)}")
        if sticker_fields is not None:
            value = str(sticker_fields)
            if isinstance(sticker_fields, bool):
                value = value.lower()
            query_params.append(f"sticker_fields={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def delete_card_async(
        self,
        card_id: str,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Delete a card

        Delete an existing card from the account.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "DELETE", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "DELETE",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_boards_async(
        self,
        filter: Optional[str] = None,
        fields: Optional[str] = None,
        actions: Optional[str] = None,
        actions_entities: Optional[bool] = None,
        actions_limit: Optional[int] = None,
        actions_format: Optional[str] = None,
        actions_since: Optional[str] = None,
        action_fields: Optional[str] = None,
        memberships: Optional[str] = None,
        organization: Optional[bool] = None,
        organization_fields: Optional[str] = None,
        lists: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List boards

        List the boards matching the specified query parameters.
        """
        request_url = f"{self._connection_runtime_url}/member/me/boards"
        query_params = []
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"filter={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if actions is not None:
            value = str(actions)
            if isinstance(actions, bool):
                value = value.lower()
            query_params.append(f"actions={quote(value)}")
        if actions_entities is not None:
            value = str(actions_entities)
            if isinstance(actions_entities, bool):
                value = value.lower()
            query_params.append(f"actions_entities={quote(value)}")
        if actions_limit is not None:
            value = str(actions_limit)
            if isinstance(actions_limit, bool):
                value = value.lower()
            query_params.append(f"actions_limit={quote(value)}")
        if actions_format is not None:
            value = str(actions_format)
            if isinstance(actions_format, bool):
                value = value.lower()
            query_params.append(f"actions_format={quote(value)}")
        if actions_since is not None:
            value = str(actions_since)
            if isinstance(actions_since, bool):
                value = value.lower()
            query_params.append(f"actions_since={quote(value)}")
        if action_fields is not None:
            value = str(action_fields)
            if isinstance(action_fields, bool):
                value = value.lower()
            query_params.append(f"action_fields={quote(value)}")
        if memberships is not None:
            value = str(memberships)
            if isinstance(memberships, bool):
                value = value.lower()
            query_params.append(f"memberships={quote(value)}")
        if organization is not None:
            value = str(organization)
            if isinstance(organization, bool):
                value = value.lower()
            query_params.append(f"organization={quote(value)}")
        if organization_fields is not None:
            value = str(organization_fields)
            if isinstance(organization_fields, bool):
                value = value.lower()
            query_params.append(f"organization_fields={quote(value)}")
        if lists is not None:
            value = str(lists)
            if isinstance(lists, bool):
                value = value.lower()
            query_params.append(f"lists={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_boards_simple_async(
        self,
    ) -> dict[str, Any] | None:
        """
        A simple version of the list boards

        A simple version of the list boards API call with no parameters to be
        used by dynamic values.
        """
        request_url = f"{self._connection_runtime_url}/simple/member/me/boards"

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_board_async(
        self,
        board_id: str,
        actions: Optional[str] = None,
        action_entities: Optional[bool] = None,
        actions_display: Optional[bool] = None,
        actions_format: Optional[str] = None,
        actions_since: Optional[str] = None,
        actions_limit: Optional[int] = None,
        action_fields: Optional[str] = None,
        action_member: Optional[bool] = None,
        action_member_fields: Optional[str] = None,
        action_member_creator: Optional[bool] = None,
        action_member_creator_fields: Optional[str] = None,
        cards: Optional[str] = None,
        card_fields: Optional[str] = None,
        card_attachments: Optional[bool] = None,
        card_attachment_fields: Optional[str] = None,
        card_checklists: Optional[str] = None,
        card_stickers: Optional[bool] = None,
        board_stars: Optional[str] = None,
        labels: Optional[str] = None,
        label_fields: Optional[str] = None,
        labels_limit: Optional[int] = None,
        lists: Optional[str] = None,
        list_fields: Optional[str] = None,
        memberships: Optional[str] = None,
        memberships_member: Optional[bool] = None,
        memberships_member_fields: Optional[str] = None,
        members: Optional[str] = None,
        member_fields: Optional[str] = None,
        members_invited: Optional[str] = None,
        members_invited_fields: Optional[str] = None,
        checklists: Optional[str] = None,
        checklist_fields: Optional[str] = None,
        organization: Optional[bool] = None,
        organization_fields: Optional[str] = None,
        organization_memberships: Optional[str] = None,
        my_perfs: Optional[bool] = None,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get a board by Id

        Get a specific board by its unique identifier.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}"
        )
        query_params = []
        if actions is not None:
            value = str(actions)
            if isinstance(actions, bool):
                value = value.lower()
            query_params.append(f"actions={quote(value)}")
        if action_entities is not None:
            value = str(action_entities)
            if isinstance(action_entities, bool):
                value = value.lower()
            query_params.append(f"action_entities={quote(value)}")
        if actions_display is not None:
            value = str(actions_display)
            if isinstance(actions_display, bool):
                value = value.lower()
            query_params.append(f"actions_display={quote(value)}")
        if actions_format is not None:
            value = str(actions_format)
            if isinstance(actions_format, bool):
                value = value.lower()
            query_params.append(f"actions_format={quote(value)}")
        if actions_since is not None:
            value = str(actions_since)
            if isinstance(actions_since, bool):
                value = value.lower()
            query_params.append(f"actions_since={quote(value)}")
        if actions_limit is not None:
            value = str(actions_limit)
            if isinstance(actions_limit, bool):
                value = value.lower()
            query_params.append(f"actions_limit={quote(value)}")
        if action_fields is not None:
            value = str(action_fields)
            if isinstance(action_fields, bool):
                value = value.lower()
            query_params.append(f"action_fields={quote(value)}")
        if action_member is not None:
            value = str(action_member)
            if isinstance(action_member, bool):
                value = value.lower()
            query_params.append(f"action_member={quote(value)}")
        if action_member_fields is not None:
            value = str(action_member_fields)
            if isinstance(action_member_fields, bool):
                value = value.lower()
            query_params.append(f"action_member_fields={quote(value)}")
        if action_member_creator is not None:
            value = str(action_member_creator)
            if isinstance(action_member_creator, bool):
                value = value.lower()
            query_params.append(f"action_memberCreator={quote(value)}")
        if action_member_creator_fields is not None:
            value = str(action_member_creator_fields)
            if isinstance(action_member_creator_fields, bool):
                value = value.lower()
            query_params.append(f"action_memberCreator_fields={quote(value)}")
        if cards is not None:
            value = str(cards)
            if isinstance(cards, bool):
                value = value.lower()
            query_params.append(f"cards={quote(value)}")
        if card_fields is not None:
            value = str(card_fields)
            if isinstance(card_fields, bool):
                value = value.lower()
            query_params.append(f"card_fields={quote(value)}")
        if card_attachments is not None:
            value = str(card_attachments)
            if isinstance(card_attachments, bool):
                value = value.lower()
            query_params.append(f"card_attachments={quote(value)}")
        if card_attachment_fields is not None:
            value = str(card_attachment_fields)
            if isinstance(card_attachment_fields, bool):
                value = value.lower()
            query_params.append(f"card_attachment_fields={quote(value)}")
        if card_checklists is not None:
            value = str(card_checklists)
            if isinstance(card_checklists, bool):
                value = value.lower()
            query_params.append(f"card_checklists={quote(value)}")
        if card_stickers is not None:
            value = str(card_stickers)
            if isinstance(card_stickers, bool):
                value = value.lower()
            query_params.append(f"card_stickers={quote(value)}")
        if board_stars is not None:
            value = str(board_stars)
            if isinstance(board_stars, bool):
                value = value.lower()
            query_params.append(f"boardStars={quote(value)}")
        if labels is not None:
            value = str(labels)
            if isinstance(labels, bool):
                value = value.lower()
            query_params.append(f"labels={quote(value)}")
        if label_fields is not None:
            value = str(label_fields)
            if isinstance(label_fields, bool):
                value = value.lower()
            query_params.append(f"label_fields={quote(value)}")
        if labels_limit is not None:
            value = str(labels_limit)
            if isinstance(labels_limit, bool):
                value = value.lower()
            query_params.append(f"labels_limit={quote(value)}")
        if lists is not None:
            value = str(lists)
            if isinstance(lists, bool):
                value = value.lower()
            query_params.append(f"lists={quote(value)}")
        if list_fields is not None:
            value = str(list_fields)
            if isinstance(list_fields, bool):
                value = value.lower()
            query_params.append(f"list_fields={quote(value)}")
        if memberships is not None:
            value = str(memberships)
            if isinstance(memberships, bool):
                value = value.lower()
            query_params.append(f"memberships={quote(value)}")
        if memberships_member is not None:
            value = str(memberships_member)
            if isinstance(memberships_member, bool):
                value = value.lower()
            query_params.append(f"memberships_member={quote(value)}")
        if memberships_member_fields is not None:
            value = str(memberships_member_fields)
            if isinstance(memberships_member_fields, bool):
                value = value.lower()
            query_params.append(f"memberships_member_fields={quote(value)}")
        if members is not None:
            value = str(members)
            if isinstance(members, bool):
                value = value.lower()
            query_params.append(f"members={quote(value)}")
        if member_fields is not None:
            value = str(member_fields)
            if isinstance(member_fields, bool):
                value = value.lower()
            query_params.append(f"member_fields={quote(value)}")
        if members_invited is not None:
            value = str(members_invited)
            if isinstance(members_invited, bool):
                value = value.lower()
            query_params.append(f"membersInvited={quote(value)}")
        if members_invited_fields is not None:
            value = str(members_invited_fields)
            if isinstance(members_invited_fields, bool):
                value = value.lower()
            query_params.append(f"membersInvited_fields={quote(value)}")
        if checklists is not None:
            value = str(checklists)
            if isinstance(checklists, bool):
                value = value.lower()
            query_params.append(f"checklists={quote(value)}")
        if checklist_fields is not None:
            value = str(checklist_fields)
            if isinstance(checklist_fields, bool):
                value = value.lower()
            query_params.append(f"checklist_fields={quote(value)}")
        if organization is not None:
            value = str(organization)
            if isinstance(organization, bool):
                value = value.lower()
            query_params.append(f"organization={quote(value)}")
        if organization_fields is not None:
            value = str(organization_fields)
            if isinstance(organization_fields, bool):
                value = value.lower()
            query_params.append(f"organization_fields={quote(value)}")
        if organization_memberships is not None:
            value = str(organization_memberships)
            if isinstance(organization_memberships, bool):
                value = value.lower()
            query_params.append(f"organization_memberships={quote(value)}")
        if my_perfs is not None:
            value = str(my_perfs)
            if isinstance(my_perfs, bool):
                value = value.lower()
            query_params.append(f"myPerfs={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_board_async(
        self,
        input: UpdateBoard,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Update a board

        Update an existing board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_lists_async(
        self,
        board_id: str,
        cards: Optional[str] = None,
        card_fields: Optional[str] = None,
        filter: Optional[str] = None,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        List the card lists in a board

        List the card lists and the cards within the list in a specific board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/lists"
        )
        query_params = []
        if cards is not None:
            value = str(cards)
            if isinstance(cards, bool):
                value = value.lower()
            query_params.append(f"cards={quote(value)}")
        if card_fields is not None:
            value = str(card_fields)
            if isinstance(card_fields, bool):
                value = value.lower()
            query_params.append(f"card_fields={quote(value)}")
        if filter is not None:
            value = str(filter)
            if isinstance(filter, bool):
                value = value.lower()
            query_params.append(f"filter={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_lists_simple_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        A simple version of list lists

        A simple version of list lists to be used by dynamic values.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/simple/boards/{quote(str(board_id), safe='')}/lists"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_list_async(
        self,
        list_id: str,
        board_id: str,
        cards: Optional[str] = None,
        card_fields: Optional[str] = None,
        board: Optional[bool] = None,
        board_fields: Optional[str] = None,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get list by Id

        Get an existing list and the cards in the list by the unique identifier
        of the list.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/lists/{quote(str(list_id), safe='')}"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if cards is not None:
            value = str(cards)
            if isinstance(cards, bool):
                value = value.lower()
            query_params.append(f"cards={quote(value)}")
        if card_fields is not None:
            value = str(card_fields)
            if isinstance(card_fields, bool):
                value = value.lower()
            query_params.append(f"card_fields={quote(value)}")
        if board is not None:
            value = str(board)
            if isinstance(board, bool):
                value = value.lower()
            query_params.append(f"board={quote(value)}")
        if board_fields is not None:
            value = str(board_fields)
            if isinstance(board_fields, bool):
                value = value.lower()
            query_params.append(f"board_fields={quote(value)}")
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_list_async(
        self,
        list_id: str,
        board_id: str,
        name: Optional[str] = None,
        closed: Optional[str] = None,
        id_board: Optional[str] = None,
        pos: Optional[str] = None,
        subscribed: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Update a list

        Update an existing list.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/lists/{quote(str(list_id), safe='')}"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if name is not None:
            value = str(name)
            if isinstance(name, bool):
                value = value.lower()
            query_params.append(f"name={quote(value)}")
        if closed is not None:
            value = str(closed)
            if isinstance(closed, bool):
                value = value.lower()
            query_params.append(f"closed={quote(value)}")
        if id_board is not None:
            value = str(id_board)
            if isinstance(id_board, bool):
                value = value.lower()
            query_params.append(f"idBoard={quote(value)}")
        if pos is not None:
            value = str(pos)
            if isinstance(pos, bool):
                value = value.lower()
            query_params.append(f"pos={quote(value)}")
        if subscribed is not None:
            value = str(subscribed)
            if isinstance(subscribed, bool):
                value = value.lower()
            query_params.append(f"subscribed={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_user_profile_async(
        self,
        fields: Optional[str] = None,
    ) -> dict[str, Any] | None:
        """
        Get your User Profile info

        Get information about your User Profile
        """
        request_url = f"{self._connection_runtime_url}/members/me"
        query_params = []
        if fields is not None:
            value = str(fields)
            if isinstance(fields, bool):
                value = value.lower()
            query_params.append(f"fields={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_teams_async(
        self,
    ) -> dict[str, Any] | None:
        """
        List teams you are a member of

        The list of teams or organizations the account is a member of.
        """
        request_url = (
            f"{self._connection_runtime_url}/members/me/organizations"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_team_members_async(
        self,
        team_id: str,
    ) -> dict[str, Any] | None:
        """
        List members of a team

        List the members in a specific team.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/organizations/{quote(str(team_id), safe='')}/members"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_board_members_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        List members of a board

        List the members of a specific board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/members"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_board_labels_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        List labels of a board

        List the labels of a specific board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/labels"
        )
        query_params = []
        query_params.append("limit=" + quote("1000"))
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def get_team_for_board_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Get the team for a board

        Get the team details for a specific board.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/organization"
        )

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_card_members_async(
        self,
        card_id: str,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        List members for a card

        List the members for a specific card.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}/members"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def list_card_comments_async(
        self,
        card_id: str,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        List comments for a card

        List the comments for a specific card.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}/actions"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "GET", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "GET",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_comment_to_card_async(
        self,
        input: CommentPost,
        card_id: str,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Add a comment to a card

        Add a comment to a specific card.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}/actions/comments"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def add_member_to_card_async(
        self,
        card_id: str,
        board_id: str,
        member_id: str,
    ) -> dict[str, Any] | None:
        """
        Add member to a card

        Add a member to a specific card.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/cards/{quote(str(card_id), safe='')}/idMembers"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        value = str(member_id)
        if isinstance(member_id, bool):
            value = value.lower()
        query_params.append(f"memberId={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_board_async(
        self,
        input: CreateBoard,
    ) -> dict[str, Any] | None:
        """
        Create a board

        Create a new board with the specified fields.
        """
        request_url = f"{self._connection_runtime_url}/boards"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_list_async(
        self,
        input: CreateList,
    ) -> dict[str, Any] | None:
        """
        Create a list

        Create a new list.
        """
        request_url = f"{self._connection_runtime_url}/lists"

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def close_board_async(
        self,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Close a board

        Set a specific board to closed.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/boards/{quote(str(board_id), safe='')}/closed"
        )

        response = await self.http_client.send_async(
            "PUT", request_url, body=None
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def create_card_async(
        self,
        input: CreateCard,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Create a card

        Creates a new card with the parameters provided.
        """
        request_url = f"{self._connection_runtime_url}/v2/cards"
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "POST", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "POST",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)

    async def update_card_async(
        self,
        input: UpdateCard,
        card_id: str,
        board_id: str,
    ) -> dict[str, Any] | None:
        """
        Update a card

        Update an existing card with new parameters.
        """
        request_url = (
            f"{self._connection_runtime_url}"
            f"/v2/cards/{quote(str(card_id), safe='')}"
        )
        query_params = []
        value = str(board_id)
        if isinstance(board_id, bool):
            value = value.lower()
        query_params.append(f"board_id={quote(value)}")
        if query_params:
            request_url += '?' + '&'.join(query_params)

        response = await self.http_client.send_async(
            "PUT", request_url, body=input
        )

        if not (200 <= response.status < 300):
            raise ConnectorException(
                "PUT",
                request_url,
                response.status,
                response.text,
            )

        if not response.text:
            return None

        return json.loads(response.text)


# Trigger Operations
#
# Trigger routes are not callable client methods. Register a trigger with the
# Connector Namespace trigger-config API using the operation id and required
# parameters below; Connector Namespace invokes the callback when the trigger
# fires. When the callback body has a JSON schema, ``callback_payload_type``
# names the generated dataclass to deserialize the callback payload into.
TRIGGER_OPERATIONS: Dict[str, Dict[str, Any]] = {
    "OnNewCardInBoardV3": {
        "operation_id": "OnNewCardInBoardV3",
        "path": "/{connectionId}/v3/trigger/boards/{board_id}/actions",
        "method": "get",
        "required_parameters": ["board_id"],
        "callback_payload_type": "CardInAction",
    },
    "OnNewCardInListV3": {
        "operation_id": "OnNewCardInListV3",
        "path": "/{connectionId}/v3/trigger/lists/{list_id}/actions",
        "method": "get",
        "required_parameters": ["board_id", "list_id"],
        "callback_payload_type": "CardInAction",
    },
}
