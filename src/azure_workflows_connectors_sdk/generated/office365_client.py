# Copyright (c) Microsoft Corporation. All rights reserved.

# office365_client.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from dataclasses import dataclass
from typing import Optional, List, Any, Dict
from datetime import datetime
from urllib.parse import quote

from azure_workflows_connectors_sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class GraphOutlookCategory:
    """Get Outlook category names"""
    
    id: Optional[str] = None
    """The ID of the Outlook Category."""
    
    display_name: Optional[str] = None
    """The display name of the Outlook Category."""


@dataclass
class ClientSendHtmlMessage:
    """Send an email (V2)"""
    
    to: Optional[str] = None
    """Specify email addresses separated by semicolons like someone@contoso.com"""
    
    subject: Optional[str] = None
    """Specify the subject of the mail"""
    
    body: Optional[str] = None
    """Specify the body of the mail"""
    
    from_send_as: Optional[str] = None
    """Email address to send mail from (requires "Send as" or "Send on behalf of" permission for that mailbox)"""
    
    cc: Optional[str] = None
    """Specify email addresses separated by semicolons like someone@contoso.com"""
    
    bcc: Optional[str] = None
    """Specify email addresses separated by semicolons like someone@contoso.com"""
    
    attachments: Optional[List['ClientSendAttachment']] = None
    """Attachments"""
    
    sensitivity: Optional[str] = None
    """Sensitivity"""
    
    reply_to: Optional[str] = None
    """The email addresses to use when replying"""
    
    importance: Optional[str] = None
    """Importance"""


@dataclass
class ClientSendAttachment:
    """Attachment item"""
    
    name: Optional[str] = None
    """Attachment name"""
    
    content: Optional[str] = None
    """Attachment content"""


@dataclass
class GraphClientReceiveMessage:
    """Response for Get email (V2)"""
    
    from_address: Optional[str] = None
    """The mailbox owner and sender of the message"""
    
    to: Optional[str] = None
    """The recipients for the message"""
    
    cc: Optional[str] = None
    """The Cc recipients for the message"""
    
    bcc: Optional[str] = None
    """The Bcc recipients for the message"""
    
    reply_to: Optional[str] = None
    """The email addresses to use when replying"""
    
    subject: Optional[str] = None
    """The subject of the message"""
    
    body: Optional[str] = None
    """The body of the message"""
    
    importance: Optional[str] = None
    """The importance of the message (low, normal, high)"""
    
    body_preview: Optional[str] = None
    """The preview of the message"""
    
    has_attachment: Optional[bool] = None
    """Indicates whether the message has attachments"""
    
    message_id: Optional[str] = None
    """The unique identifier of the message"""
    
    internet_message_id: Optional[str] = None
    """The message ID in the format specified by RFC2822"""
    
    conversation_id: Optional[str] = None
    """The Id of the conversation the email belongs to"""
    
    received_time: Optional[datetime] = None
    """The date and time the message was received"""
    
    is_read: Optional[bool] = None
    """Indicates whether the message has been read"""
    
    attachments: Optional[List['GraphClientReceiveFileAttachment']] = None
    """The file attachments for the message"""
    
    is_html: Optional[bool] = None
    """Is Html?"""


@dataclass
class GraphClientReceiveFileAttachment:
    """File attachment item"""
    
    attachment_id: Optional[str] = None
    """Attachment Id"""
    
    name: Optional[str] = None
    """Attachment name"""
    
    content: Optional[str] = None
    """Attachment content"""
    
    content_type: Optional[str] = None
    """Attachment content type"""
    
    size: Optional[int] = None
    """The size in bytes of the attachment"""
    
    is_inline: Optional[bool] = None
    """Set to true if this is an inline attachment"""
    
    last_modified_date_time: Optional[datetime] = None
    """The date and time when the attachment was last modified"""
    
    content_id: Optional[str] = None
    """Content Id"""


@dataclass
class BatchResponseGraphClientReceiveMessage:
    """Response for Get emails (V3)"""
    
    value: Optional[List[GraphClientReceiveMessage]] = None
    """A list of the response objects"""


@dataclass
class GraphCalendarEventClient:
    """Create/Update calendar event"""
    
    subject: Optional[str] = None
    """Event subject"""
    
    start_time: Optional[str] = None
    """Start time of the event (example: '2017-08-29T04:00:00')"""
    
    end_time: Optional[str] = None
    """End time of the event (example: '2017-08-29T05:00:00')"""
    
    time_zone: Optional[str] = None
    """Time zone of the event"""
    
    required_attendees: Optional[str] = None
    """Required attendees for the event separated by semicolons"""
    
    optional_attendees: Optional[str] = None
    """Optional attendees for the event separated by semicolons"""
    
    resource_attendees: Optional[str] = None
    """Resource attendees for the event separated by semicolons"""
    
    body: Optional[str] = None
    """Body of the message associated with the event"""
    
    location: Optional[str] = None
    """Location of the event"""
    
    importance: Optional[str] = None
    """The importance of the event: low, normal, or high"""
    
    is_all_day_event: Optional[bool] = None
    """Set to true if the event lasts all day"""
    
    recurrence: Optional[str] = None
    """The recurrence pattern for the event: none, daily, weekly, monthly or yearly"""
    
    reminder: Optional[int] = None
    """Time in minutes before event start to remind"""


@dataclass
class GraphCalendarEventClientReceive:
    """Response for calendar event operations"""
    
    subject: Optional[str] = None
    """Event subject"""
    
    start_time: Optional[str] = None
    """Start time of the event"""
    
    end_time: Optional[str] = None
    """End time of the event"""
    
    start_time_with_time_zone: Optional[datetime] = None
    """Start time of the event with time zone"""
    
    end_time_with_time_zone: Optional[datetime] = None
    """End time of the event with time zone"""
    
    body: Optional[str] = None
    """Body of the message associated with the event"""
    
    is_html: Optional[bool] = None
    """Set to true if the body is Html"""
    
    id: Optional[str] = None
    """The event's unique identifier"""
    
    organizer: Optional[str] = None
    """The organizer of the event"""
    
    location: Optional[str] = None
    """Location of the event"""
    
    importance: Optional[str] = None
    """The importance of the event"""


# Client Class

class Office365Client(ConnectorClientBase):
    """Typed client for office365 connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize an Office365Client.

        Args:
            connection_runtime_url: The connection runtime URL from Azure Portal.
            token_provider: Optional token provider. Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "office365"

    async def get_outlook_category_names_async(self):
        """
        Get Outlook category names
        
        This operation gets Outlook category display names.
        """
        path = f"{self._connection_runtime_url}/Categories"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def send_email_v2_async(
        self,
        input: ClientSendHtmlMessage,
    ):
        """
        Send an email (V2)
        
        This operation sends an email message.
        """
        path = f"{self._connection_runtime_url}/v2/Mail"
        
        await self.http_client.send_async("POST", path, body=input)

    async def get_email_v2_async(
        self,
        message_id: str,
        original_mailbox_address: Optional[str] = None,
        include_attachments: Optional[bool] = None,
        internet_message_id: Optional[str] = None,
        extract_sensitivity_label: Optional[bool] = None,
        sensitivity_label_metadata: Optional[bool] = None,
    ):
        """
        Get email (V2)
        
        This operation gets an email by id.
        """
        query_params = []
        if original_mailbox_address is not None:
            query_params.append(f"mailboxAddress={quote(str(original_mailbox_address))}")
        if include_attachments is not None:
            query_params.append(f"includeAttachments={quote(str(include_attachments))}")
        if internet_message_id is not None:
            query_params.append(f"internetMessageId={quote(str(internet_message_id))}")
        if extract_sensitivity_label is not None:
            query_params.append(f"extractSensitivityLabel={quote(str(extract_sensitivity_label))}")
        if sensitivity_label_metadata is not None:
            query_params.append(f"fetchSensitivityLabelMetadata={quote(str(sensitivity_label_metadata))}")
        
        path = f"{self._connection_runtime_url}/v2/Mail/{quote(str(message_id))}"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_emails_v3_async(
        self,
        folder: Optional[str] = None,
        to: Optional[str] = None,
        cc: Optional[str] = None,
        to_or_cc: Optional[str] = None,
        from_address: Optional[str] = None,
        importance: Optional[str] = None,
        only_with_attachments: Optional[bool] = None,
        subject_filter: Optional[str] = None,
        fetch_only_unread_messages: Optional[bool] = None,
        original_mailbox_address: Optional[str] = None,
        include_attachments: Optional[bool] = None,
        search_query: Optional[str] = None,
        top: Optional[int] = None,
    ):
        """
        Get emails (V3)
        
        This operation gets emails from a folder via graph apis.
        """
        query_params = []
        if folder is not None:
            query_params.append(f"folderPath={quote(str(folder))}")
        if to is not None:
            query_params.append(f"to={quote(str(to))}")
        if cc is not None:
            query_params.append(f"cc={quote(str(cc))}")
        if to_or_cc is not None:
            query_params.append(f"toOrCc={quote(str(to_or_cc))}")
        if from_address is not None:
            query_params.append(f"from={quote(str(from_address))}")
        if importance is not None:
            query_params.append(f"importance={quote(str(importance))}")
        if only_with_attachments is not None:
            query_params.append(f"fetchOnlyWithAttachment={quote(str(only_with_attachments))}")
        if subject_filter is not None:
            query_params.append(f"subjectFilter={quote(str(subject_filter))}")
        if fetch_only_unread_messages is not None:
            query_params.append(f"fetchOnlyUnread={quote(str(fetch_only_unread_messages))}")
        if original_mailbox_address is not None:
            query_params.append(f"mailboxAddress={quote(str(original_mailbox_address))}")
        if include_attachments is not None:
            query_params.append(f"includeAttachments={quote(str(include_attachments))}")
        if search_query is not None:
            query_params.append(f"searchQuery={quote(str(search_query))}")
        if top is not None:
            query_params.append(f"top={quote(str(top))}")
        
        path = f"{self._connection_runtime_url}/v3/Mail"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def delete_email_v2_async(
        self,
        message_id: str,
        original_mailbox_address: Optional[str] = None,
    ):
        """
        Delete email (V2)
        
        This operation deletes an email by id.
        """
        query_params = []
        if original_mailbox_address is not None:
            query_params.append(f"mailboxAddress={quote(str(original_mailbox_address))}")
        
        path = f"{self._connection_runtime_url}/codeless/v1.0/me/messages/{quote(str(message_id))}"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        await self.http_client.send_async("DELETE", path)

    async def v4_calendar_get_items_async(
        self,
        calendar_id: str,
        filter_query: Optional[str] = None,
        order_by: Optional[str] = None,
        top_count: Optional[int] = None,
        skip_count: Optional[int] = None,
    ):
        """
        Get events (V4)
        
        This operation gets events from a calendar using Graph API. (V4)
        """
        query_params = []
        if filter_query is not None:
            query_params.append(f"$filter={quote(str(filter_query))}")
        if order_by is not None:
            query_params.append(f"$orderby={quote(str(order_by))}")
        if top_count is not None:
            query_params.append(f"$top={quote(str(top_count))}")
        if skip_count is not None:
            query_params.append(f"$skip={quote(str(skip_count))}")
        
        path = f"{self._connection_runtime_url}/datasets/calendars/v4/tables/{quote(str(calendar_id))}/items"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def v4_calendar_post_item_async(
        self,
        calendar_id: str,
        input: GraphCalendarEventClient,
    ):
        """
        Create event (V4)
        
        This operation creates a new event in a calendar.
        """
        path = f"{self._connection_runtime_url}/datasets/calendars/v4/tables/{quote(str(calendar_id))}/items"
        
        response = await self.http_client.send_async("POST", path, body=input)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def v3_calendar_get_item_async(
        self,
        calendar_id: str,
        item_id: str,
    ):
        """
        Get event (V3)
        
        This operation gets a specific event from a calendar using Graph API. (V3)
        """
        path = f"{self._connection_runtime_url}/datasets/calendars/v3/tables/{quote(str(calendar_id))}/items/{quote(str(item_id))}"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def calendar_delete_item_v2_async(
        self,
        calendar_id: str,
        id: str,
    ):
        """
        Delete event (V2)
        
        This operation deletes an event in a calendar.
        """
        path = f"{self._connection_runtime_url}/codeless/v1.0/me/calendars/{quote(str(calendar_id))}/events/{quote(str(id))}"
        
        await self.http_client.send_async("DELETE", path)
