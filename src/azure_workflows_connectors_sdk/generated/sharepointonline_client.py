# Copyright (c) Microsoft Corporation. All rights reserved.

# sharepointonline_client.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from dataclasses import dataclass
from typing import Optional, List, Any, Dict
from datetime import datetime
from urllib.parse import quote

from azure_workflows_connectors_sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class DataSet:
    """Dataset item"""
    
    name: Optional[str] = None
    """Dataset name"""
    
    display_name: Optional[str] = None
    """Dataset display name"""
    
    query: Optional[List['PassThroughNativeQuery']] = None
    """Pass-through Native Queries"""


@dataclass
class PassThroughNativeQuery:
    """Pass-through query item"""
    
    language: Optional[str] = None
    """Query language"""


@dataclass
class DataSetsList:
    """Response for Get datasets"""
    
    value: Optional[List[DataSet]] = None
    """List of datasets"""


@dataclass
class Table:
    """Table/List item"""
    
    name: Optional[str] = None
    """The name of the table. The name is used at runtime."""
    
    display_name: Optional[str] = None
    """The display name of the table."""
    
    dynamic_properties: Optional[Any] = None
    """Additional table properties provided by the connector to the clients."""


@dataclass
class TablesList:
    """Response for Get all lists and libraries"""
    
    value: Optional[List[Table]] = None
    """List of Tables"""


@dataclass
class BlobMetadata:
    """Response for file operations"""
    
    id: Optional[str] = None
    """The unique id of the file or folder."""
    
    name: Optional[str] = None
    """The name of the file or folder."""
    
    display_name: Optional[str] = None
    """The display name of the file or folder."""
    
    path: Optional[str] = None
    """The path of the file or folder."""
    
    last_modified: Optional[datetime] = None
    """The date and time the file or folder was last modified."""
    
    size: Optional[int] = None
    """The size of the file or folder."""
    
    media_type: Optional[str] = None
    """The media type of the file or folder."""
    
    is_folder: Optional[bool] = None
    """A boolean value (true, false) to indicate whether or not the blob is a folder."""
    
    etag: Optional[str] = None
    """The etag of the file or folder."""
    
    file_locator: Optional[str] = None
    """The filelocator of the file or folder."""


@dataclass
class SPBlobMetadataResponse:
    """Response for Copy file"""
    
    item_id: Optional[int] = None
    """The value that can be used to Get or Update file properties in libraries."""
    
    id: Optional[str] = None
    """The unique id of the file or folder."""
    
    name: Optional[str] = None
    """The name of the file or folder."""
    
    display_name: Optional[str] = None
    """The display name of the file or folder."""
    
    path: Optional[str] = None
    """The path of the file or folder."""
    
    last_modified: Optional[datetime] = None
    """The date and time the file or folder was last modified."""
    
    size: Optional[int] = None
    """The size of the file or folder."""
    
    media_type: Optional[str] = None
    """The media type of the file or folder."""
    
    is_folder: Optional[bool] = None
    """A boolean value (true, false) to indicate whether or not the blob is a folder."""
    
    etag: Optional[str] = None
    """The etag of the file or folder."""
    
    file_locator: Optional[str] = None
    """The filelocator of the file or folder."""


@dataclass
class Item:
    """SharePoint list item"""
    
    item_internal_id: Optional[str] = None
    """Item Internal Id"""


@dataclass
class SPListItemAttachment:
    """SharePoint list item attachment"""
    
    name: Optional[str] = None
    """Name"""
    
    content_type: Optional[str] = None
    """Content Type"""
    
    size: Optional[int] = None
    """Size"""
    
    absolute_uri: Optional[str] = None
    """Absolute URI"""
    
    id: Optional[str] = None
    """Id"""


@dataclass
class SharingLink:
    """Sharing link"""
    
    sharing_link_value: Optional[str] = None
    """A link to the item"""


@dataclass
class SharingLinkPermission:
    """Response for Create sharing link for a file or folder"""
    
    link: Optional[SharingLink] = None
    """link"""


# Client Class

class SharepointonlineClient(ConnectorClientBase):
    """Typed client for sharepointonline connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a SharepointonlineClient.

        Args:
            connection_runtime_url: The connection runtime URL from Azure Portal.
            token_provider: Optional token provider. Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "sharepointonline"

    async def get_datasets_async(self):
        """
        Get datasets
        
        Get datasets from SharePoint
        """
        path = f"{self._connection_runtime_url}/datasets"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_tables_async(
        self,
        dataset: str,
    ):
        """
        Get all lists and libraries
        
        Get SharePoint lists and libraries
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_items_async(
        self,
        dataset: str,
        table: str,
        filter_query: Optional[str] = None,
        order_by: Optional[str] = None,
        top_count: Optional[int] = None,
        skip_count: Optional[int] = None,
    ):
        """
        Get items
        
        Get items from a SharePoint list
        """
        query_params = []
        if filter_query is not None:
            query_params.append(f"$filter={quote(str(filter_query))}")
        if order_by is not None:
            query_params.append(f"$orderby={quote(str(order_by))}")
        if top_count is not None:
            query_params.append(f"$top={quote(str(top_count))}")
        if skip_count is not None:
            query_params.append(f"$skip={quote(str(skip_count))}")
        
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables/{quote(str(table))}/items"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def post_item_async(
        self,
        dataset: str,
        table: str,
        input: Dict[str, Any],
    ):
        """
        Create item
        
        Creates a new item in a SharePoint list
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables/{quote(str(table))}/items"
        
        response = await self.http_client.send_async("POST", path, body=input)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Get item
        
        Get a single item from a SharePoint list
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables/{quote(str(table))}/items/{quote(str(id))}"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def patch_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
        input: Dict[str, Any],
    ):
        """
        Update item
        
        Updates an item in a SharePoint list
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables/{quote(str(table))}/items/{quote(str(id))}"
        
        response = await self.http_client.send_async("PATCH", path, body=input)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"PATCH {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def delete_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Delete item
        
        Deletes an item from a SharePoint list
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/tables/{quote(str(table))}/items/{quote(str(id))}"
        
        await self.http_client.send_async("DELETE", path)

    async def get_file_metadata_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        Get file metadata
        
        Gets the metadata for a file stored in a SharePoint library
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files/{quote(str(id))}"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_file_content_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        Get file content
        
        Gets the content of a file stored in a SharePoint library
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files/{quote(str(id))}/content"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        return response.content

    async def create_file_async(
        self,
        dataset: str,
        folder_path: str,
        name: str,
        input: bytes,
    ):
        """
        Create file
        
        Uploads a file to a SharePoint library
        """
        query_params = []
        query_params.append(f"folderPath={quote(str(folder_path))}")
        query_params.append(f"name={quote(str(name))}")
        
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("POST", path, body=input)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def copy_file_async(
        self,
        dataset: str,
        id: str,
        destination: str,
        overwrite: Optional[bool] = None,
    ):
        """
        Copy file
        
        Copies a file to a SharePoint location
        """
        query_params = []
        query_params.append(f"destination={quote(str(destination))}")
        if overwrite is not None:
            query_params.append(f"overwrite={quote(str(overwrite))}")
        
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files/{quote(str(id))}/copy"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("POST", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def delete_file_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        Delete file
        
        Deletes a file from a SharePoint library
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files/{quote(str(id))}"
        
        await self.http_client.send_async("DELETE", path)

    async def create_sharing_link_async(
        self,
        dataset: str,
        id: str,
        link_type: str,
    ):
        """
        Create sharing link for a file or folder
        
        Creates a sharing link for a file or folder
        """
        query_params = []
        query_params.append(f"linkType={quote(str(link_type))}")
        
        path = f"{self._connection_runtime_url}/datasets/{quote(str(dataset))}/files/{quote(str(id))}/CreateLink"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("POST", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def get_item_attachments_async(
        self,
        site_address: str,
        list_name: str,
        id: str,
    ):
        """
        Get attachments
        
        Returns the list of attachments for the specified list item
        """
        path = f"{self._connection_runtime_url}/datasets/{quote(str(site_address))}/tables/{quote(str(list_name))}/items/{quote(str(id))}/attachments"
        
        response = await self.http_client.send_async("GET", path)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)

    async def create_attachment_async(
        self,
        site_address: str,
        list_name: str,
        id: int,
        input: bytes,
        file_name: str,
    ):
        """
        Add attachment
        
        Adds a new attachment to the specified list item
        """
        query_params = []
        query_params.append(f"fileName={quote(str(file_name))}")
        
        path = f"{self._connection_runtime_url}/datasets/{quote(str(site_address))}/tables/{quote(str(list_name))}/items/{quote(str(id))}/attachments"
        if query_params:
            path += '?' + '&'.join(query_params)
        
        response = await self.http_client.send_async("POST", path, body=input)
        
        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )
        
        if not response.text:
            return None
        
        import json
        return json.loads(response.text)
