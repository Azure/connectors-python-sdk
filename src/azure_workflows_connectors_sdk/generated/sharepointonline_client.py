# Copyright (c) Microsoft Corporation. All rights reserved.

# SharepointonlineClient.py - Auto-generated DirectClient SDK
# Do not edit this file directly.

from dataclasses import dataclass, field
from typing import Optional, List, Any, Dict
from datetime import datetime

from azure_workflows_connectors_sdk import (
    ConnectorClientBase,
    ConnectorClientOptions,
    TokenProvider,
    ManagedIdentityTokenProvider,
    ConnectorException,
)


# Type Definitions

@dataclass
class TablesList:
    """Response for Get all lists and libraries"""

    pass

@dataclass
class ApproveHubSiteJoinResponse:
    """Response for Approve hub site join request"""

    pass

@dataclass
class SharingLinkPermission:
    """Response for Create sharing link for a file or folder"""

    pass

@dataclass
class BlobMetadata:
    """Response for Copy file (deprecated)"""

    pass

@dataclass
class SPBlobMetadataResponse:
    """Response for Copy file"""

    pass

@dataclass
class CreateFileInput:
    """Create file"""

    pass

@dataclass
class UpdateFileInput:
    """Update file"""

    pass

@dataclass
class BlobMetadataResponse:
    """Response for Update file"""

    pass

@dataclass
class CreateNewDocumentSetResponse:
    """Response for Create new document set"""

    pass

@dataclass
class CreateNewFolderResponse:
    """Response for Create new folder"""

    pass

@dataclass
class SPListExpandedUser:
    """Response for Resolve person"""

    pass

@dataclass
class ItemsList:
    """Response for Get files (properties only)"""

    pass

@dataclass
class PostItemInput:
    """Create item"""

    pass

@dataclass
class PostItemResponse:
    """Response for Create item"""

    pass

@dataclass
class GetItemResponse:
    """Response for Get item"""

    pass

@dataclass
class PatchItemInput:
    """Update item"""

    pass

@dataclass
class PatchItemResponse:
    """Response for Update item"""

    pass

@dataclass
class CreateApprovalRequestInput:
    """Create an approval request for an item or file"""

    pass

@dataclass
class ApprovalData:
    """Response for Create an approval request for an item or file"""

    pass

@dataclass
class GetItemChangesResponse:
    """Response for Get changes for an item or a file (properties only)"""

    pass

@dataclass
class Item:
    """Response for Get file properties"""

    pass

@dataclass
class PatchFileItemInput:
    """Update file properties"""

    pass

@dataclass
class PatchFileItemResponse:
    """Response for Update file properties"""

    pass

@dataclass
class SetApprovalStatusOutput:
    """Response for Set content approval status"""

    pass

@dataclass
class SPListItemAttachment:
    """Response for Get attachments"""

    pass

@dataclass
class CreateAttachmentInput:
    """Add attachment"""

    pass

@dataclass
class DeletedItemList:
    """Response for When a file is deleted"""

    pass

@dataclass
class CreateContentAssemblyDocumentInput:
    """Generate document using Microsoft Syntex (preview)"""

    pass

@dataclass
class Table:
    """Response for Get list views"""

    pass

@dataclass
class CreateAgreementsSolutionDocumentInput:
    """Agreements Solution - Generate document within Agreements Solution workspace"""

    pass

@dataclass
class ApplicationPermissionIdentity:
    """Definition: ApplicationPermissionIdentity"""

    display_name: Optional[str] = None
    """Application display name"""

@dataclass
class BlobDataSetsMetadata:
    """Definition: BlobDataSetsMetadata"""

    source: Optional[str] = None
    """Blob dataset source"""
    display_name: Optional[str] = None
    """Blob dataset display name"""
    url_encoding: Optional[str] = None
    """Blob dataset url encoding"""

@dataclass
class BlobMetadataPage:
    """Definition: BlobMetadataPage"""

    value: Optional[List[BlobMetadata]] = None
    """Blob metadata collection."""

@dataclass
class CopyFileParameters:
    """Definition: CopyFileParameters"""

    source_file_id: Optional[str] = None
    """File Identifier"""
    destination_dataset: Optional[str] = None
    """Example: https://contoso.sharepoint.com/sites/sitename"""
    destination_folder_path: Optional[str] = None
    """Destination Folder"""
    name_conflict_behavior: Optional[int] = None
    """Pick one of the options available"""

@dataclass
class CopyFolderParameters:
    """Definition: CopyFolderParameters"""

    source_folder_id: Optional[str] = None
    """File Identifier"""
    destination_dataset: Optional[str] = None
    """Example: https://contoso.sharepoint.com/sites/sitename"""
    destination_folder_path: Optional[str] = None
    """Destination Folder"""
    name_conflict_behavior: Optional[int] = None
    """Pick one of the options available"""

@dataclass
class CreateNewDocumentSetParameters:
    """Definition: CreateNewDocumentSetParameters"""

    path: Optional[str] = None
    """Example: folder1/folder2/dsName"""
    content_type_id: Optional[str] = None
    """Example: 0x0120D520"""
    dynamic_properties: Optional[Dict[str, Any]] = None

@dataclass
class CreateNewFolderParameters:
    """Definition: CreateNewFolderParameters"""

    path: Optional[str] = None
    """Example: folder1/folder2/folder3"""

@dataclass
class DataSet:
    """Definition: DataSet"""

    name: Optional[str] = None
    """Dataset name"""
    display_name: Optional[str] = None
    """Dataset display name"""
    query: Optional[List[PassThroughNativeQuery]] = None
    """Pass-through Native Queries"""

@dataclass
class DataSetsList:
    """Definition: DataSetsList"""

    value: Optional[List[DataSet]] = None
    """List of datasets"""

@dataclass
class DataSetsMetadata:
    """Definition: DataSetsMetadata"""

    tabular: Optional[TabularDataSetsMetadata] = None
    blob: Optional[BlobDataSetsMetadata] = None

@dataclass
class DeletedItem:
    """Definition: DeletedItem"""

    i_d: Optional[int] = None
    """List item id"""
    name: Optional[str] = None
    """File name of the item in document libraries, display name of the item in lists"""
    file_name_with_extension: Optional[str] = None
    """File name with extension of the item in document libraries, same as Name of the item in lists"""
    deleted_by_user_name: Optional[str] = None
    """The name of the user who deleted this item"""
    time_deleted: Optional[str] = None
    """When this item was deleted"""
    is_folder: Optional[bool] = None
    """A true/false value to indicate if the item is a folder"""

@dataclass
class FileCheckInParameters:
    """Definition: FileCheckInParameters"""

    comment: Optional[str] = None
    """Type comments describing what has changed in this version"""
    checkin_type: Optional[int] = None
    """Select the type of version you would like to check in"""

@dataclass
class GetItemChangesMetadataResponse:
    """Definition: GetItemChangesMetadataResponse"""

    schema: Optional[ObjectEntity] = None

@dataclass
class GroupPermissionIdentity:
    """Definition: GroupPermissionIdentity"""

    display_name: Optional[str] = None
    """Group display name"""

@dataclass
class HubSiteJoinApprovalOutput:
    """Definition: HubSiteJoinApprovalOutput"""

    requesting_site_url: Optional[str] = None
    """Requesting Site Url"""
    requesting_site_id: Optional[str] = None
    """Requesting Site Id"""
    requesting_site_title: Optional[str] = None
    """Requesting Site Title"""
    approval_correlation_id: Optional[str] = None
    """Approval Correlation Id"""

@dataclass
class ItemGrantAccessBody:
    """Definition: ItemGrantAccessBody"""

    recipients: Optional[str] = None
    """A collection of recipients who will receive the sharing invitation"""
    role_value: Optional[str] = None
    """Specify a role that is to be granted to the recipients"""
    email_body: Optional[str] = None
    """A plain text formatted message that is included in the sharing invitation"""
    send_email: Optional[bool] = None
    """Specify whether recipients should receive an email notification message"""

@dataclass
class ItemPermissionAddBody:
    """Definition: ItemPermissionAddBody"""

    recipients: Optional[List[ItemPermissionRecipient]] = None
    """A collection of recipients who will receive access and the sharing invitation."""
    roles: Optional[List[str]] = None
    """Specify the roles that are be granted to the recipients of the sharing invitation."""
    require_sign_in: Optional[bool] = None
    """Specifies where the recipient of the invitation is required to sign-in to view the shared item."""
    send_invitation: Optional[bool] = None
    """Specifies if an email or post is generated (true) or if the permission is just created (false)."""
    message: Optional[str] = None
    """A plain text formatted message that is included in the sharing invitation. Maximum length 2000 characters."""

@dataclass
class ItemPermissionCreateLinkBody:
    """Definition: ItemPermissionCreateLinkBody"""

    type: Optional[str] = None
    """The type of sharing link to create"""
    scope: Optional[str] = None
    """Choose who your sharing link gives access to. \"Anyone\" option will only work if your administrator has enabled it."""
    expiration_date_time: Optional[str] = None
    """The date after which the link will expire in yyyy-MM-dd format. Only applicable for anonymous links."""

@dataclass
class ItemPermissionRecipient:
    """Definition: ItemPermissionRecipient"""

    email: Optional[str] = None
    """The email address for the recipient, if the recipient has an associated email address."""

@dataclass
class ItemPermissionUpdateBody:
    """Definition: ItemPermissionUpdateBody"""

    roles: Optional[List[str]] = None
    """Specify updated roles for this permission."""

@dataclass
class ItemReference:
    """Definition: ItemReference"""

    name: Optional[str] = None
    """The name of the parent item"""
    path: Optional[str] = None
    """Path that can be used to navigate to the parent item"""
    sharepoint_ids: Optional[SharePointIds] = None

@dataclass
class MoveFileParameters:
    """Definition: MoveFileParameters"""

    source_file_id: Optional[str] = None
    """File Identifier"""
    destination_dataset: Optional[str] = None
    """Example: https://contoso.sharepoint.com/sites/sitename"""
    destination_folder_path: Optional[str] = None
    """Destination Folder"""
    name_conflict_behavior: Optional[int] = None
    """Pick one of the options available"""

@dataclass
class MoveFolderParameters:
    """Definition: MoveFolderParameters"""

    source_folder_id: Optional[str] = None
    """File Identifier"""
    destination_dataset: Optional[str] = None
    """Example: https://contoso.sharepoint.com/sites/sitename"""
    destination_folder_path: Optional[str] = None
    """Destination Folder"""
    name_conflict_behavior: Optional[int] = None
    """Pick one of the options available"""

@dataclass
class ObjectEntity:
    """Definition: Object"""

    pass

@dataclass
class PassThroughNativeQuery:
    """Definition: PassThroughNativeQuery"""

    language: Optional[str] = None
    """Query language"""

@dataclass
class PatchFileItemWithPredictedValuesParameters:
    """Definition: PatchFileItemWithPredictedValuesParameters"""

    model_id: Optional[str] = None
    """Enter modelId of the Model which is used for prediction"""
    predict_result: Optional[str] = None
    """Enter request content in JSON"""

@dataclass
class Permission:
    """Definition: Permission"""

    id: Optional[str] = None
    """Permission id"""
    granted_to: Optional[PermissionIdentitySet] = None
    granted_to_identities: Optional[List[SharingLinkPermissionIdentitySet]] = None
    """For link type permissions, the details of the users to whom permission was granted"""
    invitation: Optional[SharingInvitation] = None
    inherited_from: Optional[ItemReference] = None
    link: Optional[SharingLink] = None
    roles: Optional[List[str]] = None
    """The type of permission - read, write, owner, member"""

@dataclass
class PermissionIdentitySet:
    """Definition: PermissionIdentitySet"""

    application: Optional[ApplicationPermissionIdentity] = None
    group: Optional[GroupPermissionIdentity] = None
    user: Optional[UserPermissionIdentity] = None

@dataclass
class PermissionsList:
    """Definition: PermissionsList"""

    value: Optional[List[Permission]] = None
    """List of Permissions"""

@dataclass
class Procedure:
    """Definition: Procedure"""

    name: Optional[str] = None
    """Procedure name"""
    display_name: Optional[str] = None
    """Procedure display name"""

@dataclass
class PublishedResult:
    """Definition: PublishedResult"""

    is_file_published: Optional[bool] = None
    """A boolean value (true, false) to indicate whether the scheduled version of the file has been published"""

@dataclass
class SharePointHttpRequestBodyParameters:
    """Definition: SharePointHttpRequestBodyParameters"""

    method: Optional[str] = None
    """Http Method"""
    uri: Optional[str] = None
    """Example: _api/web/lists/getbytitle('Documents')"""
    headers: Optional[Dict[str, Any]] = None
    """Enter JSON object of request headers"""
    body: Optional[str] = None
    """Enter request content in JSON"""

@dataclass
class SharePointIds:
    """Definition: SharePointIds"""

    list_id: Optional[str] = None
    """The unique identifier (guid) for the item's list in SharePoint"""
    list_item_id: Optional[str] = None
    """An integer identifier for the item within the containing list"""
    list_item_unique_id: Optional[str] = None
    """The unique identifier (guid) for the item within OneDrive for Business or a SharePoint site"""
    site_id: Optional[str] = None
    """The unique identifier (guid) for the item's site collection (SPSite)"""
    site_url: Optional[str] = None
    """The SharePoint URL for the site that contains the item"""
    tenant_id: Optional[str] = None
    """The unique identifier (guid) for the tenancy"""
    web_id: Optional[str] = None
    """The unique identifier (guid) for the item's site (SPWeb)"""

@dataclass
class SharingInvitation:
    """Definition: SharingInvitation"""

    email: Optional[str] = None
    """The email address provided for the recipient of the sharing invitation"""
    sign_in_required: Optional[bool] = None
    """If true the recipient of the invitation needs to sign in in order to access the shared item"""

@dataclass
class SharingLink:
    """Definition: SharingLink"""

    web_url: Optional[str] = None
    """A link to the item"""

@dataclass
class SharingLinkPermissionIdentitySet:
    """Definition: SharingLinkPermissionIdentitySet"""

    user: Optional[SharingLinkUserPermissionIdentity] = None

@dataclass
class SharingLinkUserPermissionIdentity:
    """Definition: SharingLinkUserPermissionIdentity"""

    display_name: Optional[str] = None
    """User display name"""
    email: Optional[str] = None
    """User email"""

@dataclass
class SPContentType:
    """Definition: SPContentType"""

    id: Optional[str] = None
    """Content type Id"""
    name: Optional[str] = None
    """Content type name"""

@dataclass
class SPForASelectedFileResponse:
    """Definition: SPForASelectedFileResponse"""

    i_d: Optional[int] = None
    """File Identifier"""
    item_url: Optional[str] = None
    """File Url"""
    file_name: Optional[str] = None
    """File Name"""

@dataclass
class SPListEntity:
    """Definition: SPListEntity"""

    id: Optional[str] = None
    """The Id of the SPField"""
    entity_type: Optional[str] = None
    """What type of entity (field) this is"""

@dataclass
class TableCapabilitiesMetadata:
    """Definition: TableCapabilitiesMetadata"""

    sort_restrictions: Optional[TableSortRestrictionsMetadata] = None
    filter_restrictions: Optional[TableFilterRestrictionsMetadata] = None
    select_restrictions: Optional[TableSelectRestrictionsMetadata] = None
    is_only_server_pagable: Optional[bool] = None
    """Server paging restrictions"""
    filter_function_support: Optional[List[str]] = None
    """List of supported filter capabilities"""
    server_paging_options: Optional[List[str]] = None
    """List of supported server-driven paging capabilities"""

@dataclass
class TableFilterRestrictionsMetadata:
    """Definition: TableFilterRestrictionsMetadata"""

    filterable: Optional[bool] = None
    """Indicates whether this table has filterable columns"""
    non_filterable_properties: Optional[List[str]] = None
    """List of non filterable properties"""
    required_properties: Optional[List[str]] = None
    """List of required properties"""

@dataclass
class TableMetadata:
    """Definition: TableMetadata"""

    name: Optional[str] = None
    """Table name"""
    title: Optional[str] = None
    """Table title"""
    x_ms_permission: Optional[str] = None
    """Table permission"""
    x_ms_capabilities: Optional[TableCapabilitiesMetadata] = None
    schema: Optional[ObjectEntity] = None
    referenced_entities: Optional[ObjectEntity] = None
    web_url: Optional[str] = None
    """Url link"""

@dataclass
class TableSelectRestrictionsMetadata:
    """Definition: TableSelectRestrictionsMetadata"""

    selectable: Optional[bool] = None
    """Indicates whether this table has selectable columns"""

@dataclass
class TableSortRestrictionsMetadata:
    """Definition: TableSortRestrictionsMetadata"""

    sortable: Optional[bool] = None
    """Indicates whether this table has sortable columns"""
    unsortable_properties: Optional[List[str]] = None
    """List of unsortable properties"""
    ascending_only_properties: Optional[List[str]] = None
    """List of properties which support ascending order only"""

@dataclass
class TabularDataSetsMetadata:
    """Definition: TabularDataSetsMetadata"""

    source: Optional[str] = None
    """Dataset source"""
    display_name: Optional[str] = None
    """Dataset display name"""
    url_encoding: Optional[str] = None
    """Dataset url encoding"""
    table_display_name: Optional[str] = None
    """Table display name"""
    table_plural_name: Optional[str] = None
    """Table plural display name"""

@dataclass
class UserPermissionIdentity:
    """Definition: UserPermissionIdentity"""

    display_name: Optional[str] = None
    """User display name"""
    email: Optional[str] = None
    """User email"""


# Client Class

class SharepointonlineClient(ConnectorClientBase):
    """Typed client for sharepointonline connector."""

    def __init__(
        self,
        connection_runtime_url: str,
        token_provider: Optional[TokenProvider] = None,
        options: Optional[ConnectorClientOptions] = None,
    ):
        """
        Initialize a SharepointonlineClient.

        Args:
            connection_runtime_url: The connection runtime URL from Azure Portal.
            token_provider: Optional token provider. Defaults to ManagedIdentityTokenProvider.
            options: Optional connector client options.
        """
        if not connection_runtime_url:
            raise ValueError("connection_runtime_url cannot be None or empty")

        if token_provider is None:
            token_provider = ManagedIdentityTokenProvider()

        super().__init__(token_provider, options)
        self._connection_runtime_url = connection_runtime_url.rstrip('/')

    @property
    def connector_name(self) -> str:
        return "sharepointonline"

    async def get_all_tables_async(
        self,
        dataset: str,
    ):
        """
        Get all lists and libraries

        Get all lists and libraries.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/alltables"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def approve_hub_site_join_async(
        self,
        dataset: str,
        joining_site_id: Optional[str],
    ):
        """
        Approve hub site join request

        Approve hub site join request. This will return an approval token that can be used to complete the join request using the join hub site action.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/approvehubsitejoin"
        query_params = []
        if joining_site_id is not None:
            query_params.append(f"joiningSiteId={joining_site_id}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def cancel_hub_site_join_approval_async(
        self,
        dataset: str,
        approval_correlation_id: Optional[str] = None,
    ):
        """
        Cancel hub site join request

        Cancel hub join request. If applicable, you should specify the same Approval Correlation Id as used in the \"Set hub site join status to pending\" action.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/cancelhubsitejoinapproval"
        query_params = []
        if approval_correlation_id is not None:
            query_params.append(f"approvalCorrelationId={approval_correlation_id}")
        if query_params:
            path += '?' + '&'.join(query_params)

        await self.http_client.send_async("POST", path, body=None)

    async def create_sharing_link_async(
        self,
        input: ItemPermissionCreateLinkBody,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Create sharing link for a file or folder

        Create sharing link for a file or folder.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/codeless/_api/v2.0/sites/root/lists/{{quote(str(table))}}/items/{{quote(str(id))}}/driveItem/createLink"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def copy_file_async(
        self,
        dataset: str,
        source: Optional[str],
        destination: Optional[str],
        overwrite: Optional[str] = None,
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Copy file (deprecated)

        Copies a file to a SharePoint site.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/copyFile"
        query_params = []
        if source is not None:
            query_params.append(f"source={source}")
        if destination is not None:
            query_params.append(f"destination={destination}")
        if overwrite is not None:
            query_params.append(f"overwrite={overwrite}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def copy_file_async_async(
        self,
        input: CopyFileParameters,
        dataset: str,
    ):
        """
        Copy file

        Copies a file. Works in a similar way to the \"Copy to\" command in SharePoint libraries. Returns information about the new file after copy.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/copyFileAsync"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def copy_folder_async_async(
        self,
        input: CopyFolderParameters,
        dataset: str,
    ):
        """
        Copy folder

        Copies a folder. Works in a similar way to the \"Copy to\" command in SharePoint libraries. Returns information about the new folder after copy.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/copyFolderAsync"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def extract_folder_v2_async(
        self,
        dataset: str,
        source: Optional[str],
        destination: Optional[str],
        overwrite: Optional[str] = None,
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Extract folder

        Extracts an archive file into a SharePoint folder (example: .zip).
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/extractFolderV2"
        query_params = []
        if source is not None:
            query_params.append(f"source={source}")
        if destination is not None:
            query_params.append(f"destination={destination}")
        if overwrite is not None:
            query_params.append(f"overwrite={overwrite}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_file_async(
        self,
        input: CreateFileInput,
        dataset: str,
        folder_path: Optional[str],
        name: Optional[str],
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Create file

        Uploads a file to a SharePoint site. Make sure to pick an existing library.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/files"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if name is not None:
            query_params.append(f"name={name}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_file_metadata_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        Get file metadata

        Gets information about the file such as size, etag, created date, etc. Uses a file identifier to pick the file. Use \"Get file properties\" action to get to the values stored in the columns in the library.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/files/{{quote(str(id))}}"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def update_file_async(
        self,
        input: UpdateFileInput,
        dataset: str,
        id: str,
    ):
        """
        Update file

        Updates the contents of the file specified by the file identifier.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/files/{{quote(str(id))}}"

        response = await self.http_client.send_async("PUT", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"PUT {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def delete_file_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        Delete file

        Deletes the file specified by the file identifier.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/files/{{quote(str(id))}}"

        await self.http_client.send_async("DELETE", path, body=None)

    async def get_file_content_async(
        self,
        dataset: str,
        id: str,
        infer_content_type: Optional[str] = None,
    ):
        """
        Get file content

        Gets file contents using the file identifier. The contents can be copied somewhere else, or be used as an attachment.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/files/{{quote(str(id))}}/content"
        query_params = []
        if infer_content_type is not None:
            query_params.append(f"inferContentType={infer_content_type}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        return response.content

    async def list_root_folder_async(
        self,
        dataset: str,
    ):
        """
        List root folder

        Returns files in the root SharePoint folder.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/folders"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def list_folder_async(
        self,
        dataset: str,
        id: str,
    ):
        """
        List folder

        Returns files contained in a SharePoint folder.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/folders/{{quote(str(id))}}"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_file_metadata_by_path_async(
        self,
        dataset: str,
        path: Optional[str],
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Get file metadata using path

        Gets information about the file such as size, etag, created date, etc. Uses a file path to pick the file. Use \"Get file properties\" action to get to the values stored in the columns in the library.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/GetFileByPath"
        query_params = []
        if path is not None:
            query_params.append(f"path={path}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_file_content_by_path_async(
        self,
        dataset: str,
        path: Optional[str],
        infer_content_type: Optional[str] = None,
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Get file content using path

        Gets file contents using the file path.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/GetFileContentByPath"
        query_params = []
        if path is not None:
            query_params.append(f"path={path}")
        if infer_content_type is not None:
            query_params.append(f"inferContentType={infer_content_type}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        return response.content

    async def get_folder_metadata_async(
        self,
        dataset: str,
        id: Optional[str],
    ):
        """
        Get folder metadata

        Gets information about the folder. Uses a file identifier to pick the folder.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/GetFolder"
        query_params = []
        if id is not None:
            query_params.append(f"id={id}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_folder_metadata_by_path_async(
        self,
        dataset: str,
        path: Optional[str],
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        Get folder metadata using path

        Gets information about the folder. Uses a folder path to pick the folder.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/GetFolderByPath"
        query_params = []
        if path is not None:
            query_params.append(f"path={path}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def http_request_async(
        self,
        input: SharePointHttpRequestBodyParameters,
        dataset: str,
    ):
        """
        Send an HTTP request to SharePoint

        Construct a SharePoint REST API to invoke. Note - This action may execute any SharePoint REST API you have access to. Please proceed with caution.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/httprequest"

        await self.http_client.send_async("POST", path, body=input)

    async def join_hub_site_async(
        self,
        dataset: str,
        hub_site_id: Optional[str],
        approval_token: Optional[str] = None,
        approval_correlation_id: Optional[str] = None,
    ):
        """
        Join hub site

        Join the requested site to the hub site. An Approval Token is required to complete the join successfully if that hub requires approval. If applicable, you should specify the same Approval Correlation Id as used in the \"Set hub site join status to pending\" action.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/joinhubsite"
        query_params = []
        if hub_site_id is not None:
            query_params.append(f"hubSiteId={hub_site_id}")
        if approval_token is not None:
            query_params.append(f"approvalToken={approval_token}")
        if approval_correlation_id is not None:
            query_params.append(f"approvalCorrelationId={approval_correlation_id}")
        if query_params:
            path += '?' + '&'.join(query_params)

        await self.http_client.send_async("POST", path, body=None)

    async def move_file_async_async(
        self,
        input: MoveFileParameters,
        dataset: str,
    ):
        """
        Move file

        Moves a file. Works in a similar way to the \"Move to\" command in SharePoint libraries. Returns information about the new file after move.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/moveFileAsync"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def move_folder_async_async(
        self,
        input: MoveFolderParameters,
        dataset: str,
    ):
        """
        Move folder

        Moves a folder. Works in a similar way to the \"Move to\" command in SharePoint libraries. Returns information about the new folder after move.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/moveFolderAsync"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def notify_hub_site_join_approval_started_async(
        self,
        dataset: str,
        approval_correlation_id: Optional[str] = None,
    ):
        """
        Set hub site join status to pending

        Set the requested site's hub join request status to pending. The Approval Correlation Id is an optional parameter that helps SharePoint identify a particular hub join request. The requesting site can only have one pending request at a given time.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/notifyhubsitejoinapprovalstarted"
        query_params = []
        if approval_correlation_id is not None:
            query_params.append(f"approvalCorrelationId={approval_correlation_id}")
        if query_params:
            path += '?' + '&'.join(query_params)

        await self.http_client.send_async("POST", path, body=None)

    async def get_tables_async(
        self,
        dataset: str,
    ):
        """
        Get lists

        Gets SharePoint lists from a site.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_new_document_set_async(
        self,
        input: CreateNewDocumentSetParameters,
        dataset: str,
        table: str,
    ):
        """
        Create new document set

        Creates a new document set list item.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/createnewdocumentset"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_new_folder_async(
        self,
        input: CreateNewFolderParameters,
        dataset: str,
        table: str,
        view: Optional[str] = None,
    ):
        """
        Create new folder

        Creates a new folder or folder path.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/createnewfolder"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def search_for_user_async(
        self,
        dataset: str,
        table: str,
        entity_id: str,
        search_value: Optional[str],
        view: Optional[str] = None,
    ):
        """
        Resolve person

        Returns a single matching user value so it can be assigned to a column of type person. If there are no matches, or multiple matches, this action will error out.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/entities/{{quote(str(entity_id))}}/searchforuser"
        query_params = []
        if search_value is not None:
            query_params.append(f"searchValue={search_value}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_file_items_async(
        self,
        dataset: str,
        table: str,
        filter: Optional[str] = None,
        orderby: Optional[str] = None,
        top: Optional[str] = None,
        folder_path: Optional[str] = None,
        view_scope_option: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        Get files (properties only)

        Gets the properties saved in the columns in the library for all folders and files stored in the library.             You can also filter down to the items that match a condition. An \"Apply to each\" section is usually used to work with the output from this action.             When using this with the On-Premises Data Gateway, the name of the library to connect to may need to be entered manually.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/getfileitems"
        query_params = []
        if filter is not None:
            query_params.append(f"$filter={filter}")
        if orderby is not None:
            query_params.append(f"$orderby={orderby}")
        if top is not None:
            query_params.append(f"$top={top}")
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view_scope_option is not None:
            query_params.append(f"viewScopeOption={view_scope_option}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_items_async(
        self,
        dataset: str,
        table: str,
        filter: Optional[str] = None,
        orderby: Optional[str] = None,
        top: Optional[str] = None,
        folder_path: Optional[str] = None,
        view_scope_option: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        Get items

        Gets items from a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items"
        query_params = []
        if filter is not None:
            query_params.append(f"$filter={filter}")
        if orderby is not None:
            query_params.append(f"$orderby={orderby}")
        if top is not None:
            query_params.append(f"$top={top}")
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view_scope_option is not None:
            query_params.append(f"viewScopeOption={view_scope_option}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def post_item_async(
        self,
        input: PostItemInput,
        dataset: str,
        table: str,
        view: Optional[str] = None,
    ):
        """
        Create item

        Creates a new item in a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
        view: Optional[str] = None,
    ):
        """
        Get item

        Gets a single item by its id from a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def delete_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Delete item

        Deletes an item from a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}"

        await self.http_client.send_async("DELETE", path, body=None)

    async def patch_item_async(
        self,
        input: PatchItemInput,
        dataset: str,
        table: str,
        id: str,
        view: Optional[str] = None,
    ):
        """
        Update item

        Updates an item in a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("PATCH", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"PATCH {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_approval_request_async(
        self,
        input: CreateApprovalRequestInput,
        dataset: str,
        table: str,
        id: str,
        approval_type: Optional[str],
    ):
        """
        Create an approval request for an item or file

        Creates an approval request for an item or file.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/approval"
        query_params = []
        if approval_type is not None:
            query_params.append(f"approvalType={approval_type}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_item_changes_async(
        self,
        dataset: str,
        table: str,
        id: str,
        since: Optional[str],
        until: Optional[str] = None,
        include_drafts: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        Get changes for an item or a file (properties only)

        Returns information about columns that have changed within a given time window. Note: The list must have Versioning turned on.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/changes"
        query_params = []
        if since is not None:
            query_params.append(f"since={since}")
        if until is not None:
            query_params.append(f"until={until}")
        if include_drafts is not None:
            query_params.append(f"includeDrafts={include_drafts}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def check_in_file_async(
        self,
        input: FileCheckInParameters,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Check in file

        Check in a checked out file in a document library, which makes the version of the document available to others.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/checkinfile"

        await self.http_client.send_async("POST", path, body=input)

    async def check_out_file_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Check out file

        Check out a file in a document library to prevent others from editing the document, and your changes from being visible until the documented is checked in.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/checkoutfile"

        await self.http_client.send_async("POST", path, body=None)

    async def discard_file_check_out_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Discard check out

        If you check out a file and don't make changes to it, or you make changes that you don't want to keep, you can simply discard the checkout, rather than saving the file. If your organization tracks versions, a new version is created each time you check a file back into the library. By discarding the checkout, you can avoid making new versions when you haven't made any changes to the file.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/discardfilecheckout"

        await self.http_client.send_async("POST", path, body=None)

    async def get_file_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
        view: Optional[str] = None,
    ):
        """
        Get file properties

        Gets the properties saved in the columns in the library for the item specified by the item id.             You can add a \"Get file content\" step and use the \"File identifier\" property returned by this action to get to the contents of the file.             When using this with the On-Premises Data Gateway, the name of the library to connect to may need to be entered manually.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/getfileitem"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def grant_access_async(
        self,
        input: ItemGrantAccessBody,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Grant access to an item or a folder

        Grant access to an item or a folder in SharePoint to specific people.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/grantaccess"

        await self.http_client.send_async("POST", path, body=input)

    async def patch_file_item_async(
        self,
        input: PatchFileItemInput,
        dataset: str,
        table: str,
        id: str,
        view: Optional[str] = None,
    ):
        """
        Update file properties

        Updates the properties stored in columns in a library for the item specified by the item id. Use \"Update file\" action to update file contents.             When using this with the On-Premises Data Gateway, the name of the library to connect to may need to be entered manually.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/patchfileitem"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("PATCH", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"PATCH {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def patch_file_item_with_predicted_values_async(
        self,
        input: PatchFileItemWithPredictedValuesParameters,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Update file properties using AI Builder model results

        Updates the values stored in library columns for a file analyzed by the model specified by the ModelId.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/patchfileitemwithpredictedvalues"

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def set_approval_status_async(
        self,
        dataset: str,
        table: str,
        id: str,
        approval_action: Optional[str],
        comments: Optional[str] = None,
        entity_tag: Optional[str] = None,
    ):
        """
        Set content approval status

        Sets the content approval status for an item in a list or library that has content approval turned on. You must provide an ETag for pages and files. You can get the ETag using the Get File Metadata action. This action is only available for SharePoint Online and SharePoint 2019.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/setapprovalstatus"
        query_params = []
        if approval_action is not None:
            query_params.append(f"approvalAction={approval_action}")
        if comments is not None:
            query_params.append(f"comments={comments}")
        if entity_tag is not None:
            query_params.append(f"entityTag={entity_tag}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def unshare_item_async(
        self,
        dataset: str,
        table: str,
        id: str,
    ):
        """
        Stop sharing an item or a file

        Delete all links giving access to an item or a file and remove all people with direct access except for owners.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(id))}}/unshare"

        await self.http_client.send_async("POST", path, body=None)

    async def get_item_attachments_async(
        self,
        dataset: str,
        table: str,
        item_id: str,
    ):
        """
        Get attachments

        Returns the list of attachments for the specified list item. You can add a \"Get attachment content\" step and use the \"File identifier\" property returned by this action to get to the contents of the file.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(item_id))}}/attachments"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_attachment_async(
        self,
        input: CreateAttachmentInput,
        dataset: str,
        table: str,
        item_id: str,
        display_name: Optional[str],
    ):
        """
        Add attachment

        Adds a new attachment to the specified list item.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(item_id))}}/attachments"
        query_params = []
        if display_name is not None:
            query_params.append(f"displayName={display_name}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def delete_attachment_async(
        self,
        dataset: str,
        table: str,
        item_id: str,
        attachment_id: str,
    ):
        """
        Delete attachment

        Deletes the specified attachment.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(item_id))}}/attachments/{{quote(str(attachment_id))}}"

        await self.http_client.send_async("DELETE", path, body=None)

    async def get_attachment_content_async(
        self,
        dataset: str,
        table: str,
        item_id: str,
        attachment_id: str,
    ):
        """
        Get attachment content

        Returns file contents using the file identifier. The contents can be copied somewhere else, or be used as an attachment.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/items/{{quote(str(item_id))}}/attachments/{{quote(str(attachment_id))}}/$value"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        return response.content

    async def get_on_changed_items_async(
        self,
        dataset: str,
        table: str,
        folder_path: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        When an item or a file is modified

        Triggers when an item is modified (but not when it is created).
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onchangeditems"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_deleted_file_items_async(
        self,
        dataset: str,
        table: str,
        folder_path: Optional[str] = None,
    ):
        """
        When a file is deleted

        Triggers when a file is deleted in a library. You can optionally specify a folder to watch as well. When a folder is deleted, the trigger will fire only once for the deleted folder. This can only be used by site collection admins of the site where the list is located.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/ondeletedfileitems"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_deleted_items_async(
        self,
        dataset: str,
        table: str,
    ):
        """
        When an item is deleted

        Triggers when an item is deleted in a list. This can only be used by site collection admins of the site where the list is located.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/ondeleteditems"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_new_file_items_async(
        self,
        dataset: str,
        table: str,
        folder_path: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        When a file is created (properties only)

        Triggers when an item is created in a library. Returns only the properties stored in the library columns.             You can add a \"Get file content\" step and use the \"File identifier\" property returned by this action to get to the contents of the file.             When using this with the On-Premises Data Gateway, the name of the library to connect to may need to be entered manually.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onnewfileitems"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_new_items_async(
        self,
        dataset: str,
        table: str,
        view: Optional[str] = None,
    ):
        """
        When an item is created

        Triggers when an item is created.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onnewitems"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_updated_file_classified_times_async(
        self,
        dataset: str,
        table: str,
        folder_path: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        When a file is classified by a Microsoft Syntex model

        Triggers a flow when Microsoft Syntex changes the classification date of any file in the library. The date changes when a document processing model classifies or extracts information.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onupdatedfileclassifiedtimes"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_updated_file_items_async(
        self,
        dataset: str,
        table: str,
        folder_path: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        When a file is created or modified (properties only)

        Triggers when an item is created, or modified in a library. Returns only the properties stored in the library columns.             You can add a \"Get file content\" step and use the \"File identifier\" property returned by this action to get to the contents of the file.             When using this with the On-Premises Data Gateway, the name of the library to connect to may need to be entered manually.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onupdatedfileitems"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_on_updated_items_async(
        self,
        dataset: str,
        table: str,
        view: Optional[str] = None,
    ):
        """
        When an item is created or modified

        Triggers when an item is created, and also each time it is modified.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/onupdateditems"
        query_params = []
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_content_assembly_document_async(
        self,
        input: CreateContentAssemblyDocumentInput,
        dataset: str,
        table: str,
        template: str,
        folder_path: Optional[str] = None,
        file_name: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        Generate document using Microsoft Syntex (preview)

        Use this action to create documents based on modern templates from Microsoft Syntex. This preview requires a Syntex license. Pricing is subject to change. For more info see: https://docs.microsoft.com/en-us/microsoft-365/contentunderstanding/content-assembly.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/templates/{{quote(str(template))}}/createnewdocument"
        query_params = []
        if folder_path is not None:
            query_params.append(f"folderPath={folder_path}")
        if file_name is not None:
            query_params.append(f"fileName={file_name}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def get_table_views_async(
        self,
        dataset: str,
        table: str,
    ):
        """
        Get list views

        Gets views from a SharePoint list.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/tables/{{quote(str(table))}}/views"

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def create_agreements_solution_document_async(
        self,
        input: CreateAgreementsSolutionDocumentInput,
        dataset: str,
        template: str,
        document_name: Optional[str] = None,
        table: Optional[str] = None,
        view: Optional[str] = None,
    ):
        """
        Agreements Solution - Generate document within Agreements Solution workspace

        Use this action to create documents based on modern templates in a Agreements Solution workspace. This is behind a payment wall currently in planning (either license or PayG).
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/agreements/templates/{{quote(str(template))}}/createnewdocument"
        query_params = []
        if document_name is not None:
            query_params.append(f"documentName={document_name}")
        if table is not None:
            query_params.append(f"table={table}")
        if view is not None:
            query_params.append(f"view={view}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("POST", path, body=input)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"POST {path}",
                response.status,
                response.text,
            )

        if not response.text:
            return None

        import json
        return json.loads(response.text)

    async def on_new_file_async(
        self,
        dataset: str,
        folder_id: Optional[str],
        infer_content_type: Optional[str] = None,
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        When a file is created in a folder (deprecated)

        Triggers when a file is created in a SharePoint folder. The trigger does not fire if a file is added/updated in a subfolder. If it is required to trigger on subfolders, multiple triggers should be created.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/triggers/onnewfile"
        query_params = []
        if folder_id is not None:
            query_params.append(f"folderId={folder_id}")
        if infer_content_type is not None:
            query_params.append(f"inferContentType={infer_content_type}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        return response.content

    async def on_updated_file_async(
        self,
        dataset: str,
        folder_id: Optional[str],
        include_file_content: Optional[str] = None,
        infer_content_type: Optional[str] = None,
        query_parameters_single_encoded: Optional[str] = None,
    ):
        """
        When a file is created or modified in a folder (deprecated)

        Triggers when a file is created, and also each time it is modified in a SharePoint folder. The trigger does not fire if a file is added/updated in a subfolder. If it is required to trigger on subfolders, multiple triggers should be created.
        """
        path = f"{self._connection_runtime_url}/datasets/{{quote(str(dataset))}}/triggers/onupdatedfile"
        query_params = []
        if folder_id is not None:
            query_params.append(f"folderId={folder_id}")
        if include_file_content is not None:
            query_params.append(f"includeFileContent={include_file_content}")
        if infer_content_type is not None:
            query_params.append(f"inferContentType={infer_content_type}")
        if query_parameters_single_encoded is not None:
            query_params.append(f"queryParametersSingleEncoded={query_parameters_single_encoded}")
        if query_params:
            path += '?' + '&'.join(query_params)

        response = await self.http_client.send_async("GET", path, body=None)

        if not (200 <= response.status < 300):
            raise ConnectorException(
                f"GET {path}",
                response.status,
                response.text,
            )

        return response.content

